#!/usr/bin/env node
var __getOwnPropNames = Object.getOwnPropertyNames;
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};

// .omniflux_cache/compiler/symbols.js
var require_symbols = __commonJS({
  ".omniflux_cache/compiler/symbols.js"(exports2, module2) {
    var fs2 = require("fs");
    var util2 = require("util");
    var path2 = require("path");
    global.args = process.argv.slice(2);
    global.vars = new Proxy({ args: global.args }, {
      get(target, prop) {
        return prop in target ? target[prop] : null;
      },
      set(target, prop, value) {
        target[prop] = value;
        return true;
      }
    });
    function sprintf2(format, ...args2) {
      if (typeof format !== "string") {
        return util2.format(format, ...args2);
      }
      let argIndex = 0;
      const hasSpecifiers = /%([+-]?)(0?\d*)(?:\.(\d+))?([asdijf%])/g.test(format);
      if (!hasSpecifiers) {
        return util2.format(format, ...args2);
      }
      let formatted = format.replace(/%(?<flags>[+-]?)(?<width>0?\d*)(?:\.(?<precision>\d+))?(?<type>[asdijf%])/g, (match3, flags, widthStr, precisionStr, type) => {
        if (type === "%")
          return "%";
        if (argIndex >= args2.length)
          return match3;
        const val = args2[argIndex++];
        let padChar = " ";
        let width = 0;
        let leftAlign = flags === "-";
        if (widthStr) {
          if (widthStr.startsWith("0")) {
            padChar = "0";
            width = parseInt(widthStr, 10);
          } else {
            width = parseInt(widthStr, 10);
          }
        }
        let precision = precisionStr ? parseInt(precisionStr, 10) : -1;
        let result = "";
        if (type === "d" || type === "i") {
          let intVal = parseInt(val, 10);
          if (isNaN(intVal))
            intVal = 0;
          result = intVal.toString();
          if (precision >= 0) {
            result = result.padStart(precision, "0");
          }
        } else if (type === "f") {
          let floatVal = parseFloat(val);
          if (isNaN(floatVal))
            floatVal = 0;
          if (precision >= 0) {
            result = floatVal.toFixed(precision);
          } else {
            result = floatVal.toString();
          }
        } else if (type === "s") {
          result = String(val);
          if (precision >= 0) {
            result = result.slice(0, precision);
          }
        } else if (type === "j") {
          result = JSON.stringify(val);
        } else {
          result = String(val);
        }
        if (result.length < width) {
          const padLen = width - result.length;
          const padding = padChar.repeat(padLen);
          result = leftAlign ? result + padding : padding + result;
        }
        return result;
      });
      if (argIndex < args2.length) {
        const leftover = args2.slice(argIndex).map((x) => typeof x === "object" ? util2.inspect(x) : String(x));
        formatted += " " + leftover.join(" ");
      }
      return formatted;
    }
    function print2(format, ...args2) {
      console.log(sprintf2(format, ...args2));
    }
    function printf2(format, ...args2) {
      process.stdout.write(sprintf2(format, ...args2));
    }
    var _rl2 = null;
    function _getRL2() {
      if (!_rl2 && process.stdin.isTTY) {
        const readline = require("readline");
        _rl2 = readline.createInterface({
          input: process.stdin,
          output: process.stdout,
          terminal: true,
          historySize: 200
        });
        _rl2.on("close", () => {
          _rl2 = null;
        });
      }
      return _rl2;
    }
    function input2(promptMessage) {
      const rl = _getRL2();
      if (rl) {
        return new Promise((resolve) => {
          rl.question(promptMessage !== void 0 ? String(promptMessage) : "", resolve);
        });
      }
      if (promptMessage !== void 0) {
        process.stdout.write(String(promptMessage));
      }
      const buffer = Buffer.alloc(65536);
      let bytesRead = 0;
      try {
        bytesRead = fs2.readSync(0, buffer, 0, 65536, null);
      } catch (e) {
        return "";
      }
      return buffer.toString("utf8", 0, bytesRead).replace(/\r?\n$/, "");
    }
    global.input = input2;
    function readchar2() {
      const hasRaw = typeof process.stdin.setRawMode === "function";
      const isRaw = process.stdin.isRaw;
      if (hasRaw) {
        process.stdin.setRawMode(true);
      }
      const buffer = Buffer.alloc(1);
      let bytesRead = 0;
      try {
        bytesRead = fs2.readSync(0, buffer, 0, 1, null);
      } catch (e) {
      } finally {
        if (hasRaw) {
          process.stdin.setRawMode(isRaw);
        }
      }
      const char = buffer.toString("utf8", 0, bytesRead);
      if (char === "") {
        process.exit(130);
      }
      return char;
    }
    function fileread2(path3) {
      return fs2.readFileSync(path3, "utf8");
    }
    function filewrite2(path3, data) {
      fs2.writeFileSync(path3, typeof data === "object" ? JSON.stringify(data, null, 2) : data, "utf8");
    }
    function fileexists2(path3) {
      return fs2.existsSync(path3);
    }
    function filecopy2(src, dest) {
      fs2.copyFileSync(src, dest);
    }
    function filerename2(src, dest) {
      fs2.renameSync(src, dest);
    }
    function fileappend2(path3, data) {
      fs2.appendFileSync(path3, typeof data === "object" ? JSON.stringify(data, null, 2) : data, "utf8");
    }
    function fprintf2(path3, format, ...args2) {
      const data = sprintf2(format, ...args2);
      fs2.appendFileSync(path3, data, "utf8");
    }
    function fprint2(path3, format, ...args2) {
      const data = sprintf2(format, ...args2) + "\n";
      fs2.appendFileSync(path3, data, "utf8");
    }
    function filedelete2(path3) {
      fs2.unlinkSync(path3);
    }
    function dirlist2(path3) {
      return fs2.readdirSync(path3);
    }
    function filestat2(path3) {
      const stats = fs2.statSync(path3);
      return {
        size: stats.size,
        isdirectory: stats.isDirectory(),
        is_directory: stats.isDirectory(),
        isfile: stats.isFile(),
        is_file: stats.isFile(),
        createdat: stats.birthtimeMs,
        created_at: stats.birthtimeMs,
        modifiedat: stats.mtimeMs,
        modified_at: stats.mtimeMs
      };
    }
    function dircreate2(path3) {
      if (typeof path3 !== "string")
        throw new Error("Directory path must be a string");
      fs2.mkdirSync(path3, { recursive: true });
    }
    function scriptdir2() {
      const path3 = require("path");
      return path3.dirname(require.main.filename);
    }
    function setenv2(name, value) {
      if (typeof name !== "string")
        throw new Error("Environment variable name must be a string");
      process.env[name] = value !== void 0 ? String(value) : "";
    }
    function getenv2(name) {
      if (typeof name !== "string")
        throw new Error("Environment variable name must be a string");
      return process.env[name] !== void 0 ? process.env[name] : null;
    }
    function sha2562(text) {
      if (typeof text !== "string")
        throw new Error("Input to sha256 must be a string");
      const crypto = require("crypto");
      return crypto.createHash("sha256").update(text).digest("hex");
    }
    function getcookie2(req, name) {
      if (!req || !req.headers || !req.headers.cookie)
        return null;
      const cookies = req.headers.cookie.split(";");
      for (let c of cookies) {
        const parts = c.split("=");
        if (parts[0].trim() === name)
          return parts[1].trim();
      }
      return null;
    }
    function setcookie2(res, name, value, options = {}) {
      if (!res || typeof res.cookie !== "function") {
        throw new Error("First argument to setcookie must be an HTTP response object");
      }
      res.cookie(name, value, options);
    }
    var dbFilePath2 = process.env.DB_FILE || "db.json";
    var cachedDb2 = null;
    var cachedDbMtime2 = 0;
    function dbsetfile2(path3) {
      if (typeof path3 !== "string")
        throw new Error("Database file path must be a string");
      dbFilePath2 = path3;
      cachedDb2 = null;
      cachedDbMtime2 = 0;
    }
    function loadDb2() {
      try {
        if (fs2.existsSync(dbFilePath2)) {
          const stats = fs2.statSync(dbFilePath2);
          if (cachedDb2 && stats.mtimeMs === cachedDbMtime2) {
            return cachedDb2;
          }
          const content = fs2.readFileSync(dbFilePath2, "utf8");
          cachedDb2 = JSON.parse(content || "{}");
          cachedDbMtime2 = stats.mtimeMs;
          return cachedDb2;
        }
      } catch (e) {
      }
      if (!cachedDb2) {
        cachedDb2 = {};
        cachedDbMtime2 = 0;
      }
      return cachedDb2;
    }
    function saveDb2(data) {
      cachedDb2 = data;
      fs2.writeFileSync(dbFilePath2, JSON.stringify(data, null, 2), "utf8");
      try {
        if (fs2.existsSync(dbFilePath2)) {
          cachedDbMtime2 = fs2.statSync(dbFilePath2).mtimeMs;
        }
      } catch (e) {
        cachedDbMtime2 = Date.now();
      }
    }
    function dbinsert2(collection, doc) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      if (!doc || typeof doc !== "object")
        throw new Error("Document must be an object");
      const db = loadDb2();
      if (!db[collection]) {
        db[collection] = [];
      }
      const newDoc = { ...doc };
      if (newDoc.id === void 0) {
        newDoc.id = Date.now().toString(36) + Math.random().toString(36).substring(2, 6);
      }
      db[collection].push(newDoc);
      saveDb2(db);
      return newDoc;
    }
    function dbselect2(collection, filter) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      const db = loadDb2();
      const list = db[collection] || [];
      if (!filter || typeof filter !== "object" || Object.keys(filter).length === 0) {
        return list.map((d) => ({ ...d }));
      }
      return list.filter((doc) => {
        for (const key in filter) {
          if (doc[key] !== filter[key]) {
            return false;
          }
        }
        return true;
      }).map((d) => ({ ...d }));
    }
    function dbupdate2(collection, filter, update) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      if (!update || typeof update !== "object")
        throw new Error("Update payload must be an object");
      const db = loadDb2();
      const list = db[collection] || [];
      let count = 0;
      for (let i = 0; i < list.length; i++) {
        const doc = list[i];
        let matches = true;
        if (filter && typeof filter === "object") {
          for (const key in filter) {
            if (doc[key] !== filter[key]) {
              matches = false;
              break;
            }
          }
        }
        if (matches) {
          list[i] = { ...doc, ...update };
          count++;
        }
      }
      if (count > 0) {
        db[collection] = list;
        saveDb2(db);
      }
      return count;
    }
    function dbdelete2(collection, filter) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      const db = loadDb2();
      const list = db[collection] || [];
      let count = 0;
      const newList = [];
      for (const doc of list) {
        let matches = true;
        if (filter && typeof filter === "object") {
          for (const key in filter) {
            if (doc[key] !== filter[key]) {
              matches = false;
              break;
            }
          }
        }
        if (matches) {
          count++;
        } else {
          newList.push(doc);
        }
      }
      if (count > 0) {
        db[collection] = newList;
        saveDb2(db);
      }
      return count;
    }
    var redisModule2 = null;
    var redisClient2 = null;
    async function getRedisClient2() {
      if (!redisModule2) {
        redisModule2 = require("redis");
      }
      if (!redisClient2) {
        const url = process.env.REDIS_URL || "redis://localhost:6379";
        redisClient2 = redisModule2.createClient({ url });
        await redisClient2.connect();
      }
      return redisClient2;
    }
    async function cacheset2(key, val, ttl) {
      const client = await getRedisClient2();
      const stringVal = typeof val === "object" ? JSON.stringify(val) : String(val);
      if (ttl) {
        await client.setEx(key, ttl, stringVal);
      } else {
        await client.set(key, stringVal);
      }
    }
    async function cacheget2(key) {
      const client = await getRedisClient2();
      const val = await client.get(key);
      if (!val)
        return null;
      try {
        return JSON.parse(val);
      } catch (e) {
        return val;
      }
    }
    function describe2(val) {
      if (val === null)
        return "null";
      if (val === void 0)
        return "undefined";
      if (Array.isArray(val))
        return "array";
      return typeof val;
    }
    global.describe = describe2;
    function jsonparse2(str) {
      try {
        return JSON.parse(String(str || ""));
      } catch (e) {
        return null;
      }
    }
    global.jsonparse = jsonparse2;
    global.jsondecode = jsonparse2;
    function jsonencode2(val, pretty = false) {
      try {
        return pretty ? JSON.stringify(val, null, 2) : JSON.stringify(val);
      } catch (e) {
        return "null";
      }
    }
    global.jsonencode = jsonencode2;
    function len2(val) {
      if (val === null || val === void 0)
        return 0;
      return val.length || 0;
    }
    function toRegExp2(regex) {
      if (regex instanceof RegExp) {
        return regex;
      }
      if (typeof regex === "string") {
        const firstChar = regex.charAt(0);
        if (/^[^a-zA-Z0-9\s\\]/.test(firstChar)) {
          const lastDelim = regex.lastIndexOf(firstChar);
          if (lastDelim > 0) {
            const pattern = regex.substring(1, lastDelim);
            const flags = regex.substring(lastDelim + 1);
            return new RegExp(pattern, flags);
          }
        }
        return new RegExp(regex);
      }
      return null;
    }
    function strsplit2(str, sep) {
      if (typeof str !== "string")
        return [];
      if (sep === void 0)
        return [str];
      const regexObj = toRegExp2(sep);
      if (regexObj) {
        return str.split(regexObj);
      }
      return str.split(sep);
    }
    function match2(str, regex) {
      if (str === null || str === void 0)
        return false;
      const regexObj = toRegExp2(regex);
      if (!regexObj)
        return false;
      const res = String(str).match(regexObj);
      if (!res)
        return false;
      if (res.length > 1) {
        return res.slice(1);
      }
      return true;
    }
    function strtrim2(str, side = "both") {
      const s = String(str || "");
      if (side === "left")
        return s.trimStart();
      if (side === "right")
        return s.trimEnd();
      return s.trim();
    }
    function strsub2(str, start, length) {
      const s = String(str || "");
      if (length === void 0) {
        return s.substring(start);
      }
      return s.substring(start, start + length);
    }
    function strindexof2(str, search) {
      const s = String(str || "");
      return s.indexOf(search);
    }
    function strlastindexof2(str, search) {
      const s = String(str || "");
      return s.lastIndexOf(search);
    }
    function strrepeat2(str, n) {
      const s = String(str || "");
      const count = parseInt(n, 10);
      if (isNaN(count) || count <= 0)
        return "";
      return s.repeat(count);
    }
    function strreplace2(str, search, replacement) {
      const s = String(str || "");
      const r = String(replacement !== void 0 ? replacement : "");
      const regexObj = toRegExp2(search);
      if (regexObj) {
        return s.replace(regexObj, r);
      }
      return s.replaceAll(search, r);
    }
    function strupper2(str) {
      return String(str || "").toUpperCase();
    }
    function strlower2(str) {
      return String(str || "").toLowerCase();
    }
    function arraypush2(arr, item) {
      if (Array.isArray(arr)) {
        arr.push(item);
      }
    }
    function arrayunshift2(arr, item) {
      if (Array.isArray(arr)) {
        arr.unshift(item);
      }
    }
    function arraypop2(arr) {
      if (Array.isArray(arr)) {
        return arr.pop();
      }
      return null;
    }
    function arraycontains2(arr, item) {
      if (Array.isArray(arr)) {
        return arr.includes(item);
      }
      return false;
    }
    function arrayjoin2(arr, sep) {
      if (Array.isArray(arr)) {
        return arr.join(sep);
      }
      return "";
    }
    function arrayslice2(arr, start, end) {
      if (Array.isArray(arr)) {
        return arr.slice(start, end);
      }
      return [];
    }
    function pathjoin2(...paths) {
      const path3 = require("path");
      return path3.join(...paths.map((p) => String(p || "")));
    }
    function pathresolve2(base, relative) {
      const path3 = require("path");
      if (relative === void 0) {
        return path3.resolve(String(base || ""));
      }
      return path3.resolve(String(base || ""), String(relative || ""));
    }
    function pathdirname2(filepath) {
      const path3 = require("path");
      return path3.dirname(String(filepath || ""));
    }
    function pathbasename2(filepath) {
      const path3 = require("path");
      return path3.basename(String(filepath || ""));
    }
    function pathextension2(filepath) {
      const path3 = require("path");
      return path3.extname(String(filepath || ""));
    }
    function pathisabsolute2(filepath) {
      const path3 = require("path");
      return path3.isAbsolute(String(filepath || ""));
    }
    function exit2(code = 0) {
      process.exit(parseInt(code, 10) || 0);
    }
    function arrayreverse2(arr) {
      if (!Array.isArray(arr))
        return [];
      return [...arr].reverse();
    }
    async function asyncMergeSort2(arr, compareFn) {
      if (arr.length <= 1)
        return arr;
      const mid = Math.floor(arr.length / 2);
      const left = arr.slice(0, mid);
      const right = arr.slice(mid);
      return await asyncMerge2(
        await asyncMergeSort2(left, compareFn),
        await asyncMergeSort2(right, compareFn),
        compareFn
      );
    }
    async function asyncMerge2(left, right, compareFn) {
      const result = [];
      let l = 0, r = 0;
      while (l < left.length && r < right.length) {
        const cmp = await compareFn(left[l], right[r]);
        if (cmp <= 0) {
          result.push(left[l]);
          l++;
        } else {
          result.push(right[r]);
          r++;
        }
      }
      return result.concat(left.slice(l)).concat(right.slice(r));
    }
    async function arraysort2(arr, compareFn) {
      if (!Array.isArray(arr))
        return [];
      if (typeof compareFn === "function") {
        const temp = [...arr];
        return await asyncMergeSort2(temp, compareFn);
      }
      return [...arr].sort();
    }
    async function arraymap2(arr, task) {
      if (!Array.isArray(arr))
        return [];
      if (typeof task !== "function")
        return [...arr];
      const results = [];
      for (let i = 0; i < arr.length; i++) {
        results.push(await task(arr[i], i, arr));
      }
      return results;
    }
    async function arrayfilter2(arr, task) {
      if (!Array.isArray(arr))
        return [];
      if (typeof task !== "function")
        return [...arr];
      const results = [];
      for (let i = 0; i < arr.length; i++) {
        if (await task(arr[i], i, arr)) {
          results.push(arr[i]);
        }
      }
      return results;
    }
    function arrayshift2(arr) {
      if (!Array.isArray(arr))
        return null;
      return arr.shift();
    }
    async function arrayfind2(arr, task) {
      if (!Array.isArray(arr))
        return null;
      if (typeof task !== "function")
        return null;
      for (let i = 0; i < arr.length; i++) {
        if (await task(arr[i], i, arr)) {
          return arr[i];
        }
      }
      return null;
    }
    function time2() {
      return Math.floor(Date.now() / 1e3);
    }
    function microtime2(getAsFloat = true) {
      if (getAsFloat === false) {
        const now = Date.now();
        const sec = Math.floor(now / 1e3);
        const msec = now % 1e3;
        return `${(msec / 1e3).toFixed(8)} ${sec}`;
      }
      return Date.now() / 1e3;
    }
    function dateyear2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getFullYear();
    }
    function datemonth2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getMonth() + 1;
    }
    function dateday2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getDate();
    }
    function datehour2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getHours();
    }
    function dateminute2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getMinutes();
    }
    function datesecond2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getSeconds();
    }
    function dateweekday2(ts, format = "number") {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      const day = d.getDay();
      if (format === "short" || format === "text") {
        const days = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];
        return days[day];
      }
      return day;
    }
    function template2(source, context = {}) {
      if (typeof source !== "string")
        return "";
      const appRoot = typeof global !== "undefined" && global.__app_root ? global.__app_root : process.cwd();
      const resolvePath = (p) => path2.isAbsolute(p) ? p : path2.resolve(appRoot, p);
      let html = source;
      try {
        const resolvedSource = resolvePath(source);
        if (fs2.existsSync(resolvedSource)) {
          html = fs2.readFileSync(resolvedSource, "utf8");
        }
        let includeDepth = 0;
        const maxIncludeDepth = 10;
        while (/@include\s*\(\s*["'](.*?)["']\s*\)/.test(html) && includeDepth < maxIncludeDepth) {
          html = html.replace(/@include\s*\(\s*["'](.*?)["']\s*\)/g, (match3, filepath) => {
            const resolvedInclude = resolvePath(filepath);
            if (!fs2.existsSync(resolvedInclude)) {
              throw new Error(`Include file not found: '${filepath}'`);
            }
            return fs2.readFileSync(resolvedInclude, "utf8");
          });
          includeDepth++;
        }
        html = html.replace(/@\}\s*(?=@else)/g, "@}");
        if (html.includes("</body>")) {
          const clientScript = `
<script>
function _of_exec_scripts(container) {
    const scripts = container.querySelectorAll('script');
    for (let oldScript of scripts) {
        const newScript = document.createElement('script');
        for (let attr of oldScript.attributes) {
            newScript.setAttribute(attr.name, attr.value);
        }
        newScript.textContent = oldScript.textContent;
        oldScript.parentNode.replaceChild(newScript, oldScript);
    }
}

document.addEventListener('submit', async (e) => {
    const form = e.target;
    const targetSelector = form.getAttribute('of-target');
    if (!targetSelector) return;
    
    e.preventDefault();
    try {
        const response = await fetch(form.action || window.location.href, {
            method: form.method || 'POST',
            body: new URLSearchParams(new FormData(form))
        });
        const htmlResult = await response.text();
        const targetEl = document.querySelector(targetSelector);
        if (targetEl) {
            targetEl.innerHTML = htmlResult;
            _of_exec_scripts(targetEl);
        }
    } catch (err) {
        console.error('OmniFlux Form Submission Error:', err);
    }
});

document.addEventListener('click', async (e) => {
    const link = e.target.closest('a');
    if (!link) return;
    const targetSelector = link.getAttribute('of-target');
    if (!targetSelector) return;
    
    e.preventDefault();
    try {
        const response = await fetch(link.href);
        const htmlResult = await response.text();
        const targetEl = document.querySelector(targetSelector);
        if (targetEl) {
            targetEl.innerHTML = htmlResult;
            _of_exec_scripts(targetEl);
        }
    } catch (err) {
        console.error('OmniFlux Link Navigation Error:', err);
    }
});
</script>
`;
          html = html.replace("</body>", clientScript + "</body>");
        }
        const tokenRegex = /(@if\s*\(.*?\)\s*\{|@else\s*if\s*\(.*?\)\s*\{|@else\s*\{|@for\s*\(.*?\)\s*\{|@\})/g;
        const parts = html.split(tokenRegex);
        let jsCode = "let _out = '';\n";
        jsCode += "with(global.vars || {}) {\n";
        jsCode += "with(context || {}) {\n";
        for (let part of parts) {
          if (!part)
            continue;
          if (part.startsWith("@if")) {
            const cond = part.match(/@if\s*\((.*?)\)\s*\{/)[1];
            jsCode += `if (${cond}) {
`;
          } else if (part.startsWith("@else if")) {
            const cond = part.match(/@else\s*if\s*\((.*?)\)\s*\{/)[1];
            jsCode += `} else if (${cond}) {
`;
          } else if (part.startsWith("@else")) {
            jsCode += `} else {
`;
          } else if (part.startsWith("@for")) {
            const loopMatch = part.match(/@for\s*\(\s*(?:let|var)?\s*(\w+)\s+of\s+(.*?)\)\s*\{/);
            if (loopMatch) {
              const item = loopMatch[1];
              const list = loopMatch[2];
              jsCode += `for (let ${item} of ${list}) {
`;
            }
          } else if (part === "@}") {
            jsCode += `}
`;
          } else {
            let text = part.replace(/\\/g, "\\\\").replace(/`/g, "\\`").replace(/\$/g, "\\$");
            text = text.replace(/\{\{\s*([\s\S]+?)\s*\}\}/g, "${$1}");
            jsCode += `_out += \`${text}\`;
`;
          }
        }
        jsCode += "}\n}\n";
        jsCode += "return _out;";
        const keys = ["context", ...Object.keys(context)];
        const values = [context, ...Object.values(context)];
        return new Function(...keys, jsCode)(...values);
      } catch (e) {
        return `<!-- Template Error: ${e.message} -->`;
      }
    }
    global.sprintf = sprintf2;
    global.print = print2;
    global.printf = printf2;
    global.fprint = fprint2;
    global.f_print = fprint2;
    global.fprintf = fprintf2;
    global.f_printf = fprintf2;
    global.input = input2;
    global.readchar = readchar2;
    global.read_char = readchar2;
    global.fileread = fileread2;
    global.file_read = fileread2;
    global.filewrite = filewrite2;
    global.file_write = filewrite2;
    global.fileexists = fileexists2;
    global.file_exists = fileexists2;
    global.fileappend = fileappend2;
    global.file_append = fileappend2;
    global.filedelete = filedelete2;
    global.file_delete = filedelete2;
    global.filecopy = filecopy2;
    global.file_copy = filecopy2;
    global.filerename = filerename2;
    global.file_rename = filerename2;
    global.dirlist = dirlist2;
    global.dir_list = dirlist2;
    global.dircreate = dircreate2;
    global.dir_create = dircreate2;
    global.scriptdir = scriptdir2;
    global.script_dir = scriptdir2;
    global.setenv = setenv2;
    global.set_env = setenv2;
    global.getenv = getenv2;
    global.get_env = getenv2;
    global.sha256 = sha2562;
    global.getcookie = getcookie2;
    global.get_cookie = getcookie2;
    global.setcookie = setcookie2;
    global.set_cookie = setcookie2;
    global.filestat = filestat2;
    global.file_stat = filestat2;
    global.dbsetfile = dbsetfile2;
    global.db_set_file = dbsetfile2;
    global.dbinsert = dbinsert2;
    global.db_insert = dbinsert2;
    global.dbselect = dbselect2;
    global.db_select = dbselect2;
    global.dbupdate = dbupdate2;
    global.db_update = dbupdate2;
    global.dbdelete = dbdelete2;
    global.db_delete = dbdelete2;
    global.cacheset = cacheset2;
    global.cache_set = cacheset2;
    global.cacheget = cacheget2;
    global.cache_get = cacheget2;
    global.len = len2;
    global.strsplit = strsplit2;
    global.str_split = strsplit2;
    global.match = match2;
    global.arraypush = arraypush2;
    global.array_push = arraypush2;
    global.arrayunshift = arrayunshift2;
    global.array_unshift = arrayunshift2;
    global.arraypop = arraypop2;
    global.array_pop = arraypop2;
    global.arraycontains = arraycontains2;
    global.array_contains = arraycontains2;
    global.arrayjoin = arrayjoin2;
    global.array_join = arrayjoin2;
    global.arrayslice = arrayslice2;
    global.array_slice = arrayslice2;
    global.strtrim = strtrim2;
    global.str_trim = strtrim2;
    global.strsub = strsub2;
    global.str_sub = strsub2;
    global.strindexof = strindexof2;
    global.str_index_of = strindexof2;
    global.strlastindexof = strlastindexof2;
    global.str_last_index_of = strlastindexof2;
    global.strrepeat = strrepeat2;
    global.str_repeat = strrepeat2;
    global.strreplace = strreplace2;
    global.str_replace = strreplace2;
    global.strupper = strupper2;
    global.str_upper = strupper2;
    global.strlower = strlower2;
    global.str_lower = strlower2;
    global.pathjoin = pathjoin2;
    global.path_join = pathjoin2;
    global.pathresolve = pathresolve2;
    global.path_resolve = pathresolve2;
    global.pathdirname = pathdirname2;
    global.path_dir_name = pathdirname2;
    global.pathbasename = pathbasename2;
    global.path_base_name = pathbasename2;
    global.pathextension = pathextension2;
    global.path_extension = pathextension2;
    global.pathisabsolute = pathisabsolute2;
    global.path_is_absolute = pathisabsolute2;
    global.exit = exit2;
    global.arrayreverse = arrayreverse2;
    global.array_reverse = arrayreverse2;
    global.arraysort = arraysort2;
    global.array_sort = arraysort2;
    global.arraymap = arraymap2;
    global.array_map = arraymap2;
    global.arrayfilter = arrayfilter2;
    global.array_filter = arrayfilter2;
    global.arrayshift = arrayshift2;
    global.array_shift = arrayshift2;
    global.arrayfind = arrayfind2;
    global.array_find = arrayfind2;
    global.time = time2;
    global.microtime = microtime2;
    global.micro_time = microtime2;
    global.dateyear = dateyear2;
    global.date_year = dateyear2;
    global.datemonth = datemonth2;
    global.date_month = datemonth2;
    global.dateday = dateday2;
    global.date_day = dateday2;
    global.datehour = datehour2;
    global.date_hour = datehour2;
    global.dateminute = dateminute2;
    global.date_minute = dateminute2;
    global.datesecond = datesecond2;
    global.date_second = datesecond2;
    global.dateweekday = dateweekday2;
    global.date_weekday = dateweekday2;
    global.template = template2;
    function formatRuntimeError2(err, label = "OmniFlux Runtime Error") {
      console.error("\n\x1B[1m\x1B[31m" + label + ":\x1B[0m");
      console.error("\x1B[33m" + err.message + "\x1B[0m");
      if (err.stack) {
        const stackLines = err.stack.split("\n");
        const lineMatch = stackLines.find(
          (line) => line.includes("/") && !line.includes("node:internal") && !line.includes("node_modules")
        );
        if (lineMatch) {
          const match3 = lineMatch.match(/\(([^)]+):(\d+):(\d+)\)/) || lineMatch.match(/at\s+(.+?):(\d+):(\d+)/);
          if (match3) {
            const filePath = match3[1];
            const lineNum = parseInt(match3[2], 10);
            try {
              const content = fs2.readFileSync(filePath, "utf8");
              const fileLines = content.split("\n");
              const indicesToCheck = [lineNum, lineNum - 1, lineNum - 2];
              for (const i of indicesToCheck) {
                if (i >= 0 && i < fileLines.length) {
                  const targetLine = fileLines[i];
                  const ofLineMatch = targetLine.match(/\/\/\! OF_LINE: (.+?)(?:\r?\n|$)/) || targetLine.match(/\/\/ OF_LINE: (.+?)(?:\r?\n|$)/);
                  if (ofLineMatch) {
                    console.error("\x1B[1m\x1B[32mAt: " + ofLineMatch[1] + "\x1B[0m");
                    console.error();
                    return;
                  }
                }
              }
            } catch (e) {
            }
            console.error("\x1B[2mAt: " + path2.basename(filePath) + ":" + lineNum + "\x1B[0m");
          }
        }
      }
      console.error();
    }
    process.on("uncaughtException", (err) => {
      const hooks = global.__on_error_hooks || [];
      if (hooks.length > 0) {
        for (const hook of hooks) {
          hook(err).catch(() => {
          });
        }
        return;
      }
      formatRuntimeError2(err);
      process.exit(1);
    });
    process.on("unhandledRejection", (reason) => {
      const hooks = global.__on_error_hooks || [];
      if (hooks.length > 0) {
        for (const hook of hooks) {
          hook(reason).catch(() => {
          });
        }
        return;
      }
      const err = reason instanceof Error ? reason : new Error(String(reason));
      formatRuntimeError2(err, "OmniFlux Runtime Error (Unhandled Rejection)");
      process.exit(1);
    });
    //! OF_LINE: symbols.of:1
    //! OF_LINE: symbols.of:2
    //! OF_LINE: symbols.of:3
    //! OF_LINE: symbols.of:4
    //! OF_LINE: symbols.of:5
    //! OF_LINE: symbols.of:6
    //! OF_LINE: symbols.of:7
    //! OF_LINE: symbols.of:8
    //! OF_LINE: symbols.of:9
    //! OF_LINE: symbols.of:10
    var BUILTINS = [
      //! OF_LINE: symbols.of:11
      //! OF_LINE: symbols.of:12
      "print",
      "printf",
      "sprintf",
      "fprint",
      "fprintf",
      //! OF_LINE: symbols.of:13
      "input",
      "readchar",
      "read_char",
      //! OF_LINE: symbols.of:14
      //! OF_LINE: symbols.of:15
      "fileread",
      "file_read",
      //! OF_LINE: symbols.of:16
      "filewrite",
      "file_write",
      //! OF_LINE: symbols.of:17
      "fileexists",
      "file_exists",
      //! OF_LINE: symbols.of:18
      "fileappend",
      "file_append",
      //! OF_LINE: symbols.of:19
      "filedelete",
      "file_delete",
      //! OF_LINE: symbols.of:20
      "filecopy",
      "file_copy",
      //! OF_LINE: symbols.of:21
      "filerename",
      "file_rename",
      //! OF_LINE: symbols.of:22
      "filestat",
      "file_stat",
      //! OF_LINE: symbols.of:23
      "dirlist",
      "dir_list",
      //! OF_LINE: symbols.of:24
      "dircreate",
      "dir_create",
      //! OF_LINE: symbols.of:25
      "scriptdir",
      "script_dir",
      //! OF_LINE: symbols.of:26
      //! OF_LINE: symbols.of:27
      "dbsetfile",
      "db_set_file",
      //! OF_LINE: symbols.of:28
      "dbinsert",
      "db_insert",
      //! OF_LINE: symbols.of:29
      "dbselect",
      "db_select",
      //! OF_LINE: symbols.of:30
      "dbupdate",
      "db_update",
      //! OF_LINE: symbols.of:31
      "dbdelete",
      "db_delete",
      //! OF_LINE: symbols.of:32
      //! OF_LINE: symbols.of:33
      "cacheset",
      "cache_set",
      //! OF_LINE: symbols.of:34
      "cacheget",
      "cache_get",
      //! OF_LINE: symbols.of:35
      //! OF_LINE: symbols.of:36
      "setenv",
      "set_env",
      //! OF_LINE: symbols.of:37
      "getenv",
      "get_env",
      //! OF_LINE: symbols.of:38
      //! OF_LINE: symbols.of:39
      "exit",
      //! OF_LINE: symbols.of:40
      //! OF_LINE: symbols.of:41
      "sha256",
      //! OF_LINE: symbols.of:42
      //! OF_LINE: symbols.of:43
      "getcookie",
      "get_cookie",
      //! OF_LINE: symbols.of:44
      "setcookie",
      "set_cookie",
      //! OF_LINE: symbols.of:45
      //! OF_LINE: symbols.of:46
      "template",
      //! OF_LINE: symbols.of:47
      //! OF_LINE: symbols.of:48
      "len",
      //! OF_LINE: symbols.of:49
      "match",
      //! OF_LINE: symbols.of:50
      "strsplit",
      "str_split",
      //! OF_LINE: symbols.of:51
      "strtrim",
      "str_trim",
      //! OF_LINE: symbols.of:52
      "strsub",
      "str_sub",
      //! OF_LINE: symbols.of:53
      "strindexof",
      "str_index_of",
      //! OF_LINE: symbols.of:54
      "strlastindexof",
      "str_last_index_of",
      //! OF_LINE: symbols.of:55
      "strrepeat",
      "str_repeat",
      //! OF_LINE: symbols.of:56
      "strreplace",
      "str_replace",
      //! OF_LINE: symbols.of:57
      "strupper",
      "str_upper",
      //! OF_LINE: symbols.of:58
      "strlower",
      "str_lower",
      //! OF_LINE: symbols.of:59
      //! OF_LINE: symbols.of:60
      "arraypush",
      "array_push",
      //! OF_LINE: symbols.of:61
      "arrayunshift",
      "array_unshift",
      //! OF_LINE: symbols.of:62
      "arraypop",
      "array_pop",
      //! OF_LINE: symbols.of:63
      "arrayshift",
      "array_shift",
      //! OF_LINE: symbols.of:64
      "arraycontains",
      "array_contains",
      //! OF_LINE: symbols.of:65
      "arrayjoin",
      "array_join",
      //! OF_LINE: symbols.of:66
      "arrayslice",
      "array_slice",
      //! OF_LINE: symbols.of:67
      "arrayreverse",
      "array_reverse",
      //! OF_LINE: symbols.of:68
      "arraysort",
      "array_sort",
      //! OF_LINE: symbols.of:69
      "arraymap",
      "array_map",
      //! OF_LINE: symbols.of:70
      "arrayfilter",
      "array_filter",
      //! OF_LINE: symbols.of:71
      "arrayfind",
      "array_find",
      //! OF_LINE: symbols.of:72
      //! OF_LINE: symbols.of:73
      "pathjoin",
      "path_join",
      //! OF_LINE: symbols.of:74
      "pathresolve",
      "path_resolve",
      //! OF_LINE: symbols.of:75
      "pathdirname",
      "path_dir_name",
      //! OF_LINE: symbols.of:76
      "pathbasename",
      "path_base_name",
      //! OF_LINE: symbols.of:77
      "pathextension",
      "path_extension",
      //! OF_LINE: symbols.of:78
      "pathisabsolute",
      "path_is_absolute",
      //! OF_LINE: symbols.of:79
      //! OF_LINE: symbols.of:80
      "time",
      "microtime",
      "micro_time",
      //! OF_LINE: symbols.of:81
      "dateyear",
      "date_year",
      //! OF_LINE: symbols.of:82
      "datemonth",
      "date_month",
      //! OF_LINE: symbols.of:83
      "dateday",
      "date_day",
      //! OF_LINE: symbols.of:84
      "datehour",
      "date_hour",
      //! OF_LINE: symbols.of:85
      "dateminute",
      "date_minute",
      //! OF_LINE: symbols.of:86
      "datesecond",
      "date_second",
      //! OF_LINE: symbols.of:87
      "dateweekday",
      "date_weekday",
      //! OF_LINE: symbols.of:88
      //! OF_LINE: symbols.of:89
      "describe",
      //! OF_LINE: symbols.of:90
      //! OF_LINE: symbols.of:91
      "jsonparse",
      "json_parse",
      //! OF_LINE: symbols.of:92
      "jsondecode",
      "json_decode",
      //! OF_LINE: symbols.of:93
      "jsonencode",
      "json_encode",
      //! OF_LINE: symbols.of:94
      //! OF_LINE: symbols.of:95
      "args",
      "global",
      "console",
      "require",
      "module",
      "process",
      //! OF_LINE: symbols.of:96
      "Math",
      "JSON",
      "Array",
      "Object",
      "String",
      "Number",
      //! OF_LINE: symbols.of:97
      "Boolean",
      "Error",
      "setTimeout",
      "setInterval",
      //! OF_LINE: symbols.of:98
      "clearTimeout",
      "clearInterval",
      "NaN"
      //! OF_LINE: symbols.of:99
    ];
    //! OF_LINE: symbols.of:100
    //! OF_LINE: symbols.of:101
    //! OF_LINE: symbols.of:102
    //! OF_LINE: symbols.of:103
    var ASYNC_BUILTINS = [
      //! OF_LINE: symbols.of:104
      "input",
      //! OF_LINE: symbols.of:105
      "cacheset",
      "cache_set",
      //! OF_LINE: symbols.of:106
      "cacheget",
      "cache_get",
      //! OF_LINE: symbols.of:107
      "arraymap",
      "array_map",
      //! OF_LINE: symbols.of:108
      "arrayfilter",
      "array_filter",
      //! OF_LINE: symbols.of:109
      "arrayfind",
      "array_find",
      //! OF_LINE: symbols.of:110
      "arraysort",
      "array_sort"
      //! OF_LINE: symbols.of:111
    ];
    //! OF_LINE: symbols.of:112
    //! OF_LINE: symbols.of:113
    //! OF_LINE: symbols.of:114
    //! OF_LINE: symbols.of:115
    //! OF_LINE: symbols.of:116
    //! OF_LINE: symbols.of:117
    async function prescan_tasks2(content) {
      try {
        //! OF_LINE: symbols.of:118
        let tasks = {};
        //! OF_LINE: symbols.of:119
        //! OF_LINE: symbols.of:120
        let lines = strsplit2(content, "\n");
        //! OF_LINE: symbols.of:121
        for (let line of lines) {
          //! OF_LINE: symbols.of:122
          let clean = strreplace2(line, "/#[^\n]*/", "");
          //! OF_LINE: symbols.of:123
          clean = strtrim2(clean);
          //! OF_LINE: symbols.of:124
          //! OF_LINE: symbols.of:125
          let m1 = match2(clean, "/^define\\s+task\\s+(\\w+)/i");
          //! OF_LINE: symbols.of:126
          if (m1) {
            //! OF_LINE: symbols.of:127
            tasks[m1[0]] = true;
            //! OF_LINE: symbols.of:128
          }
          //! OF_LINE: symbols.of:129
          //! OF_LINE: symbols.of:130
          let m2 = match2(clean, "/^fn\\s+(\\w+)/i");
          //! OF_LINE: symbols.of:131
          if (m2) {
            //! OF_LINE: symbols.of:132
            tasks[m2[0]] = true;
            //! OF_LINE: symbols.of:133
          }
          //! OF_LINE: symbols.of:134
        }
        //! OF_LINE: symbols.of:135
        //! OF_LINE: symbols.of:136
        return tasks;
        //! OF_LINE: symbols.of:137
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: symbols.of:138
    //! OF_LINE: symbols.of:139
    //! OF_LINE: symbols.of:140
    //! OF_LINE: symbols.of:141
    async function is_builtin(name) {
      try {
        //! OF_LINE: symbols.of:142
        return arraycontains2(BUILTINS, name);
        //! OF_LINE: symbols.of:143
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: symbols.of:144
    //! OF_LINE: symbols.of:145
    //! OF_LINE: symbols.of:146
    //! OF_LINE: symbols.of:147
    //! OF_LINE: symbols.of:148
    async function is_async(name, user_tasks) {
      try {
        //! OF_LINE: symbols.of:149
        if (arraycontains2(ASYNC_BUILTINS, name)) {
          //! OF_LINE: symbols.of:150
          return true;
          //! OF_LINE: symbols.of:151
        }
        //! OF_LINE: symbols.of:152
        if (user_tasks[name]) {
          //! OF_LINE: symbols.of:153
          return true;
          //! OF_LINE: symbols.of:154
        }
        //! OF_LINE: symbols.of:155
        return false;
        //! OF_LINE: symbols.of:156
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: symbols.of:157
    //! OF_LINE: symbols.of:158
    global.BUILTINS = BUILTINS;
    //! OF_LINE: symbols.of:159
    global.ASYNC_BUILTINS = ASYNC_BUILTINS;
    //! OF_LINE: symbols.of:160
    //! OF_LINE: symbols.of:161
    //! OF_LINE: symbols.of:162
    global.prescan_tasks = prescan_tasks2;
    global.is_builtin = is_builtin;
    global.is_async = is_async;
    if (require.main === module2) {
      (async () => {
        const hooks = global.__on_start_hooks || [];
        for (const hook of hooks) {
          await hook();
        }
      })().catch((err) => {
        if (typeof formatRuntimeError2 === "function") {
          formatRuntimeError2(err);
        } else {
          console.error(err);
        }
        process.exit(1);
      });
    }
  }
});

// .omniflux_cache/compiler/preprocessor.js
var require_preprocessor = __commonJS({
  ".omniflux_cache/compiler/preprocessor.js"(exports2, module2) {
    var fs2 = require("fs");
    var util2 = require("util");
    var path2 = require("path");
    global.args = process.argv.slice(2);
    global.vars = new Proxy({ args: global.args }, {
      get(target, prop) {
        return prop in target ? target[prop] : null;
      },
      set(target, prop, value) {
        target[prop] = value;
        return true;
      }
    });
    function sprintf2(format, ...args2) {
      if (typeof format !== "string") {
        return util2.format(format, ...args2);
      }
      let argIndex = 0;
      const hasSpecifiers = /%([+-]?)(0?\d*)(?:\.(\d+))?([asdijf%])/g.test(format);
      if (!hasSpecifiers) {
        return util2.format(format, ...args2);
      }
      let formatted = format.replace(/%(?<flags>[+-]?)(?<width>0?\d*)(?:\.(?<precision>\d+))?(?<type>[asdijf%])/g, (match3, flags, widthStr, precisionStr, type) => {
        if (type === "%")
          return "%";
        if (argIndex >= args2.length)
          return match3;
        const val = args2[argIndex++];
        let padChar = " ";
        let width = 0;
        let leftAlign = flags === "-";
        if (widthStr) {
          if (widthStr.startsWith("0")) {
            padChar = "0";
            width = parseInt(widthStr, 10);
          } else {
            width = parseInt(widthStr, 10);
          }
        }
        let precision = precisionStr ? parseInt(precisionStr, 10) : -1;
        let result = "";
        if (type === "d" || type === "i") {
          let intVal = parseInt(val, 10);
          if (isNaN(intVal))
            intVal = 0;
          result = intVal.toString();
          if (precision >= 0) {
            result = result.padStart(precision, "0");
          }
        } else if (type === "f") {
          let floatVal = parseFloat(val);
          if (isNaN(floatVal))
            floatVal = 0;
          if (precision >= 0) {
            result = floatVal.toFixed(precision);
          } else {
            result = floatVal.toString();
          }
        } else if (type === "s") {
          result = String(val);
          if (precision >= 0) {
            result = result.slice(0, precision);
          }
        } else if (type === "j") {
          result = JSON.stringify(val);
        } else {
          result = String(val);
        }
        if (result.length < width) {
          const padLen = width - result.length;
          const padding = padChar.repeat(padLen);
          result = leftAlign ? result + padding : padding + result;
        }
        return result;
      });
      if (argIndex < args2.length) {
        const leftover = args2.slice(argIndex).map((x) => typeof x === "object" ? util2.inspect(x) : String(x));
        formatted += " " + leftover.join(" ");
      }
      return formatted;
    }
    function print2(format, ...args2) {
      console.log(sprintf2(format, ...args2));
    }
    function printf2(format, ...args2) {
      process.stdout.write(sprintf2(format, ...args2));
    }
    var _rl2 = null;
    function _getRL2() {
      if (!_rl2 && process.stdin.isTTY) {
        const readline = require("readline");
        _rl2 = readline.createInterface({
          input: process.stdin,
          output: process.stdout,
          terminal: true,
          historySize: 200
        });
        _rl2.on("close", () => {
          _rl2 = null;
        });
      }
      return _rl2;
    }
    function input2(promptMessage) {
      const rl = _getRL2();
      if (rl) {
        return new Promise((resolve) => {
          rl.question(promptMessage !== void 0 ? String(promptMessage) : "", resolve);
        });
      }
      if (promptMessage !== void 0) {
        process.stdout.write(String(promptMessage));
      }
      const buffer = Buffer.alloc(65536);
      let bytesRead = 0;
      try {
        bytesRead = fs2.readSync(0, buffer, 0, 65536, null);
      } catch (e) {
        return "";
      }
      return buffer.toString("utf8", 0, bytesRead).replace(/\r?\n$/, "");
    }
    global.input = input2;
    function readchar2() {
      const hasRaw = typeof process.stdin.setRawMode === "function";
      const isRaw = process.stdin.isRaw;
      if (hasRaw) {
        process.stdin.setRawMode(true);
      }
      const buffer = Buffer.alloc(1);
      let bytesRead = 0;
      try {
        bytesRead = fs2.readSync(0, buffer, 0, 1, null);
      } catch (e) {
      } finally {
        if (hasRaw) {
          process.stdin.setRawMode(isRaw);
        }
      }
      const char = buffer.toString("utf8", 0, bytesRead);
      if (char === "") {
        process.exit(130);
      }
      return char;
    }
    function fileread2(path3) {
      return fs2.readFileSync(path3, "utf8");
    }
    function filewrite2(path3, data) {
      fs2.writeFileSync(path3, typeof data === "object" ? JSON.stringify(data, null, 2) : data, "utf8");
    }
    function fileexists2(path3) {
      return fs2.existsSync(path3);
    }
    function filecopy2(src, dest) {
      fs2.copyFileSync(src, dest);
    }
    function filerename2(src, dest) {
      fs2.renameSync(src, dest);
    }
    function fileappend2(path3, data) {
      fs2.appendFileSync(path3, typeof data === "object" ? JSON.stringify(data, null, 2) : data, "utf8");
    }
    function fprintf2(path3, format, ...args2) {
      const data = sprintf2(format, ...args2);
      fs2.appendFileSync(path3, data, "utf8");
    }
    function fprint2(path3, format, ...args2) {
      const data = sprintf2(format, ...args2) + "\n";
      fs2.appendFileSync(path3, data, "utf8");
    }
    function filedelete2(path3) {
      fs2.unlinkSync(path3);
    }
    function dirlist2(path3) {
      return fs2.readdirSync(path3);
    }
    function filestat2(path3) {
      const stats = fs2.statSync(path3);
      return {
        size: stats.size,
        isdirectory: stats.isDirectory(),
        is_directory: stats.isDirectory(),
        isfile: stats.isFile(),
        is_file: stats.isFile(),
        createdat: stats.birthtimeMs,
        created_at: stats.birthtimeMs,
        modifiedat: stats.mtimeMs,
        modified_at: stats.mtimeMs
      };
    }
    function dircreate2(path3) {
      if (typeof path3 !== "string")
        throw new Error("Directory path must be a string");
      fs2.mkdirSync(path3, { recursive: true });
    }
    function scriptdir2() {
      const path3 = require("path");
      return path3.dirname(require.main.filename);
    }
    function setenv2(name, value) {
      if (typeof name !== "string")
        throw new Error("Environment variable name must be a string");
      process.env[name] = value !== void 0 ? String(value) : "";
    }
    function getenv2(name) {
      if (typeof name !== "string")
        throw new Error("Environment variable name must be a string");
      return process.env[name] !== void 0 ? process.env[name] : null;
    }
    function sha2562(text) {
      if (typeof text !== "string")
        throw new Error("Input to sha256 must be a string");
      const crypto = require("crypto");
      return crypto.createHash("sha256").update(text).digest("hex");
    }
    function getcookie2(req, name) {
      if (!req || !req.headers || !req.headers.cookie)
        return null;
      const cookies = req.headers.cookie.split(";");
      for (let c of cookies) {
        const parts = c.split("=");
        if (parts[0].trim() === name)
          return parts[1].trim();
      }
      return null;
    }
    function setcookie2(res, name, value, options = {}) {
      if (!res || typeof res.cookie !== "function") {
        throw new Error("First argument to setcookie must be an HTTP response object");
      }
      res.cookie(name, value, options);
    }
    var dbFilePath2 = process.env.DB_FILE || "db.json";
    var cachedDb2 = null;
    var cachedDbMtime2 = 0;
    function dbsetfile2(path3) {
      if (typeof path3 !== "string")
        throw new Error("Database file path must be a string");
      dbFilePath2 = path3;
      cachedDb2 = null;
      cachedDbMtime2 = 0;
    }
    function loadDb2() {
      try {
        if (fs2.existsSync(dbFilePath2)) {
          const stats = fs2.statSync(dbFilePath2);
          if (cachedDb2 && stats.mtimeMs === cachedDbMtime2) {
            return cachedDb2;
          }
          const content = fs2.readFileSync(dbFilePath2, "utf8");
          cachedDb2 = JSON.parse(content || "{}");
          cachedDbMtime2 = stats.mtimeMs;
          return cachedDb2;
        }
      } catch (e) {
      }
      if (!cachedDb2) {
        cachedDb2 = {};
        cachedDbMtime2 = 0;
      }
      return cachedDb2;
    }
    function saveDb2(data) {
      cachedDb2 = data;
      fs2.writeFileSync(dbFilePath2, JSON.stringify(data, null, 2), "utf8");
      try {
        if (fs2.existsSync(dbFilePath2)) {
          cachedDbMtime2 = fs2.statSync(dbFilePath2).mtimeMs;
        }
      } catch (e) {
        cachedDbMtime2 = Date.now();
      }
    }
    function dbinsert2(collection, doc) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      if (!doc || typeof doc !== "object")
        throw new Error("Document must be an object");
      const db = loadDb2();
      if (!db[collection]) {
        db[collection] = [];
      }
      const newDoc = { ...doc };
      if (newDoc.id === void 0) {
        newDoc.id = Date.now().toString(36) + Math.random().toString(36).substring(2, 6);
      }
      db[collection].push(newDoc);
      saveDb2(db);
      return newDoc;
    }
    function dbselect2(collection, filter) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      const db = loadDb2();
      const list = db[collection] || [];
      if (!filter || typeof filter !== "object" || Object.keys(filter).length === 0) {
        return list.map((d) => ({ ...d }));
      }
      return list.filter((doc) => {
        for (const key in filter) {
          if (doc[key] !== filter[key]) {
            return false;
          }
        }
        return true;
      }).map((d) => ({ ...d }));
    }
    function dbupdate2(collection, filter, update) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      if (!update || typeof update !== "object")
        throw new Error("Update payload must be an object");
      const db = loadDb2();
      const list = db[collection] || [];
      let count = 0;
      for (let i = 0; i < list.length; i++) {
        const doc = list[i];
        let matches = true;
        if (filter && typeof filter === "object") {
          for (const key in filter) {
            if (doc[key] !== filter[key]) {
              matches = false;
              break;
            }
          }
        }
        if (matches) {
          list[i] = { ...doc, ...update };
          count++;
        }
      }
      if (count > 0) {
        db[collection] = list;
        saveDb2(db);
      }
      return count;
    }
    function dbdelete2(collection, filter) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      const db = loadDb2();
      const list = db[collection] || [];
      let count = 0;
      const newList = [];
      for (const doc of list) {
        let matches = true;
        if (filter && typeof filter === "object") {
          for (const key in filter) {
            if (doc[key] !== filter[key]) {
              matches = false;
              break;
            }
          }
        }
        if (matches) {
          count++;
        } else {
          newList.push(doc);
        }
      }
      if (count > 0) {
        db[collection] = newList;
        saveDb2(db);
      }
      return count;
    }
    var redisModule2 = null;
    var redisClient2 = null;
    async function getRedisClient2() {
      if (!redisModule2) {
        redisModule2 = require("redis");
      }
      if (!redisClient2) {
        const url = process.env.REDIS_URL || "redis://localhost:6379";
        redisClient2 = redisModule2.createClient({ url });
        await redisClient2.connect();
      }
      return redisClient2;
    }
    async function cacheset2(key, val, ttl) {
      const client = await getRedisClient2();
      const stringVal = typeof val === "object" ? JSON.stringify(val) : String(val);
      if (ttl) {
        await client.setEx(key, ttl, stringVal);
      } else {
        await client.set(key, stringVal);
      }
    }
    async function cacheget2(key) {
      const client = await getRedisClient2();
      const val = await client.get(key);
      if (!val)
        return null;
      try {
        return JSON.parse(val);
      } catch (e) {
        return val;
      }
    }
    function describe2(val) {
      if (val === null)
        return "null";
      if (val === void 0)
        return "undefined";
      if (Array.isArray(val))
        return "array";
      return typeof val;
    }
    global.describe = describe2;
    function jsonparse2(str) {
      try {
        return JSON.parse(String(str || ""));
      } catch (e) {
        return null;
      }
    }
    global.jsonparse = jsonparse2;
    global.jsondecode = jsonparse2;
    function jsonencode2(val, pretty = false) {
      try {
        return pretty ? JSON.stringify(val, null, 2) : JSON.stringify(val);
      } catch (e) {
        return "null";
      }
    }
    global.jsonencode = jsonencode2;
    function len2(val) {
      if (val === null || val === void 0)
        return 0;
      return val.length || 0;
    }
    function toRegExp2(regex) {
      if (regex instanceof RegExp) {
        return regex;
      }
      if (typeof regex === "string") {
        const firstChar = regex.charAt(0);
        if (/^[^a-zA-Z0-9\s\\]/.test(firstChar)) {
          const lastDelim = regex.lastIndexOf(firstChar);
          if (lastDelim > 0) {
            const pattern = regex.substring(1, lastDelim);
            const flags = regex.substring(lastDelim + 1);
            return new RegExp(pattern, flags);
          }
        }
        return new RegExp(regex);
      }
      return null;
    }
    function strsplit2(str, sep) {
      if (typeof str !== "string")
        return [];
      if (sep === void 0)
        return [str];
      const regexObj = toRegExp2(sep);
      if (regexObj) {
        return str.split(regexObj);
      }
      return str.split(sep);
    }
    function match2(str, regex) {
      if (str === null || str === void 0)
        return false;
      const regexObj = toRegExp2(regex);
      if (!regexObj)
        return false;
      const res = String(str).match(regexObj);
      if (!res)
        return false;
      if (res.length > 1) {
        return res.slice(1);
      }
      return true;
    }
    function strtrim2(str, side = "both") {
      const s = String(str || "");
      if (side === "left")
        return s.trimStart();
      if (side === "right")
        return s.trimEnd();
      return s.trim();
    }
    function strsub2(str, start, length) {
      const s = String(str || "");
      if (length === void 0) {
        return s.substring(start);
      }
      return s.substring(start, start + length);
    }
    function strindexof2(str, search) {
      const s = String(str || "");
      return s.indexOf(search);
    }
    function strlastindexof2(str, search) {
      const s = String(str || "");
      return s.lastIndexOf(search);
    }
    function strrepeat2(str, n) {
      const s = String(str || "");
      const count = parseInt(n, 10);
      if (isNaN(count) || count <= 0)
        return "";
      return s.repeat(count);
    }
    function strreplace2(str, search, replacement) {
      const s = String(str || "");
      const r = String(replacement !== void 0 ? replacement : "");
      const regexObj = toRegExp2(search);
      if (regexObj) {
        return s.replace(regexObj, r);
      }
      return s.replaceAll(search, r);
    }
    function strupper2(str) {
      return String(str || "").toUpperCase();
    }
    function strlower2(str) {
      return String(str || "").toLowerCase();
    }
    function arraypush2(arr, item) {
      if (Array.isArray(arr)) {
        arr.push(item);
      }
    }
    function arrayunshift2(arr, item) {
      if (Array.isArray(arr)) {
        arr.unshift(item);
      }
    }
    function arraypop2(arr) {
      if (Array.isArray(arr)) {
        return arr.pop();
      }
      return null;
    }
    function arraycontains2(arr, item) {
      if (Array.isArray(arr)) {
        return arr.includes(item);
      }
      return false;
    }
    function arrayjoin2(arr, sep) {
      if (Array.isArray(arr)) {
        return arr.join(sep);
      }
      return "";
    }
    function arrayslice2(arr, start, end) {
      if (Array.isArray(arr)) {
        return arr.slice(start, end);
      }
      return [];
    }
    function pathjoin2(...paths) {
      const path3 = require("path");
      return path3.join(...paths.map((p) => String(p || "")));
    }
    function pathresolve2(base, relative) {
      const path3 = require("path");
      if (relative === void 0) {
        return path3.resolve(String(base || ""));
      }
      return path3.resolve(String(base || ""), String(relative || ""));
    }
    function pathdirname2(filepath) {
      const path3 = require("path");
      return path3.dirname(String(filepath || ""));
    }
    function pathbasename2(filepath) {
      const path3 = require("path");
      return path3.basename(String(filepath || ""));
    }
    function pathextension2(filepath) {
      const path3 = require("path");
      return path3.extname(String(filepath || ""));
    }
    function pathisabsolute2(filepath) {
      const path3 = require("path");
      return path3.isAbsolute(String(filepath || ""));
    }
    function exit2(code = 0) {
      process.exit(parseInt(code, 10) || 0);
    }
    function arrayreverse2(arr) {
      if (!Array.isArray(arr))
        return [];
      return [...arr].reverse();
    }
    async function asyncMergeSort2(arr, compareFn) {
      if (arr.length <= 1)
        return arr;
      const mid = Math.floor(arr.length / 2);
      const left = arr.slice(0, mid);
      const right = arr.slice(mid);
      return await asyncMerge2(
        await asyncMergeSort2(left, compareFn),
        await asyncMergeSort2(right, compareFn),
        compareFn
      );
    }
    async function asyncMerge2(left, right, compareFn) {
      const result = [];
      let l = 0, r = 0;
      while (l < left.length && r < right.length) {
        const cmp = await compareFn(left[l], right[r]);
        if (cmp <= 0) {
          result.push(left[l]);
          l++;
        } else {
          result.push(right[r]);
          r++;
        }
      }
      return result.concat(left.slice(l)).concat(right.slice(r));
    }
    async function arraysort2(arr, compareFn) {
      if (!Array.isArray(arr))
        return [];
      if (typeof compareFn === "function") {
        const temp = [...arr];
        return await asyncMergeSort2(temp, compareFn);
      }
      return [...arr].sort();
    }
    async function arraymap2(arr, task) {
      if (!Array.isArray(arr))
        return [];
      if (typeof task !== "function")
        return [...arr];
      const results = [];
      for (let i = 0; i < arr.length; i++) {
        results.push(await task(arr[i], i, arr));
      }
      return results;
    }
    async function arrayfilter2(arr, task) {
      if (!Array.isArray(arr))
        return [];
      if (typeof task !== "function")
        return [...arr];
      const results = [];
      for (let i = 0; i < arr.length; i++) {
        if (await task(arr[i], i, arr)) {
          results.push(arr[i]);
        }
      }
      return results;
    }
    function arrayshift2(arr) {
      if (!Array.isArray(arr))
        return null;
      return arr.shift();
    }
    async function arrayfind2(arr, task) {
      if (!Array.isArray(arr))
        return null;
      if (typeof task !== "function")
        return null;
      for (let i = 0; i < arr.length; i++) {
        if (await task(arr[i], i, arr)) {
          return arr[i];
        }
      }
      return null;
    }
    function time2() {
      return Math.floor(Date.now() / 1e3);
    }
    function microtime2(getAsFloat = true) {
      if (getAsFloat === false) {
        const now = Date.now();
        const sec = Math.floor(now / 1e3);
        const msec = now % 1e3;
        return `${(msec / 1e3).toFixed(8)} ${sec}`;
      }
      return Date.now() / 1e3;
    }
    function dateyear2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getFullYear();
    }
    function datemonth2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getMonth() + 1;
    }
    function dateday2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getDate();
    }
    function datehour2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getHours();
    }
    function dateminute2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getMinutes();
    }
    function datesecond2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getSeconds();
    }
    function dateweekday2(ts, format = "number") {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      const day = d.getDay();
      if (format === "short" || format === "text") {
        const days = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];
        return days[day];
      }
      return day;
    }
    function template2(source, context = {}) {
      if (typeof source !== "string")
        return "";
      const appRoot = typeof global !== "undefined" && global.__app_root ? global.__app_root : process.cwd();
      const resolvePath = (p) => path2.isAbsolute(p) ? p : path2.resolve(appRoot, p);
      let html = source;
      try {
        const resolvedSource = resolvePath(source);
        if (fs2.existsSync(resolvedSource)) {
          html = fs2.readFileSync(resolvedSource, "utf8");
        }
        let includeDepth = 0;
        const maxIncludeDepth = 10;
        while (/@include\s*\(\s*["'](.*?)["']\s*\)/.test(html) && includeDepth < maxIncludeDepth) {
          html = html.replace(/@include\s*\(\s*["'](.*?)["']\s*\)/g, (match3, filepath) => {
            const resolvedInclude = resolvePath(filepath);
            if (!fs2.existsSync(resolvedInclude)) {
              throw new Error(`Include file not found: '${filepath}'`);
            }
            return fs2.readFileSync(resolvedInclude, "utf8");
          });
          includeDepth++;
        }
        html = html.replace(/@\}\s*(?=@else)/g, "@}");
        if (html.includes("</body>")) {
          const clientScript = `
<script>
function _of_exec_scripts(container) {
    const scripts = container.querySelectorAll('script');
    for (let oldScript of scripts) {
        const newScript = document.createElement('script');
        for (let attr of oldScript.attributes) {
            newScript.setAttribute(attr.name, attr.value);
        }
        newScript.textContent = oldScript.textContent;
        oldScript.parentNode.replaceChild(newScript, oldScript);
    }
}

document.addEventListener('submit', async (e) => {
    const form = e.target;
    const targetSelector = form.getAttribute('of-target');
    if (!targetSelector) return;
    
    e.preventDefault();
    try {
        const response = await fetch(form.action || window.location.href, {
            method: form.method || 'POST',
            body: new URLSearchParams(new FormData(form))
        });
        const htmlResult = await response.text();
        const targetEl = document.querySelector(targetSelector);
        if (targetEl) {
            targetEl.innerHTML = htmlResult;
            _of_exec_scripts(targetEl);
        }
    } catch (err) {
        console.error('OmniFlux Form Submission Error:', err);
    }
});

document.addEventListener('click', async (e) => {
    const link = e.target.closest('a');
    if (!link) return;
    const targetSelector = link.getAttribute('of-target');
    if (!targetSelector) return;
    
    e.preventDefault();
    try {
        const response = await fetch(link.href);
        const htmlResult = await response.text();
        const targetEl = document.querySelector(targetSelector);
        if (targetEl) {
            targetEl.innerHTML = htmlResult;
            _of_exec_scripts(targetEl);
        }
    } catch (err) {
        console.error('OmniFlux Link Navigation Error:', err);
    }
});
</script>
`;
          html = html.replace("</body>", clientScript + "</body>");
        }
        const tokenRegex = /(@if\s*\(.*?\)\s*\{|@else\s*if\s*\(.*?\)\s*\{|@else\s*\{|@for\s*\(.*?\)\s*\{|@\})/g;
        const parts = html.split(tokenRegex);
        let jsCode = "let _out = '';\n";
        jsCode += "with(global.vars || {}) {\n";
        jsCode += "with(context || {}) {\n";
        for (let part of parts) {
          if (!part)
            continue;
          if (part.startsWith("@if")) {
            const cond = part.match(/@if\s*\((.*?)\)\s*\{/)[1];
            jsCode += `if (${cond}) {
`;
          } else if (part.startsWith("@else if")) {
            const cond = part.match(/@else\s*if\s*\((.*?)\)\s*\{/)[1];
            jsCode += `} else if (${cond}) {
`;
          } else if (part.startsWith("@else")) {
            jsCode += `} else {
`;
          } else if (part.startsWith("@for")) {
            const loopMatch = part.match(/@for\s*\(\s*(?:let|var)?\s*(\w+)\s+of\s+(.*?)\)\s*\{/);
            if (loopMatch) {
              const item = loopMatch[1];
              const list = loopMatch[2];
              jsCode += `for (let ${item} of ${list}) {
`;
            }
          } else if (part === "@}") {
            jsCode += `}
`;
          } else {
            let text = part.replace(/\\/g, "\\\\").replace(/`/g, "\\`").replace(/\$/g, "\\$");
            text = text.replace(/\{\{\s*([\s\S]+?)\s*\}\}/g, "${$1}");
            jsCode += `_out += \`${text}\`;
`;
          }
        }
        jsCode += "}\n}\n";
        jsCode += "return _out;";
        const keys = ["context", ...Object.keys(context)];
        const values = [context, ...Object.values(context)];
        return new Function(...keys, jsCode)(...values);
      } catch (e) {
        return `<!-- Template Error: ${e.message} -->`;
      }
    }
    global.sprintf = sprintf2;
    global.print = print2;
    global.printf = printf2;
    global.fprint = fprint2;
    global.f_print = fprint2;
    global.fprintf = fprintf2;
    global.f_printf = fprintf2;
    global.input = input2;
    global.readchar = readchar2;
    global.read_char = readchar2;
    global.fileread = fileread2;
    global.file_read = fileread2;
    global.filewrite = filewrite2;
    global.file_write = filewrite2;
    global.fileexists = fileexists2;
    global.file_exists = fileexists2;
    global.fileappend = fileappend2;
    global.file_append = fileappend2;
    global.filedelete = filedelete2;
    global.file_delete = filedelete2;
    global.filecopy = filecopy2;
    global.file_copy = filecopy2;
    global.filerename = filerename2;
    global.file_rename = filerename2;
    global.dirlist = dirlist2;
    global.dir_list = dirlist2;
    global.dircreate = dircreate2;
    global.dir_create = dircreate2;
    global.scriptdir = scriptdir2;
    global.script_dir = scriptdir2;
    global.setenv = setenv2;
    global.set_env = setenv2;
    global.getenv = getenv2;
    global.get_env = getenv2;
    global.sha256 = sha2562;
    global.getcookie = getcookie2;
    global.get_cookie = getcookie2;
    global.setcookie = setcookie2;
    global.set_cookie = setcookie2;
    global.filestat = filestat2;
    global.file_stat = filestat2;
    global.dbsetfile = dbsetfile2;
    global.db_set_file = dbsetfile2;
    global.dbinsert = dbinsert2;
    global.db_insert = dbinsert2;
    global.dbselect = dbselect2;
    global.db_select = dbselect2;
    global.dbupdate = dbupdate2;
    global.db_update = dbupdate2;
    global.dbdelete = dbdelete2;
    global.db_delete = dbdelete2;
    global.cacheset = cacheset2;
    global.cache_set = cacheset2;
    global.cacheget = cacheget2;
    global.cache_get = cacheget2;
    global.len = len2;
    global.strsplit = strsplit2;
    global.str_split = strsplit2;
    global.match = match2;
    global.arraypush = arraypush2;
    global.array_push = arraypush2;
    global.arrayunshift = arrayunshift2;
    global.array_unshift = arrayunshift2;
    global.arraypop = arraypop2;
    global.array_pop = arraypop2;
    global.arraycontains = arraycontains2;
    global.array_contains = arraycontains2;
    global.arrayjoin = arrayjoin2;
    global.array_join = arrayjoin2;
    global.arrayslice = arrayslice2;
    global.array_slice = arrayslice2;
    global.strtrim = strtrim2;
    global.str_trim = strtrim2;
    global.strsub = strsub2;
    global.str_sub = strsub2;
    global.strindexof = strindexof2;
    global.str_index_of = strindexof2;
    global.strlastindexof = strlastindexof2;
    global.str_last_index_of = strlastindexof2;
    global.strrepeat = strrepeat2;
    global.str_repeat = strrepeat2;
    global.strreplace = strreplace2;
    global.str_replace = strreplace2;
    global.strupper = strupper2;
    global.str_upper = strupper2;
    global.strlower = strlower2;
    global.str_lower = strlower2;
    global.pathjoin = pathjoin2;
    global.path_join = pathjoin2;
    global.pathresolve = pathresolve2;
    global.path_resolve = pathresolve2;
    global.pathdirname = pathdirname2;
    global.path_dir_name = pathdirname2;
    global.pathbasename = pathbasename2;
    global.path_base_name = pathbasename2;
    global.pathextension = pathextension2;
    global.path_extension = pathextension2;
    global.pathisabsolute = pathisabsolute2;
    global.path_is_absolute = pathisabsolute2;
    global.exit = exit2;
    global.arrayreverse = arrayreverse2;
    global.array_reverse = arrayreverse2;
    global.arraysort = arraysort2;
    global.array_sort = arraysort2;
    global.arraymap = arraymap2;
    global.array_map = arraymap2;
    global.arrayfilter = arrayfilter2;
    global.array_filter = arrayfilter2;
    global.arrayshift = arrayshift2;
    global.array_shift = arrayshift2;
    global.arrayfind = arrayfind2;
    global.array_find = arrayfind2;
    global.time = time2;
    global.microtime = microtime2;
    global.micro_time = microtime2;
    global.dateyear = dateyear2;
    global.date_year = dateyear2;
    global.datemonth = datemonth2;
    global.date_month = datemonth2;
    global.dateday = dateday2;
    global.date_day = dateday2;
    global.datehour = datehour2;
    global.date_hour = datehour2;
    global.dateminute = dateminute2;
    global.date_minute = dateminute2;
    global.datesecond = datesecond2;
    global.date_second = datesecond2;
    global.dateweekday = dateweekday2;
    global.date_weekday = dateweekday2;
    global.template = template2;
    function formatRuntimeError2(err, label = "OmniFlux Runtime Error") {
      console.error("\n\x1B[1m\x1B[31m" + label + ":\x1B[0m");
      console.error("\x1B[33m" + err.message + "\x1B[0m");
      if (err.stack) {
        const stackLines = err.stack.split("\n");
        const lineMatch = stackLines.find(
          (line) => line.includes("/") && !line.includes("node:internal") && !line.includes("node_modules")
        );
        if (lineMatch) {
          const match3 = lineMatch.match(/\(([^)]+):(\d+):(\d+)\)/) || lineMatch.match(/at\s+(.+?):(\d+):(\d+)/);
          if (match3) {
            const filePath = match3[1];
            const lineNum = parseInt(match3[2], 10);
            try {
              const content = fs2.readFileSync(filePath, "utf8");
              const fileLines = content.split("\n");
              const indicesToCheck = [lineNum, lineNum - 1, lineNum - 2];
              for (const i of indicesToCheck) {
                if (i >= 0 && i < fileLines.length) {
                  const targetLine = fileLines[i];
                  const ofLineMatch = targetLine.match(/\/\/\! OF_LINE: (.+?)(?:\r?\n|$)/) || targetLine.match(/\/\/ OF_LINE: (.+?)(?:\r?\n|$)/);
                  if (ofLineMatch) {
                    console.error("\x1B[1m\x1B[32mAt: " + ofLineMatch[1] + "\x1B[0m");
                    console.error();
                    return;
                  }
                }
              }
            } catch (e) {
            }
            console.error("\x1B[2mAt: " + path2.basename(filePath) + ":" + lineNum + "\x1B[0m");
          }
        }
      }
      console.error();
    }
    process.on("uncaughtException", (err) => {
      const hooks = global.__on_error_hooks || [];
      if (hooks.length > 0) {
        for (const hook of hooks) {
          hook(err).catch(() => {
          });
        }
        return;
      }
      formatRuntimeError2(err);
      process.exit(1);
    });
    process.on("unhandledRejection", (reason) => {
      const hooks = global.__on_error_hooks || [];
      if (hooks.length > 0) {
        for (const hook of hooks) {
          hook(reason).catch(() => {
          });
        }
        return;
      }
      const err = reason instanceof Error ? reason : new Error(String(reason));
      formatRuntimeError2(err, "OmniFlux Runtime Error (Unhandled Rejection)");
      process.exit(1);
    });
    //! OF_LINE: preprocessor.of:1
    //! OF_LINE: preprocessor.of:2
    //! OF_LINE: preprocessor.of:3
    //! OF_LINE: preprocessor.of:4
    //! OF_LINE: preprocessor.of:5
    //! OF_LINE: preprocessor.of:6
    //! OF_LINE: preprocessor.of:7
    //! OF_LINE: preprocessor.of:8
    //! OF_LINE: preprocessor.of:9
    //! OF_LINE: preprocessor.of:10
    //! OF_LINE: preprocessor.of:11
    require_symbols();
    //! OF_LINE: preprocessor.of:12
    //! OF_LINE: preprocessor.of:13
    //! OF_LINE: preprocessor.of:14
    //! OF_LINE: preprocessor.of:15
    //! OF_LINE: preprocessor.of:16
    //! OF_LINE: preprocessor.of:17
    //! OF_LINE: preprocessor.of:18
    //! OF_LINE: preprocessor.of:19
    //! OF_LINE: preprocessor.of:20
    //! OF_LINE: preprocessor.of:21
    //! OF_LINE: preprocessor.of:22
    //! OF_LINE: preprocessor.of:23
    //! OF_LINE: preprocessor.of:24
    //! OF_LINE: preprocessor.of:25
    async function preprocess_ai_directives(content) {
      try {
        //! OF_LINE: preprocessor.of:26
        let processed = content;
        //! OF_LINE: preprocessor.of:27
        //! OF_LINE: preprocessor.of:28
        try {
          //! OF_LINE: preprocessor.of:29
          let result = "";
          //! OF_LINE: preprocessor.of:30
          let i = 0;
          //! OF_LINE: preprocessor.of:31
          const len3 = content.length;
          //! OF_LINE: preprocessor.of:32
          let in_string = "";
          //! OF_LINE: preprocessor.of:33
          let in_node_block = false;
          //! OF_LINE: preprocessor.of:34
          let in_block_comment = false;
          //! OF_LINE: preprocessor.of:35
          //! OF_LINE: preprocessor.of:36
          const is_line_start = (idx) => {
            //! OF_LINE: preprocessor.of:37
            let p = idx - 1;
            //! OF_LINE: preprocessor.of:38
            while (p >= 0 && content[p] !== "\n") {
              //! OF_LINE: preprocessor.of:39
              if (content[p] !== " " && content[p] !== "	" && content[p] !== "\r") {
                //! OF_LINE: preprocessor.of:40
                return false;
                //! OF_LINE: preprocessor.of:41
              }
              //! OF_LINE: preprocessor.of:42
              p--;
              //! OF_LINE: preprocessor.of:43
            }
            //! OF_LINE: preprocessor.of:44
            return true;
            //! OF_LINE: preprocessor.of:45
          };
          //! OF_LINE: preprocessor.of:46
          //! OF_LINE: preprocessor.of:47
          while (i < len3) {
            //! OF_LINE: preprocessor.of:48
            const ch = content[i];
            //! OF_LINE: preprocessor.of:49
            //! OF_LINE: preprocessor.of:50
            if (in_node_block) {
              //! OF_LINE: preprocessor.of:51
              if (ch === "@" && content[i + 1] === "}") {
                //! OF_LINE: preprocessor.of:52
                result += "@}";
                //! OF_LINE: preprocessor.of:53
                i += 2;
                //! OF_LINE: preprocessor.of:54
                in_node_block = false;
                //! OF_LINE: preprocessor.of:55
              } else {
                //! OF_LINE: preprocessor.of:56
                result += ch;
                //! OF_LINE: preprocessor.of:57
                i++;
                //! OF_LINE: preprocessor.of:58
              }
              //! OF_LINE: preprocessor.of:59
              continue;
              //! OF_LINE: preprocessor.of:60
            }
            //! OF_LINE: preprocessor.of:61
            //! OF_LINE: preprocessor.of:62
            if (in_block_comment) {
              //! OF_LINE: preprocessor.of:63
              if (ch === "/" && content[i + 1] === "*") {
                //! OF_LINE: preprocessor.of:64
                result += "/*";
                //! OF_LINE: preprocessor.of:65
                i += 2;
                //! OF_LINE: preprocessor.of:66
              } else if (ch === "*" && content[i + 1] === "/") {
                //! OF_LINE: preprocessor.of:67
                result += "*/";
                //! OF_LINE: preprocessor.of:68
                i += 2;
                //! OF_LINE: preprocessor.of:69
                in_block_comment = false;
                //! OF_LINE: preprocessor.of:70
              } else {
                //! OF_LINE: preprocessor.of:71
                result += ch;
                //! OF_LINE: preprocessor.of:72
                i++;
                //! OF_LINE: preprocessor.of:73
              }
              //! OF_LINE: preprocessor.of:74
              continue;
              //! OF_LINE: preprocessor.of:75
            }
            //! OF_LINE: preprocessor.of:76
            //! OF_LINE: preprocessor.of:77
            if (in_string !== "") {
              //! OF_LINE: preprocessor.of:78
              if (ch === "\\") {
                //! OF_LINE: preprocessor.of:79
                result += ch + (content[i + 1] || "");
                //! OF_LINE: preprocessor.of:80
                i += 2;
                //! OF_LINE: preprocessor.of:81
              } else if (ch === in_string) {
                //! OF_LINE: preprocessor.of:82
                result += ch;
                //! OF_LINE: preprocessor.of:83
                i++;
                //! OF_LINE: preprocessor.of:84
                in_string = "";
                //! OF_LINE: preprocessor.of:85
              } else {
                //! OF_LINE: preprocessor.of:86
                result += ch;
                //! OF_LINE: preprocessor.of:87
                i++;
                //! OF_LINE: preprocessor.of:88
              }
              //! OF_LINE: preprocessor.of:89
              continue;
              //! OF_LINE: preprocessor.of:90
            }
            //! OF_LINE: preprocessor.of:91
            //! OF_LINE: preprocessor.of:92
            if (ch === "@" && content[i + 1] === "{") {
              //! OF_LINE: preprocessor.of:93
              result += "@{";
              //! OF_LINE: preprocessor.of:94
              i += 2;
              //! OF_LINE: preprocessor.of:95
              in_node_block = true;
              //! OF_LINE: preprocessor.of:96
              continue;
              //! OF_LINE: preprocessor.of:97
            }
            //! OF_LINE: preprocessor.of:98
            //! OF_LINE: preprocessor.of:99
            if (ch === "/" && content[i + 1] === "*") {
              //! OF_LINE: preprocessor.of:100
              result += "/*";
              //! OF_LINE: preprocessor.of:101
              i += 2;
              //! OF_LINE: preprocessor.of:102
              in_block_comment = true;
              //! OF_LINE: preprocessor.of:103
              continue;
              //! OF_LINE: preprocessor.of:104
            }
            //! OF_LINE: preprocessor.of:105
            //! OF_LINE: preprocessor.of:106
            if (ch === "#" || ch === "/" && content[i + 1] === "/") {
              //! OF_LINE: preprocessor.of:107
              while (i < len3 && content[i] !== "\n") {
                //! OF_LINE: preprocessor.of:108
                result += content[i];
                //! OF_LINE: preprocessor.of:109
                i++;
                //! OF_LINE: preprocessor.of:110
              }
              //! OF_LINE: preprocessor.of:111
              continue;
              //! OF_LINE: preprocessor.of:112
            }
            //! OF_LINE: preprocessor.of:113
            //! OF_LINE: preprocessor.of:114
            if (ch === '"' || ch === "'") {
              //! OF_LINE: preprocessor.of:115
              in_string = ch;
              //! OF_LINE: preprocessor.of:116
              result += ch;
              //! OF_LINE: preprocessor.of:117
              i++;
              //! OF_LINE: preprocessor.of:118
              continue;
              //! OF_LINE: preprocessor.of:119
            }
            //! OF_LINE: preprocessor.of:120
            //! OF_LINE: preprocessor.of:121
            if (ch === ">" && content[i + 1] === ">" && content[i + 2] === ">" && is_line_start(i)) {
              //! OF_LINE: preprocessor.of:122
              let start = i + 3;
              //! OF_LINE: preprocessor.of:123
              let end = i + 3;
              //! OF_LINE: preprocessor.of:124
              while (end < len3 && content[end] !== "\n") {
                //! OF_LINE: preprocessor.of:125
                end++;
                //! OF_LINE: preprocessor.of:126
              }
              //! OF_LINE: preprocessor.of:127
              const directive = content.slice(start, end).trim();
              //! OF_LINE: preprocessor.of:128
              const escaped = directive.replace(new RegExp("\\\\", "g"), "\\\\").replace(new RegExp('"', "g"), '\\"');
              //! OF_LINE: preprocessor.of:131
              result += `ai("${escaped}")`;
              //! OF_LINE: preprocessor.of:132
              i = end;
              //! OF_LINE: preprocessor.of:133
              continue;
              //! OF_LINE: preprocessor.of:134
            }
            //! OF_LINE: preprocessor.of:135
            //! OF_LINE: preprocessor.of:136
            result += ch;
            //! OF_LINE: preprocessor.of:137
            i++;
            //! OF_LINE: preprocessor.of:138
          }
          //! OF_LINE: preprocessor.of:139
          processed = result;
          //! OF_LINE: preprocessor.of:140
        } catch (e) {
        }
        //! OF_LINE: preprocessor.of:141
        //! OF_LINE: preprocessor.of:142
        return processed;
        //! OF_LINE: preprocessor.of:143
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: preprocessor.of:144
    //! OF_LINE: preprocessor.of:145
    //! OF_LINE: preprocessor.of:146
    //! OF_LINE: preprocessor.of:147
    //! OF_LINE: preprocessor.of:148
    //! OF_LINE: preprocessor.of:149
    //! OF_LINE: preprocessor.of:150
    //! OF_LINE: preprocessor.of:151
    //! OF_LINE: preprocessor.of:152
    //! OF_LINE: preprocessor.of:153
    //! OF_LINE: preprocessor.of:154
    async function strip_comments2(content) {
      try {
        //! OF_LINE: preprocessor.of:155
        content = await preprocess_ai_directives(content);
        //! OF_LINE: preprocessor.of:156
        let result = "";
        //! OF_LINE: preprocessor.of:157
        let i = 0;
        //! OF_LINE: preprocessor.of:158
        let length = len2(content);
        //! OF_LINE: preprocessor.of:159
        let in_string = "";
        //! OF_LINE: preprocessor.of:160
        let in_node_block = false;
        //! OF_LINE: preprocessor.of:161
        let block_comment_depth = 0;
        //! OF_LINE: preprocessor.of:162
        //! OF_LINE: preprocessor.of:163
        while (i < length) {
          //! OF_LINE: preprocessor.of:164
          let ch = strsub2(content, i, 1);
          //! OF_LINE: preprocessor.of:165
          //! OF_LINE: preprocessor.of:166
          //! OF_LINE: preprocessor.of:167
          if (in_node_block) {
            //! OF_LINE: preprocessor.of:168
            let node_close = "@}";
            //! OF_LINE: preprocessor.of:169
            if (ch == "@" && strsub2(content, i + 1, 1) == "}") {
              //! OF_LINE: preprocessor.of:170
              result = result + node_close;
              //! OF_LINE: preprocessor.of:171
              i = i + 2;
              //! OF_LINE: preprocessor.of:172
              in_node_block = false;
              //! OF_LINE: preprocessor.of:173
            } else {
              //! OF_LINE: preprocessor.of:174
              result = result + ch;
              //! OF_LINE: preprocessor.of:175
              i = i + 1;
              //! OF_LINE: preprocessor.of:176
            }
            //! OF_LINE: preprocessor.of:177
            continue;
            //! OF_LINE: preprocessor.of:178
          }
          //! OF_LINE: preprocessor.of:179
          //! OF_LINE: preprocessor.of:180
          //! OF_LINE: preprocessor.of:181
          //! OF_LINE: preprocessor.of:182
          if (block_comment_depth > 0) {
            //! OF_LINE: preprocessor.of:183
            if (ch == "/" && strsub2(content, i + 1, 1) == "*") {
              //! OF_LINE: preprocessor.of:184
              block_comment_depth = block_comment_depth + 1;
              //! OF_LINE: preprocessor.of:185
              i = i + 2;
              //! OF_LINE: preprocessor.of:186
            } else if (ch == "*" && strsub2(content, i + 1, 1) == "/") {
              //! OF_LINE: preprocessor.of:187
              block_comment_depth = block_comment_depth - 1;
              //! OF_LINE: preprocessor.of:188
              i = i + 2;
              //! OF_LINE: preprocessor.of:189
            } else {
              //! OF_LINE: preprocessor.of:190
              //! OF_LINE: preprocessor.of:191
              if (ch == "\n") {
                //! OF_LINE: preprocessor.of:192
                result = result + "\n";
                //! OF_LINE: preprocessor.of:193
              }
              //! OF_LINE: preprocessor.of:194
              i = i + 1;
              //! OF_LINE: preprocessor.of:195
            }
            //! OF_LINE: preprocessor.of:196
            continue;
            //! OF_LINE: preprocessor.of:197
          }
          //! OF_LINE: preprocessor.of:198
          //! OF_LINE: preprocessor.of:199
          //! OF_LINE: preprocessor.of:200
          if (in_string != "") {
            //! OF_LINE: preprocessor.of:201
            if (ch == "\\") {
              //! OF_LINE: preprocessor.of:202
              //! OF_LINE: preprocessor.of:203
              result = result + ch + strsub2(content, i + 1, 1);
              //! OF_LINE: preprocessor.of:204
              i = i + 2;
              //! OF_LINE: preprocessor.of:205
            } else if (ch == in_string) {
              //! OF_LINE: preprocessor.of:206
              result = result + ch;
              //! OF_LINE: preprocessor.of:207
              i = i + 1;
              //! OF_LINE: preprocessor.of:208
              in_string = "";
              //! OF_LINE: preprocessor.of:209
            } else {
              //! OF_LINE: preprocessor.of:210
              result = result + ch;
              //! OF_LINE: preprocessor.of:211
              i = i + 1;
              //! OF_LINE: preprocessor.of:212
            }
            //! OF_LINE: preprocessor.of:213
            continue;
            //! OF_LINE: preprocessor.of:214
          }
          //! OF_LINE: preprocessor.of:215
          //! OF_LINE: preprocessor.of:216
          //! OF_LINE: preprocessor.of:217
          let node_open = "@{";
          //! OF_LINE: preprocessor.of:218
          if (ch == "@" && strsub2(content, i + 1, 1) == "{") {
            //! OF_LINE: preprocessor.of:219
            result = result + node_open;
            //! OF_LINE: preprocessor.of:220
            i = i + 2;
            //! OF_LINE: preprocessor.of:221
            in_node_block = true;
            //! OF_LINE: preprocessor.of:222
            continue;
            //! OF_LINE: preprocessor.of:223
          }
          //! OF_LINE: preprocessor.of:224
          //! OF_LINE: preprocessor.of:225
          //! OF_LINE: preprocessor.of:226
          if (ch == "/" && strsub2(content, i + 1, 1) == "*") {
            //! OF_LINE: preprocessor.of:227
            i = i + 2;
            //! OF_LINE: preprocessor.of:228
            block_comment_depth = 1;
            //! OF_LINE: preprocessor.of:229
            continue;
            //! OF_LINE: preprocessor.of:230
          }
          //! OF_LINE: preprocessor.of:231
          //! OF_LINE: preprocessor.of:232
          //! OF_LINE: preprocessor.of:233
          if (ch == "#" || ch == "/" && strsub2(content, i + 1, 1) == "/") {
            //! OF_LINE: preprocessor.of:234
            while (i < length && strsub2(content, i, 1) != "\n") {
              //! OF_LINE: preprocessor.of:235
              i = i + 1;
              //! OF_LINE: preprocessor.of:236
            }
            //! OF_LINE: preprocessor.of:237
            continue;
            //! OF_LINE: preprocessor.of:238
          }
          //! OF_LINE: preprocessor.of:239
          //! OF_LINE: preprocessor.of:240
          //! OF_LINE: preprocessor.of:241
          if (ch == '"' || ch == "'") {
            //! OF_LINE: preprocessor.of:242
            in_string = ch;
            //! OF_LINE: preprocessor.of:243
            result = result + ch;
            //! OF_LINE: preprocessor.of:244
            i = i + 1;
            //! OF_LINE: preprocessor.of:245
            continue;
            //! OF_LINE: preprocessor.of:246
          }
          //! OF_LINE: preprocessor.of:247
          //! OF_LINE: preprocessor.of:248
          //! OF_LINE: preprocessor.of:249
          result = result + ch;
          //! OF_LINE: preprocessor.of:250
          i = i + 1;
          //! OF_LINE: preprocessor.of:251
        }
        //! OF_LINE: preprocessor.of:252
        //! OF_LINE: preprocessor.of:253
        return result;
        //! OF_LINE: preprocessor.of:254
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: preprocessor.of:255
    //! OF_LINE: preprocessor.of:256
    //! OF_LINE: preprocessor.of:257
    //! OF_LINE: preprocessor.of:258
    //! OF_LINE: preprocessor.of:259
    //! OF_LINE: preprocessor.of:260
    //! OF_LINE: preprocessor.of:261
    //! OF_LINE: preprocessor.of:262
    //! OF_LINE: preprocessor.of:263
    //! OF_LINE: preprocessor.of:264
    async function resolve_include2(inc_path, src_file) {
      try {
        //! OF_LINE: preprocessor.of:265
        let src_dir = pathdirname2(pathresolve2(src_file));
        //! OF_LINE: preprocessor.of:266
        let cwd = pathresolve2(".");
        //! OF_LINE: preprocessor.of:267
        //! OF_LINE: preprocessor.of:268
        let candidates = [
          //! OF_LINE: preprocessor.of:269
          pathjoin2(src_dir, inc_path),
          //! OF_LINE: preprocessor.of:270
          pathjoin2(cwd, inc_path),
          //! OF_LINE: preprocessor.of:271
          pathjoin2("/usr/local/share/omniflux", inc_path),
          //! OF_LINE: preprocessor.of:272
          pathjoin2("/usr/share/omniflux", inc_path)
          //! OF_LINE: preprocessor.of:273
        ];
        //! OF_LINE: preprocessor.of:274
        //! OF_LINE: preprocessor.of:275
        for (let candidate of candidates) {
          //! OF_LINE: preprocessor.of:276
          if (fileexists2(candidate)) {
            //! OF_LINE: preprocessor.of:277
            return pathresolve2(candidate);
            //! OF_LINE: preprocessor.of:278
          }
          //! OF_LINE: preprocessor.of:279
        }
        //! OF_LINE: preprocessor.of:280
        //! OF_LINE: preprocessor.of:281
        return null;
        //! OF_LINE: preprocessor.of:282
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: preprocessor.of:283
    //! OF_LINE: preprocessor.of:284
    //! OF_LINE: preprocessor.of:285
    //! OF_LINE: preprocessor.of:286
    async function load_source(filepath) {
      try {
        //! OF_LINE: preprocessor.of:287
        let raw = fileread2(filepath);
        //! OF_LINE: preprocessor.of:288
        return await strip_comments2(raw);
        //! OF_LINE: preprocessor.of:289
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: preprocessor.of:290
    //! OF_LINE: preprocessor.of:291
    //! OF_LINE: preprocessor.of:292
    //! OF_LINE: preprocessor.of:293
    //! OF_LINE: preprocessor.of:294
    //! OF_LINE: preprocessor.of:295
    async function get_all_dependencies2(filepath, visited) {
      try {
        //! OF_LINE: preprocessor.of:296
        let abs_path = pathresolve2(filepath);
        //! OF_LINE: preprocessor.of:297
        //! OF_LINE: preprocessor.of:298
        if (!visited) {
          //! OF_LINE: preprocessor.of:299
          visited = {};
          //! OF_LINE: preprocessor.of:300
        }
        //! OF_LINE: preprocessor.of:301
        //! OF_LINE: preprocessor.of:302
        //! OF_LINE: preprocessor.of:303
        if (visited[abs_path]) {
          //! OF_LINE: preprocessor.of:304
          return [];
          //! OF_LINE: preprocessor.of:305
        }
        //! OF_LINE: preprocessor.of:306
        visited[abs_path] = true;
        //! OF_LINE: preprocessor.of:307
        //! OF_LINE: preprocessor.of:308
        let content = await load_source(abs_path);
        //! OF_LINE: preprocessor.of:309
        let lines = strsplit2(content, "\n");
        //! OF_LINE: preprocessor.of:310
        let deps = [];
        //! OF_LINE: preprocessor.of:311
        //! OF_LINE: preprocessor.of:312
        for (let line of lines) {
          //! OF_LINE: preprocessor.of:313
          let trimmed = strtrim2(line);
          //! OF_LINE: preprocessor.of:314
          let m = match2(trimmed, `/^include\\s+["']?([^"'\\s;]+)["']?/i`);
          //! OF_LINE: preprocessor.of:315
          if (m) {
            //! OF_LINE: preprocessor.of:316
            let inc_path = m[0];
            //! OF_LINE: preprocessor.of:317
            let resolved = await resolve_include2(inc_path, abs_path);
            //! OF_LINE: preprocessor.of:318
            if (resolved) {
              //! OF_LINE: preprocessor.of:319
              let sub_deps = await get_all_dependencies2(resolved, visited);
              //! OF_LINE: preprocessor.of:320
              for (let dep of sub_deps) {
                //! OF_LINE: preprocessor.of:321
                arraypush2(deps, dep);
                //! OF_LINE: preprocessor.of:322
              }
              //! OF_LINE: preprocessor.of:323
            }
            //! OF_LINE: preprocessor.of:324
          }
          //! OF_LINE: preprocessor.of:325
        }
        //! OF_LINE: preprocessor.of:326
        //! OF_LINE: preprocessor.of:327
        arraypush2(deps, abs_path);
        //! OF_LINE: preprocessor.of:328
        return deps;
        //! OF_LINE: preprocessor.of:329
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: preprocessor.of:330
    //! OF_LINE: preprocessor.of:331
    global.preprocess_ai_directives = preprocess_ai_directives;
    global.strip_comments = strip_comments2;
    global.resolve_include = resolve_include2;
    global.load_source = load_source;
    global.get_all_dependencies = get_all_dependencies2;
    if (require.main === module2) {
      (async () => {
        const hooks = global.__on_start_hooks || [];
        for (const hook of hooks) {
          await hook();
        }
      })().catch((err) => {
        if (typeof formatRuntimeError2 === "function") {
          formatRuntimeError2(err);
        } else {
          console.error(err);
        }
        process.exit(1);
      });
    }
  }
});

// .omniflux_cache/stdlib/system.js
var require_system = __commonJS({
  ".omniflux_cache/stdlib/system.js"(exports2, module2) {
    var fs2 = require("fs");
    var util2 = require("util");
    var path2 = require("path");
    global.args = process.argv.slice(2);
    global.vars = new Proxy({ args: global.args }, {
      get(target, prop) {
        return prop in target ? target[prop] : null;
      },
      set(target, prop, value) {
        target[prop] = value;
        return true;
      }
    });
    function sprintf2(format, ...args2) {
      if (typeof format !== "string") {
        return util2.format(format, ...args2);
      }
      let argIndex = 0;
      const hasSpecifiers = /%([+-]?)(0?\d*)(?:\.(\d+))?([asdijf%])/g.test(format);
      if (!hasSpecifiers) {
        return util2.format(format, ...args2);
      }
      let formatted = format.replace(/%(?<flags>[+-]?)(?<width>0?\d*)(?:\.(?<precision>\d+))?(?<type>[asdijf%])/g, (match3, flags, widthStr, precisionStr, type) => {
        if (type === "%")
          return "%";
        if (argIndex >= args2.length)
          return match3;
        const val = args2[argIndex++];
        let padChar = " ";
        let width = 0;
        let leftAlign = flags === "-";
        if (widthStr) {
          if (widthStr.startsWith("0")) {
            padChar = "0";
            width = parseInt(widthStr, 10);
          } else {
            width = parseInt(widthStr, 10);
          }
        }
        let precision = precisionStr ? parseInt(precisionStr, 10) : -1;
        let result = "";
        if (type === "d" || type === "i") {
          let intVal = parseInt(val, 10);
          if (isNaN(intVal))
            intVal = 0;
          result = intVal.toString();
          if (precision >= 0) {
            result = result.padStart(precision, "0");
          }
        } else if (type === "f") {
          let floatVal = parseFloat(val);
          if (isNaN(floatVal))
            floatVal = 0;
          if (precision >= 0) {
            result = floatVal.toFixed(precision);
          } else {
            result = floatVal.toString();
          }
        } else if (type === "s") {
          result = String(val);
          if (precision >= 0) {
            result = result.slice(0, precision);
          }
        } else if (type === "j") {
          result = JSON.stringify(val);
        } else {
          result = String(val);
        }
        if (result.length < width) {
          const padLen = width - result.length;
          const padding = padChar.repeat(padLen);
          result = leftAlign ? result + padding : padding + result;
        }
        return result;
      });
      if (argIndex < args2.length) {
        const leftover = args2.slice(argIndex).map((x) => typeof x === "object" ? util2.inspect(x) : String(x));
        formatted += " " + leftover.join(" ");
      }
      return formatted;
    }
    function print2(format, ...args2) {
      console.log(sprintf2(format, ...args2));
    }
    function printf2(format, ...args2) {
      process.stdout.write(sprintf2(format, ...args2));
    }
    var _rl2 = null;
    function _getRL2() {
      if (!_rl2 && process.stdin.isTTY) {
        const readline = require("readline");
        _rl2 = readline.createInterface({
          input: process.stdin,
          output: process.stdout,
          terminal: true,
          historySize: 200
        });
        _rl2.on("close", () => {
          _rl2 = null;
        });
      }
      return _rl2;
    }
    function input2(promptMessage) {
      const rl = _getRL2();
      if (rl) {
        return new Promise((resolve) => {
          rl.question(promptMessage !== void 0 ? String(promptMessage) : "", resolve);
        });
      }
      if (promptMessage !== void 0) {
        process.stdout.write(String(promptMessage));
      }
      const buffer = Buffer.alloc(65536);
      let bytesRead = 0;
      try {
        bytesRead = fs2.readSync(0, buffer, 0, 65536, null);
      } catch (e) {
        return "";
      }
      return buffer.toString("utf8", 0, bytesRead).replace(/\r?\n$/, "");
    }
    global.input = input2;
    function readchar2() {
      const hasRaw = typeof process.stdin.setRawMode === "function";
      const isRaw = process.stdin.isRaw;
      if (hasRaw) {
        process.stdin.setRawMode(true);
      }
      const buffer = Buffer.alloc(1);
      let bytesRead = 0;
      try {
        bytesRead = fs2.readSync(0, buffer, 0, 1, null);
      } catch (e) {
      } finally {
        if (hasRaw) {
          process.stdin.setRawMode(isRaw);
        }
      }
      const char = buffer.toString("utf8", 0, bytesRead);
      if (char === "") {
        process.exit(130);
      }
      return char;
    }
    function fileread2(path3) {
      return fs2.readFileSync(path3, "utf8");
    }
    function filewrite2(path3, data) {
      fs2.writeFileSync(path3, typeof data === "object" ? JSON.stringify(data, null, 2) : data, "utf8");
    }
    function fileexists2(path3) {
      return fs2.existsSync(path3);
    }
    function filecopy2(src, dest) {
      fs2.copyFileSync(src, dest);
    }
    function filerename2(src, dest) {
      fs2.renameSync(src, dest);
    }
    function fileappend2(path3, data) {
      fs2.appendFileSync(path3, typeof data === "object" ? JSON.stringify(data, null, 2) : data, "utf8");
    }
    function fprintf2(path3, format, ...args2) {
      const data = sprintf2(format, ...args2);
      fs2.appendFileSync(path3, data, "utf8");
    }
    function fprint2(path3, format, ...args2) {
      const data = sprintf2(format, ...args2) + "\n";
      fs2.appendFileSync(path3, data, "utf8");
    }
    function filedelete2(path3) {
      fs2.unlinkSync(path3);
    }
    function dirlist2(path3) {
      return fs2.readdirSync(path3);
    }
    function filestat2(path3) {
      const stats = fs2.statSync(path3);
      return {
        size: stats.size,
        isdirectory: stats.isDirectory(),
        is_directory: stats.isDirectory(),
        isfile: stats.isFile(),
        is_file: stats.isFile(),
        createdat: stats.birthtimeMs,
        created_at: stats.birthtimeMs,
        modifiedat: stats.mtimeMs,
        modified_at: stats.mtimeMs
      };
    }
    function dircreate2(path3) {
      if (typeof path3 !== "string")
        throw new Error("Directory path must be a string");
      fs2.mkdirSync(path3, { recursive: true });
    }
    function scriptdir2() {
      const path3 = require("path");
      return path3.dirname(require.main.filename);
    }
    function setenv2(name, value) {
      if (typeof name !== "string")
        throw new Error("Environment variable name must be a string");
      process.env[name] = value !== void 0 ? String(value) : "";
    }
    function getenv2(name) {
      if (typeof name !== "string")
        throw new Error("Environment variable name must be a string");
      return process.env[name] !== void 0 ? process.env[name] : null;
    }
    function sha2562(text) {
      if (typeof text !== "string")
        throw new Error("Input to sha256 must be a string");
      const crypto = require("crypto");
      return crypto.createHash("sha256").update(text).digest("hex");
    }
    function getcookie2(req, name) {
      if (!req || !req.headers || !req.headers.cookie)
        return null;
      const cookies = req.headers.cookie.split(";");
      for (let c of cookies) {
        const parts = c.split("=");
        if (parts[0].trim() === name)
          return parts[1].trim();
      }
      return null;
    }
    function setcookie2(res, name, value, options = {}) {
      if (!res || typeof res.cookie !== "function") {
        throw new Error("First argument to setcookie must be an HTTP response object");
      }
      res.cookie(name, value, options);
    }
    var dbFilePath2 = process.env.DB_FILE || "db.json";
    var cachedDb2 = null;
    var cachedDbMtime2 = 0;
    function dbsetfile2(path3) {
      if (typeof path3 !== "string")
        throw new Error("Database file path must be a string");
      dbFilePath2 = path3;
      cachedDb2 = null;
      cachedDbMtime2 = 0;
    }
    function loadDb2() {
      try {
        if (fs2.existsSync(dbFilePath2)) {
          const stats = fs2.statSync(dbFilePath2);
          if (cachedDb2 && stats.mtimeMs === cachedDbMtime2) {
            return cachedDb2;
          }
          const content = fs2.readFileSync(dbFilePath2, "utf8");
          cachedDb2 = JSON.parse(content || "{}");
          cachedDbMtime2 = stats.mtimeMs;
          return cachedDb2;
        }
      } catch (e) {
      }
      if (!cachedDb2) {
        cachedDb2 = {};
        cachedDbMtime2 = 0;
      }
      return cachedDb2;
    }
    function saveDb2(data) {
      cachedDb2 = data;
      fs2.writeFileSync(dbFilePath2, JSON.stringify(data, null, 2), "utf8");
      try {
        if (fs2.existsSync(dbFilePath2)) {
          cachedDbMtime2 = fs2.statSync(dbFilePath2).mtimeMs;
        }
      } catch (e) {
        cachedDbMtime2 = Date.now();
      }
    }
    function dbinsert2(collection, doc) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      if (!doc || typeof doc !== "object")
        throw new Error("Document must be an object");
      const db = loadDb2();
      if (!db[collection]) {
        db[collection] = [];
      }
      const newDoc = { ...doc };
      if (newDoc.id === void 0) {
        newDoc.id = Date.now().toString(36) + Math.random().toString(36).substring(2, 6);
      }
      db[collection].push(newDoc);
      saveDb2(db);
      return newDoc;
    }
    function dbselect2(collection, filter) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      const db = loadDb2();
      const list = db[collection] || [];
      if (!filter || typeof filter !== "object" || Object.keys(filter).length === 0) {
        return list.map((d) => ({ ...d }));
      }
      return list.filter((doc) => {
        for (const key in filter) {
          if (doc[key] !== filter[key]) {
            return false;
          }
        }
        return true;
      }).map((d) => ({ ...d }));
    }
    function dbupdate2(collection, filter, update) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      if (!update || typeof update !== "object")
        throw new Error("Update payload must be an object");
      const db = loadDb2();
      const list = db[collection] || [];
      let count = 0;
      for (let i = 0; i < list.length; i++) {
        const doc = list[i];
        let matches = true;
        if (filter && typeof filter === "object") {
          for (const key in filter) {
            if (doc[key] !== filter[key]) {
              matches = false;
              break;
            }
          }
        }
        if (matches) {
          list[i] = { ...doc, ...update };
          count++;
        }
      }
      if (count > 0) {
        db[collection] = list;
        saveDb2(db);
      }
      return count;
    }
    function dbdelete2(collection, filter) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      const db = loadDb2();
      const list = db[collection] || [];
      let count = 0;
      const newList = [];
      for (const doc of list) {
        let matches = true;
        if (filter && typeof filter === "object") {
          for (const key in filter) {
            if (doc[key] !== filter[key]) {
              matches = false;
              break;
            }
          }
        }
        if (matches) {
          count++;
        } else {
          newList.push(doc);
        }
      }
      if (count > 0) {
        db[collection] = newList;
        saveDb2(db);
      }
      return count;
    }
    var redisModule2 = null;
    var redisClient2 = null;
    async function getRedisClient2() {
      if (!redisModule2) {
        redisModule2 = require("redis");
      }
      if (!redisClient2) {
        const url = process.env.REDIS_URL || "redis://localhost:6379";
        redisClient2 = redisModule2.createClient({ url });
        await redisClient2.connect();
      }
      return redisClient2;
    }
    async function cacheset2(key, val, ttl) {
      const client = await getRedisClient2();
      const stringVal = typeof val === "object" ? JSON.stringify(val) : String(val);
      if (ttl) {
        await client.setEx(key, ttl, stringVal);
      } else {
        await client.set(key, stringVal);
      }
    }
    async function cacheget2(key) {
      const client = await getRedisClient2();
      const val = await client.get(key);
      if (!val)
        return null;
      try {
        return JSON.parse(val);
      } catch (e) {
        return val;
      }
    }
    function describe2(val) {
      if (val === null)
        return "null";
      if (val === void 0)
        return "undefined";
      if (Array.isArray(val))
        return "array";
      return typeof val;
    }
    global.describe = describe2;
    function jsonparse2(str) {
      try {
        return JSON.parse(String(str || ""));
      } catch (e) {
        return null;
      }
    }
    global.jsonparse = jsonparse2;
    global.jsondecode = jsonparse2;
    function jsonencode2(val, pretty = false) {
      try {
        return pretty ? JSON.stringify(val, null, 2) : JSON.stringify(val);
      } catch (e) {
        return "null";
      }
    }
    global.jsonencode = jsonencode2;
    function len2(val) {
      if (val === null || val === void 0)
        return 0;
      return val.length || 0;
    }
    function toRegExp2(regex) {
      if (regex instanceof RegExp) {
        return regex;
      }
      if (typeof regex === "string") {
        const firstChar = regex.charAt(0);
        if (/^[^a-zA-Z0-9\s\\]/.test(firstChar)) {
          const lastDelim = regex.lastIndexOf(firstChar);
          if (lastDelim > 0) {
            const pattern = regex.substring(1, lastDelim);
            const flags = regex.substring(lastDelim + 1);
            return new RegExp(pattern, flags);
          }
        }
        return new RegExp(regex);
      }
      return null;
    }
    function strsplit2(str, sep) {
      if (typeof str !== "string")
        return [];
      if (sep === void 0)
        return [str];
      const regexObj = toRegExp2(sep);
      if (regexObj) {
        return str.split(regexObj);
      }
      return str.split(sep);
    }
    function match2(str, regex) {
      if (str === null || str === void 0)
        return false;
      const regexObj = toRegExp2(regex);
      if (!regexObj)
        return false;
      const res = String(str).match(regexObj);
      if (!res)
        return false;
      if (res.length > 1) {
        return res.slice(1);
      }
      return true;
    }
    function strtrim2(str, side = "both") {
      const s = String(str || "");
      if (side === "left")
        return s.trimStart();
      if (side === "right")
        return s.trimEnd();
      return s.trim();
    }
    function strsub2(str, start, length) {
      const s = String(str || "");
      if (length === void 0) {
        return s.substring(start);
      }
      return s.substring(start, start + length);
    }
    function strindexof2(str, search) {
      const s = String(str || "");
      return s.indexOf(search);
    }
    function strlastindexof2(str, search) {
      const s = String(str || "");
      return s.lastIndexOf(search);
    }
    function strrepeat2(str, n) {
      const s = String(str || "");
      const count = parseInt(n, 10);
      if (isNaN(count) || count <= 0)
        return "";
      return s.repeat(count);
    }
    function strreplace2(str, search, replacement) {
      const s = String(str || "");
      const r = String(replacement !== void 0 ? replacement : "");
      const regexObj = toRegExp2(search);
      if (regexObj) {
        return s.replace(regexObj, r);
      }
      return s.replaceAll(search, r);
    }
    function strupper2(str) {
      return String(str || "").toUpperCase();
    }
    function strlower2(str) {
      return String(str || "").toLowerCase();
    }
    function arraypush2(arr, item) {
      if (Array.isArray(arr)) {
        arr.push(item);
      }
    }
    function arrayunshift2(arr, item) {
      if (Array.isArray(arr)) {
        arr.unshift(item);
      }
    }
    function arraypop2(arr) {
      if (Array.isArray(arr)) {
        return arr.pop();
      }
      return null;
    }
    function arraycontains2(arr, item) {
      if (Array.isArray(arr)) {
        return arr.includes(item);
      }
      return false;
    }
    function arrayjoin2(arr, sep) {
      if (Array.isArray(arr)) {
        return arr.join(sep);
      }
      return "";
    }
    function arrayslice2(arr, start, end) {
      if (Array.isArray(arr)) {
        return arr.slice(start, end);
      }
      return [];
    }
    function pathjoin2(...paths) {
      const path3 = require("path");
      return path3.join(...paths.map((p) => String(p || "")));
    }
    function pathresolve2(base, relative) {
      const path3 = require("path");
      if (relative === void 0) {
        return path3.resolve(String(base || ""));
      }
      return path3.resolve(String(base || ""), String(relative || ""));
    }
    function pathdirname2(filepath) {
      const path3 = require("path");
      return path3.dirname(String(filepath || ""));
    }
    function pathbasename2(filepath) {
      const path3 = require("path");
      return path3.basename(String(filepath || ""));
    }
    function pathextension2(filepath) {
      const path3 = require("path");
      return path3.extname(String(filepath || ""));
    }
    function pathisabsolute2(filepath) {
      const path3 = require("path");
      return path3.isAbsolute(String(filepath || ""));
    }
    function exit2(code = 0) {
      process.exit(parseInt(code, 10) || 0);
    }
    function arrayreverse2(arr) {
      if (!Array.isArray(arr))
        return [];
      return [...arr].reverse();
    }
    async function asyncMergeSort2(arr, compareFn) {
      if (arr.length <= 1)
        return arr;
      const mid = Math.floor(arr.length / 2);
      const left = arr.slice(0, mid);
      const right = arr.slice(mid);
      return await asyncMerge2(
        await asyncMergeSort2(left, compareFn),
        await asyncMergeSort2(right, compareFn),
        compareFn
      );
    }
    async function asyncMerge2(left, right, compareFn) {
      const result = [];
      let l = 0, r = 0;
      while (l < left.length && r < right.length) {
        const cmp = await compareFn(left[l], right[r]);
        if (cmp <= 0) {
          result.push(left[l]);
          l++;
        } else {
          result.push(right[r]);
          r++;
        }
      }
      return result.concat(left.slice(l)).concat(right.slice(r));
    }
    async function arraysort2(arr, compareFn) {
      if (!Array.isArray(arr))
        return [];
      if (typeof compareFn === "function") {
        const temp = [...arr];
        return await asyncMergeSort2(temp, compareFn);
      }
      return [...arr].sort();
    }
    async function arraymap2(arr, task) {
      if (!Array.isArray(arr))
        return [];
      if (typeof task !== "function")
        return [...arr];
      const results = [];
      for (let i = 0; i < arr.length; i++) {
        results.push(await task(arr[i], i, arr));
      }
      return results;
    }
    async function arrayfilter2(arr, task) {
      if (!Array.isArray(arr))
        return [];
      if (typeof task !== "function")
        return [...arr];
      const results = [];
      for (let i = 0; i < arr.length; i++) {
        if (await task(arr[i], i, arr)) {
          results.push(arr[i]);
        }
      }
      return results;
    }
    function arrayshift2(arr) {
      if (!Array.isArray(arr))
        return null;
      return arr.shift();
    }
    async function arrayfind2(arr, task) {
      if (!Array.isArray(arr))
        return null;
      if (typeof task !== "function")
        return null;
      for (let i = 0; i < arr.length; i++) {
        if (await task(arr[i], i, arr)) {
          return arr[i];
        }
      }
      return null;
    }
    function time2() {
      return Math.floor(Date.now() / 1e3);
    }
    function microtime2(getAsFloat = true) {
      if (getAsFloat === false) {
        const now = Date.now();
        const sec = Math.floor(now / 1e3);
        const msec = now % 1e3;
        return `${(msec / 1e3).toFixed(8)} ${sec}`;
      }
      return Date.now() / 1e3;
    }
    function dateyear2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getFullYear();
    }
    function datemonth2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getMonth() + 1;
    }
    function dateday2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getDate();
    }
    function datehour2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getHours();
    }
    function dateminute2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getMinutes();
    }
    function datesecond2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getSeconds();
    }
    function dateweekday2(ts, format = "number") {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      const day = d.getDay();
      if (format === "short" || format === "text") {
        const days = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];
        return days[day];
      }
      return day;
    }
    function template2(source, context = {}) {
      if (typeof source !== "string")
        return "";
      const appRoot = typeof global !== "undefined" && global.__app_root ? global.__app_root : process.cwd();
      const resolvePath = (p) => path2.isAbsolute(p) ? p : path2.resolve(appRoot, p);
      let html = source;
      try {
        const resolvedSource = resolvePath(source);
        if (fs2.existsSync(resolvedSource)) {
          html = fs2.readFileSync(resolvedSource, "utf8");
        }
        let includeDepth = 0;
        const maxIncludeDepth = 10;
        while (/@include\s*\(\s*["'](.*?)["']\s*\)/.test(html) && includeDepth < maxIncludeDepth) {
          html = html.replace(/@include\s*\(\s*["'](.*?)["']\s*\)/g, (match3, filepath) => {
            const resolvedInclude = resolvePath(filepath);
            if (!fs2.existsSync(resolvedInclude)) {
              throw new Error(`Include file not found: '${filepath}'`);
            }
            return fs2.readFileSync(resolvedInclude, "utf8");
          });
          includeDepth++;
        }
        html = html.replace(/@\}\s*(?=@else)/g, "@}");
        if (html.includes("</body>")) {
          const clientScript = `
<script>
function _of_exec_scripts(container) {
    const scripts = container.querySelectorAll('script');
    for (let oldScript of scripts) {
        const newScript = document.createElement('script');
        for (let attr of oldScript.attributes) {
            newScript.setAttribute(attr.name, attr.value);
        }
        newScript.textContent = oldScript.textContent;
        oldScript.parentNode.replaceChild(newScript, oldScript);
    }
}

document.addEventListener('submit', async (e) => {
    const form = e.target;
    const targetSelector = form.getAttribute('of-target');
    if (!targetSelector) return;
    
    e.preventDefault();
    try {
        const response = await fetch(form.action || window.location.href, {
            method: form.method || 'POST',
            body: new URLSearchParams(new FormData(form))
        });
        const htmlResult = await response.text();
        const targetEl = document.querySelector(targetSelector);
        if (targetEl) {
            targetEl.innerHTML = htmlResult;
            _of_exec_scripts(targetEl);
        }
    } catch (err) {
        console.error('OmniFlux Form Submission Error:', err);
    }
});

document.addEventListener('click', async (e) => {
    const link = e.target.closest('a');
    if (!link) return;
    const targetSelector = link.getAttribute('of-target');
    if (!targetSelector) return;
    
    e.preventDefault();
    try {
        const response = await fetch(link.href);
        const htmlResult = await response.text();
        const targetEl = document.querySelector(targetSelector);
        if (targetEl) {
            targetEl.innerHTML = htmlResult;
            _of_exec_scripts(targetEl);
        }
    } catch (err) {
        console.error('OmniFlux Link Navigation Error:', err);
    }
});
</script>
`;
          html = html.replace("</body>", clientScript + "</body>");
        }
        const tokenRegex = /(@if\s*\(.*?\)\s*\{|@else\s*if\s*\(.*?\)\s*\{|@else\s*\{|@for\s*\(.*?\)\s*\{|@\})/g;
        const parts = html.split(tokenRegex);
        let jsCode = "let _out = '';\n";
        jsCode += "with(global.vars || {}) {\n";
        jsCode += "with(context || {}) {\n";
        for (let part of parts) {
          if (!part)
            continue;
          if (part.startsWith("@if")) {
            const cond = part.match(/@if\s*\((.*?)\)\s*\{/)[1];
            jsCode += `if (${cond}) {
`;
          } else if (part.startsWith("@else if")) {
            const cond = part.match(/@else\s*if\s*\((.*?)\)\s*\{/)[1];
            jsCode += `} else if (${cond}) {
`;
          } else if (part.startsWith("@else")) {
            jsCode += `} else {
`;
          } else if (part.startsWith("@for")) {
            const loopMatch = part.match(/@for\s*\(\s*(?:let|var)?\s*(\w+)\s+of\s+(.*?)\)\s*\{/);
            if (loopMatch) {
              const item = loopMatch[1];
              const list = loopMatch[2];
              jsCode += `for (let ${item} of ${list}) {
`;
            }
          } else if (part === "@}") {
            jsCode += `}
`;
          } else {
            let text = part.replace(/\\/g, "\\\\").replace(/`/g, "\\`").replace(/\$/g, "\\$");
            text = text.replace(/\{\{\s*([\s\S]+?)\s*\}\}/g, "${$1}");
            jsCode += `_out += \`${text}\`;
`;
          }
        }
        jsCode += "}\n}\n";
        jsCode += "return _out;";
        const keys = ["context", ...Object.keys(context)];
        const values = [context, ...Object.values(context)];
        return new Function(...keys, jsCode)(...values);
      } catch (e) {
        return `<!-- Template Error: ${e.message} -->`;
      }
    }
    global.sprintf = sprintf2;
    global.print = print2;
    global.printf = printf2;
    global.fprint = fprint2;
    global.f_print = fprint2;
    global.fprintf = fprintf2;
    global.f_printf = fprintf2;
    global.input = input2;
    global.readchar = readchar2;
    global.read_char = readchar2;
    global.fileread = fileread2;
    global.file_read = fileread2;
    global.filewrite = filewrite2;
    global.file_write = filewrite2;
    global.fileexists = fileexists2;
    global.file_exists = fileexists2;
    global.fileappend = fileappend2;
    global.file_append = fileappend2;
    global.filedelete = filedelete2;
    global.file_delete = filedelete2;
    global.filecopy = filecopy2;
    global.file_copy = filecopy2;
    global.filerename = filerename2;
    global.file_rename = filerename2;
    global.dirlist = dirlist2;
    global.dir_list = dirlist2;
    global.dircreate = dircreate2;
    global.dir_create = dircreate2;
    global.scriptdir = scriptdir2;
    global.script_dir = scriptdir2;
    global.setenv = setenv2;
    global.set_env = setenv2;
    global.getenv = getenv2;
    global.get_env = getenv2;
    global.sha256 = sha2562;
    global.getcookie = getcookie2;
    global.get_cookie = getcookie2;
    global.setcookie = setcookie2;
    global.set_cookie = setcookie2;
    global.filestat = filestat2;
    global.file_stat = filestat2;
    global.dbsetfile = dbsetfile2;
    global.db_set_file = dbsetfile2;
    global.dbinsert = dbinsert2;
    global.db_insert = dbinsert2;
    global.dbselect = dbselect2;
    global.db_select = dbselect2;
    global.dbupdate = dbupdate2;
    global.db_update = dbupdate2;
    global.dbdelete = dbdelete2;
    global.db_delete = dbdelete2;
    global.cacheset = cacheset2;
    global.cache_set = cacheset2;
    global.cacheget = cacheget2;
    global.cache_get = cacheget2;
    global.len = len2;
    global.strsplit = strsplit2;
    global.str_split = strsplit2;
    global.match = match2;
    global.arraypush = arraypush2;
    global.array_push = arraypush2;
    global.arrayunshift = arrayunshift2;
    global.array_unshift = arrayunshift2;
    global.arraypop = arraypop2;
    global.array_pop = arraypop2;
    global.arraycontains = arraycontains2;
    global.array_contains = arraycontains2;
    global.arrayjoin = arrayjoin2;
    global.array_join = arrayjoin2;
    global.arrayslice = arrayslice2;
    global.array_slice = arrayslice2;
    global.strtrim = strtrim2;
    global.str_trim = strtrim2;
    global.strsub = strsub2;
    global.str_sub = strsub2;
    global.strindexof = strindexof2;
    global.str_index_of = strindexof2;
    global.strlastindexof = strlastindexof2;
    global.str_last_index_of = strlastindexof2;
    global.strrepeat = strrepeat2;
    global.str_repeat = strrepeat2;
    global.strreplace = strreplace2;
    global.str_replace = strreplace2;
    global.strupper = strupper2;
    global.str_upper = strupper2;
    global.strlower = strlower2;
    global.str_lower = strlower2;
    global.pathjoin = pathjoin2;
    global.path_join = pathjoin2;
    global.pathresolve = pathresolve2;
    global.path_resolve = pathresolve2;
    global.pathdirname = pathdirname2;
    global.path_dir_name = pathdirname2;
    global.pathbasename = pathbasename2;
    global.path_base_name = pathbasename2;
    global.pathextension = pathextension2;
    global.path_extension = pathextension2;
    global.pathisabsolute = pathisabsolute2;
    global.path_is_absolute = pathisabsolute2;
    global.exit = exit2;
    global.arrayreverse = arrayreverse2;
    global.array_reverse = arrayreverse2;
    global.arraysort = arraysort2;
    global.array_sort = arraysort2;
    global.arraymap = arraymap2;
    global.array_map = arraymap2;
    global.arrayfilter = arrayfilter2;
    global.array_filter = arrayfilter2;
    global.arrayshift = arrayshift2;
    global.array_shift = arrayshift2;
    global.arrayfind = arrayfind2;
    global.array_find = arrayfind2;
    global.time = time2;
    global.microtime = microtime2;
    global.micro_time = microtime2;
    global.dateyear = dateyear2;
    global.date_year = dateyear2;
    global.datemonth = datemonth2;
    global.date_month = datemonth2;
    global.dateday = dateday2;
    global.date_day = dateday2;
    global.datehour = datehour2;
    global.date_hour = datehour2;
    global.dateminute = dateminute2;
    global.date_minute = dateminute2;
    global.datesecond = datesecond2;
    global.date_second = datesecond2;
    global.dateweekday = dateweekday2;
    global.date_weekday = dateweekday2;
    global.template = template2;
    function formatRuntimeError2(err, label = "OmniFlux Runtime Error") {
      console.error("\n\x1B[1m\x1B[31m" + label + ":\x1B[0m");
      console.error("\x1B[33m" + err.message + "\x1B[0m");
      if (err.stack) {
        const stackLines = err.stack.split("\n");
        const lineMatch = stackLines.find(
          (line) => line.includes("/") && !line.includes("node:internal") && !line.includes("node_modules")
        );
        if (lineMatch) {
          const match3 = lineMatch.match(/\(([^)]+):(\d+):(\d+)\)/) || lineMatch.match(/at\s+(.+?):(\d+):(\d+)/);
          if (match3) {
            const filePath = match3[1];
            const lineNum = parseInt(match3[2], 10);
            try {
              const content = fs2.readFileSync(filePath, "utf8");
              const fileLines = content.split("\n");
              const indicesToCheck = [lineNum, lineNum - 1, lineNum - 2];
              for (const i of indicesToCheck) {
                if (i >= 0 && i < fileLines.length) {
                  const targetLine = fileLines[i];
                  const ofLineMatch = targetLine.match(/\/\/\! OF_LINE: (.+?)(?:\r?\n|$)/) || targetLine.match(/\/\/ OF_LINE: (.+?)(?:\r?\n|$)/);
                  if (ofLineMatch) {
                    console.error("\x1B[1m\x1B[32mAt: " + ofLineMatch[1] + "\x1B[0m");
                    console.error();
                    return;
                  }
                }
              }
            } catch (e) {
            }
            console.error("\x1B[2mAt: " + path2.basename(filePath) + ":" + lineNum + "\x1B[0m");
          }
        }
      }
      console.error();
    }
    process.on("uncaughtException", (err) => {
      const hooks = global.__on_error_hooks || [];
      if (hooks.length > 0) {
        for (const hook of hooks) {
          hook(err).catch(() => {
          });
        }
        return;
      }
      formatRuntimeError2(err);
      process.exit(1);
    });
    process.on("unhandledRejection", (reason) => {
      const hooks = global.__on_error_hooks || [];
      if (hooks.length > 0) {
        for (const hook of hooks) {
          hook(reason).catch(() => {
          });
        }
        return;
      }
      const err = reason instanceof Error ? reason : new Error(String(reason));
      formatRuntimeError2(err, "OmniFlux Runtime Error (Unhandled Rejection)");
      process.exit(1);
    });
    //! OF_LINE: system.of:1
    //! OF_LINE: system.of:2
    async function system2(cmd, args2) {
      try {
        //! OF_LINE: system.of:3
        //! OF_LINE: system.of:4
        const { spawn: spawn2 } = require("child_process");
        //! OF_LINE: system.of:5
        return new Promise((resolve) => {
          //! OF_LINE: system.of:6
          const child = spawn2(cmd, args2 || [], { stdio: ["inherit", "pipe", "pipe"], shell: true });
          //! OF_LINE: system.of:7
          let output = "";
          //! OF_LINE: system.of:8
          if (child.stdout) {
            //! OF_LINE: system.of:9
            child.stdout.on("data", (data) => {
              //! OF_LINE: system.of:10
              output += data.toString();
              //! OF_LINE: system.of:11
            });
            //! OF_LINE: system.of:12
          }
          //! OF_LINE: system.of:13
          if (child.stderr) {
            //! OF_LINE: system.of:14
            child.stderr.on("data", (data) => {
              //! OF_LINE: system.of:15
              output += data.toString();
              //! OF_LINE: system.of:16
            });
            //! OF_LINE: system.of:17
          }
          //! OF_LINE: system.of:18
          child.on("close", () => {
            //! OF_LINE: system.of:19
            resolve(output);
            //! OF_LINE: system.of:20
          });
          //! OF_LINE: system.of:21
          child.on("error", (err) => {
            //! OF_LINE: system.of:22
            resolve(output + (err ? err.message : ""));
            //! OF_LINE: system.of:23
          });
          //! OF_LINE: system.of:24
        });
        //! OF_LINE: system.of:25
        //! OF_LINE: system.of:26
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: system.of:27
    //! OF_LINE: system.of:28
    async function exec2(cmd, args2) {
      try {
        //! OF_LINE: system.of:29
        //! OF_LINE: system.of:30
        const { spawn: spawn2 } = require("child_process");
        //! OF_LINE: system.of:31
        return new Promise((resolve) => {
          //! OF_LINE: system.of:32
          const child = spawn2(cmd, args2 || [], { stdio: "inherit", shell: true });
          //! OF_LINE: system.of:33
          child.on("close", (code) => {
            //! OF_LINE: system.of:34
            resolve(code !== null ? code : 1);
            //! OF_LINE: system.of:35
          });
          //! OF_LINE: system.of:36
          child.on("error", () => {
            //! OF_LINE: system.of:37
            resolve(1);
            //! OF_LINE: system.of:38
          });
          //! OF_LINE: system.of:39
        });
        //! OF_LINE: system.of:40
        //! OF_LINE: system.of:41
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: system.of:42
    //! OF_LINE: system.of:43
    async function spawn(cmd, args2) {
      try {
        //! OF_LINE: system.of:44
        //! OF_LINE: system.of:45
        const { spawn: spawn2 } = require("child_process");
        //! OF_LINE: system.of:46
        const child = spawn2(cmd, args2 || [], { stdio: "inherit", shell: true });
        //! OF_LINE: system.of:47
        //! OF_LINE: system.of:48
        if (!global.__spawned_processes) {
          //! OF_LINE: system.of:49
          global.__spawned_processes = /* @__PURE__ */ new Map();
          //! OF_LINE: system.of:50
        }
        //! OF_LINE: system.of:51
        //! OF_LINE: system.of:52
        const procInfo = {
          //! OF_LINE: system.of:53
          child,
          //! OF_LINE: system.of:54
          running: true,
          //! OF_LINE: system.of:55
          exitCode: null,
          //! OF_LINE: system.of:56
          signal: null,
          //! OF_LINE: system.of:57
          promise: null,
          //! OF_LINE: system.of:58
          resolve: null
          //! OF_LINE: system.of:59
        };
        //! OF_LINE: system.of:60
        //! OF_LINE: system.of:61
        procInfo.promise = new Promise((resolve) => {
          //! OF_LINE: system.of:62
          procInfo.resolve = resolve;
          //! OF_LINE: system.of:63
        });
        //! OF_LINE: system.of:64
        //! OF_LINE: system.of:65
        child.on("exit", (code, signal) => {
          //! OF_LINE: system.of:66
          procInfo.running = false;
          //! OF_LINE: system.of:67
          procInfo.exitCode = code;
          //! OF_LINE: system.of:68
          procInfo.signal = signal;
          //! OF_LINE: system.of:69
          if (procInfo.resolve) {
            //! OF_LINE: system.of:70
            procInfo.resolve({ code, signal });
            //! OF_LINE: system.of:71
          }
          //! OF_LINE: system.of:72
        });
        //! OF_LINE: system.of:73
        //! OF_LINE: system.of:74
        global.__spawned_processes.set(child.pid, procInfo);
        //! OF_LINE: system.of:75
        return child.pid;
        //! OF_LINE: system.of:76
        //! OF_LINE: system.of:77
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: system.of:78
    //! OF_LINE: system.of:79
    async function pterminate(pid, signal) {
      try {
        //! OF_LINE: system.of:80
        //! OF_LINE: system.of:81
        try {
          //! OF_LINE: system.of:82
          process.kill(pid, signal || "SIGTERM");
          //! OF_LINE: system.of:83
          return true;
          //! OF_LINE: system.of:84
        } catch (e) {
          //! OF_LINE: system.of:85
          return false;
          //! OF_LINE: system.of:86
        }
        //! OF_LINE: system.of:87
        //! OF_LINE: system.of:88
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: system.of:89
    //! OF_LINE: system.of:90
    async function pwait(pid) {
      try {
        //! OF_LINE: system.of:91
        //! OF_LINE: system.of:92
        const info = global.__spawned_processes && global.__spawned_processes.get(pid);
        //! OF_LINE: system.of:93
        if (info) {
          //! OF_LINE: system.of:94
          if (!info.running) {
            //! OF_LINE: system.of:95
            return info.exitCode;
            //! OF_LINE: system.of:96
          }
          //! OF_LINE: system.of:97
          return info.promise.then((res) => res.code);
          //! OF_LINE: system.of:98
        }
        //! OF_LINE: system.of:99
        //! OF_LINE: system.of:100
        return new Promise((resolve) => {
          //! OF_LINE: system.of:102
          const interval = setInterval(() => {
            //! OF_LINE: system.of:103
            try {
              //! OF_LINE: system.of:104
              process.kill(pid, 0);
              //! OF_LINE: system.of:105
            } catch (e) {
              //! OF_LINE: system.of:106
              clearInterval(interval);
              //! OF_LINE: system.of:107
              resolve(null);
              //! OF_LINE: system.of:108
            }
            //! OF_LINE: system.of:109
          }, 100);
          //! OF_LINE: system.of:110
        });
        //! OF_LINE: system.of:111
        //! OF_LINE: system.of:112
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: system.of:113
    //! OF_LINE: system.of:114
    async function pstatus(pid) {
      try {
        //! OF_LINE: system.of:115
        //! OF_LINE: system.of:116
        const info = global.__spawned_processes && global.__spawned_processes.get(pid);
        //! OF_LINE: system.of:117
        if (info) {
          //! OF_LINE: system.of:118
          return info.running ? 1 : 0;
          //! OF_LINE: system.of:119
        }
        //! OF_LINE: system.of:120
        try {
          //! OF_LINE: system.of:121
          process.kill(pid, 0);
          //! OF_LINE: system.of:122
          return 1;
          //! OF_LINE: system.of:123
        } catch (e) {
          //! OF_LINE: system.of:124
          if (e.code === "EPERM") {
            //! OF_LINE: system.of:125
            return 1;
            //! OF_LINE: system.of:126
          }
          //! OF_LINE: system.of:127
          return 0;
          //! OF_LINE: system.of:128
        }
        //! OF_LINE: system.of:129
        //! OF_LINE: system.of:130
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: system.of:131
    //! OF_LINE: system.of:132
    global.system = system2;
    global.exec = exec2;
    global.spawn = spawn;
    global.pterminate = pterminate;
    global.pwait = pwait;
    global.pstatus = pstatus;
    if (require.main === module2) {
      (async () => {
        const hooks = global.__on_start_hooks || [];
        for (const hook of hooks) {
          await hook();
        }
      })().catch((err) => {
        if (typeof formatRuntimeError2 === "function") {
          formatRuntimeError2(err);
        } else {
          console.error(err);
        }
        process.exit(1);
      });
    }
  }
});

// .omniflux_cache/compiler/bundler.js
var require_bundler = __commonJS({
  ".omniflux_cache/compiler/bundler.js"(exports2, module2) {
    var fs2 = require("fs");
    var util2 = require("util");
    var path2 = require("path");
    global.args = process.argv.slice(2);
    global.vars = new Proxy({ args: global.args }, {
      get(target, prop) {
        return prop in target ? target[prop] : null;
      },
      set(target, prop, value) {
        target[prop] = value;
        return true;
      }
    });
    function sprintf2(format, ...args2) {
      if (typeof format !== "string") {
        return util2.format(format, ...args2);
      }
      let argIndex = 0;
      const hasSpecifiers = /%([+-]?)(0?\d*)(?:\.(\d+))?([asdijf%])/g.test(format);
      if (!hasSpecifiers) {
        return util2.format(format, ...args2);
      }
      let formatted = format.replace(/%(?<flags>[+-]?)(?<width>0?\d*)(?:\.(?<precision>\d+))?(?<type>[asdijf%])/g, (match3, flags, widthStr, precisionStr, type) => {
        if (type === "%")
          return "%";
        if (argIndex >= args2.length)
          return match3;
        const val = args2[argIndex++];
        let padChar = " ";
        let width = 0;
        let leftAlign = flags === "-";
        if (widthStr) {
          if (widthStr.startsWith("0")) {
            padChar = "0";
            width = parseInt(widthStr, 10);
          } else {
            width = parseInt(widthStr, 10);
          }
        }
        let precision = precisionStr ? parseInt(precisionStr, 10) : -1;
        let result = "";
        if (type === "d" || type === "i") {
          let intVal = parseInt(val, 10);
          if (isNaN(intVal))
            intVal = 0;
          result = intVal.toString();
          if (precision >= 0) {
            result = result.padStart(precision, "0");
          }
        } else if (type === "f") {
          let floatVal = parseFloat(val);
          if (isNaN(floatVal))
            floatVal = 0;
          if (precision >= 0) {
            result = floatVal.toFixed(precision);
          } else {
            result = floatVal.toString();
          }
        } else if (type === "s") {
          result = String(val);
          if (precision >= 0) {
            result = result.slice(0, precision);
          }
        } else if (type === "j") {
          result = JSON.stringify(val);
        } else {
          result = String(val);
        }
        if (result.length < width) {
          const padLen = width - result.length;
          const padding = padChar.repeat(padLen);
          result = leftAlign ? result + padding : padding + result;
        }
        return result;
      });
      if (argIndex < args2.length) {
        const leftover = args2.slice(argIndex).map((x) => typeof x === "object" ? util2.inspect(x) : String(x));
        formatted += " " + leftover.join(" ");
      }
      return formatted;
    }
    function print2(format, ...args2) {
      console.log(sprintf2(format, ...args2));
    }
    function printf2(format, ...args2) {
      process.stdout.write(sprintf2(format, ...args2));
    }
    var _rl2 = null;
    function _getRL2() {
      if (!_rl2 && process.stdin.isTTY) {
        const readline = require("readline");
        _rl2 = readline.createInterface({
          input: process.stdin,
          output: process.stdout,
          terminal: true,
          historySize: 200
        });
        _rl2.on("close", () => {
          _rl2 = null;
        });
      }
      return _rl2;
    }
    function input2(promptMessage) {
      const rl = _getRL2();
      if (rl) {
        return new Promise((resolve) => {
          rl.question(promptMessage !== void 0 ? String(promptMessage) : "", resolve);
        });
      }
      if (promptMessage !== void 0) {
        process.stdout.write(String(promptMessage));
      }
      const buffer = Buffer.alloc(65536);
      let bytesRead = 0;
      try {
        bytesRead = fs2.readSync(0, buffer, 0, 65536, null);
      } catch (e) {
        return "";
      }
      return buffer.toString("utf8", 0, bytesRead).replace(/\r?\n$/, "");
    }
    global.input = input2;
    function readchar2() {
      const hasRaw = typeof process.stdin.setRawMode === "function";
      const isRaw = process.stdin.isRaw;
      if (hasRaw) {
        process.stdin.setRawMode(true);
      }
      const buffer = Buffer.alloc(1);
      let bytesRead = 0;
      try {
        bytesRead = fs2.readSync(0, buffer, 0, 1, null);
      } catch (e) {
      } finally {
        if (hasRaw) {
          process.stdin.setRawMode(isRaw);
        }
      }
      const char = buffer.toString("utf8", 0, bytesRead);
      if (char === "") {
        process.exit(130);
      }
      return char;
    }
    function fileread2(path3) {
      return fs2.readFileSync(path3, "utf8");
    }
    function filewrite2(path3, data) {
      fs2.writeFileSync(path3, typeof data === "object" ? JSON.stringify(data, null, 2) : data, "utf8");
    }
    function fileexists2(path3) {
      return fs2.existsSync(path3);
    }
    function filecopy2(src, dest) {
      fs2.copyFileSync(src, dest);
    }
    function filerename2(src, dest) {
      fs2.renameSync(src, dest);
    }
    function fileappend2(path3, data) {
      fs2.appendFileSync(path3, typeof data === "object" ? JSON.stringify(data, null, 2) : data, "utf8");
    }
    function fprintf2(path3, format, ...args2) {
      const data = sprintf2(format, ...args2);
      fs2.appendFileSync(path3, data, "utf8");
    }
    function fprint2(path3, format, ...args2) {
      const data = sprintf2(format, ...args2) + "\n";
      fs2.appendFileSync(path3, data, "utf8");
    }
    function filedelete2(path3) {
      fs2.unlinkSync(path3);
    }
    function dirlist2(path3) {
      return fs2.readdirSync(path3);
    }
    function filestat2(path3) {
      const stats = fs2.statSync(path3);
      return {
        size: stats.size,
        isdirectory: stats.isDirectory(),
        is_directory: stats.isDirectory(),
        isfile: stats.isFile(),
        is_file: stats.isFile(),
        createdat: stats.birthtimeMs,
        created_at: stats.birthtimeMs,
        modifiedat: stats.mtimeMs,
        modified_at: stats.mtimeMs
      };
    }
    function dircreate2(path3) {
      if (typeof path3 !== "string")
        throw new Error("Directory path must be a string");
      fs2.mkdirSync(path3, { recursive: true });
    }
    function scriptdir2() {
      const path3 = require("path");
      return path3.dirname(require.main.filename);
    }
    function setenv2(name, value) {
      if (typeof name !== "string")
        throw new Error("Environment variable name must be a string");
      process.env[name] = value !== void 0 ? String(value) : "";
    }
    function getenv2(name) {
      if (typeof name !== "string")
        throw new Error("Environment variable name must be a string");
      return process.env[name] !== void 0 ? process.env[name] : null;
    }
    function sha2562(text) {
      if (typeof text !== "string")
        throw new Error("Input to sha256 must be a string");
      const crypto = require("crypto");
      return crypto.createHash("sha256").update(text).digest("hex");
    }
    function getcookie2(req, name) {
      if (!req || !req.headers || !req.headers.cookie)
        return null;
      const cookies = req.headers.cookie.split(";");
      for (let c of cookies) {
        const parts = c.split("=");
        if (parts[0].trim() === name)
          return parts[1].trim();
      }
      return null;
    }
    function setcookie2(res, name, value, options = {}) {
      if (!res || typeof res.cookie !== "function") {
        throw new Error("First argument to setcookie must be an HTTP response object");
      }
      res.cookie(name, value, options);
    }
    var dbFilePath2 = process.env.DB_FILE || "db.json";
    var cachedDb2 = null;
    var cachedDbMtime2 = 0;
    function dbsetfile2(path3) {
      if (typeof path3 !== "string")
        throw new Error("Database file path must be a string");
      dbFilePath2 = path3;
      cachedDb2 = null;
      cachedDbMtime2 = 0;
    }
    function loadDb2() {
      try {
        if (fs2.existsSync(dbFilePath2)) {
          const stats = fs2.statSync(dbFilePath2);
          if (cachedDb2 && stats.mtimeMs === cachedDbMtime2) {
            return cachedDb2;
          }
          const content = fs2.readFileSync(dbFilePath2, "utf8");
          cachedDb2 = JSON.parse(content || "{}");
          cachedDbMtime2 = stats.mtimeMs;
          return cachedDb2;
        }
      } catch (e) {
      }
      if (!cachedDb2) {
        cachedDb2 = {};
        cachedDbMtime2 = 0;
      }
      return cachedDb2;
    }
    function saveDb2(data) {
      cachedDb2 = data;
      fs2.writeFileSync(dbFilePath2, JSON.stringify(data, null, 2), "utf8");
      try {
        if (fs2.existsSync(dbFilePath2)) {
          cachedDbMtime2 = fs2.statSync(dbFilePath2).mtimeMs;
        }
      } catch (e) {
        cachedDbMtime2 = Date.now();
      }
    }
    function dbinsert2(collection, doc) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      if (!doc || typeof doc !== "object")
        throw new Error("Document must be an object");
      const db = loadDb2();
      if (!db[collection]) {
        db[collection] = [];
      }
      const newDoc = { ...doc };
      if (newDoc.id === void 0) {
        newDoc.id = Date.now().toString(36) + Math.random().toString(36).substring(2, 6);
      }
      db[collection].push(newDoc);
      saveDb2(db);
      return newDoc;
    }
    function dbselect2(collection, filter) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      const db = loadDb2();
      const list = db[collection] || [];
      if (!filter || typeof filter !== "object" || Object.keys(filter).length === 0) {
        return list.map((d) => ({ ...d }));
      }
      return list.filter((doc) => {
        for (const key in filter) {
          if (doc[key] !== filter[key]) {
            return false;
          }
        }
        return true;
      }).map((d) => ({ ...d }));
    }
    function dbupdate2(collection, filter, update) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      if (!update || typeof update !== "object")
        throw new Error("Update payload must be an object");
      const db = loadDb2();
      const list = db[collection] || [];
      let count = 0;
      for (let i = 0; i < list.length; i++) {
        const doc = list[i];
        let matches = true;
        if (filter && typeof filter === "object") {
          for (const key in filter) {
            if (doc[key] !== filter[key]) {
              matches = false;
              break;
            }
          }
        }
        if (matches) {
          list[i] = { ...doc, ...update };
          count++;
        }
      }
      if (count > 0) {
        db[collection] = list;
        saveDb2(db);
      }
      return count;
    }
    function dbdelete2(collection, filter) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      const db = loadDb2();
      const list = db[collection] || [];
      let count = 0;
      const newList = [];
      for (const doc of list) {
        let matches = true;
        if (filter && typeof filter === "object") {
          for (const key in filter) {
            if (doc[key] !== filter[key]) {
              matches = false;
              break;
            }
          }
        }
        if (matches) {
          count++;
        } else {
          newList.push(doc);
        }
      }
      if (count > 0) {
        db[collection] = newList;
        saveDb2(db);
      }
      return count;
    }
    var redisModule2 = null;
    var redisClient2 = null;
    async function getRedisClient2() {
      if (!redisModule2) {
        redisModule2 = require("redis");
      }
      if (!redisClient2) {
        const url = process.env.REDIS_URL || "redis://localhost:6379";
        redisClient2 = redisModule2.createClient({ url });
        await redisClient2.connect();
      }
      return redisClient2;
    }
    async function cacheset2(key, val, ttl) {
      const client = await getRedisClient2();
      const stringVal = typeof val === "object" ? JSON.stringify(val) : String(val);
      if (ttl) {
        await client.setEx(key, ttl, stringVal);
      } else {
        await client.set(key, stringVal);
      }
    }
    async function cacheget2(key) {
      const client = await getRedisClient2();
      const val = await client.get(key);
      if (!val)
        return null;
      try {
        return JSON.parse(val);
      } catch (e) {
        return val;
      }
    }
    function describe2(val) {
      if (val === null)
        return "null";
      if (val === void 0)
        return "undefined";
      if (Array.isArray(val))
        return "array";
      return typeof val;
    }
    global.describe = describe2;
    function jsonparse2(str) {
      try {
        return JSON.parse(String(str || ""));
      } catch (e) {
        return null;
      }
    }
    global.jsonparse = jsonparse2;
    global.jsondecode = jsonparse2;
    function jsonencode2(val, pretty = false) {
      try {
        return pretty ? JSON.stringify(val, null, 2) : JSON.stringify(val);
      } catch (e) {
        return "null";
      }
    }
    global.jsonencode = jsonencode2;
    function len2(val) {
      if (val === null || val === void 0)
        return 0;
      return val.length || 0;
    }
    function toRegExp2(regex) {
      if (regex instanceof RegExp) {
        return regex;
      }
      if (typeof regex === "string") {
        const firstChar = regex.charAt(0);
        if (/^[^a-zA-Z0-9\s\\]/.test(firstChar)) {
          const lastDelim = regex.lastIndexOf(firstChar);
          if (lastDelim > 0) {
            const pattern = regex.substring(1, lastDelim);
            const flags = regex.substring(lastDelim + 1);
            return new RegExp(pattern, flags);
          }
        }
        return new RegExp(regex);
      }
      return null;
    }
    function strsplit2(str, sep) {
      if (typeof str !== "string")
        return [];
      if (sep === void 0)
        return [str];
      const regexObj = toRegExp2(sep);
      if (regexObj) {
        return str.split(regexObj);
      }
      return str.split(sep);
    }
    function match2(str, regex) {
      if (str === null || str === void 0)
        return false;
      const regexObj = toRegExp2(regex);
      if (!regexObj)
        return false;
      const res = String(str).match(regexObj);
      if (!res)
        return false;
      if (res.length > 1) {
        return res.slice(1);
      }
      return true;
    }
    function strtrim2(str, side = "both") {
      const s = String(str || "");
      if (side === "left")
        return s.trimStart();
      if (side === "right")
        return s.trimEnd();
      return s.trim();
    }
    function strsub2(str, start, length) {
      const s = String(str || "");
      if (length === void 0) {
        return s.substring(start);
      }
      return s.substring(start, start + length);
    }
    function strindexof2(str, search) {
      const s = String(str || "");
      return s.indexOf(search);
    }
    function strlastindexof2(str, search) {
      const s = String(str || "");
      return s.lastIndexOf(search);
    }
    function strrepeat2(str, n) {
      const s = String(str || "");
      const count = parseInt(n, 10);
      if (isNaN(count) || count <= 0)
        return "";
      return s.repeat(count);
    }
    function strreplace2(str, search, replacement) {
      const s = String(str || "");
      const r = String(replacement !== void 0 ? replacement : "");
      const regexObj = toRegExp2(search);
      if (regexObj) {
        return s.replace(regexObj, r);
      }
      return s.replaceAll(search, r);
    }
    function strupper2(str) {
      return String(str || "").toUpperCase();
    }
    function strlower2(str) {
      return String(str || "").toLowerCase();
    }
    function arraypush2(arr, item) {
      if (Array.isArray(arr)) {
        arr.push(item);
      }
    }
    function arrayunshift2(arr, item) {
      if (Array.isArray(arr)) {
        arr.unshift(item);
      }
    }
    function arraypop2(arr) {
      if (Array.isArray(arr)) {
        return arr.pop();
      }
      return null;
    }
    function arraycontains2(arr, item) {
      if (Array.isArray(arr)) {
        return arr.includes(item);
      }
      return false;
    }
    function arrayjoin2(arr, sep) {
      if (Array.isArray(arr)) {
        return arr.join(sep);
      }
      return "";
    }
    function arrayslice2(arr, start, end) {
      if (Array.isArray(arr)) {
        return arr.slice(start, end);
      }
      return [];
    }
    function pathjoin2(...paths) {
      const path3 = require("path");
      return path3.join(...paths.map((p) => String(p || "")));
    }
    function pathresolve2(base, relative) {
      const path3 = require("path");
      if (relative === void 0) {
        return path3.resolve(String(base || ""));
      }
      return path3.resolve(String(base || ""), String(relative || ""));
    }
    function pathdirname2(filepath) {
      const path3 = require("path");
      return path3.dirname(String(filepath || ""));
    }
    function pathbasename2(filepath) {
      const path3 = require("path");
      return path3.basename(String(filepath || ""));
    }
    function pathextension2(filepath) {
      const path3 = require("path");
      return path3.extname(String(filepath || ""));
    }
    function pathisabsolute2(filepath) {
      const path3 = require("path");
      return path3.isAbsolute(String(filepath || ""));
    }
    function exit2(code = 0) {
      process.exit(parseInt(code, 10) || 0);
    }
    function arrayreverse2(arr) {
      if (!Array.isArray(arr))
        return [];
      return [...arr].reverse();
    }
    async function asyncMergeSort2(arr, compareFn) {
      if (arr.length <= 1)
        return arr;
      const mid = Math.floor(arr.length / 2);
      const left = arr.slice(0, mid);
      const right = arr.slice(mid);
      return await asyncMerge2(
        await asyncMergeSort2(left, compareFn),
        await asyncMergeSort2(right, compareFn),
        compareFn
      );
    }
    async function asyncMerge2(left, right, compareFn) {
      const result = [];
      let l = 0, r = 0;
      while (l < left.length && r < right.length) {
        const cmp = await compareFn(left[l], right[r]);
        if (cmp <= 0) {
          result.push(left[l]);
          l++;
        } else {
          result.push(right[r]);
          r++;
        }
      }
      return result.concat(left.slice(l)).concat(right.slice(r));
    }
    async function arraysort2(arr, compareFn) {
      if (!Array.isArray(arr))
        return [];
      if (typeof compareFn === "function") {
        const temp = [...arr];
        return await asyncMergeSort2(temp, compareFn);
      }
      return [...arr].sort();
    }
    async function arraymap2(arr, task) {
      if (!Array.isArray(arr))
        return [];
      if (typeof task !== "function")
        return [...arr];
      const results = [];
      for (let i = 0; i < arr.length; i++) {
        results.push(await task(arr[i], i, arr));
      }
      return results;
    }
    async function arrayfilter2(arr, task) {
      if (!Array.isArray(arr))
        return [];
      if (typeof task !== "function")
        return [...arr];
      const results = [];
      for (let i = 0; i < arr.length; i++) {
        if (await task(arr[i], i, arr)) {
          results.push(arr[i]);
        }
      }
      return results;
    }
    function arrayshift2(arr) {
      if (!Array.isArray(arr))
        return null;
      return arr.shift();
    }
    async function arrayfind2(arr, task) {
      if (!Array.isArray(arr))
        return null;
      if (typeof task !== "function")
        return null;
      for (let i = 0; i < arr.length; i++) {
        if (await task(arr[i], i, arr)) {
          return arr[i];
        }
      }
      return null;
    }
    function time2() {
      return Math.floor(Date.now() / 1e3);
    }
    function microtime2(getAsFloat = true) {
      if (getAsFloat === false) {
        const now = Date.now();
        const sec = Math.floor(now / 1e3);
        const msec = now % 1e3;
        return `${(msec / 1e3).toFixed(8)} ${sec}`;
      }
      return Date.now() / 1e3;
    }
    function dateyear2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getFullYear();
    }
    function datemonth2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getMonth() + 1;
    }
    function dateday2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getDate();
    }
    function datehour2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getHours();
    }
    function dateminute2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getMinutes();
    }
    function datesecond2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getSeconds();
    }
    function dateweekday2(ts, format = "number") {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      const day = d.getDay();
      if (format === "short" || format === "text") {
        const days = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];
        return days[day];
      }
      return day;
    }
    function template2(source, context = {}) {
      if (typeof source !== "string")
        return "";
      const appRoot = typeof global !== "undefined" && global.__app_root ? global.__app_root : process.cwd();
      const resolvePath = (p) => path2.isAbsolute(p) ? p : path2.resolve(appRoot, p);
      let html = source;
      try {
        const resolvedSource = resolvePath(source);
        if (fs2.existsSync(resolvedSource)) {
          html = fs2.readFileSync(resolvedSource, "utf8");
        }
        let includeDepth = 0;
        const maxIncludeDepth = 10;
        while (/@include\s*\(\s*["'](.*?)["']\s*\)/.test(html) && includeDepth < maxIncludeDepth) {
          html = html.replace(/@include\s*\(\s*["'](.*?)["']\s*\)/g, (match3, filepath) => {
            const resolvedInclude = resolvePath(filepath);
            if (!fs2.existsSync(resolvedInclude)) {
              throw new Error(`Include file not found: '${filepath}'`);
            }
            return fs2.readFileSync(resolvedInclude, "utf8");
          });
          includeDepth++;
        }
        html = html.replace(/@\}\s*(?=@else)/g, "@}");
        if (html.includes("</body>")) {
          const clientScript = `
<script>
function _of_exec_scripts(container) {
    const scripts = container.querySelectorAll('script');
    for (let oldScript of scripts) {
        const newScript = document.createElement('script');
        for (let attr of oldScript.attributes) {
            newScript.setAttribute(attr.name, attr.value);
        }
        newScript.textContent = oldScript.textContent;
        oldScript.parentNode.replaceChild(newScript, oldScript);
    }
}

document.addEventListener('submit', async (e) => {
    const form = e.target;
    const targetSelector = form.getAttribute('of-target');
    if (!targetSelector) return;
    
    e.preventDefault();
    try {
        const response = await fetch(form.action || window.location.href, {
            method: form.method || 'POST',
            body: new URLSearchParams(new FormData(form))
        });
        const htmlResult = await response.text();
        const targetEl = document.querySelector(targetSelector);
        if (targetEl) {
            targetEl.innerHTML = htmlResult;
            _of_exec_scripts(targetEl);
        }
    } catch (err) {
        console.error('OmniFlux Form Submission Error:', err);
    }
});

document.addEventListener('click', async (e) => {
    const link = e.target.closest('a');
    if (!link) return;
    const targetSelector = link.getAttribute('of-target');
    if (!targetSelector) return;
    
    e.preventDefault();
    try {
        const response = await fetch(link.href);
        const htmlResult = await response.text();
        const targetEl = document.querySelector(targetSelector);
        if (targetEl) {
            targetEl.innerHTML = htmlResult;
            _of_exec_scripts(targetEl);
        }
    } catch (err) {
        console.error('OmniFlux Link Navigation Error:', err);
    }
});
</script>
`;
          html = html.replace("</body>", clientScript + "</body>");
        }
        const tokenRegex = /(@if\s*\(.*?\)\s*\{|@else\s*if\s*\(.*?\)\s*\{|@else\s*\{|@for\s*\(.*?\)\s*\{|@\})/g;
        const parts = html.split(tokenRegex);
        let jsCode = "let _out = '';\n";
        jsCode += "with(global.vars || {}) {\n";
        jsCode += "with(context || {}) {\n";
        for (let part of parts) {
          if (!part)
            continue;
          if (part.startsWith("@if")) {
            const cond = part.match(/@if\s*\((.*?)\)\s*\{/)[1];
            jsCode += `if (${cond}) {
`;
          } else if (part.startsWith("@else if")) {
            const cond = part.match(/@else\s*if\s*\((.*?)\)\s*\{/)[1];
            jsCode += `} else if (${cond}) {
`;
          } else if (part.startsWith("@else")) {
            jsCode += `} else {
`;
          } else if (part.startsWith("@for")) {
            const loopMatch = part.match(/@for\s*\(\s*(?:let|var)?\s*(\w+)\s+of\s+(.*?)\)\s*\{/);
            if (loopMatch) {
              const item = loopMatch[1];
              const list = loopMatch[2];
              jsCode += `for (let ${item} of ${list}) {
`;
            }
          } else if (part === "@}") {
            jsCode += `}
`;
          } else {
            let text = part.replace(/\\/g, "\\\\").replace(/`/g, "\\`").replace(/\$/g, "\\$");
            text = text.replace(/\{\{\s*([\s\S]+?)\s*\}\}/g, "${$1}");
            jsCode += `_out += \`${text}\`;
`;
          }
        }
        jsCode += "}\n}\n";
        jsCode += "return _out;";
        const keys = ["context", ...Object.keys(context)];
        const values = [context, ...Object.values(context)];
        return new Function(...keys, jsCode)(...values);
      } catch (e) {
        return `<!-- Template Error: ${e.message} -->`;
      }
    }
    global.sprintf = sprintf2;
    global.print = print2;
    global.printf = printf2;
    global.fprint = fprint2;
    global.f_print = fprint2;
    global.fprintf = fprintf2;
    global.f_printf = fprintf2;
    global.input = input2;
    global.readchar = readchar2;
    global.read_char = readchar2;
    global.fileread = fileread2;
    global.file_read = fileread2;
    global.filewrite = filewrite2;
    global.file_write = filewrite2;
    global.fileexists = fileexists2;
    global.file_exists = fileexists2;
    global.fileappend = fileappend2;
    global.file_append = fileappend2;
    global.filedelete = filedelete2;
    global.file_delete = filedelete2;
    global.filecopy = filecopy2;
    global.file_copy = filecopy2;
    global.filerename = filerename2;
    global.file_rename = filerename2;
    global.dirlist = dirlist2;
    global.dir_list = dirlist2;
    global.dircreate = dircreate2;
    global.dir_create = dircreate2;
    global.scriptdir = scriptdir2;
    global.script_dir = scriptdir2;
    global.setenv = setenv2;
    global.set_env = setenv2;
    global.getenv = getenv2;
    global.get_env = getenv2;
    global.sha256 = sha2562;
    global.getcookie = getcookie2;
    global.get_cookie = getcookie2;
    global.setcookie = setcookie2;
    global.set_cookie = setcookie2;
    global.filestat = filestat2;
    global.file_stat = filestat2;
    global.dbsetfile = dbsetfile2;
    global.db_set_file = dbsetfile2;
    global.dbinsert = dbinsert2;
    global.db_insert = dbinsert2;
    global.dbselect = dbselect2;
    global.db_select = dbselect2;
    global.dbupdate = dbupdate2;
    global.db_update = dbupdate2;
    global.dbdelete = dbdelete2;
    global.db_delete = dbdelete2;
    global.cacheset = cacheset2;
    global.cache_set = cacheset2;
    global.cacheget = cacheget2;
    global.cache_get = cacheget2;
    global.len = len2;
    global.strsplit = strsplit2;
    global.str_split = strsplit2;
    global.match = match2;
    global.arraypush = arraypush2;
    global.array_push = arraypush2;
    global.arrayunshift = arrayunshift2;
    global.array_unshift = arrayunshift2;
    global.arraypop = arraypop2;
    global.array_pop = arraypop2;
    global.arraycontains = arraycontains2;
    global.array_contains = arraycontains2;
    global.arrayjoin = arrayjoin2;
    global.array_join = arrayjoin2;
    global.arrayslice = arrayslice2;
    global.array_slice = arrayslice2;
    global.strtrim = strtrim2;
    global.str_trim = strtrim2;
    global.strsub = strsub2;
    global.str_sub = strsub2;
    global.strindexof = strindexof2;
    global.str_index_of = strindexof2;
    global.strlastindexof = strlastindexof2;
    global.str_last_index_of = strlastindexof2;
    global.strrepeat = strrepeat2;
    global.str_repeat = strrepeat2;
    global.strreplace = strreplace2;
    global.str_replace = strreplace2;
    global.strupper = strupper2;
    global.str_upper = strupper2;
    global.strlower = strlower2;
    global.str_lower = strlower2;
    global.pathjoin = pathjoin2;
    global.path_join = pathjoin2;
    global.pathresolve = pathresolve2;
    global.path_resolve = pathresolve2;
    global.pathdirname = pathdirname2;
    global.path_dir_name = pathdirname2;
    global.pathbasename = pathbasename2;
    global.path_base_name = pathbasename2;
    global.pathextension = pathextension2;
    global.path_extension = pathextension2;
    global.pathisabsolute = pathisabsolute2;
    global.path_is_absolute = pathisabsolute2;
    global.exit = exit2;
    global.arrayreverse = arrayreverse2;
    global.array_reverse = arrayreverse2;
    global.arraysort = arraysort2;
    global.array_sort = arraysort2;
    global.arraymap = arraymap2;
    global.array_map = arraymap2;
    global.arrayfilter = arrayfilter2;
    global.array_filter = arrayfilter2;
    global.arrayshift = arrayshift2;
    global.array_shift = arrayshift2;
    global.arrayfind = arrayfind2;
    global.array_find = arrayfind2;
    global.time = time2;
    global.microtime = microtime2;
    global.micro_time = microtime2;
    global.dateyear = dateyear2;
    global.date_year = dateyear2;
    global.datemonth = datemonth2;
    global.date_month = datemonth2;
    global.dateday = dateday2;
    global.date_day = dateday2;
    global.datehour = datehour2;
    global.date_hour = datehour2;
    global.dateminute = dateminute2;
    global.date_minute = dateminute2;
    global.datesecond = datesecond2;
    global.date_second = datesecond2;
    global.dateweekday = dateweekday2;
    global.date_weekday = dateweekday2;
    global.template = template2;
    function formatRuntimeError2(err, label = "OmniFlux Runtime Error") {
      console.error("\n\x1B[1m\x1B[31m" + label + ":\x1B[0m");
      console.error("\x1B[33m" + err.message + "\x1B[0m");
      if (err.stack) {
        const stackLines = err.stack.split("\n");
        const lineMatch = stackLines.find(
          (line) => line.includes("/") && !line.includes("node:internal") && !line.includes("node_modules")
        );
        if (lineMatch) {
          const match3 = lineMatch.match(/\(([^)]+):(\d+):(\d+)\)/) || lineMatch.match(/at\s+(.+?):(\d+):(\d+)/);
          if (match3) {
            const filePath = match3[1];
            const lineNum = parseInt(match3[2], 10);
            try {
              const content = fs2.readFileSync(filePath, "utf8");
              const fileLines = content.split("\n");
              const indicesToCheck = [lineNum, lineNum - 1, lineNum - 2];
              for (const i of indicesToCheck) {
                if (i >= 0 && i < fileLines.length) {
                  const targetLine = fileLines[i];
                  const ofLineMatch = targetLine.match(/\/\/\! OF_LINE: (.+?)(?:\r?\n|$)/) || targetLine.match(/\/\/ OF_LINE: (.+?)(?:\r?\n|$)/);
                  if (ofLineMatch) {
                    console.error("\x1B[1m\x1B[32mAt: " + ofLineMatch[1] + "\x1B[0m");
                    console.error();
                    return;
                  }
                }
              }
            } catch (e) {
            }
            console.error("\x1B[2mAt: " + path2.basename(filePath) + ":" + lineNum + "\x1B[0m");
          }
        }
      }
      console.error();
    }
    process.on("uncaughtException", (err) => {
      const hooks = global.__on_error_hooks || [];
      if (hooks.length > 0) {
        for (const hook of hooks) {
          hook(err).catch(() => {
          });
        }
        return;
      }
      formatRuntimeError2(err);
      process.exit(1);
    });
    process.on("unhandledRejection", (reason) => {
      const hooks = global.__on_error_hooks || [];
      if (hooks.length > 0) {
        for (const hook of hooks) {
          hook(reason).catch(() => {
          });
        }
        return;
      }
      const err = reason instanceof Error ? reason : new Error(String(reason));
      formatRuntimeError2(err, "OmniFlux Runtime Error (Unhandled Rejection)");
      process.exit(1);
    });
    //! OF_LINE: bundler.of:1
    //! OF_LINE: bundler.of:2
    //! OF_LINE: bundler.of:3
    //! OF_LINE: bundler.of:4
    //! OF_LINE: bundler.of:5
    //! OF_LINE: bundler.of:6
    //! OF_LINE: bundler.of:7
    //! OF_LINE: bundler.of:8
    //! OF_LINE: bundler.of:9
    //! OF_LINE: bundler.of:10
    //! OF_LINE: bundler.of:11
    //! OF_LINE: bundler.of:12
    //! OF_LINE: bundler.of:13
    require_preprocessor();
    //! OF_LINE: bundler.of:14
    require_system();
    //! OF_LINE: bundler.of:15
    //! OF_LINE: bundler.of:16
    //! OF_LINE: bundler.of:17
    //! OF_LINE: bundler.of:18
    //! OF_LINE: bundler.of:19
    //! OF_LINE: bundler.of:20
    async function get_cache_path2(source_abs_path) {
      try {
        //! OF_LINE: bundler.of:21
        let cwd = pathresolve2(".");
        //! OF_LINE: bundler.of:22
        let cache_dir = pathjoin2(cwd, ".omniflux_cache");
        //! OF_LINE: bundler.of:23
        //! OF_LINE: bundler.of:24
        let rel = source_abs_path;
        //! OF_LINE: bundler.of:25
        let prefix = cwd + "/";
        //! OF_LINE: bundler.of:26
        if (strindexof2(source_abs_path, prefix) == 0) {
          //! OF_LINE: bundler.of:27
          rel = strsub2(source_abs_path, len2(prefix), len2(source_abs_path) - len2(prefix));
          //! OF_LINE: bundler.of:28
        }
        //! OF_LINE: bundler.of:29
        //! OF_LINE: bundler.of:30
        let js_rel = strreplace2(rel, "/\\.of$/", ".js");
        //! OF_LINE: bundler.of:31
        if (js_rel == rel) {
          //! OF_LINE: bundler.of:32
          js_rel = rel + ".js";
          //! OF_LINE: bundler.of:33
        }
        //! OF_LINE: bundler.of:34
        //! OF_LINE: bundler.of:35
        return pathjoin2(cache_dir, js_rel);
        //! OF_LINE: bundler.of:36
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: bundler.of:37
    //! OF_LINE: bundler.of:38
    //! OF_LINE: bundler.of:39
    //! OF_LINE: bundler.of:40
    //! OF_LINE: bundler.of:41
    async function get_output_path2(source_abs_path) {
      try {
        //! OF_LINE: bundler.of:42
        let dir = pathdirname2(source_abs_path);
        //! OF_LINE: bundler.of:43
        let base = pathbasename2(source_abs_path);
        //! OF_LINE: bundler.of:44
        let name = strreplace2(base, "/\\.[^.]+$/", "");
        //! OF_LINE: bundler.of:45
        return pathjoin2(dir, name);
        //! OF_LINE: bundler.of:46
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: bundler.of:47
    //! OF_LINE: bundler.of:48
    //! OF_LINE: bundler.of:49
    //! OF_LINE: bundler.of:50
    async function write_js(cache_path, js_content) {
      try {
        //! OF_LINE: bundler.of:51
        let dir = pathdirname2(cache_path);
        //! OF_LINE: bundler.of:52
        dircreate2(dir);
        //! OF_LINE: bundler.of:53
        filewrite2(cache_path, js_content);
        //! OF_LINE: bundler.of:54
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: bundler.of:55
    //! OF_LINE: bundler.of:56
    //! OF_LINE: bundler.of:57
    //! OF_LINE: bundler.of:58
    //! OF_LINE: bundler.of:59
    async function bundle(main_cache_js, output_file) {
      try {
        //! OF_LINE: bundler.of:60
        let rel_main = main_cache_js;
        //! OF_LINE: bundler.of:61
        //! OF_LINE: bundler.of:62
        print2("Bundling with esbuild: %s -> %s...", rel_main, output_file);
        //! OF_LINE: bundler.of:63
        //! OF_LINE: bundler.of:64
        if (fileexists2(output_file)) {
          //! OF_LINE: bundler.of:65
          filedelete2(output_file);
          //! OF_LINE: bundler.of:66
        }
        //! OF_LINE: bundler.of:67
        //! OF_LINE: bundler.of:68
        let cmd = 'npx -y esbuild "' + rel_main + '" --bundle --platform=node --external:mysql2/promise --external:redis --legal-comments=inline --banner:js="#!/usr/bin/env node" --outfile="' + output_file + '"';
        //! OF_LINE: bundler.of:69
        //! OF_LINE: bundler.of:70
        //! OF_LINE: bundler.of:71
        let output = await system(cmd);
        //! OF_LINE: bundler.of:72
        //! OF_LINE: bundler.of:73
        if (!fileexists2(output_file)) {
          //! OF_LINE: bundler.of:74
          let formatted = output;
          //! OF_LINE: bundler.of:75
          //! OF_LINE: bundler.of:76
          try {
            //! OF_LINE: bundler.of:77
            const fs3 = require("fs");
            //! OF_LINE: bundler.of:78
            let currentOriginalLine = null;
            //! OF_LINE: bundler.of:79
            const pathRegex = /(\.omniflux_cache\/([^\s:]+)\.js):(\d+):(\d+)/g;
            //! OF_LINE: bundler.of:80
            formatted = output.replace(pathRegex, (match3, cachePath, relPath, lineStr, colStr) => {
              //! OF_LINE: bundler.of:81
              if (fs3.existsSync(cachePath)) {
                //! OF_LINE: bundler.of:82
                const lines = fs3.readFileSync(cachePath, "utf8").split("\n");
                //! OF_LINE: bundler.of:83
                const lineIdx = parseInt(lineStr, 10) - 1;
                //! OF_LINE: bundler.of:84
                if (lineIdx >= 0 && lineIdx < lines.length) {
                  //! OF_LINE: bundler.of:85
                  const lineContent = lines[lineIdx];
                  //! OF_LINE: bundler.of:86
                  const ofMatch = /\/\/!\s*OF_LINE:\s*([^\s:]+):(\d+)/.exec(lineContent);
                  //! OF_LINE: bundler.of:87
                  if (ofMatch) {
                    //! OF_LINE: bundler.of:88
                    currentOriginalLine = ofMatch[2];
                    //! OF_LINE: bundler.of:89
                    return `${ofMatch[1]}:${ofMatch[2]}:${colStr}`;
                    //! OF_LINE: bundler.of:90
                  }
                  //! OF_LINE: bundler.of:91
                }
                //! OF_LINE: bundler.of:92
              }
              //! OF_LINE: bundler.of:93
              return match3;
              //! OF_LINE: bundler.of:94
            });
            //! OF_LINE: bundler.of:95
            //! OF_LINE: bundler.of:96
            const outLines = formatted.split("\n");
            //! OF_LINE: bundler.of:97
            for (let i = 0; i < outLines.length; i++) {
              //! OF_LINE: bundler.of:98
              const line = outLines[i];
              //! OF_LINE: bundler.of:99
              const listMatch = /^(\s*)(\d+)(\s+│.*)/.exec(line);
              //! OF_LINE: bundler.of:100
              if (listMatch) {
                //! OF_LINE: bundler.of:101
                const ofMatch = /\/\/!\s*OF_LINE:\s*[^\s:]+:(\d+)/.exec(line);
                //! OF_LINE: bundler.of:102
                const originalLine = ofMatch ? ofMatch[1] : currentOriginalLine;
                //! OF_LINE: bundler.of:103
                if (originalLine) {
                  //! OF_LINE: bundler.of:104
                  outLines[i] = `${listMatch[1]}${originalLine}${listMatch[3]}`;
                  //! OF_LINE: bundler.of:105
                }
                //! OF_LINE: bundler.of:106
              }
              //! OF_LINE: bundler.of:107
            }
            //! OF_LINE: bundler.of:108
            formatted = outLines.join("\n");
            //! OF_LINE: bundler.of:109
          } catch (e) {
          }
          //! OF_LINE: bundler.of:110
          //! OF_LINE: bundler.of:111
          print2("esbuild error output:\n%s", formatted);
          //! OF_LINE: bundler.of:112
          return {
            //! OF_LINE: bundler.of:113
            ok: false,
            //! OF_LINE: bundler.of:114
            error: formatted
            //! OF_LINE: bundler.of:115
          };
          //! OF_LINE: bundler.of:116
        }
        //! OF_LINE: bundler.of:117
        //! OF_LINE: bundler.of:118
        //! OF_LINE: bundler.of:119
        await exec('chmod +x "' + output_file + '"');
        //! OF_LINE: bundler.of:120
        //! OF_LINE: bundler.of:121
        return { ok: true, output: output_file };
        //! OF_LINE: bundler.of:122
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: bundler.of:123
    //! OF_LINE: bundler.of:124
    //! OF_LINE: bundler.of:125
    //! OF_LINE: bundler.of:126
    //! OF_LINE: bundler.of:127
    //! OF_LINE: bundler.of:128
    async function build2(main_source_path, output_file, compiled_js_map) {
      try {
        //! OF_LINE: bundler.of:129
        let abs_main = pathresolve2(main_source_path);
        //! OF_LINE: bundler.of:130
        //! OF_LINE: bundler.of:131
        if (!output_file) {
          //! OF_LINE: bundler.of:132
          output_file = await get_output_path2(abs_main);
          //! OF_LINE: bundler.of:133
        }
        //! OF_LINE: bundler.of:134
        //! OF_LINE: bundler.of:135
        //! OF_LINE: bundler.of:136
        var map_keys = Object.keys(compiled_js_map);
        //! OF_LINE: bundler.of:137
        for (let source_path of map_keys) {
          //! OF_LINE: bundler.of:138
          let js_content = compiled_js_map[source_path];
          //! OF_LINE: bundler.of:139
          let cache_path = await get_cache_path2(source_path);
          //! OF_LINE: bundler.of:140
          print2("Writing cache: %s", cache_path);
          //! OF_LINE: bundler.of:141
          await write_js(cache_path, js_content);
          //! OF_LINE: bundler.of:142
        }
        //! OF_LINE: bundler.of:143
        //! OF_LINE: bundler.of:144
        //! OF_LINE: bundler.of:145
        let main_cache_js = await get_cache_path2(abs_main);
        //! OF_LINE: bundler.of:146
        //! OF_LINE: bundler.of:147
        //! OF_LINE: bundler.of:148
        let result = await bundle(main_cache_js, output_file);
        //! OF_LINE: bundler.of:149
        return result;
        //! OF_LINE: bundler.of:150
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: bundler.of:151
    //! OF_LINE: bundler.of:152
    global.get_cache_path = get_cache_path2;
    global.get_output_path = get_output_path2;
    global.write_js = write_js;
    global.bundle = bundle;
    global.build = build2;
    if (require.main === module2) {
      (async () => {
        const hooks = global.__on_start_hooks || [];
        for (const hook of hooks) {
          await hook();
        }
      })().catch((err) => {
        if (typeof formatRuntimeError2 === "function") {
          formatRuntimeError2(err);
        } else {
          console.error(err);
        }
        process.exit(1);
      });
    }
  }
});

// .omniflux_cache/compiler/validator.js
var require_validator = __commonJS({
  ".omniflux_cache/compiler/validator.js"(exports2, module2) {
    var fs2 = require("fs");
    var util2 = require("util");
    var path2 = require("path");
    global.args = process.argv.slice(2);
    global.vars = new Proxy({ args: global.args }, {
      get(target, prop) {
        return prop in target ? target[prop] : null;
      },
      set(target, prop, value) {
        target[prop] = value;
        return true;
      }
    });
    function sprintf2(format, ...args2) {
      if (typeof format !== "string") {
        return util2.format(format, ...args2);
      }
      let argIndex = 0;
      const hasSpecifiers = /%([+-]?)(0?\d*)(?:\.(\d+))?([asdijf%])/g.test(format);
      if (!hasSpecifiers) {
        return util2.format(format, ...args2);
      }
      let formatted = format.replace(/%(?<flags>[+-]?)(?<width>0?\d*)(?:\.(?<precision>\d+))?(?<type>[asdijf%])/g, (match3, flags, widthStr, precisionStr, type) => {
        if (type === "%")
          return "%";
        if (argIndex >= args2.length)
          return match3;
        const val = args2[argIndex++];
        let padChar = " ";
        let width = 0;
        let leftAlign = flags === "-";
        if (widthStr) {
          if (widthStr.startsWith("0")) {
            padChar = "0";
            width = parseInt(widthStr, 10);
          } else {
            width = parseInt(widthStr, 10);
          }
        }
        let precision = precisionStr ? parseInt(precisionStr, 10) : -1;
        let result = "";
        if (type === "d" || type === "i") {
          let intVal = parseInt(val, 10);
          if (isNaN(intVal))
            intVal = 0;
          result = intVal.toString();
          if (precision >= 0) {
            result = result.padStart(precision, "0");
          }
        } else if (type === "f") {
          let floatVal = parseFloat(val);
          if (isNaN(floatVal))
            floatVal = 0;
          if (precision >= 0) {
            result = floatVal.toFixed(precision);
          } else {
            result = floatVal.toString();
          }
        } else if (type === "s") {
          result = String(val);
          if (precision >= 0) {
            result = result.slice(0, precision);
          }
        } else if (type === "j") {
          result = JSON.stringify(val);
        } else {
          result = String(val);
        }
        if (result.length < width) {
          const padLen = width - result.length;
          const padding = padChar.repeat(padLen);
          result = leftAlign ? result + padding : padding + result;
        }
        return result;
      });
      if (argIndex < args2.length) {
        const leftover = args2.slice(argIndex).map((x) => typeof x === "object" ? util2.inspect(x) : String(x));
        formatted += " " + leftover.join(" ");
      }
      return formatted;
    }
    function print2(format, ...args2) {
      console.log(sprintf2(format, ...args2));
    }
    function printf2(format, ...args2) {
      process.stdout.write(sprintf2(format, ...args2));
    }
    var _rl2 = null;
    function _getRL2() {
      if (!_rl2 && process.stdin.isTTY) {
        const readline = require("readline");
        _rl2 = readline.createInterface({
          input: process.stdin,
          output: process.stdout,
          terminal: true,
          historySize: 200
        });
        _rl2.on("close", () => {
          _rl2 = null;
        });
      }
      return _rl2;
    }
    function input2(promptMessage) {
      const rl = _getRL2();
      if (rl) {
        return new Promise((resolve) => {
          rl.question(promptMessage !== void 0 ? String(promptMessage) : "", resolve);
        });
      }
      if (promptMessage !== void 0) {
        process.stdout.write(String(promptMessage));
      }
      const buffer = Buffer.alloc(65536);
      let bytesRead = 0;
      try {
        bytesRead = fs2.readSync(0, buffer, 0, 65536, null);
      } catch (e) {
        return "";
      }
      return buffer.toString("utf8", 0, bytesRead).replace(/\r?\n$/, "");
    }
    global.input = input2;
    function readchar2() {
      const hasRaw = typeof process.stdin.setRawMode === "function";
      const isRaw = process.stdin.isRaw;
      if (hasRaw) {
        process.stdin.setRawMode(true);
      }
      const buffer = Buffer.alloc(1);
      let bytesRead = 0;
      try {
        bytesRead = fs2.readSync(0, buffer, 0, 1, null);
      } catch (e) {
      } finally {
        if (hasRaw) {
          process.stdin.setRawMode(isRaw);
        }
      }
      const char = buffer.toString("utf8", 0, bytesRead);
      if (char === "") {
        process.exit(130);
      }
      return char;
    }
    function fileread2(path3) {
      return fs2.readFileSync(path3, "utf8");
    }
    function filewrite2(path3, data) {
      fs2.writeFileSync(path3, typeof data === "object" ? JSON.stringify(data, null, 2) : data, "utf8");
    }
    function fileexists2(path3) {
      return fs2.existsSync(path3);
    }
    function filecopy2(src, dest) {
      fs2.copyFileSync(src, dest);
    }
    function filerename2(src, dest) {
      fs2.renameSync(src, dest);
    }
    function fileappend2(path3, data) {
      fs2.appendFileSync(path3, typeof data === "object" ? JSON.stringify(data, null, 2) : data, "utf8");
    }
    function fprintf2(path3, format, ...args2) {
      const data = sprintf2(format, ...args2);
      fs2.appendFileSync(path3, data, "utf8");
    }
    function fprint2(path3, format, ...args2) {
      const data = sprintf2(format, ...args2) + "\n";
      fs2.appendFileSync(path3, data, "utf8");
    }
    function filedelete2(path3) {
      fs2.unlinkSync(path3);
    }
    function dirlist2(path3) {
      return fs2.readdirSync(path3);
    }
    function filestat2(path3) {
      const stats = fs2.statSync(path3);
      return {
        size: stats.size,
        isdirectory: stats.isDirectory(),
        is_directory: stats.isDirectory(),
        isfile: stats.isFile(),
        is_file: stats.isFile(),
        createdat: stats.birthtimeMs,
        created_at: stats.birthtimeMs,
        modifiedat: stats.mtimeMs,
        modified_at: stats.mtimeMs
      };
    }
    function dircreate2(path3) {
      if (typeof path3 !== "string")
        throw new Error("Directory path must be a string");
      fs2.mkdirSync(path3, { recursive: true });
    }
    function scriptdir2() {
      const path3 = require("path");
      return path3.dirname(require.main.filename);
    }
    function setenv2(name, value) {
      if (typeof name !== "string")
        throw new Error("Environment variable name must be a string");
      process.env[name] = value !== void 0 ? String(value) : "";
    }
    function getenv2(name) {
      if (typeof name !== "string")
        throw new Error("Environment variable name must be a string");
      return process.env[name] !== void 0 ? process.env[name] : null;
    }
    function sha2562(text) {
      if (typeof text !== "string")
        throw new Error("Input to sha256 must be a string");
      const crypto = require("crypto");
      return crypto.createHash("sha256").update(text).digest("hex");
    }
    function getcookie2(req, name) {
      if (!req || !req.headers || !req.headers.cookie)
        return null;
      const cookies = req.headers.cookie.split(";");
      for (let c of cookies) {
        const parts = c.split("=");
        if (parts[0].trim() === name)
          return parts[1].trim();
      }
      return null;
    }
    function setcookie2(res, name, value, options = {}) {
      if (!res || typeof res.cookie !== "function") {
        throw new Error("First argument to setcookie must be an HTTP response object");
      }
      res.cookie(name, value, options);
    }
    var dbFilePath2 = process.env.DB_FILE || "db.json";
    var cachedDb2 = null;
    var cachedDbMtime2 = 0;
    function dbsetfile2(path3) {
      if (typeof path3 !== "string")
        throw new Error("Database file path must be a string");
      dbFilePath2 = path3;
      cachedDb2 = null;
      cachedDbMtime2 = 0;
    }
    function loadDb2() {
      try {
        if (fs2.existsSync(dbFilePath2)) {
          const stats = fs2.statSync(dbFilePath2);
          if (cachedDb2 && stats.mtimeMs === cachedDbMtime2) {
            return cachedDb2;
          }
          const content = fs2.readFileSync(dbFilePath2, "utf8");
          cachedDb2 = JSON.parse(content || "{}");
          cachedDbMtime2 = stats.mtimeMs;
          return cachedDb2;
        }
      } catch (e) {
      }
      if (!cachedDb2) {
        cachedDb2 = {};
        cachedDbMtime2 = 0;
      }
      return cachedDb2;
    }
    function saveDb2(data) {
      cachedDb2 = data;
      fs2.writeFileSync(dbFilePath2, JSON.stringify(data, null, 2), "utf8");
      try {
        if (fs2.existsSync(dbFilePath2)) {
          cachedDbMtime2 = fs2.statSync(dbFilePath2).mtimeMs;
        }
      } catch (e) {
        cachedDbMtime2 = Date.now();
      }
    }
    function dbinsert2(collection, doc) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      if (!doc || typeof doc !== "object")
        throw new Error("Document must be an object");
      const db = loadDb2();
      if (!db[collection]) {
        db[collection] = [];
      }
      const newDoc = { ...doc };
      if (newDoc.id === void 0) {
        newDoc.id = Date.now().toString(36) + Math.random().toString(36).substring(2, 6);
      }
      db[collection].push(newDoc);
      saveDb2(db);
      return newDoc;
    }
    function dbselect2(collection, filter) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      const db = loadDb2();
      const list = db[collection] || [];
      if (!filter || typeof filter !== "object" || Object.keys(filter).length === 0) {
        return list.map((d) => ({ ...d }));
      }
      return list.filter((doc) => {
        for (const key in filter) {
          if (doc[key] !== filter[key]) {
            return false;
          }
        }
        return true;
      }).map((d) => ({ ...d }));
    }
    function dbupdate2(collection, filter, update) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      if (!update || typeof update !== "object")
        throw new Error("Update payload must be an object");
      const db = loadDb2();
      const list = db[collection] || [];
      let count = 0;
      for (let i = 0; i < list.length; i++) {
        const doc = list[i];
        let matches = true;
        if (filter && typeof filter === "object") {
          for (const key in filter) {
            if (doc[key] !== filter[key]) {
              matches = false;
              break;
            }
          }
        }
        if (matches) {
          list[i] = { ...doc, ...update };
          count++;
        }
      }
      if (count > 0) {
        db[collection] = list;
        saveDb2(db);
      }
      return count;
    }
    function dbdelete2(collection, filter) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      const db = loadDb2();
      const list = db[collection] || [];
      let count = 0;
      const newList = [];
      for (const doc of list) {
        let matches = true;
        if (filter && typeof filter === "object") {
          for (const key in filter) {
            if (doc[key] !== filter[key]) {
              matches = false;
              break;
            }
          }
        }
        if (matches) {
          count++;
        } else {
          newList.push(doc);
        }
      }
      if (count > 0) {
        db[collection] = newList;
        saveDb2(db);
      }
      return count;
    }
    var redisModule2 = null;
    var redisClient2 = null;
    async function getRedisClient2() {
      if (!redisModule2) {
        redisModule2 = require("redis");
      }
      if (!redisClient2) {
        const url = process.env.REDIS_URL || "redis://localhost:6379";
        redisClient2 = redisModule2.createClient({ url });
        await redisClient2.connect();
      }
      return redisClient2;
    }
    async function cacheset2(key, val, ttl) {
      const client = await getRedisClient2();
      const stringVal = typeof val === "object" ? JSON.stringify(val) : String(val);
      if (ttl) {
        await client.setEx(key, ttl, stringVal);
      } else {
        await client.set(key, stringVal);
      }
    }
    async function cacheget2(key) {
      const client = await getRedisClient2();
      const val = await client.get(key);
      if (!val)
        return null;
      try {
        return JSON.parse(val);
      } catch (e) {
        return val;
      }
    }
    function describe2(val) {
      if (val === null)
        return "null";
      if (val === void 0)
        return "undefined";
      if (Array.isArray(val))
        return "array";
      return typeof val;
    }
    global.describe = describe2;
    function jsonparse2(str) {
      try {
        return JSON.parse(String(str || ""));
      } catch (e) {
        return null;
      }
    }
    global.jsonparse = jsonparse2;
    global.jsondecode = jsonparse2;
    function jsonencode2(val, pretty = false) {
      try {
        return pretty ? JSON.stringify(val, null, 2) : JSON.stringify(val);
      } catch (e) {
        return "null";
      }
    }
    global.jsonencode = jsonencode2;
    function len2(val) {
      if (val === null || val === void 0)
        return 0;
      return val.length || 0;
    }
    function toRegExp2(regex) {
      if (regex instanceof RegExp) {
        return regex;
      }
      if (typeof regex === "string") {
        const firstChar = regex.charAt(0);
        if (/^[^a-zA-Z0-9\s\\]/.test(firstChar)) {
          const lastDelim = regex.lastIndexOf(firstChar);
          if (lastDelim > 0) {
            const pattern = regex.substring(1, lastDelim);
            const flags = regex.substring(lastDelim + 1);
            return new RegExp(pattern, flags);
          }
        }
        return new RegExp(regex);
      }
      return null;
    }
    function strsplit2(str, sep) {
      if (typeof str !== "string")
        return [];
      if (sep === void 0)
        return [str];
      const regexObj = toRegExp2(sep);
      if (regexObj) {
        return str.split(regexObj);
      }
      return str.split(sep);
    }
    function match2(str, regex) {
      if (str === null || str === void 0)
        return false;
      const regexObj = toRegExp2(regex);
      if (!regexObj)
        return false;
      const res = String(str).match(regexObj);
      if (!res)
        return false;
      if (res.length > 1) {
        return res.slice(1);
      }
      return true;
    }
    function strtrim2(str, side = "both") {
      const s = String(str || "");
      if (side === "left")
        return s.trimStart();
      if (side === "right")
        return s.trimEnd();
      return s.trim();
    }
    function strsub2(str, start, length) {
      const s = String(str || "");
      if (length === void 0) {
        return s.substring(start);
      }
      return s.substring(start, start + length);
    }
    function strindexof2(str, search) {
      const s = String(str || "");
      return s.indexOf(search);
    }
    function strlastindexof2(str, search) {
      const s = String(str || "");
      return s.lastIndexOf(search);
    }
    function strrepeat2(str, n) {
      const s = String(str || "");
      const count = parseInt(n, 10);
      if (isNaN(count) || count <= 0)
        return "";
      return s.repeat(count);
    }
    function strreplace2(str, search, replacement) {
      const s = String(str || "");
      const r = String(replacement !== void 0 ? replacement : "");
      const regexObj = toRegExp2(search);
      if (regexObj) {
        return s.replace(regexObj, r);
      }
      return s.replaceAll(search, r);
    }
    function strupper2(str) {
      return String(str || "").toUpperCase();
    }
    function strlower2(str) {
      return String(str || "").toLowerCase();
    }
    function arraypush2(arr, item) {
      if (Array.isArray(arr)) {
        arr.push(item);
      }
    }
    function arrayunshift2(arr, item) {
      if (Array.isArray(arr)) {
        arr.unshift(item);
      }
    }
    function arraypop2(arr) {
      if (Array.isArray(arr)) {
        return arr.pop();
      }
      return null;
    }
    function arraycontains2(arr, item) {
      if (Array.isArray(arr)) {
        return arr.includes(item);
      }
      return false;
    }
    function arrayjoin2(arr, sep) {
      if (Array.isArray(arr)) {
        return arr.join(sep);
      }
      return "";
    }
    function arrayslice2(arr, start, end) {
      if (Array.isArray(arr)) {
        return arr.slice(start, end);
      }
      return [];
    }
    function pathjoin2(...paths) {
      const path3 = require("path");
      return path3.join(...paths.map((p) => String(p || "")));
    }
    function pathresolve2(base, relative) {
      const path3 = require("path");
      if (relative === void 0) {
        return path3.resolve(String(base || ""));
      }
      return path3.resolve(String(base || ""), String(relative || ""));
    }
    function pathdirname2(filepath) {
      const path3 = require("path");
      return path3.dirname(String(filepath || ""));
    }
    function pathbasename2(filepath) {
      const path3 = require("path");
      return path3.basename(String(filepath || ""));
    }
    function pathextension2(filepath) {
      const path3 = require("path");
      return path3.extname(String(filepath || ""));
    }
    function pathisabsolute2(filepath) {
      const path3 = require("path");
      return path3.isAbsolute(String(filepath || ""));
    }
    function exit2(code = 0) {
      process.exit(parseInt(code, 10) || 0);
    }
    function arrayreverse2(arr) {
      if (!Array.isArray(arr))
        return [];
      return [...arr].reverse();
    }
    async function asyncMergeSort2(arr, compareFn) {
      if (arr.length <= 1)
        return arr;
      const mid = Math.floor(arr.length / 2);
      const left = arr.slice(0, mid);
      const right = arr.slice(mid);
      return await asyncMerge2(
        await asyncMergeSort2(left, compareFn),
        await asyncMergeSort2(right, compareFn),
        compareFn
      );
    }
    async function asyncMerge2(left, right, compareFn) {
      const result = [];
      let l = 0, r = 0;
      while (l < left.length && r < right.length) {
        const cmp = await compareFn(left[l], right[r]);
        if (cmp <= 0) {
          result.push(left[l]);
          l++;
        } else {
          result.push(right[r]);
          r++;
        }
      }
      return result.concat(left.slice(l)).concat(right.slice(r));
    }
    async function arraysort2(arr, compareFn) {
      if (!Array.isArray(arr))
        return [];
      if (typeof compareFn === "function") {
        const temp = [...arr];
        return await asyncMergeSort2(temp, compareFn);
      }
      return [...arr].sort();
    }
    async function arraymap2(arr, task) {
      if (!Array.isArray(arr))
        return [];
      if (typeof task !== "function")
        return [...arr];
      const results = [];
      for (let i = 0; i < arr.length; i++) {
        results.push(await task(arr[i], i, arr));
      }
      return results;
    }
    async function arrayfilter2(arr, task) {
      if (!Array.isArray(arr))
        return [];
      if (typeof task !== "function")
        return [...arr];
      const results = [];
      for (let i = 0; i < arr.length; i++) {
        if (await task(arr[i], i, arr)) {
          results.push(arr[i]);
        }
      }
      return results;
    }
    function arrayshift2(arr) {
      if (!Array.isArray(arr))
        return null;
      return arr.shift();
    }
    async function arrayfind2(arr, task) {
      if (!Array.isArray(arr))
        return null;
      if (typeof task !== "function")
        return null;
      for (let i = 0; i < arr.length; i++) {
        if (await task(arr[i], i, arr)) {
          return arr[i];
        }
      }
      return null;
    }
    function time2() {
      return Math.floor(Date.now() / 1e3);
    }
    function microtime2(getAsFloat = true) {
      if (getAsFloat === false) {
        const now = Date.now();
        const sec = Math.floor(now / 1e3);
        const msec = now % 1e3;
        return `${(msec / 1e3).toFixed(8)} ${sec}`;
      }
      return Date.now() / 1e3;
    }
    function dateyear2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getFullYear();
    }
    function datemonth2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getMonth() + 1;
    }
    function dateday2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getDate();
    }
    function datehour2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getHours();
    }
    function dateminute2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getMinutes();
    }
    function datesecond2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getSeconds();
    }
    function dateweekday2(ts, format = "number") {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      const day = d.getDay();
      if (format === "short" || format === "text") {
        const days = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];
        return days[day];
      }
      return day;
    }
    function template2(source, context = {}) {
      if (typeof source !== "string")
        return "";
      const appRoot = typeof global !== "undefined" && global.__app_root ? global.__app_root : process.cwd();
      const resolvePath = (p) => path2.isAbsolute(p) ? p : path2.resolve(appRoot, p);
      let html = source;
      try {
        const resolvedSource = resolvePath(source);
        if (fs2.existsSync(resolvedSource)) {
          html = fs2.readFileSync(resolvedSource, "utf8");
        }
        let includeDepth = 0;
        const maxIncludeDepth = 10;
        while (/@include\s*\(\s*["'](.*?)["']\s*\)/.test(html) && includeDepth < maxIncludeDepth) {
          html = html.replace(/@include\s*\(\s*["'](.*?)["']\s*\)/g, (match3, filepath) => {
            const resolvedInclude = resolvePath(filepath);
            if (!fs2.existsSync(resolvedInclude)) {
              throw new Error(`Include file not found: '${filepath}'`);
            }
            return fs2.readFileSync(resolvedInclude, "utf8");
          });
          includeDepth++;
        }
        html = html.replace(/@\}\s*(?=@else)/g, "@}");
        if (html.includes("</body>")) {
          const clientScript = `
<script>
function _of_exec_scripts(container) {
    const scripts = container.querySelectorAll('script');
    for (let oldScript of scripts) {
        const newScript = document.createElement('script');
        for (let attr of oldScript.attributes) {
            newScript.setAttribute(attr.name, attr.value);
        }
        newScript.textContent = oldScript.textContent;
        oldScript.parentNode.replaceChild(newScript, oldScript);
    }
}

document.addEventListener('submit', async (e) => {
    const form = e.target;
    const targetSelector = form.getAttribute('of-target');
    if (!targetSelector) return;
    
    e.preventDefault();
    try {
        const response = await fetch(form.action || window.location.href, {
            method: form.method || 'POST',
            body: new URLSearchParams(new FormData(form))
        });
        const htmlResult = await response.text();
        const targetEl = document.querySelector(targetSelector);
        if (targetEl) {
            targetEl.innerHTML = htmlResult;
            _of_exec_scripts(targetEl);
        }
    } catch (err) {
        console.error('OmniFlux Form Submission Error:', err);
    }
});

document.addEventListener('click', async (e) => {
    const link = e.target.closest('a');
    if (!link) return;
    const targetSelector = link.getAttribute('of-target');
    if (!targetSelector) return;
    
    e.preventDefault();
    try {
        const response = await fetch(link.href);
        const htmlResult = await response.text();
        const targetEl = document.querySelector(targetSelector);
        if (targetEl) {
            targetEl.innerHTML = htmlResult;
            _of_exec_scripts(targetEl);
        }
    } catch (err) {
        console.error('OmniFlux Link Navigation Error:', err);
    }
});
</script>
`;
          html = html.replace("</body>", clientScript + "</body>");
        }
        const tokenRegex = /(@if\s*\(.*?\)\s*\{|@else\s*if\s*\(.*?\)\s*\{|@else\s*\{|@for\s*\(.*?\)\s*\{|@\})/g;
        const parts = html.split(tokenRegex);
        let jsCode = "let _out = '';\n";
        jsCode += "with(global.vars || {}) {\n";
        jsCode += "with(context || {}) {\n";
        for (let part of parts) {
          if (!part)
            continue;
          if (part.startsWith("@if")) {
            const cond = part.match(/@if\s*\((.*?)\)\s*\{/)[1];
            jsCode += `if (${cond}) {
`;
          } else if (part.startsWith("@else if")) {
            const cond = part.match(/@else\s*if\s*\((.*?)\)\s*\{/)[1];
            jsCode += `} else if (${cond}) {
`;
          } else if (part.startsWith("@else")) {
            jsCode += `} else {
`;
          } else if (part.startsWith("@for")) {
            const loopMatch = part.match(/@for\s*\(\s*(?:let|var)?\s*(\w+)\s+of\s+(.*?)\)\s*\{/);
            if (loopMatch) {
              const item = loopMatch[1];
              const list = loopMatch[2];
              jsCode += `for (let ${item} of ${list}) {
`;
            }
          } else if (part === "@}") {
            jsCode += `}
`;
          } else {
            let text = part.replace(/\\/g, "\\\\").replace(/`/g, "\\`").replace(/\$/g, "\\$");
            text = text.replace(/\{\{\s*([\s\S]+?)\s*\}\}/g, "${$1}");
            jsCode += `_out += \`${text}\`;
`;
          }
        }
        jsCode += "}\n}\n";
        jsCode += "return _out;";
        const keys = ["context", ...Object.keys(context)];
        const values = [context, ...Object.values(context)];
        return new Function(...keys, jsCode)(...values);
      } catch (e) {
        return `<!-- Template Error: ${e.message} -->`;
      }
    }
    global.sprintf = sprintf2;
    global.print = print2;
    global.printf = printf2;
    global.fprint = fprint2;
    global.f_print = fprint2;
    global.fprintf = fprintf2;
    global.f_printf = fprintf2;
    global.input = input2;
    global.readchar = readchar2;
    global.read_char = readchar2;
    global.fileread = fileread2;
    global.file_read = fileread2;
    global.filewrite = filewrite2;
    global.file_write = filewrite2;
    global.fileexists = fileexists2;
    global.file_exists = fileexists2;
    global.fileappend = fileappend2;
    global.file_append = fileappend2;
    global.filedelete = filedelete2;
    global.file_delete = filedelete2;
    global.filecopy = filecopy2;
    global.file_copy = filecopy2;
    global.filerename = filerename2;
    global.file_rename = filerename2;
    global.dirlist = dirlist2;
    global.dir_list = dirlist2;
    global.dircreate = dircreate2;
    global.dir_create = dircreate2;
    global.scriptdir = scriptdir2;
    global.script_dir = scriptdir2;
    global.setenv = setenv2;
    global.set_env = setenv2;
    global.getenv = getenv2;
    global.get_env = getenv2;
    global.sha256 = sha2562;
    global.getcookie = getcookie2;
    global.get_cookie = getcookie2;
    global.setcookie = setcookie2;
    global.set_cookie = setcookie2;
    global.filestat = filestat2;
    global.file_stat = filestat2;
    global.dbsetfile = dbsetfile2;
    global.db_set_file = dbsetfile2;
    global.dbinsert = dbinsert2;
    global.db_insert = dbinsert2;
    global.dbselect = dbselect2;
    global.db_select = dbselect2;
    global.dbupdate = dbupdate2;
    global.db_update = dbupdate2;
    global.dbdelete = dbdelete2;
    global.db_delete = dbdelete2;
    global.cacheset = cacheset2;
    global.cache_set = cacheset2;
    global.cacheget = cacheget2;
    global.cache_get = cacheget2;
    global.len = len2;
    global.strsplit = strsplit2;
    global.str_split = strsplit2;
    global.match = match2;
    global.arraypush = arraypush2;
    global.array_push = arraypush2;
    global.arrayunshift = arrayunshift2;
    global.array_unshift = arrayunshift2;
    global.arraypop = arraypop2;
    global.array_pop = arraypop2;
    global.arraycontains = arraycontains2;
    global.array_contains = arraycontains2;
    global.arrayjoin = arrayjoin2;
    global.array_join = arrayjoin2;
    global.arrayslice = arrayslice2;
    global.array_slice = arrayslice2;
    global.strtrim = strtrim2;
    global.str_trim = strtrim2;
    global.strsub = strsub2;
    global.str_sub = strsub2;
    global.strindexof = strindexof2;
    global.str_index_of = strindexof2;
    global.strlastindexof = strlastindexof2;
    global.str_last_index_of = strlastindexof2;
    global.strrepeat = strrepeat2;
    global.str_repeat = strrepeat2;
    global.strreplace = strreplace2;
    global.str_replace = strreplace2;
    global.strupper = strupper2;
    global.str_upper = strupper2;
    global.strlower = strlower2;
    global.str_lower = strlower2;
    global.pathjoin = pathjoin2;
    global.path_join = pathjoin2;
    global.pathresolve = pathresolve2;
    global.path_resolve = pathresolve2;
    global.pathdirname = pathdirname2;
    global.path_dir_name = pathdirname2;
    global.pathbasename = pathbasename2;
    global.path_base_name = pathbasename2;
    global.pathextension = pathextension2;
    global.path_extension = pathextension2;
    global.pathisabsolute = pathisabsolute2;
    global.path_is_absolute = pathisabsolute2;
    global.exit = exit2;
    global.arrayreverse = arrayreverse2;
    global.array_reverse = arrayreverse2;
    global.arraysort = arraysort2;
    global.array_sort = arraysort2;
    global.arraymap = arraymap2;
    global.array_map = arraymap2;
    global.arrayfilter = arrayfilter2;
    global.array_filter = arrayfilter2;
    global.arrayshift = arrayshift2;
    global.array_shift = arrayshift2;
    global.arrayfind = arrayfind2;
    global.array_find = arrayfind2;
    global.time = time2;
    global.microtime = microtime2;
    global.micro_time = microtime2;
    global.dateyear = dateyear2;
    global.date_year = dateyear2;
    global.datemonth = datemonth2;
    global.date_month = datemonth2;
    global.dateday = dateday2;
    global.date_day = dateday2;
    global.datehour = datehour2;
    global.date_hour = datehour2;
    global.dateminute = dateminute2;
    global.date_minute = dateminute2;
    global.datesecond = datesecond2;
    global.date_second = datesecond2;
    global.dateweekday = dateweekday2;
    global.date_weekday = dateweekday2;
    global.template = template2;
    function formatRuntimeError2(err, label = "OmniFlux Runtime Error") {
      console.error("\n\x1B[1m\x1B[31m" + label + ":\x1B[0m");
      console.error("\x1B[33m" + err.message + "\x1B[0m");
      if (err.stack) {
        const stackLines = err.stack.split("\n");
        const lineMatch = stackLines.find(
          (line) => line.includes("/") && !line.includes("node:internal") && !line.includes("node_modules")
        );
        if (lineMatch) {
          const match3 = lineMatch.match(/\(([^)]+):(\d+):(\d+)\)/) || lineMatch.match(/at\s+(.+?):(\d+):(\d+)/);
          if (match3) {
            const filePath = match3[1];
            const lineNum = parseInt(match3[2], 10);
            try {
              const content = fs2.readFileSync(filePath, "utf8");
              const fileLines = content.split("\n");
              const indicesToCheck = [lineNum, lineNum - 1, lineNum - 2];
              for (const i of indicesToCheck) {
                if (i >= 0 && i < fileLines.length) {
                  const targetLine = fileLines[i];
                  const ofLineMatch = targetLine.match(/\/\/\! OF_LINE: (.+?)(?:\r?\n|$)/) || targetLine.match(/\/\/ OF_LINE: (.+?)(?:\r?\n|$)/);
                  if (ofLineMatch) {
                    console.error("\x1B[1m\x1B[32mAt: " + ofLineMatch[1] + "\x1B[0m");
                    console.error();
                    return;
                  }
                }
              }
            } catch (e) {
            }
            console.error("\x1B[2mAt: " + path2.basename(filePath) + ":" + lineNum + "\x1B[0m");
          }
        }
      }
      console.error();
    }
    process.on("uncaughtException", (err) => {
      const hooks = global.__on_error_hooks || [];
      if (hooks.length > 0) {
        for (const hook of hooks) {
          hook(err).catch(() => {
          });
        }
        return;
      }
      formatRuntimeError2(err);
      process.exit(1);
    });
    process.on("unhandledRejection", (reason) => {
      const hooks = global.__on_error_hooks || [];
      if (hooks.length > 0) {
        for (const hook of hooks) {
          hook(reason).catch(() => {
          });
        }
        return;
      }
      const err = reason instanceof Error ? reason : new Error(String(reason));
      formatRuntimeError2(err, "OmniFlux Runtime Error (Unhandled Rejection)");
      process.exit(1);
    });
    //! OF_LINE: validator.of:1
    //! OF_LINE: validator.of:2
    //! OF_LINE: validator.of:3
    //! OF_LINE: validator.of:4
    //! OF_LINE: validator.of:5
    //! OF_LINE: validator.of:6
    //! OF_LINE: validator.of:7
    //! OF_LINE: validator.of:8
    //! OF_LINE: validator.of:9
    //! OF_LINE: validator.of:10
    require_preprocessor();
    //! OF_LINE: validator.of:11
    //! OF_LINE: validator.of:12
    //! OF_LINE: validator.of:13
    //! OF_LINE: validator.of:14
    //! OF_LINE: validator.of:15
    async function validate2(content, filename) {
      try {
        //! OF_LINE: validator.of:16
        let brace_stack = [];
        //! OF_LINE: validator.of:17
        let paren_stack = [];
        //! OF_LINE: validator.of:18
        let bracket_stack = [];
        //! OF_LINE: validator.of:19
        //! OF_LINE: validator.of:20
        let in_string = "";
        //! OF_LINE: validator.of:21
        let in_node_block = false;
        //! OF_LINE: validator.of:22
        let in_block_comment = false;
        //! OF_LINE: validator.of:23
        let node_block_start = 0;
        //! OF_LINE: validator.of:24
        let block_comment_start = 0;
        //! OF_LINE: validator.of:25
        let in_definition_state = false;
        //! OF_LINE: validator.of:26
        let last_word = "";
        //! OF_LINE: validator.of:27
        //! OF_LINE: validator.of:28
        let lines = strsplit2(content, "\n");
        //! OF_LINE: validator.of:29
        let line_num = 0;
        //! OF_LINE: validator.of:30
        //! OF_LINE: validator.of:31
        for (let line of lines) {
          //! OF_LINE: validator.of:32
          line_num = line_num + 1;
          //! OF_LINE: validator.of:33
          let length = len2(line);
          //! OF_LINE: validator.of:34
          let i = 0;
          //! OF_LINE: validator.of:35
          //! OF_LINE: validator.of:36
          while (i < length) {
            //! OF_LINE: validator.of:37
            let ch = strsub2(line, i, 1);
            //! OF_LINE: validator.of:38
            //! OF_LINE: validator.of:39
            //! OF_LINE: validator.of:40
            if (in_node_block) {
              //! OF_LINE: validator.of:41
              if (ch == "@" && strsub2(line, i + 1, 1) == "}") {
                //! OF_LINE: validator.of:42
                in_node_block = false;
                //! OF_LINE: validator.of:43
                i = i + 2;
                //! OF_LINE: validator.of:44
              } else {
                //! OF_LINE: validator.of:45
                i = i + 1;
                //! OF_LINE: validator.of:46
              }
              //! OF_LINE: validator.of:47
              continue;
              //! OF_LINE: validator.of:48
            }
            //! OF_LINE: validator.of:49
            //! OF_LINE: validator.of:50
            //! OF_LINE: validator.of:51
            if (in_block_comment) {
              //! OF_LINE: validator.of:52
              if (ch == "*" && strsub2(line, i + 1, 1) == "/") {
                //! OF_LINE: validator.of:53
                in_block_comment = false;
                //! OF_LINE: validator.of:54
                i = i + 2;
                //! OF_LINE: validator.of:55
              } else {
                //! OF_LINE: validator.of:56
                i = i + 1;
                //! OF_LINE: validator.of:57
              }
              //! OF_LINE: validator.of:58
              continue;
              //! OF_LINE: validator.of:59
            }
            //! OF_LINE: validator.of:60
            //! OF_LINE: validator.of:61
            //! OF_LINE: validator.of:62
            if (in_string != "") {
              //! OF_LINE: validator.of:63
              if (ch == "\\") {
                //! OF_LINE: validator.of:64
                i = i + 2;
                //! OF_LINE: validator.of:65
              } else if (ch == in_string) {
                //! OF_LINE: validator.of:66
                in_string = "";
                //! OF_LINE: validator.of:67
                i = i + 1;
                //! OF_LINE: validator.of:68
              } else {
                //! OF_LINE: validator.of:69
                i = i + 1;
                //! OF_LINE: validator.of:70
              }
              //! OF_LINE: validator.of:71
              continue;
              //! OF_LINE: validator.of:72
            }
            //! OF_LINE: validator.of:73
            //! OF_LINE: validator.of:74
            //! OF_LINE: validator.of:75
            if (ch == "@" && strsub2(line, i + 1, 1) == "{") {
              //! OF_LINE: validator.of:76
              in_node_block = true;
              //! OF_LINE: validator.of:77
              node_block_start = line_num;
              //! OF_LINE: validator.of:78
              i = i + 2;
              //! OF_LINE: validator.of:79
              continue;
              //! OF_LINE: validator.of:80
            }
            //! OF_LINE: validator.of:81
            //! OF_LINE: validator.of:82
            //! OF_LINE: validator.of:83
            if (ch == "/" && strsub2(line, i + 1, 1) == "*") {
              //! OF_LINE: validator.of:84
              in_block_comment = true;
              //! OF_LINE: validator.of:85
              block_comment_start = line_num;
              //! OF_LINE: validator.of:86
              i = i + 2;
              //! OF_LINE: validator.of:87
              continue;
              //! OF_LINE: validator.of:88
            }
            //! OF_LINE: validator.of:89
            //! OF_LINE: validator.of:90
            //! OF_LINE: validator.of:91
            if (ch == "#") {
              //! OF_LINE: validator.of:92
              i = length;
              //! OF_LINE: validator.of:93
              continue;
              //! OF_LINE: validator.of:94
            }
            //! OF_LINE: validator.of:95
            if (ch == "/" && strsub2(line, i + 1, 1) == "/") {
              //! OF_LINE: validator.of:96
              i = length;
              //! OF_LINE: validator.of:97
              continue;
              //! OF_LINE: validator.of:98
            }
            //! OF_LINE: validator.of:99
            //! OF_LINE: validator.of:100
            //! OF_LINE: validator.of:101
            if (ch == '"' || ch == "'") {
              //! OF_LINE: validator.of:102
              in_string = ch;
              //! OF_LINE: validator.of:103
              last_word = "";
              //! OF_LINE: validator.of:104
              i = i + 1;
              //! OF_LINE: validator.of:105
              continue;
              //! OF_LINE: validator.of:106
            }
            //! OF_LINE: validator.of:107
            //! OF_LINE: validator.of:108
            //! OF_LINE: validator.of:109
            if (ch == " " || ch == "	" || ch == "\r") {
              //! OF_LINE: validator.of:110
              i = i + 1;
              //! OF_LINE: validator.of:111
              continue;
              //! OF_LINE: validator.of:112
            }
            //! OF_LINE: validator.of:113
            //! OF_LINE: validator.of:114
            //! OF_LINE: validator.of:115
            let word_match = match2(strsub2(line, i, length - i), "/^([a-zA-Z_]\\w*)/");
            //! OF_LINE: validator.of:116
            if (word_match) {
              //! OF_LINE: validator.of:117
              let word = word_match[0];
              //! OF_LINE: validator.of:118
              last_word = word;
              //! OF_LINE: validator.of:119
              if (word == "define" || word == "task" || word == "fn" || word == "on" || word == "GET" || word == "POST" || word == "PUT" || word == "DELETE" || word == "PATCH") {
                //! OF_LINE: validator.of:120
                in_definition_state = true;
                //! OF_LINE: validator.of:121
              }
              //! OF_LINE: validator.of:122
              i = i + len2(word);
              //! OF_LINE: validator.of:123
              continue;
              //! OF_LINE: validator.of:124
            }
            //! OF_LINE: validator.of:125
            //! OF_LINE: validator.of:126
            //! OF_LINE: validator.of:127
            if (ch == "{") {
              //! OF_LINE: validator.of:128
              if (in_definition_state && len2(brace_stack) > 0) {
                //! OF_LINE: validator.of:129
                return {
                  //! OF_LINE: validator.of:130
                  ok: false,
                  //! OF_LINE: validator.of:131
                  error: "SyntaxError in " + filename + " on line " + line_num + ": Nested definition of task or lifecycle hook is not allowed"
                  //! OF_LINE: validator.of:132
                };
                //! OF_LINE: validator.of:133
              }
              //! OF_LINE: validator.of:134
              arraypush2(brace_stack, { line: line_num, type: last_word || "block" });
              //! OF_LINE: validator.of:135
              last_word = "";
              //! OF_LINE: validator.of:136
              in_definition_state = false;
              //! OF_LINE: validator.of:137
              i = i + 1;
              //! OF_LINE: validator.of:138
              continue;
              //! OF_LINE: validator.of:139
            }
            //! OF_LINE: validator.of:140
            //! OF_LINE: validator.of:141
            //! OF_LINE: validator.of:142
            if (ch == "}") {
              //! OF_LINE: validator.of:143
              if (len2(brace_stack) == 0) {
                //! OF_LINE: validator.of:144
                return {
                  //! OF_LINE: validator.of:145
                  ok: false,
                  //! OF_LINE: validator.of:146
                  error: "SyntaxError in " + filename + " on line " + line_num + ": Unexpected closing brace '}' without a matching '{'"
                  //! OF_LINE: validator.of:147
                };
                //! OF_LINE: validator.of:148
              }
              //! OF_LINE: validator.of:149
              arraypop2(brace_stack);
              //! OF_LINE: validator.of:150
              last_word = "";
              //! OF_LINE: validator.of:151
              i = i + 1;
              //! OF_LINE: validator.of:152
              continue;
              //! OF_LINE: validator.of:153
            }
            //! OF_LINE: validator.of:154
            //! OF_LINE: validator.of:155
            //! OF_LINE: validator.of:156
            if (ch == "(") {
              //! OF_LINE: validator.of:157
              arraypush2(paren_stack, { line: line_num });
              //! OF_LINE: validator.of:158
              last_word = "";
              //! OF_LINE: validator.of:159
              i = i + 1;
              //! OF_LINE: validator.of:160
              continue;
              //! OF_LINE: validator.of:161
            }
            //! OF_LINE: validator.of:162
            //! OF_LINE: validator.of:163
            //! OF_LINE: validator.of:164
            if (ch == ")") {
              //! OF_LINE: validator.of:165
              if (len2(paren_stack) == 0) {
                //! OF_LINE: validator.of:166
                return {
                  //! OF_LINE: validator.of:167
                  ok: false,
                  //! OF_LINE: validator.of:168
                  error: "SyntaxError in " + filename + " on line " + line_num + ": Unexpected closing parenthesis ')' without a matching '('"
                  //! OF_LINE: validator.of:169
                };
                //! OF_LINE: validator.of:170
              }
              //! OF_LINE: validator.of:171
              arraypop2(paren_stack);
              //! OF_LINE: validator.of:172
              last_word = "";
              //! OF_LINE: validator.of:173
              i = i + 1;
              //! OF_LINE: validator.of:174
              continue;
              //! OF_LINE: validator.of:175
            }
            //! OF_LINE: validator.of:176
            //! OF_LINE: validator.of:177
            //! OF_LINE: validator.of:178
            if (ch == "[") {
              //! OF_LINE: validator.of:179
              arraypush2(bracket_stack, { line: line_num });
              //! OF_LINE: validator.of:180
              last_word = "";
              //! OF_LINE: validator.of:181
              i = i + 1;
              //! OF_LINE: validator.of:182
              continue;
              //! OF_LINE: validator.of:183
            }
            //! OF_LINE: validator.of:184
            //! OF_LINE: validator.of:185
            //! OF_LINE: validator.of:186
            if (ch == "]") {
              //! OF_LINE: validator.of:187
              if (len2(bracket_stack) == 0) {
                //! OF_LINE: validator.of:188
                return {
                  //! OF_LINE: validator.of:189
                  ok: false,
                  //! OF_LINE: validator.of:190
                  error: "SyntaxError in " + filename + " on line " + line_num + ": Unexpected closing bracket ']' without a matching '['"
                  //! OF_LINE: validator.of:191
                };
                //! OF_LINE: validator.of:192
              }
              //! OF_LINE: validator.of:193
              arraypop2(bracket_stack);
              //! OF_LINE: validator.of:194
              last_word = "";
              //! OF_LINE: validator.of:195
              i = i + 1;
              //! OF_LINE: validator.of:196
              continue;
              //! OF_LINE: validator.of:197
            }
            //! OF_LINE: validator.of:198
            //! OF_LINE: validator.of:199
            //! OF_LINE: validator.of:200
            last_word = "";
            //! OF_LINE: validator.of:201
            in_definition_state = false;
            //! OF_LINE: validator.of:202
            i = i + 1;
            //! OF_LINE: validator.of:203
          }
          //! OF_LINE: validator.of:204
        }
        //! OF_LINE: validator.of:205
        //! OF_LINE: validator.of:206
        //! OF_LINE: validator.of:207
        if (in_block_comment) {
          //! OF_LINE: validator.of:208
          return {
            //! OF_LINE: validator.of:209
            ok: false,
            //! OF_LINE: validator.of:210
            error: "SyntaxError in " + filename + ": Unclosed block comment opened on line " + block_comment_start
            //! OF_LINE: validator.of:211
          };
          //! OF_LINE: validator.of:212
        }
        //! OF_LINE: validator.of:213
        //! OF_LINE: validator.of:214
        if (in_node_block) {
          //! OF_LINE: validator.of:215
          return {
            //! OF_LINE: validator.of:216
            ok: false,
            //! OF_LINE: validator.of:217
            error: "SyntaxError in " + filename + ": Unclosed JS escape block opened on line " + node_block_start
            //! OF_LINE: validator.of:218
          };
          //! OF_LINE: validator.of:219
        }
        //! OF_LINE: validator.of:220
        //! OF_LINE: validator.of:221
        if (len2(brace_stack) > 0) {
          //! OF_LINE: validator.of:222
          let first_brace = brace_stack[0];
          //! OF_LINE: validator.of:223
          return {
            //! OF_LINE: validator.of:224
            ok: false,
            //! OF_LINE: validator.of:225
            error: "SyntaxError in " + filename + ": Unclosed block '" + first_brace.type + "' opened on line " + first_brace.line
            //! OF_LINE: validator.of:226
          };
          //! OF_LINE: validator.of:227
        }
        //! OF_LINE: validator.of:228
        //! OF_LINE: validator.of:229
        if (len2(paren_stack) > 0) {
          //! OF_LINE: validator.of:230
          let first_paren = paren_stack[0];
          //! OF_LINE: validator.of:231
          return {
            //! OF_LINE: validator.of:232
            ok: false,
            //! OF_LINE: validator.of:233
            error: "SyntaxError in " + filename + ": Unclosed parenthesis '(' opened on line " + first_paren.line
            //! OF_LINE: validator.of:234
          };
          //! OF_LINE: validator.of:235
        }
        //! OF_LINE: validator.of:236
        //! OF_LINE: validator.of:237
        if (len2(bracket_stack) > 0) {
          //! OF_LINE: validator.of:238
          let first_bracket = bracket_stack[0];
          //! OF_LINE: validator.of:239
          return {
            //! OF_LINE: validator.of:240
            ok: false,
            //! OF_LINE: validator.of:241
            error: "SyntaxError in " + filename + ": Unclosed bracket '[' opened on line " + first_bracket.line
            //! OF_LINE: validator.of:242
          };
          //! OF_LINE: validator.of:243
        }
        //! OF_LINE: validator.of:244
        //! OF_LINE: validator.of:245
        return { ok: true };
        //! OF_LINE: validator.of:246
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: validator.of:247
    //! OF_LINE: validator.of:248
    global.validate = validate2;
    if (require.main === module2) {
      (async () => {
        const hooks = global.__on_start_hooks || [];
        for (const hook of hooks) {
          await hook();
        }
      })().catch((err) => {
        if (typeof formatRuntimeError2 === "function") {
          formatRuntimeError2(err);
        } else {
          console.error(err);
        }
        process.exit(1);
      });
    }
  }
});

// .omniflux_cache/compiler/transpiler.js
var require_transpiler = __commonJS({
  ".omniflux_cache/compiler/transpiler.js"(exports2, module2) {
    var fs2 = require("fs");
    var util2 = require("util");
    var path2 = require("path");
    global.args = process.argv.slice(2);
    global.vars = new Proxy({ args: global.args }, {
      get(target, prop) {
        return prop in target ? target[prop] : null;
      },
      set(target, prop, value) {
        target[prop] = value;
        return true;
      }
    });
    function sprintf2(format, ...args2) {
      if (typeof format !== "string") {
        return util2.format(format, ...args2);
      }
      let argIndex = 0;
      const hasSpecifiers = /%([+-]?)(0?\d*)(?:\.(\d+))?([asdijf%])/g.test(format);
      if (!hasSpecifiers) {
        return util2.format(format, ...args2);
      }
      let formatted = format.replace(/%(?<flags>[+-]?)(?<width>0?\d*)(?:\.(?<precision>\d+))?(?<type>[asdijf%])/g, (match3, flags, widthStr, precisionStr, type) => {
        if (type === "%")
          return "%";
        if (argIndex >= args2.length)
          return match3;
        const val = args2[argIndex++];
        let padChar = " ";
        let width = 0;
        let leftAlign = flags === "-";
        if (widthStr) {
          if (widthStr.startsWith("0")) {
            padChar = "0";
            width = parseInt(widthStr, 10);
          } else {
            width = parseInt(widthStr, 10);
          }
        }
        let precision = precisionStr ? parseInt(precisionStr, 10) : -1;
        let result = "";
        if (type === "d" || type === "i") {
          let intVal = parseInt(val, 10);
          if (isNaN(intVal))
            intVal = 0;
          result = intVal.toString();
          if (precision >= 0) {
            result = result.padStart(precision, "0");
          }
        } else if (type === "f") {
          let floatVal = parseFloat(val);
          if (isNaN(floatVal))
            floatVal = 0;
          if (precision >= 0) {
            result = floatVal.toFixed(precision);
          } else {
            result = floatVal.toString();
          }
        } else if (type === "s") {
          result = String(val);
          if (precision >= 0) {
            result = result.slice(0, precision);
          }
        } else if (type === "j") {
          result = JSON.stringify(val);
        } else {
          result = String(val);
        }
        if (result.length < width) {
          const padLen = width - result.length;
          const padding = padChar.repeat(padLen);
          result = leftAlign ? result + padding : padding + result;
        }
        return result;
      });
      if (argIndex < args2.length) {
        const leftover = args2.slice(argIndex).map((x) => typeof x === "object" ? util2.inspect(x) : String(x));
        formatted += " " + leftover.join(" ");
      }
      return formatted;
    }
    function print2(format, ...args2) {
      console.log(sprintf2(format, ...args2));
    }
    function printf2(format, ...args2) {
      process.stdout.write(sprintf2(format, ...args2));
    }
    var _rl2 = null;
    function _getRL2() {
      if (!_rl2 && process.stdin.isTTY) {
        const readline = require("readline");
        _rl2 = readline.createInterface({
          input: process.stdin,
          output: process.stdout,
          terminal: true,
          historySize: 200
        });
        _rl2.on("close", () => {
          _rl2 = null;
        });
      }
      return _rl2;
    }
    function input2(promptMessage) {
      const rl = _getRL2();
      if (rl) {
        return new Promise((resolve) => {
          rl.question(promptMessage !== void 0 ? String(promptMessage) : "", resolve);
        });
      }
      if (promptMessage !== void 0) {
        process.stdout.write(String(promptMessage));
      }
      const buffer = Buffer.alloc(65536);
      let bytesRead = 0;
      try {
        bytesRead = fs2.readSync(0, buffer, 0, 65536, null);
      } catch (e) {
        return "";
      }
      return buffer.toString("utf8", 0, bytesRead).replace(/\r?\n$/, "");
    }
    global.input = input2;
    function readchar2() {
      const hasRaw = typeof process.stdin.setRawMode === "function";
      const isRaw = process.stdin.isRaw;
      if (hasRaw) {
        process.stdin.setRawMode(true);
      }
      const buffer = Buffer.alloc(1);
      let bytesRead = 0;
      try {
        bytesRead = fs2.readSync(0, buffer, 0, 1, null);
      } catch (e) {
      } finally {
        if (hasRaw) {
          process.stdin.setRawMode(isRaw);
        }
      }
      const char = buffer.toString("utf8", 0, bytesRead);
      if (char === "") {
        process.exit(130);
      }
      return char;
    }
    function fileread2(path3) {
      return fs2.readFileSync(path3, "utf8");
    }
    function filewrite2(path3, data) {
      fs2.writeFileSync(path3, typeof data === "object" ? JSON.stringify(data, null, 2) : data, "utf8");
    }
    function fileexists2(path3) {
      return fs2.existsSync(path3);
    }
    function filecopy2(src, dest) {
      fs2.copyFileSync(src, dest);
    }
    function filerename2(src, dest) {
      fs2.renameSync(src, dest);
    }
    function fileappend2(path3, data) {
      fs2.appendFileSync(path3, typeof data === "object" ? JSON.stringify(data, null, 2) : data, "utf8");
    }
    function fprintf2(path3, format, ...args2) {
      const data = sprintf2(format, ...args2);
      fs2.appendFileSync(path3, data, "utf8");
    }
    function fprint2(path3, format, ...args2) {
      const data = sprintf2(format, ...args2) + "\n";
      fs2.appendFileSync(path3, data, "utf8");
    }
    function filedelete2(path3) {
      fs2.unlinkSync(path3);
    }
    function dirlist2(path3) {
      return fs2.readdirSync(path3);
    }
    function filestat2(path3) {
      const stats = fs2.statSync(path3);
      return {
        size: stats.size,
        isdirectory: stats.isDirectory(),
        is_directory: stats.isDirectory(),
        isfile: stats.isFile(),
        is_file: stats.isFile(),
        createdat: stats.birthtimeMs,
        created_at: stats.birthtimeMs,
        modifiedat: stats.mtimeMs,
        modified_at: stats.mtimeMs
      };
    }
    function dircreate2(path3) {
      if (typeof path3 !== "string")
        throw new Error("Directory path must be a string");
      fs2.mkdirSync(path3, { recursive: true });
    }
    function scriptdir2() {
      const path3 = require("path");
      return path3.dirname(require.main.filename);
    }
    function setenv2(name, value) {
      if (typeof name !== "string")
        throw new Error("Environment variable name must be a string");
      process.env[name] = value !== void 0 ? String(value) : "";
    }
    function getenv2(name) {
      if (typeof name !== "string")
        throw new Error("Environment variable name must be a string");
      return process.env[name] !== void 0 ? process.env[name] : null;
    }
    function sha2562(text) {
      if (typeof text !== "string")
        throw new Error("Input to sha256 must be a string");
      const crypto = require("crypto");
      return crypto.createHash("sha256").update(text).digest("hex");
    }
    function getcookie2(req, name) {
      if (!req || !req.headers || !req.headers.cookie)
        return null;
      const cookies = req.headers.cookie.split(";");
      for (let c of cookies) {
        const parts = c.split("=");
        if (parts[0].trim() === name)
          return parts[1].trim();
      }
      return null;
    }
    function setcookie2(res, name, value, options = {}) {
      if (!res || typeof res.cookie !== "function") {
        throw new Error("First argument to setcookie must be an HTTP response object");
      }
      res.cookie(name, value, options);
    }
    var dbFilePath2 = process.env.DB_FILE || "db.json";
    var cachedDb2 = null;
    var cachedDbMtime2 = 0;
    function dbsetfile2(path3) {
      if (typeof path3 !== "string")
        throw new Error("Database file path must be a string");
      dbFilePath2 = path3;
      cachedDb2 = null;
      cachedDbMtime2 = 0;
    }
    function loadDb2() {
      try {
        if (fs2.existsSync(dbFilePath2)) {
          const stats = fs2.statSync(dbFilePath2);
          if (cachedDb2 && stats.mtimeMs === cachedDbMtime2) {
            return cachedDb2;
          }
          const content = fs2.readFileSync(dbFilePath2, "utf8");
          cachedDb2 = JSON.parse(content || "{}");
          cachedDbMtime2 = stats.mtimeMs;
          return cachedDb2;
        }
      } catch (e) {
      }
      if (!cachedDb2) {
        cachedDb2 = {};
        cachedDbMtime2 = 0;
      }
      return cachedDb2;
    }
    function saveDb2(data) {
      cachedDb2 = data;
      fs2.writeFileSync(dbFilePath2, JSON.stringify(data, null, 2), "utf8");
      try {
        if (fs2.existsSync(dbFilePath2)) {
          cachedDbMtime2 = fs2.statSync(dbFilePath2).mtimeMs;
        }
      } catch (e) {
        cachedDbMtime2 = Date.now();
      }
    }
    function dbinsert2(collection, doc) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      if (!doc || typeof doc !== "object")
        throw new Error("Document must be an object");
      const db = loadDb2();
      if (!db[collection]) {
        db[collection] = [];
      }
      const newDoc = { ...doc };
      if (newDoc.id === void 0) {
        newDoc.id = Date.now().toString(36) + Math.random().toString(36).substring(2, 6);
      }
      db[collection].push(newDoc);
      saveDb2(db);
      return newDoc;
    }
    function dbselect2(collection, filter) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      const db = loadDb2();
      const list = db[collection] || [];
      if (!filter || typeof filter !== "object" || Object.keys(filter).length === 0) {
        return list.map((d) => ({ ...d }));
      }
      return list.filter((doc) => {
        for (const key in filter) {
          if (doc[key] !== filter[key]) {
            return false;
          }
        }
        return true;
      }).map((d) => ({ ...d }));
    }
    function dbupdate2(collection, filter, update) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      if (!update || typeof update !== "object")
        throw new Error("Update payload must be an object");
      const db = loadDb2();
      const list = db[collection] || [];
      let count = 0;
      for (let i = 0; i < list.length; i++) {
        const doc = list[i];
        let matches = true;
        if (filter && typeof filter === "object") {
          for (const key in filter) {
            if (doc[key] !== filter[key]) {
              matches = false;
              break;
            }
          }
        }
        if (matches) {
          list[i] = { ...doc, ...update };
          count++;
        }
      }
      if (count > 0) {
        db[collection] = list;
        saveDb2(db);
      }
      return count;
    }
    function dbdelete2(collection, filter) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      const db = loadDb2();
      const list = db[collection] || [];
      let count = 0;
      const newList = [];
      for (const doc of list) {
        let matches = true;
        if (filter && typeof filter === "object") {
          for (const key in filter) {
            if (doc[key] !== filter[key]) {
              matches = false;
              break;
            }
          }
        }
        if (matches) {
          count++;
        } else {
          newList.push(doc);
        }
      }
      if (count > 0) {
        db[collection] = newList;
        saveDb2(db);
      }
      return count;
    }
    var redisModule2 = null;
    var redisClient2 = null;
    async function getRedisClient2() {
      if (!redisModule2) {
        redisModule2 = require("redis");
      }
      if (!redisClient2) {
        const url = process.env.REDIS_URL || "redis://localhost:6379";
        redisClient2 = redisModule2.createClient({ url });
        await redisClient2.connect();
      }
      return redisClient2;
    }
    async function cacheset2(key, val, ttl) {
      const client = await getRedisClient2();
      const stringVal = typeof val === "object" ? JSON.stringify(val) : String(val);
      if (ttl) {
        await client.setEx(key, ttl, stringVal);
      } else {
        await client.set(key, stringVal);
      }
    }
    async function cacheget2(key) {
      const client = await getRedisClient2();
      const val = await client.get(key);
      if (!val)
        return null;
      try {
        return JSON.parse(val);
      } catch (e) {
        return val;
      }
    }
    function describe2(val) {
      if (val === null)
        return "null";
      if (val === void 0)
        return "undefined";
      if (Array.isArray(val))
        return "array";
      return typeof val;
    }
    global.describe = describe2;
    function jsonparse2(str) {
      try {
        return JSON.parse(String(str || ""));
      } catch (e) {
        return null;
      }
    }
    global.jsonparse = jsonparse2;
    global.jsondecode = jsonparse2;
    function jsonencode2(val, pretty = false) {
      try {
        return pretty ? JSON.stringify(val, null, 2) : JSON.stringify(val);
      } catch (e) {
        return "null";
      }
    }
    global.jsonencode = jsonencode2;
    function len2(val) {
      if (val === null || val === void 0)
        return 0;
      return val.length || 0;
    }
    function toRegExp2(regex) {
      if (regex instanceof RegExp) {
        return regex;
      }
      if (typeof regex === "string") {
        const firstChar = regex.charAt(0);
        if (/^[^a-zA-Z0-9\s\\]/.test(firstChar)) {
          const lastDelim = regex.lastIndexOf(firstChar);
          if (lastDelim > 0) {
            const pattern = regex.substring(1, lastDelim);
            const flags = regex.substring(lastDelim + 1);
            return new RegExp(pattern, flags);
          }
        }
        return new RegExp(regex);
      }
      return null;
    }
    function strsplit2(str, sep) {
      if (typeof str !== "string")
        return [];
      if (sep === void 0)
        return [str];
      const regexObj = toRegExp2(sep);
      if (regexObj) {
        return str.split(regexObj);
      }
      return str.split(sep);
    }
    function match2(str, regex) {
      if (str === null || str === void 0)
        return false;
      const regexObj = toRegExp2(regex);
      if (!regexObj)
        return false;
      const res = String(str).match(regexObj);
      if (!res)
        return false;
      if (res.length > 1) {
        return res.slice(1);
      }
      return true;
    }
    function strtrim2(str, side = "both") {
      const s = String(str || "");
      if (side === "left")
        return s.trimStart();
      if (side === "right")
        return s.trimEnd();
      return s.trim();
    }
    function strsub2(str, start, length) {
      const s = String(str || "");
      if (length === void 0) {
        return s.substring(start);
      }
      return s.substring(start, start + length);
    }
    function strindexof2(str, search) {
      const s = String(str || "");
      return s.indexOf(search);
    }
    function strlastindexof2(str, search) {
      const s = String(str || "");
      return s.lastIndexOf(search);
    }
    function strrepeat2(str, n) {
      const s = String(str || "");
      const count = parseInt(n, 10);
      if (isNaN(count) || count <= 0)
        return "";
      return s.repeat(count);
    }
    function strreplace2(str, search, replacement) {
      const s = String(str || "");
      const r = String(replacement !== void 0 ? replacement : "");
      const regexObj = toRegExp2(search);
      if (regexObj) {
        return s.replace(regexObj, r);
      }
      return s.replaceAll(search, r);
    }
    function strupper2(str) {
      return String(str || "").toUpperCase();
    }
    function strlower2(str) {
      return String(str || "").toLowerCase();
    }
    function arraypush2(arr, item) {
      if (Array.isArray(arr)) {
        arr.push(item);
      }
    }
    function arrayunshift2(arr, item) {
      if (Array.isArray(arr)) {
        arr.unshift(item);
      }
    }
    function arraypop2(arr) {
      if (Array.isArray(arr)) {
        return arr.pop();
      }
      return null;
    }
    function arraycontains2(arr, item) {
      if (Array.isArray(arr)) {
        return arr.includes(item);
      }
      return false;
    }
    function arrayjoin2(arr, sep) {
      if (Array.isArray(arr)) {
        return arr.join(sep);
      }
      return "";
    }
    function arrayslice2(arr, start, end) {
      if (Array.isArray(arr)) {
        return arr.slice(start, end);
      }
      return [];
    }
    function pathjoin2(...paths) {
      const path3 = require("path");
      return path3.join(...paths.map((p) => String(p || "")));
    }
    function pathresolve2(base, relative) {
      const path3 = require("path");
      if (relative === void 0) {
        return path3.resolve(String(base || ""));
      }
      return path3.resolve(String(base || ""), String(relative || ""));
    }
    function pathdirname2(filepath) {
      const path3 = require("path");
      return path3.dirname(String(filepath || ""));
    }
    function pathbasename2(filepath) {
      const path3 = require("path");
      return path3.basename(String(filepath || ""));
    }
    function pathextension2(filepath) {
      const path3 = require("path");
      return path3.extname(String(filepath || ""));
    }
    function pathisabsolute2(filepath) {
      const path3 = require("path");
      return path3.isAbsolute(String(filepath || ""));
    }
    function exit2(code = 0) {
      process.exit(parseInt(code, 10) || 0);
    }
    function arrayreverse2(arr) {
      if (!Array.isArray(arr))
        return [];
      return [...arr].reverse();
    }
    async function asyncMergeSort2(arr, compareFn) {
      if (arr.length <= 1)
        return arr;
      const mid = Math.floor(arr.length / 2);
      const left = arr.slice(0, mid);
      const right = arr.slice(mid);
      return await asyncMerge2(
        await asyncMergeSort2(left, compareFn),
        await asyncMergeSort2(right, compareFn),
        compareFn
      );
    }
    async function asyncMerge2(left, right, compareFn) {
      const result = [];
      let l = 0, r = 0;
      while (l < left.length && r < right.length) {
        const cmp = await compareFn(left[l], right[r]);
        if (cmp <= 0) {
          result.push(left[l]);
          l++;
        } else {
          result.push(right[r]);
          r++;
        }
      }
      return result.concat(left.slice(l)).concat(right.slice(r));
    }
    async function arraysort2(arr, compareFn) {
      if (!Array.isArray(arr))
        return [];
      if (typeof compareFn === "function") {
        const temp = [...arr];
        return await asyncMergeSort2(temp, compareFn);
      }
      return [...arr].sort();
    }
    async function arraymap2(arr, task) {
      if (!Array.isArray(arr))
        return [];
      if (typeof task !== "function")
        return [...arr];
      const results = [];
      for (let i = 0; i < arr.length; i++) {
        results.push(await task(arr[i], i, arr));
      }
      return results;
    }
    async function arrayfilter2(arr, task) {
      if (!Array.isArray(arr))
        return [];
      if (typeof task !== "function")
        return [...arr];
      const results = [];
      for (let i = 0; i < arr.length; i++) {
        if (await task(arr[i], i, arr)) {
          results.push(arr[i]);
        }
      }
      return results;
    }
    function arrayshift2(arr) {
      if (!Array.isArray(arr))
        return null;
      return arr.shift();
    }
    async function arrayfind2(arr, task) {
      if (!Array.isArray(arr))
        return null;
      if (typeof task !== "function")
        return null;
      for (let i = 0; i < arr.length; i++) {
        if (await task(arr[i], i, arr)) {
          return arr[i];
        }
      }
      return null;
    }
    function time2() {
      return Math.floor(Date.now() / 1e3);
    }
    function microtime2(getAsFloat = true) {
      if (getAsFloat === false) {
        const now = Date.now();
        const sec = Math.floor(now / 1e3);
        const msec = now % 1e3;
        return `${(msec / 1e3).toFixed(8)} ${sec}`;
      }
      return Date.now() / 1e3;
    }
    function dateyear2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getFullYear();
    }
    function datemonth2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getMonth() + 1;
    }
    function dateday2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getDate();
    }
    function datehour2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getHours();
    }
    function dateminute2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getMinutes();
    }
    function datesecond2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getSeconds();
    }
    function dateweekday2(ts, format = "number") {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      const day = d.getDay();
      if (format === "short" || format === "text") {
        const days = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];
        return days[day];
      }
      return day;
    }
    function template2(source, context = {}) {
      if (typeof source !== "string")
        return "";
      const appRoot = typeof global !== "undefined" && global.__app_root ? global.__app_root : process.cwd();
      const resolvePath = (p) => path2.isAbsolute(p) ? p : path2.resolve(appRoot, p);
      let html = source;
      try {
        const resolvedSource = resolvePath(source);
        if (fs2.existsSync(resolvedSource)) {
          html = fs2.readFileSync(resolvedSource, "utf8");
        }
        let includeDepth = 0;
        const maxIncludeDepth = 10;
        while (/@include\s*\(\s*["'](.*?)["']\s*\)/.test(html) && includeDepth < maxIncludeDepth) {
          html = html.replace(/@include\s*\(\s*["'](.*?)["']\s*\)/g, (match3, filepath) => {
            const resolvedInclude = resolvePath(filepath);
            if (!fs2.existsSync(resolvedInclude)) {
              throw new Error(`Include file not found: '${filepath}'`);
            }
            return fs2.readFileSync(resolvedInclude, "utf8");
          });
          includeDepth++;
        }
        html = html.replace(/@\}\s*(?=@else)/g, "@}");
        if (html.includes("</body>")) {
          const clientScript = `
<script>
function _of_exec_scripts(container) {
    const scripts = container.querySelectorAll('script');
    for (let oldScript of scripts) {
        const newScript = document.createElement('script');
        for (let attr of oldScript.attributes) {
            newScript.setAttribute(attr.name, attr.value);
        }
        newScript.textContent = oldScript.textContent;
        oldScript.parentNode.replaceChild(newScript, oldScript);
    }
}

document.addEventListener('submit', async (e) => {
    const form = e.target;
    const targetSelector = form.getAttribute('of-target');
    if (!targetSelector) return;
    
    e.preventDefault();
    try {
        const response = await fetch(form.action || window.location.href, {
            method: form.method || 'POST',
            body: new URLSearchParams(new FormData(form))
        });
        const htmlResult = await response.text();
        const targetEl = document.querySelector(targetSelector);
        if (targetEl) {
            targetEl.innerHTML = htmlResult;
            _of_exec_scripts(targetEl);
        }
    } catch (err) {
        console.error('OmniFlux Form Submission Error:', err);
    }
});

document.addEventListener('click', async (e) => {
    const link = e.target.closest('a');
    if (!link) return;
    const targetSelector = link.getAttribute('of-target');
    if (!targetSelector) return;
    
    e.preventDefault();
    try {
        const response = await fetch(link.href);
        const htmlResult = await response.text();
        const targetEl = document.querySelector(targetSelector);
        if (targetEl) {
            targetEl.innerHTML = htmlResult;
            _of_exec_scripts(targetEl);
        }
    } catch (err) {
        console.error('OmniFlux Link Navigation Error:', err);
    }
});
</script>
`;
          html = html.replace("</body>", clientScript + "</body>");
        }
        const tokenRegex = /(@if\s*\(.*?\)\s*\{|@else\s*if\s*\(.*?\)\s*\{|@else\s*\{|@for\s*\(.*?\)\s*\{|@\})/g;
        const parts = html.split(tokenRegex);
        let jsCode = "let _out = '';\n";
        jsCode += "with(global.vars || {}) {\n";
        jsCode += "with(context || {}) {\n";
        for (let part of parts) {
          if (!part)
            continue;
          if (part.startsWith("@if")) {
            const cond = part.match(/@if\s*\((.*?)\)\s*\{/)[1];
            jsCode += `if (${cond}) {
`;
          } else if (part.startsWith("@else if")) {
            const cond = part.match(/@else\s*if\s*\((.*?)\)\s*\{/)[1];
            jsCode += `} else if (${cond}) {
`;
          } else if (part.startsWith("@else")) {
            jsCode += `} else {
`;
          } else if (part.startsWith("@for")) {
            const loopMatch = part.match(/@for\s*\(\s*(?:let|var)?\s*(\w+)\s+of\s+(.*?)\)\s*\{/);
            if (loopMatch) {
              const item = loopMatch[1];
              const list = loopMatch[2];
              jsCode += `for (let ${item} of ${list}) {
`;
            }
          } else if (part === "@}") {
            jsCode += `}
`;
          } else {
            let text = part.replace(/\\/g, "\\\\").replace(/`/g, "\\`").replace(/\$/g, "\\$");
            text = text.replace(/\{\{\s*([\s\S]+?)\s*\}\}/g, "${$1}");
            jsCode += `_out += \`${text}\`;
`;
          }
        }
        jsCode += "}\n}\n";
        jsCode += "return _out;";
        const keys = ["context", ...Object.keys(context)];
        const values = [context, ...Object.values(context)];
        return new Function(...keys, jsCode)(...values);
      } catch (e) {
        return `<!-- Template Error: ${e.message} -->`;
      }
    }
    global.sprintf = sprintf2;
    global.print = print2;
    global.printf = printf2;
    global.fprint = fprint2;
    global.f_print = fprint2;
    global.fprintf = fprintf2;
    global.f_printf = fprintf2;
    global.input = input2;
    global.readchar = readchar2;
    global.read_char = readchar2;
    global.fileread = fileread2;
    global.file_read = fileread2;
    global.filewrite = filewrite2;
    global.file_write = filewrite2;
    global.fileexists = fileexists2;
    global.file_exists = fileexists2;
    global.fileappend = fileappend2;
    global.file_append = fileappend2;
    global.filedelete = filedelete2;
    global.file_delete = filedelete2;
    global.filecopy = filecopy2;
    global.file_copy = filecopy2;
    global.filerename = filerename2;
    global.file_rename = filerename2;
    global.dirlist = dirlist2;
    global.dir_list = dirlist2;
    global.dircreate = dircreate2;
    global.dir_create = dircreate2;
    global.scriptdir = scriptdir2;
    global.script_dir = scriptdir2;
    global.setenv = setenv2;
    global.set_env = setenv2;
    global.getenv = getenv2;
    global.get_env = getenv2;
    global.sha256 = sha2562;
    global.getcookie = getcookie2;
    global.get_cookie = getcookie2;
    global.setcookie = setcookie2;
    global.set_cookie = setcookie2;
    global.filestat = filestat2;
    global.file_stat = filestat2;
    global.dbsetfile = dbsetfile2;
    global.db_set_file = dbsetfile2;
    global.dbinsert = dbinsert2;
    global.db_insert = dbinsert2;
    global.dbselect = dbselect2;
    global.db_select = dbselect2;
    global.dbupdate = dbupdate2;
    global.db_update = dbupdate2;
    global.dbdelete = dbdelete2;
    global.db_delete = dbdelete2;
    global.cacheset = cacheset2;
    global.cache_set = cacheset2;
    global.cacheget = cacheget2;
    global.cache_get = cacheget2;
    global.len = len2;
    global.strsplit = strsplit2;
    global.str_split = strsplit2;
    global.match = match2;
    global.arraypush = arraypush2;
    global.array_push = arraypush2;
    global.arrayunshift = arrayunshift2;
    global.array_unshift = arrayunshift2;
    global.arraypop = arraypop2;
    global.array_pop = arraypop2;
    global.arraycontains = arraycontains2;
    global.array_contains = arraycontains2;
    global.arrayjoin = arrayjoin2;
    global.array_join = arrayjoin2;
    global.arrayslice = arrayslice2;
    global.array_slice = arrayslice2;
    global.strtrim = strtrim2;
    global.str_trim = strtrim2;
    global.strsub = strsub2;
    global.str_sub = strsub2;
    global.strindexof = strindexof2;
    global.str_index_of = strindexof2;
    global.strlastindexof = strlastindexof2;
    global.str_last_index_of = strlastindexof2;
    global.strrepeat = strrepeat2;
    global.str_repeat = strrepeat2;
    global.strreplace = strreplace2;
    global.str_replace = strreplace2;
    global.strupper = strupper2;
    global.str_upper = strupper2;
    global.strlower = strlower2;
    global.str_lower = strlower2;
    global.pathjoin = pathjoin2;
    global.path_join = pathjoin2;
    global.pathresolve = pathresolve2;
    global.path_resolve = pathresolve2;
    global.pathdirname = pathdirname2;
    global.path_dir_name = pathdirname2;
    global.pathbasename = pathbasename2;
    global.path_base_name = pathbasename2;
    global.pathextension = pathextension2;
    global.path_extension = pathextension2;
    global.pathisabsolute = pathisabsolute2;
    global.path_is_absolute = pathisabsolute2;
    global.exit = exit2;
    global.arrayreverse = arrayreverse2;
    global.array_reverse = arrayreverse2;
    global.arraysort = arraysort2;
    global.array_sort = arraysort2;
    global.arraymap = arraymap2;
    global.array_map = arraymap2;
    global.arrayfilter = arrayfilter2;
    global.array_filter = arrayfilter2;
    global.arrayshift = arrayshift2;
    global.array_shift = arrayshift2;
    global.arrayfind = arrayfind2;
    global.array_find = arrayfind2;
    global.time = time2;
    global.microtime = microtime2;
    global.micro_time = microtime2;
    global.dateyear = dateyear2;
    global.date_year = dateyear2;
    global.datemonth = datemonth2;
    global.date_month = datemonth2;
    global.dateday = dateday2;
    global.date_day = dateday2;
    global.datehour = datehour2;
    global.date_hour = datehour2;
    global.dateminute = dateminute2;
    global.date_minute = dateminute2;
    global.datesecond = datesecond2;
    global.date_second = datesecond2;
    global.dateweekday = dateweekday2;
    global.date_weekday = dateweekday2;
    global.template = template2;
    function formatRuntimeError2(err, label = "OmniFlux Runtime Error") {
      console.error("\n\x1B[1m\x1B[31m" + label + ":\x1B[0m");
      console.error("\x1B[33m" + err.message + "\x1B[0m");
      if (err.stack) {
        const stackLines = err.stack.split("\n");
        const lineMatch = stackLines.find(
          (line) => line.includes("/") && !line.includes("node:internal") && !line.includes("node_modules")
        );
        if (lineMatch) {
          const match3 = lineMatch.match(/\(([^)]+):(\d+):(\d+)\)/) || lineMatch.match(/at\s+(.+?):(\d+):(\d+)/);
          if (match3) {
            const filePath = match3[1];
            const lineNum = parseInt(match3[2], 10);
            try {
              const content = fs2.readFileSync(filePath, "utf8");
              const fileLines = content.split("\n");
              const indicesToCheck = [lineNum, lineNum - 1, lineNum - 2];
              for (const i of indicesToCheck) {
                if (i >= 0 && i < fileLines.length) {
                  const targetLine = fileLines[i];
                  const ofLineMatch = targetLine.match(/\/\/\! OF_LINE: (.+?)(?:\r?\n|$)/) || targetLine.match(/\/\/ OF_LINE: (.+?)(?:\r?\n|$)/);
                  if (ofLineMatch) {
                    console.error("\x1B[1m\x1B[32mAt: " + ofLineMatch[1] + "\x1B[0m");
                    console.error();
                    return;
                  }
                }
              }
            } catch (e) {
            }
            console.error("\x1B[2mAt: " + path2.basename(filePath) + ":" + lineNum + "\x1B[0m");
          }
        }
      }
      console.error();
    }
    process.on("uncaughtException", (err) => {
      const hooks = global.__on_error_hooks || [];
      if (hooks.length > 0) {
        for (const hook of hooks) {
          hook(err).catch(() => {
          });
        }
        return;
      }
      formatRuntimeError2(err);
      process.exit(1);
    });
    process.on("unhandledRejection", (reason) => {
      const hooks = global.__on_error_hooks || [];
      if (hooks.length > 0) {
        for (const hook of hooks) {
          hook(reason).catch(() => {
          });
        }
        return;
      }
      const err = reason instanceof Error ? reason : new Error(String(reason));
      formatRuntimeError2(err, "OmniFlux Runtime Error (Unhandled Rejection)");
      process.exit(1);
    });
    //! OF_LINE: transpiler.of:1
    //! OF_LINE: transpiler.of:2
    //! OF_LINE: transpiler.of:3
    //! OF_LINE: transpiler.of:4
    //! OF_LINE: transpiler.of:5
    //! OF_LINE: transpiler.of:6
    //! OF_LINE: transpiler.of:7
    //! OF_LINE: transpiler.of:8
    require_symbols();
    //! OF_LINE: transpiler.of:9
    //! OF_LINE: transpiler.of:10
    //! OF_LINE: transpiler.of:11
    //! OF_LINE: transpiler.of:12
    //! OF_LINE: transpiler.of:13
    async function ensure_parens(cond) {
      try {
        //! OF_LINE: transpiler.of:14
        let trimmed = strtrim2(cond);
        //! OF_LINE: transpiler.of:15
        if (strsub2(trimmed, 0, 1) == "(" && strsub2(trimmed, len2(trimmed) - 1, 1) == ")") {
          //! OF_LINE: transpiler.of:16
          return trimmed;
          //! OF_LINE: transpiler.of:17
        }
        //! OF_LINE: transpiler.of:18
        return "(" + trimmed + ")";
        //! OF_LINE: transpiler.of:19
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: transpiler.of:20
    //! OF_LINE: transpiler.of:21
    //! OF_LINE: transpiler.of:22
    //! OF_LINE: transpiler.of:23
    //! OF_LINE: transpiler.of:24
    async function clean_template_args(content) {
      try {
        //! OF_LINE: transpiler.of:25
        let cleaned = content;
        //! OF_LINE: transpiler.of:26
        if (match2(cleaned, "/^\\((.*)\\)$/")) {
          //! OF_LINE: transpiler.of:27
          let m = match2(cleaned, "/^\\((.*)\\)$/");
          //! OF_LINE: transpiler.of:28
          cleaned = m[0];
          //! OF_LINE: transpiler.of:29
        }
        //! OF_LINE: transpiler.of:30
        return cleaned;
        //! OF_LINE: transpiler.of:31
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: transpiler.of:32
    //! OF_LINE: transpiler.of:33
    //! OF_LINE: transpiler.of:34
    //! OF_LINE: transpiler.of:35
    //! OF_LINE: transpiler.of:36
    async function replace_globals(line) {
      try {
        //! OF_LINE: transpiler.of:37
        return strreplace2(line, "/\\$([a-zA-Z_]\\w*)/g", "global.vars.$1");
        //! OF_LINE: transpiler.of:38
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: transpiler.of:39
    //! OF_LINE: transpiler.of:40
    //! OF_LINE: transpiler.of:41
    //! OF_LINE: transpiler.of:42
    //! OF_LINE: transpiler.of:43
    async function insert_awaits(line, user_tasks) {
      try {
        //! OF_LINE: transpiler.of:44
        let result = line;
        //! OF_LINE: transpiler.of:45
        var all_async = Array.isArray(global.ASYNC_BUILTINS) ? global.ASYNC_BUILTINS : Array.from(global.ASYNC_BUILTINS || []);
        //! OF_LINE: transpiler.of:46
        for (let name of all_async) {
          //! OF_LINE: transpiler.of:47
          //! OF_LINE: transpiler.of:48
          let pat_with = "/\\b(?<!await\\s)(?<!task\\s)(?<!fn\\s)(?<!global\\.)" + name + "(?=\\s*\\()/g";
          //! OF_LINE: transpiler.of:49
          result = strreplace2(result, pat_with, "await " + name);
          //! OF_LINE: transpiler.of:50
          //! OF_LINE: transpiler.of:51
          let pat_bare = "/\\b(?<!await\\s)(?<!task\\s)(?<!fn\\s)(?<!global\\.)(?<!global\\.\\w+\\s*=\\s*)(?<!global\\.vars\\.\\w+\\s*=\\s*)" + name + "\\b(?!\\s*\\()/g";
          //! OF_LINE: transpiler.of:52
          result = strreplace2(result, pat_bare, "await " + name + "()");
          //! OF_LINE: transpiler.of:53
        }
        //! OF_LINE: transpiler.of:54
        var task_names = Object.keys(user_tasks);
        //! OF_LINE: transpiler.of:55
        for (let name2 of task_names) {
          //! OF_LINE: transpiler.of:56
          //! OF_LINE: transpiler.of:57
          let pat2_with = "/\\b(?<!await\\s)(?<!task\\s)(?<!fn\\s)(?<!global\\.)(?<!REFERENCE_)" + name2 + "(?=\\s*\\()/g";
          //! OF_LINE: transpiler.of:58
          result = strreplace2(result, pat2_with, "await " + name2);
          //! OF_LINE: transpiler.of:59
          //! OF_LINE: transpiler.of:60
          let pat2_bare = "/\\b(?<!await\\s)(?<!task\\s)(?<!fn\\s)(?<!global\\.)(?<!global\\.\\w+\\s*=\\s*)(?<!global\\.vars\\.\\w+\\s*=\\s*)(?<!REFERENCE_)" + name2 + "\\b(?!\\s*\\()/g";
          //! OF_LINE: transpiler.of:61
          result = strreplace2(result, pat2_bare, "await " + name2 + "()");
          //! OF_LINE: transpiler.of:62
        }
        //! OF_LINE: transpiler.of:63
        return result;
        //! OF_LINE: transpiler.of:64
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: transpiler.of:65
    //! OF_LINE: transpiler.of:66
    //! OF_LINE: transpiler.of:67
    //! OF_LINE: transpiler.of:68
    //! OF_LINE: transpiler.of:69
    //! OF_LINE: transpiler.of:70
    //! OF_LINE: transpiler.of:71
    //! OF_LINE: transpiler.of:72
    async function transpile_line(raw_line, filename, user_tasks, line_num, state) {
      try {
        //! OF_LINE: transpiler.of:73
        let line = strtrim2(raw_line);
        //! OF_LINE: transpiler.of:74
        //! OF_LINE: transpiler.of:75
        //! OF_LINE: transpiler.of:76
        let single_line_match = null;
        //! OF_LINE: transpiler.of:77
        //! OF_LINE: transpiler.of:78
        single_line_match = raw_line.match(/^(\s*)(if\b[^{]+|while\b[^{]+|for\b[^{]+|else\s+if\b[^{]+|else\s*)\{\s*([^}]+?)\s*\}\s*$/i);
        //! OF_LINE: transpiler.of:79
        //! OF_LINE: transpiler.of:80
        if (single_line_match != null) {
          //! OF_LINE: transpiler.of:81
          let indent = "";
          //! OF_LINE: transpiler.of:82
          let header = "";
          //! OF_LINE: transpiler.of:83
          let body = "";
          //! OF_LINE: transpiler.of:84
          //! OF_LINE: transpiler.of:85
          indent = single_line_match[1];
          //! OF_LINE: transpiler.of:86
          header = single_line_match[2].trim();
          //! OF_LINE: transpiler.of:87
          body = single_line_match[3].trim();
          //! OF_LINE: transpiler.of:88
          //! OF_LINE: transpiler.of:89
          let line1 = indent + header + " {";
          //! OF_LINE: transpiler.of:90
          let line2 = indent + "    " + body;
          //! OF_LINE: transpiler.of:91
          let line3 = indent + "}";
          //! OF_LINE: transpiler.of:92
          //! OF_LINE: transpiler.of:93
          let out1 = await transpile_line(line1, filename, user_tasks, line_num, state);
          //! OF_LINE: transpiler.of:94
          let out2 = await transpile_line(line2, filename, user_tasks, line_num, state);
          //! OF_LINE: transpiler.of:95
          let out3 = await transpile_line(line3, filename, user_tasks, line_num, state);
          //! OF_LINE: transpiler.of:96
          //! OF_LINE: transpiler.of:97
          let combined_out = [];
          //! OF_LINE: transpiler.of:98
          for (let l1 of out1) {
            //! OF_LINE: transpiler.of:99
            arraypush2(combined_out, l1);
            //! OF_LINE: transpiler.of:100
          }
          //! OF_LINE: transpiler.of:101
          for (let l2 of out2) {
            //! OF_LINE: transpiler.of:102
            arraypush2(combined_out, l2);
            //! OF_LINE: transpiler.of:103
          }
          //! OF_LINE: transpiler.of:104
          for (let l3 of out3) {
            //! OF_LINE: transpiler.of:105
            arraypush2(combined_out, l3);
            //! OF_LINE: transpiler.of:106
          }
          //! OF_LINE: transpiler.of:107
          return combined_out;
          //! OF_LINE: transpiler.of:108
        }
        //! OF_LINE: transpiler.of:109
        //! OF_LINE: transpiler.of:110
        let node_open = "@{";
        //! OF_LINE: transpiler.of:111
        let node_close = "@}";
        //! OF_LINE: transpiler.of:112
        let node_pattern = "/@\\{|@\\}/g";
        //! OF_LINE: transpiler.of:113
        //! OF_LINE: transpiler.of:114
        let out = [];
        //! OF_LINE: transpiler.of:115
        //! OF_LINE: transpiler.of:116
        //! OF_LINE: transpiler.of:117
        if (state.in_node_block) {
          //! OF_LINE: transpiler.of:118
          let has_close = strindexof2(raw_line, node_close);
          //! OF_LINE: transpiler.of:119
          if (has_close >= 0) {
            //! OF_LINE: transpiler.of:120
            state.in_node_block = false;
            //! OF_LINE: transpiler.of:121
          }
          //! OF_LINE: transpiler.of:122
          let clean = strreplace2(raw_line, node_pattern, "");
          //! OF_LINE: transpiler.of:123
          arraypush2(out, clean);
          //! OF_LINE: transpiler.of:124
        } else {
          //! OF_LINE: transpiler.of:125
          let has_open = strindexof2(raw_line, node_open);
          //! OF_LINE: transpiler.of:126
          if (has_open >= 0) {
            //! OF_LINE: transpiler.of:127
            let has_close2 = strindexof2(raw_line, node_close);
            //! OF_LINE: transpiler.of:128
            if (has_close2 >= 0) {
              //! OF_LINE: transpiler.of:129
              let clean2 = strreplace2(raw_line, node_pattern, "");
              //! OF_LINE: transpiler.of:130
              arraypush2(out, clean2);
              //! OF_LINE: transpiler.of:131
            } else {
              //! OF_LINE: transpiler.of:132
              state.in_node_block = true;
              //! OF_LINE: transpiler.of:133
              let clean3 = strreplace2(raw_line, node_pattern, "");
              //! OF_LINE: transpiler.of:134
              arraypush2(out, clean3);
              //! OF_LINE: transpiler.of:135
            }
            //! OF_LINE: transpiler.of:136
          } else {
            //! OF_LINE: transpiler.of:137
            //! OF_LINE: transpiler.of:138
            if (line == "") {
              //! OF_LINE: transpiler.of:139
              arraypush2(out, "");
              //! OF_LINE: transpiler.of:140
            } else {
              //! OF_LINE: transpiler.of:141
              if (match2(line, "/^include\\b/i")) {
                //! OF_LINE: transpiler.of:142
                out = await transpile_line_core(line, filename, user_tasks, line_num, state);
                //! OF_LINE: transpiler.of:143
              } else {
                //! OF_LINE: transpiler.of:144
                out = await transpile_line_core(line, filename, user_tasks, line_num, state);
                //! OF_LINE: transpiler.of:145
              }
              //! OF_LINE: transpiler.of:146
            }
            //! OF_LINE: transpiler.of:147
          }
          //! OF_LINE: transpiler.of:148
        }
        //! OF_LINE: transpiler.of:149
        //! OF_LINE: transpiler.of:150
        let restored_out = [];
        //! OF_LINE: transpiler.of:151
        for (let ol of out) {
          //! OF_LINE: transpiler.of:152
          let restored_line = ol;
          //! OF_LINE: transpiler.of:153
          //! OF_LINE: transpiler.of:154
          restored_line = ol.replace(/__STR_PLACEHOLDER_(\d+)__/g, (match3, idx) => {
            //! OF_LINE: transpiler.of:155
            return state.strings_list[parseInt(idx, 10)];
            //! OF_LINE: transpiler.of:156
          });
          //! OF_LINE: transpiler.of:157
          //! OF_LINE: transpiler.of:158
          if (line_num > 0) {
            //! OF_LINE: transpiler.of:159
            let fn = pathbasename2(filename);
            //! OF_LINE: transpiler.of:160
            restored_line = restored_line + " //! OF_LINE: " + fn + ":" + line_num;
            //! OF_LINE: transpiler.of:161
          }
          //! OF_LINE: transpiler.of:162
          arraypush2(restored_out, restored_line);
          //! OF_LINE: transpiler.of:163
        }
        //! OF_LINE: transpiler.of:164
        return restored_out;
        //! OF_LINE: transpiler.of:165
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: transpiler.of:166
    //! OF_LINE: transpiler.of:167
    async function count_braces(str) {
      try {
        //! OF_LINE: transpiler.of:168
        let open_count = 0;
        //! OF_LINE: transpiler.of:169
        let close_count = 0;
        //! OF_LINE: transpiler.of:170
        let i = 0;
        //! OF_LINE: transpiler.of:171
        let length = len2(str);
        //! OF_LINE: transpiler.of:172
        while (i < length) {
          //! OF_LINE: transpiler.of:173
          let ch = strsub2(str, i, 1);
          //! OF_LINE: transpiler.of:174
          if (ch == "{") {
            //! OF_LINE: transpiler.of:175
            open_count = open_count + 1;
            //! OF_LINE: transpiler.of:176
          } else if (ch == "}") {
            //! OF_LINE: transpiler.of:177
            close_count = close_count + 1;
            //! OF_LINE: transpiler.of:178
          }
          //! OF_LINE: transpiler.of:179
          i = i + 1;
          //! OF_LINE: transpiler.of:180
        }
        //! OF_LINE: transpiler.of:181
        return open_count - close_count;
        //! OF_LINE: transpiler.of:182
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: transpiler.of:183
    //! OF_LINE: transpiler.of:184
    async function pre_process_line(raw_line, user_tasks) {
      try {
        //! OF_LINE: transpiler.of:185
        let line = strtrim2(raw_line);
        //! OF_LINE: transpiler.of:186
        //! OF_LINE: transpiler.of:187
        //! OF_LINE: transpiler.of:188
        line = await replace_globals(line);
        //! OF_LINE: transpiler.of:189
        //! OF_LINE: transpiler.of:190
        //! OF_LINE: transpiler.of:191
        line = strreplace2(line, "/\\bbackground\\s+(\\w+)/g", "NONBLOCKING_$1");
        //! OF_LINE: transpiler.of:192
        //! OF_LINE: transpiler.of:193
        //! OF_LINE: transpiler.of:194
        line = strreplace2(line, "/\\bcall\\s+([\\w$.]+)\\s+with\\s+(.*?)(?=\\s*(?://|;|$))/gi", "await $1($2)");
        //! OF_LINE: transpiler.of:195
        line = strreplace2(line, "/\\bNONBLOCKING_call\\s+([\\w$.]+)\\s+with\\s+(.*?)(?=\\s*(?://|;|$))/gi", "$1($2)");
        //! OF_LINE: transpiler.of:196
        line = strreplace2(line, "/\\bcall\\s+([\\w$.]+)\\s*(?=\\s*(?://|;|$))/gi", "await $1()");
        //! OF_LINE: transpiler.of:197
        line = strreplace2(line, "/\\bNONBLOCKING_call\\s+([\\w$.]+)\\s*(?=\\s*(?://|;|$))/gi", "$1()");
        //! OF_LINE: transpiler.of:198
        //! OF_LINE: transpiler.of:199
        //! OF_LINE: transpiler.of:200
        line = strreplace2(line, "/\\bvar\\b/g", "let");
        //! OF_LINE: transpiler.of:201
        //! OF_LINE: transpiler.of:202
        //! OF_LINE: transpiler.of:203
        line = strreplace2(line, "/\\bref\\s+(\\w+)/g", "REFERENCE_$1");
        //! OF_LINE: transpiler.of:204
        //! OF_LINE: transpiler.of:205
        //! OF_LINE: transpiler.of:206
        var user_task_list = Object.keys(user_tasks);
        //! OF_LINE: transpiler.of:207
        for (let tname3 of user_task_list) {
          //! OF_LINE: transpiler.of:208
          let with_pat = "/^(\\s*)" + tname3 + "\\s+with\\s+(.*?)(?=\\s*(?:\\/\\/|;|$))/m";
          //! OF_LINE: transpiler.of:209
          line = strreplace2(line, with_pat, "$1await " + tname3 + "($2)");
          //! OF_LINE: transpiler.of:210
        }
        //! OF_LINE: transpiler.of:211
        //! OF_LINE: transpiler.of:212
        //! OF_LINE: transpiler.of:213
        var builtins_list = Array.isArray(global.BUILTINS) ? global.BUILTINS : [];
        //! OF_LINE: transpiler.of:214
        for (let bname of builtins_list) {
          //! OF_LINE: transpiler.of:215
          let with_pat2 = "/^(\\s*)" + bname + "\\s+with\\s+(.*?)(?=\\s*(?:\\/\\/|;|$))/m";
          //! OF_LINE: transpiler.of:216
          if (arraycontains2(global.ASYNC_BUILTINS, bname)) {
            //! OF_LINE: transpiler.of:217
            line = strreplace2(line, with_pat2, "$1await " + bname + "($2)");
            //! OF_LINE: transpiler.of:218
          } else {
            //! OF_LINE: transpiler.of:219
            line = strreplace2(line, with_pat2, "$1" + bname + "($2)");
            //! OF_LINE: transpiler.of:220
          }
          //! OF_LINE: transpiler.of:221
        }
        //! OF_LINE: transpiler.of:222
        //! OF_LINE: transpiler.of:223
        //! OF_LINE: transpiler.of:224
        line = await insert_awaits(line, user_tasks);
        //! OF_LINE: transpiler.of:225
        //! OF_LINE: transpiler.of:226
        //! OF_LINE: transpiler.of:227
        line = strreplace2(line, "/NONBLOCKING_/g", "");
        //! OF_LINE: transpiler.of:228
        line = strreplace2(line, "/REFERENCE_/g", "");
        //! OF_LINE: transpiler.of:229
        //! OF_LINE: transpiler.of:230
        //! OF_LINE: transpiler.of:231
        line = strreplace2(line, "/\\b(\\S+)\\s+as\\s+int\\b/g", "parseInt($1, 10)");
        //! OF_LINE: transpiler.of:232
        line = strreplace2(line, "/\\b(\\S+)\\s+as\\s+string\\b/g", "String($1)");
        //! OF_LINE: transpiler.of:233
        //! OF_LINE: transpiler.of:234
        //! OF_LINE: transpiler.of:235
        line = strreplace2(line, "/\\bdescribe\\s+([^,)]+)/g", "(typeof $1 !== 'undefined' ? describe($1) : 'undefined')");
        //! OF_LINE: transpiler.of:236
        //! OF_LINE: transpiler.of:237
        return line;
        //! OF_LINE: transpiler.of:238
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: transpiler.of:239
    //! OF_LINE: transpiler.of:240
    async function transpile_line_core(raw_line, filename, user_tasks, line_num, state) {
      try {
        //! OF_LINE: transpiler.of:241
        let line = strtrim2(raw_line);
        //! OF_LINE: transpiler.of:242
        let out = [];
        //! OF_LINE: transpiler.of:243
        //! OF_LINE: transpiler.of:244
        //! OF_LINE: transpiler.of:245
        if (match2(line, "/^wait\\s+/i")) {
          //! OF_LINE: transpiler.of:246
          if (!match2(line, "/^wait\\s+(\\d+(?:\\.\\d+)?|\\w+)\\s+(second|seconds|minute|minutes|hour|hours)/i")) {
            //! OF_LINE: transpiler.of:247
            state.error = "SyntaxError in " + filename + " on line " + line_num + ": 'wait' statement requires a time unit (second/seconds, minute/minutes, hour/hours)";
            //! OF_LINE: transpiler.of:248
            return out;
            //! OF_LINE: transpiler.of:249
          }
          //! OF_LINE: transpiler.of:250
        }
        //! OF_LINE: transpiler.of:251
        //! OF_LINE: transpiler.of:252
        //! OF_LINE: transpiler.of:253
        if (match2(line, "/^every\\s+/i")) {
          //! OF_LINE: transpiler.of:254
          if (!match2(line, "/^every\\s+(\\d+(?:\\.\\d+)?|\\w+)\\s+(second|seconds|minute|minutes|hour|hours)/i")) {
            //! OF_LINE: transpiler.of:255
            state.error = "SyntaxError in " + filename + " on line " + line_num + ": 'every' statement requires a time unit (second/seconds, minute/minutes, hour/hours)";
            //! OF_LINE: transpiler.of:256
            return out;
            //! OF_LINE: transpiler.of:257
          }
          //! OF_LINE: transpiler.of:258
        }
        //! OF_LINE: transpiler.of:259
        //! OF_LINE: transpiler.of:260
        //! OF_LINE: transpiler.of:261
        let inc_match = match2(line, `/^include\\s+["']?([^"'\\s;]+)["']?/i`);
        //! OF_LINE: transpiler.of:262
        if (inc_match) {
          //! OF_LINE: transpiler.of:263
          let inc_path = inc_match[0];
          //! OF_LINE: transpiler.of:264
          let resolved = await resolve_include(inc_path, filename);
          //! OF_LINE: transpiler.of:265
          let rel_require_path = "";
          //! OF_LINE: transpiler.of:266
          if (resolved) {
            //! OF_LINE: transpiler.of:267
            let dep_cache = await get_cache_path(resolved);
            //! OF_LINE: transpiler.of:268
            let current_cache = await get_cache_path(filename);
            //! OF_LINE: transpiler.of:269
            //! OF_LINE: transpiler.of:270
            const path3 = require("path");
            //! OF_LINE: transpiler.of:271
            let rel = path3.relative(path3.dirname(current_cache), dep_cache);
            //! OF_LINE: transpiler.of:272
            if (!rel.startsWith(".")) {
              //! OF_LINE: transpiler.of:273
              rel = "./" + rel;
              //! OF_LINE: transpiler.of:274
            }
            //! OF_LINE: transpiler.of:275
            rel_require_path = rel;
            //! OF_LINE: transpiler.of:276
            //! OF_LINE: transpiler.of:277
          } else {
            //! OF_LINE: transpiler.of:278
            let inc_js = strreplace2(inc_path, "/\\.of$/", ".js");
            //! OF_LINE: transpiler.of:279
            if (!match2(inc_js, "/^\\./")) {
              //! OF_LINE: transpiler.of:280
              inc_js = "./" + inc_js;
              //! OF_LINE: transpiler.of:281
            }
            //! OF_LINE: transpiler.of:282
            rel_require_path = inc_js;
            //! OF_LINE: transpiler.of:283
          }
          //! OF_LINE: transpiler.of:284
          arraypush2(out, "require('" + rel_require_path + "');");
          //! OF_LINE: transpiler.of:285
          return out;
          //! OF_LINE: transpiler.of:286
        }
        //! OF_LINE: transpiler.of:287
        //! OF_LINE: transpiler.of:288
        line = await pre_process_line(line, user_tasks);
        //! OF_LINE: transpiler.of:289
        //! OF_LINE: transpiler.of:290
        //! OF_LINE: transpiler.of:291
        let task_with = match2(line, "/^(?:define\\s+task|fn)\\s+(\\w+)\\s+with\\s+([^{]+)\\s*\\{/i");
        //! OF_LINE: transpiler.of:292
        if (task_with) {
          //! OF_LINE: transpiler.of:293
          let tname = task_with[0];
          //! OF_LINE: transpiler.of:294
          let tparams = strtrim2(task_with[1]);
          //! OF_LINE: transpiler.of:295
          arraypush2(state.tasks, tname);
          //! OF_LINE: transpiler.of:296
          arraypush2(state.block_stack, "task");
          //! OF_LINE: transpiler.of:297
          arraypush2(out, "async function " + tname + "(" + tparams + ") { try {");
          //! OF_LINE: transpiler.of:298
          return out;
          //! OF_LINE: transpiler.of:299
        }
        //! OF_LINE: transpiler.of:300
        let task_paren = match2(line, "/^(?:define\\s+task|fn)\\s+(\\w+)\\s*(?:\\(([^)]*)\\))?\\s*\\{/i");
        //! OF_LINE: transpiler.of:301
        if (task_paren) {
          //! OF_LINE: transpiler.of:302
          let tname2 = task_paren[0];
          //! OF_LINE: transpiler.of:303
          let tparams2 = task_paren[1] || "";
          //! OF_LINE: transpiler.of:304
          arraypush2(state.tasks, tname2);
          //! OF_LINE: transpiler.of:305
          arraypush2(state.block_stack, "task");
          //! OF_LINE: transpiler.of:306
          arraypush2(out, "async function " + tname2 + "(" + tparams2 + ") { try {");
          //! OF_LINE: transpiler.of:307
          return out;
          //! OF_LINE: transpiler.of:308
        }
        //! OF_LINE: transpiler.of:309
        //! OF_LINE: transpiler.of:310
        //! OF_LINE: transpiler.of:311
        if (match2(line, "/^on\\s+start\\s*\\{/i")) {
          //! OF_LINE: transpiler.of:312
          state.on_start_count = state.on_start_count + 1;
          //! OF_LINE: transpiler.of:313
          let fn_name = "__on_start_" + state.on_start_count;
          //! OF_LINE: transpiler.of:314
          arraypush2(state.on_start_funcs, fn_name);
          //! OF_LINE: transpiler.of:315
          arraypush2(state.block_stack, "normal");
          //! OF_LINE: transpiler.of:316
          arraypush2(out, "async function " + fn_name + "() {");
          //! OF_LINE: transpiler.of:317
          return out;
          //! OF_LINE: transpiler.of:318
        }
        //! OF_LINE: transpiler.of:319
        if (match2(line, "/^on\\s+shutdown\\s*\\{/i")) {
          //! OF_LINE: transpiler.of:320
          state.on_shutdown_count = state.on_shutdown_count + 1;
          //! OF_LINE: transpiler.of:321
          let fn_sd = "__on_shutdown_" + state.on_shutdown_count;
          //! OF_LINE: transpiler.of:322
          arraypush2(state.on_shutdown_funcs, fn_sd);
          //! OF_LINE: transpiler.of:323
          arraypush2(state.block_stack, "normal");
          //! OF_LINE: transpiler.of:324
          arraypush2(out, "async function " + fn_sd + "() {");
          //! OF_LINE: transpiler.of:325
          return out;
          //! OF_LINE: transpiler.of:326
        }
        //! OF_LINE: transpiler.of:327
        let on_error_m = match2(line, "/^on\\s+error\\s*\\(([^)]*)\\)\\s*\\{/i");
        //! OF_LINE: transpiler.of:328
        if (on_error_m) {
          //! OF_LINE: transpiler.of:329
          state.on_error_count = state.on_error_count + 1;
          //! OF_LINE: transpiler.of:330
          let fn_err = "__on_error_" + state.on_error_count;
          //! OF_LINE: transpiler.of:331
          arraypush2(state.on_error_funcs, fn_err);
          //! OF_LINE: transpiler.of:332
          arraypush2(state.block_stack, "normal");
          //! OF_LINE: transpiler.of:333
          arraypush2(out, "async function " + fn_err + "(" + on_error_m[0] + ") {");
          //! OF_LINE: transpiler.of:334
          return out;
          //! OF_LINE: transpiler.of:335
        }
        //! OF_LINE: transpiler.of:336
        let on_req_m = match2(line, "/^on\\s+request\\s*\\(([^)]*)\\)\\s*\\{/i");
        //! OF_LINE: transpiler.of:337
        if (on_req_m) {
          //! OF_LINE: transpiler.of:338
          state.on_request_count = state.on_request_count + 1;
          //! OF_LINE: transpiler.of:339
          let fn_req = "__on_request_" + state.on_request_count;
          //! OF_LINE: transpiler.of:340
          arraypush2(state.on_request_funcs, fn_req);
          //! OF_LINE: transpiler.of:341
          arraypush2(state.block_stack, "normal");
          //! OF_LINE: transpiler.of:342
          arraypush2(out, "async function " + fn_req + "(" + on_req_m[0] + ") {");
          //! OF_LINE: transpiler.of:343
          return out;
          //! OF_LINE: transpiler.of:344
        }
        //! OF_LINE: transpiler.of:345
        //! OF_LINE: transpiler.of:346
        //! OF_LINE: transpiler.of:347
        let every_m = match2(line, "/^every\\s+(\\d+(?:\\.\\d+)?|\\w+)\\s+(second|seconds|minute|minutes|hour|hours)\\s*\\{/i");
        //! OF_LINE: transpiler.of:348
        if (every_m) {
          //! OF_LINE: transpiler.of:349
          let ev_val = every_m[0];
          //! OF_LINE: transpiler.of:350
          let ev_unit = every_m[1];
          //! OF_LINE: transpiler.of:351
          let ev_mult = "1000";
          //! OF_LINE: transpiler.of:352
          if (match2(ev_unit, "/^min/i")) {
            //! OF_LINE: transpiler.of:353
            ev_mult = "60000";
            //! OF_LINE: transpiler.of:354
          }
          //! OF_LINE: transpiler.of:355
          if (match2(ev_unit, "/^hour/i")) {
            //! OF_LINE: transpiler.of:356
            ev_mult = "3600000";
            //! OF_LINE: transpiler.of:357
          }
          //! OF_LINE: transpiler.of:358
          let ev_ms = ev_val + " * " + ev_mult;
          //! OF_LINE: transpiler.of:359
          arraypush2(state.block_stack, "every:" + ev_ms);
          //! OF_LINE: transpiler.of:360
          arraypush2(out, "setInterval(async () => {");
          //! OF_LINE: transpiler.of:361
          return out;
          //! OF_LINE: transpiler.of:362
        }
        //! OF_LINE: transpiler.of:363
        //! OF_LINE: transpiler.of:364
        //! OF_LINE: transpiler.of:365
        let timer_m = match2(line, "/^\\s*(?:(let|var|const)\\s+(\\w+)\\s*=\\s*|(?:(\\w+)\\s*=\\s*))?timer\\s*\\{/i");
        //! OF_LINE: transpiler.of:366
        if (timer_m) {
          //! OF_LINE: transpiler.of:367
          state.timer_count = state.timer_count + 1;
          //! OF_LINE: transpiler.of:368
          let t_id = state.timer_count;
          //! OF_LINE: transpiler.of:369
          //! OF_LINE: transpiler.of:370
          let is_decl = timer_m[0] == "let" || timer_m[0] == "var" || timer_m[0] == "const" ? true : false;
          //! OF_LINE: transpiler.of:371
          let var_name = timer_m[1] ? timer_m[1] : timer_m[2] ? timer_m[2] : "";
          //! OF_LINE: transpiler.of:372
          //! OF_LINE: transpiler.of:373
          //! OF_LINE: transpiler.of:374
          arraypush2(state.block_stack, "timer:" + var_name + ":" + (is_decl ? "1" : "0") + ":" + t_id);
          //! OF_LINE: transpiler.of:375
          //! OF_LINE: transpiler.of:376
          let init_line = "";
          //! OF_LINE: transpiler.of:377
          if (var_name != "") {
            //! OF_LINE: transpiler.of:378
            if (is_decl) {
              //! OF_LINE: transpiler.of:379
              init_line = "let " + var_name + "; ";
              //! OF_LINE: transpiler.of:380
            }
            //! OF_LINE: transpiler.of:381
          }
          //! OF_LINE: transpiler.of:382
          arraypush2(out, init_line + "{ const __timer_start_" + t_id + " = global.microtime();");
          //! OF_LINE: transpiler.of:383
          return out;
          //! OF_LINE: transpiler.of:384
        }
        //! OF_LINE: transpiler.of:385
        //! OF_LINE: transpiler.of:386
        //! OF_LINE: transpiler.of:387
        let wait_m = match2(line, "/^wait\\s+(\\d+(?:\\.\\d+)?|\\w+)\\s+(second|seconds|minute|minutes|hour|hours)/i");
        //! OF_LINE: transpiler.of:388
        if (wait_m) {
          //! OF_LINE: transpiler.of:389
          let wt_val = wait_m[0];
          //! OF_LINE: transpiler.of:390
          let wt_unit = wait_m[1];
          //! OF_LINE: transpiler.of:391
          let wt_mult = "1000";
          //! OF_LINE: transpiler.of:392
          if (match2(wt_unit, "/^min/i")) {
            //! OF_LINE: transpiler.of:393
            wt_mult = "60000";
            //! OF_LINE: transpiler.of:394
          }
          //! OF_LINE: transpiler.of:395
          if (match2(wt_unit, "/^hour/i")) {
            //! OF_LINE: transpiler.of:396
            wt_mult = "3600000";
            //! OF_LINE: transpiler.of:397
          }
          //! OF_LINE: transpiler.of:398
          let wt_ms = wt_val + " * " + wt_mult;
          //! OF_LINE: transpiler.of:399
          arraypush2(out, "await new Promise(resolve => setTimeout(resolve, " + wt_ms + "));");
          //! OF_LINE: transpiler.of:400
          return out;
          //! OF_LINE: transpiler.of:401
        }
        //! OF_LINE: transpiler.of:402
        //! OF_LINE: transpiler.of:403
        //! OF_LINE: transpiler.of:404
        let listen_m = match2(line, "/^listen\\s+on\\s+port\\s+(.*)/i");
        //! OF_LINE: transpiler.of:405
        if (listen_m) {
          //! OF_LINE: transpiler.of:406
          state.server_port = strtrim2(listen_m[0]);
          //! OF_LINE: transpiler.of:407
          arraypush2(out, "const express = require('express');");
          //! OF_LINE: transpiler.of:408
          arraypush2(out, "const app = express();");
          //! OF_LINE: transpiler.of:409
          arraypush2(out, "app.use(express.json());");
          //! OF_LINE: transpiler.of:410
          arraypush2(out, "app.use(express.urlencoded({ extended: true }));");
          //! OF_LINE: transpiler.of:411
          arraypush2(out, "app.use(async (req, res, next) => {");
          //! OF_LINE: transpiler.of:412
          arraypush2(out, "    try {");
          //! OF_LINE: transpiler.of:413
          arraypush2(out, "        const hooks = global.__on_request_hooks || [];");
          //! OF_LINE: transpiler.of:414
          arraypush2(out, "        for (const hook of hooks) {");
          //! OF_LINE: transpiler.of:415
          arraypush2(out, "            if (!res.headersSent) { await hook(req, res); }");
          //! OF_LINE: transpiler.of:416
          arraypush2(out, "        }");
          //! OF_LINE: transpiler.of:417
          arraypush2(out, "        if (!res.headersSent) { next(); }");
          //! OF_LINE: transpiler.of:418
          arraypush2(out, "    } catch (e) { next(e); }");
          //! OF_LINE: transpiler.of:419
          arraypush2(out, "});");
          //! OF_LINE: transpiler.of:420
          return out;
          //! OF_LINE: transpiler.of:421
        }
        //! OF_LINE: transpiler.of:422
        //! OF_LINE: transpiler.of:423
        //! OF_LINE: transpiler.of:424
        let route_line = line;
        //! OF_LINE: transpiler.of:425
        //! OF_LINE: transpiler.of:426
        route_line = line.replace(/__STR_PLACEHOLDER_(\d+)__/g, (match3, idx) => {
          //! OF_LINE: transpiler.of:427
          return state.strings_list[parseInt(idx, 10)];
          //! OF_LINE: transpiler.of:428
        });
        //! OF_LINE: transpiler.of:429
        //! OF_LINE: transpiler.of:430
        //! OF_LINE: transpiler.of:431
        let route_m = match2(route_line, `/^(GET|POST|PUT|DELETE)\\s+["']([^"']+)["']\\s*\\(([^)]*)\\)\\s*\\{/i`);
        //! OF_LINE: transpiler.of:432
        if (route_m) {
          //! OF_LINE: transpiler.of:433
          let route_method = strlower2(route_m[0]);
          //! OF_LINE: transpiler.of:434
          let route_path = route_m[1];
          //! OF_LINE: transpiler.of:435
          let route_params = route_m[2];
          //! OF_LINE: transpiler.of:436
          arraypush2(state.block_stack, "route");
          //! OF_LINE: transpiler.of:437
          arraypush2(out, "app." + route_method + '("' + route_path + '", async (' + route_params + ") => { try {");
          //! OF_LINE: transpiler.of:438
          return out;
          //! OF_LINE: transpiler.of:439
        }
        //! OF_LINE: transpiler.of:440
        //! OF_LINE: transpiler.of:441
        //! OF_LINE: transpiler.of:442
        let redir_m = match2(route_line, "/^redirect\\s+to\\s+(.*?)(?:\\s+with\\s+status\\s+(\\d+))?$/i");
        //! OF_LINE: transpiler.of:443
        if (redir_m) {
          //! OF_LINE: transpiler.of:444
          let redir_url = redir_m[0];
          //! OF_LINE: transpiler.of:445
          let redir_status = redir_m[1];
          //! OF_LINE: transpiler.of:446
          if (redir_status) {
            //! OF_LINE: transpiler.of:447
            arraypush2(out, "res.status(" + redir_status + ").redirect(" + redir_url + ");");
            //! OF_LINE: transpiler.of:448
          } else {
            //! OF_LINE: transpiler.of:449
            arraypush2(out, "res.redirect(" + redir_url + ");");
            //! OF_LINE: transpiler.of:450
          }
          //! OF_LINE: transpiler.of:451
          return out;
          //! OF_LINE: transpiler.of:452
        }
        //! OF_LINE: transpiler.of:453
        //! OF_LINE: transpiler.of:454
        //! OF_LINE: transpiler.of:455
        let resp_status_m = match2(line, "/^respond\\s+(?:with\\s+)?status\\s+(\\S+)\\s+and\\s+(json|html|text|file|template)\\s+(.*)/i");
        //! OF_LINE: transpiler.of:456
        if (resp_status_m) {
          //! OF_LINE: transpiler.of:457
          let rs_status = resp_status_m[0];
          //! OF_LINE: transpiler.of:458
          let rs_type = strlower2(resp_status_m[1]);
          //! OF_LINE: transpiler.of:459
          let rs_content = strtrim2(resp_status_m[2]);
          //! OF_LINE: transpiler.of:460
          //! OF_LINE: transpiler.of:461
          //! OF_LINE: transpiler.of:462
          let depth = await count_braces(rs_content);
          //! OF_LINE: transpiler.of:463
          while (depth > 0 && state.current_index + 1 < len2(state.source_lines)) {
            //! OF_LINE: transpiler.of:464
            state.current_index = state.current_index + 1;
            //! OF_LINE: transpiler.of:465
            let next_line = state.source_lines[state.current_index];
            //! OF_LINE: transpiler.of:466
            next_line = await pre_process_line(next_line, user_tasks);
            //! OF_LINE: transpiler.of:467
            rs_content = rs_content + " " + strtrim2(next_line);
            //! OF_LINE: transpiler.of:468
            depth = await count_braces(rs_content);
            //! OF_LINE: transpiler.of:469
          }
          //! OF_LINE: transpiler.of:470
          //! OF_LINE: transpiler.of:471
          if (rs_type == "json") {
            //! OF_LINE: transpiler.of:472
            arraypush2(out, "res.status(" + rs_status + ").json(" + rs_content + ");");
            //! OF_LINE: transpiler.of:473
          } else if (rs_type == "file") {
            //! OF_LINE: transpiler.of:474
            arraypush2(out, "res.status(" + rs_status + ").sendFile(path.resolve(global.__app_root, " + rs_content + "));");
            //! OF_LINE: transpiler.of:475
          } else if (rs_type == "template") {
            //! OF_LINE: transpiler.of:476
            let cleaned_expr = await clean_template_args(rs_content);
            //! OF_LINE: transpiler.of:477
            arraypush2(out, "res.status(" + rs_status + ").send(await template(" + cleaned_expr + "));");
            //! OF_LINE: transpiler.of:478
          } else {
            //! OF_LINE: transpiler.of:479
            arraypush2(out, "res.status(" + rs_status + ").send(" + rs_content + ");");
            //! OF_LINE: transpiler.of:480
          }
          //! OF_LINE: transpiler.of:481
          return out;
          //! OF_LINE: transpiler.of:482
        }
        //! OF_LINE: transpiler.of:483
        //! OF_LINE: transpiler.of:484
        //! OF_LINE: transpiler.of:485
        let resp_m = match2(line, "/^respond\\s+(?:with\\s+)?(json|html|text|file|template)\\s*(.*)/i");
        //! OF_LINE: transpiler.of:486
        if (resp_m) {
          //! OF_LINE: transpiler.of:487
          let r_type = strlower2(resp_m[0]);
          //! OF_LINE: transpiler.of:488
          let r_content = strtrim2(resp_m[1]);
          //! OF_LINE: transpiler.of:489
          //! OF_LINE: transpiler.of:490
          //! OF_LINE: transpiler.of:491
          let depth2 = await count_braces(r_content);
          //! OF_LINE: transpiler.of:492
          while (depth2 > 0 && state.current_index + 1 < len2(state.source_lines)) {
            //! OF_LINE: transpiler.of:493
            state.current_index = state.current_index + 1;
            //! OF_LINE: transpiler.of:494
            let next_line2 = state.source_lines[state.current_index];
            //! OF_LINE: transpiler.of:495
            next_line2 = await pre_process_line(next_line2, user_tasks);
            //! OF_LINE: transpiler.of:496
            r_content = r_content + " " + strtrim2(next_line2);
            //! OF_LINE: transpiler.of:497
            depth2 = await count_braces(r_content);
            //! OF_LINE: transpiler.of:498
          }
          //! OF_LINE: transpiler.of:499
          //! OF_LINE: transpiler.of:500
          if (r_type == "json") {
            //! OF_LINE: transpiler.of:501
            arraypush2(out, "res.json(" + r_content + ");");
            //! OF_LINE: transpiler.of:502
          } else if (r_type == "file") {
            //! OF_LINE: transpiler.of:503
            arraypush2(out, "res.sendFile(path.resolve(global.__app_root, " + r_content + "));");
            //! OF_LINE: transpiler.of:504
          } else if (r_type == "template") {
            //! OF_LINE: transpiler.of:505
            let cleaned_expr = await clean_template_args(r_content);
            //! OF_LINE: transpiler.of:506
            arraypush2(out, "res.send(await template(" + cleaned_expr + "));");
            //! OF_LINE: transpiler.of:507
          } else {
            //! OF_LINE: transpiler.of:508
            arraypush2(out, "res.send(" + r_content + ");");
            //! OF_LINE: transpiler.of:509
          }
          //! OF_LINE: transpiler.of:510
          return out;
          //! OF_LINE: transpiler.of:511
        }
        //! OF_LINE: transpiler.of:512
        //! OF_LINE: transpiler.of:513
        //! OF_LINE: transpiler.of:514
        let for_m = match2(line, "/^for\\s+(\\w+)\\s+of\\s+(.*?)\\s*\\{/i");
        //! OF_LINE: transpiler.of:515
        if (for_m) {
          //! OF_LINE: transpiler.of:516
          arraypush2(state.block_stack, "normal");
          //! OF_LINE: transpiler.of:517
          arraypush2(out, "for (let " + for_m[0] + " of " + strtrim2(for_m[1]) + ") {");
          //! OF_LINE: transpiler.of:518
          return out;
          //! OF_LINE: transpiler.of:519
        }
        //! OF_LINE: transpiler.of:520
        //! OF_LINE: transpiler.of:521
        //! OF_LINE: transpiler.of:522
        let while_m = match2(line, "/^while\\s+(.*?)\\s*\\{/i");
        //! OF_LINE: transpiler.of:523
        if (while_m) {
          //! OF_LINE: transpiler.of:524
          arraypush2(state.block_stack, "normal");
          //! OF_LINE: transpiler.of:525
          arraypush2(out, "while " + await ensure_parens(while_m[0]) + " {");
          //! OF_LINE: transpiler.of:526
          return out;
          //! OF_LINE: transpiler.of:527
        }
        //! OF_LINE: transpiler.of:528
        //! OF_LINE: transpiler.of:529
        //! OF_LINE: transpiler.of:530
        let catch_m = match2(line, "/^\\}\\s*on\\s+error\\s*\\(([^)]*)\\)\\s*\\{/i");
        //! OF_LINE: transpiler.of:531
        if (catch_m) {
          //! OF_LINE: transpiler.of:532
          let popped_block = arraypop2(state.block_stack);
          //! OF_LINE: transpiler.of:533
          if (popped_block == "route") {
            //! OF_LINE: transpiler.of:534
            arraypush2(state.block_stack, "route_error");
            //! OF_LINE: transpiler.of:535
          } else {
            //! OF_LINE: transpiler.of:536
            arraypush2(state.block_stack, "task_error");
            //! OF_LINE: transpiler.of:537
          }
          //! OF_LINE: transpiler.of:538
          arraypush2(out, "} catch (" + catch_m[0] + ") {");
          //! OF_LINE: transpiler.of:539
          return out;
          //! OF_LINE: transpiler.of:540
        }
        //! OF_LINE: transpiler.of:541
        //! OF_LINE: transpiler.of:542
        //! OF_LINE: transpiler.of:543
        let elif_m = match2(line, "/^(\\}\\s*)?else\\s+if\\s+(.*?)\\s*\\{/i");
        //! OF_LINE: transpiler.of:544
        if (elif_m) {
          //! OF_LINE: transpiler.of:545
          let prefix = "";
          //! OF_LINE: transpiler.of:546
          if (elif_m[0]) {
            //! OF_LINE: transpiler.of:547
            arraypop2(state.block_stack);
            //! OF_LINE: transpiler.of:548
            prefix = "} ";
            //! OF_LINE: transpiler.of:549
          }
          //! OF_LINE: transpiler.of:550
          arraypush2(state.block_stack, "normal");
          //! OF_LINE: transpiler.of:551
          arraypush2(out, prefix + "else if " + await ensure_parens(elif_m[1]) + " {");
          //! OF_LINE: transpiler.of:552
          return out;
          //! OF_LINE: transpiler.of:553
        }
        //! OF_LINE: transpiler.of:554
        //! OF_LINE: transpiler.of:555
        //! OF_LINE: transpiler.of:556
        let else_m = match2(line, "/^(\\}\\s*)?else\\s*\\{/i");
        //! OF_LINE: transpiler.of:557
        if (else_m) {
          //! OF_LINE: transpiler.of:558
          let prefix2 = "";
          //! OF_LINE: transpiler.of:559
          if (else_m[0]) {
            //! OF_LINE: transpiler.of:560
            arraypop2(state.block_stack);
            //! OF_LINE: transpiler.of:561
            prefix2 = "} ";
            //! OF_LINE: transpiler.of:562
          }
          //! OF_LINE: transpiler.of:563
          arraypush2(state.block_stack, "normal");
          //! OF_LINE: transpiler.of:564
          arraypush2(out, prefix2 + "else {");
          //! OF_LINE: transpiler.of:565
          return out;
          //! OF_LINE: transpiler.of:566
        }
        //! OF_LINE: transpiler.of:567
        //! OF_LINE: transpiler.of:568
        //! OF_LINE: transpiler.of:569
        let if_m = match2(line, "/^if\\s+(.*?)\\s*\\{/i");
        //! OF_LINE: transpiler.of:570
        if (if_m) {
          //! OF_LINE: transpiler.of:571
          arraypush2(state.block_stack, "normal");
          //! OF_LINE: transpiler.of:572
          arraypush2(out, "if " + await ensure_parens(if_m[0]) + " {");
          //! OF_LINE: transpiler.of:573
          return out;
          //! OF_LINE: transpiler.of:574
        }
        //! OF_LINE: transpiler.of:575
        //! OF_LINE: transpiler.of:576
        //! OF_LINE: transpiler.of:577
        let switch_m = match2(line, "/^switch\\s+(.*?)\\s*\\{/i");
        //! OF_LINE: transpiler.of:578
        if (switch_m) {
          //! OF_LINE: transpiler.of:579
          arraypush2(state.block_stack, "normal");
          //! OF_LINE: transpiler.of:580
          arraypush2(out, "switch (" + strtrim2(switch_m[0]) + ") {");
          //! OF_LINE: transpiler.of:581
          return out;
          //! OF_LINE: transpiler.of:582
        }
        //! OF_LINE: transpiler.of:583
        //! OF_LINE: transpiler.of:584
        //! OF_LINE: transpiler.of:585
        //! OF_LINE: transpiler.of:586
        //! OF_LINE: transpiler.of:587
        let local_stack = [];
        //! OF_LINE: transpiler.of:588
        let unmatched_closes = 0;
        //! OF_LINE: transpiler.of:589
        let unmatched_opens = 0;
        //! OF_LINE: transpiler.of:590
        let line_len = len2(line);
        //! OF_LINE: transpiler.of:591
        let char_idx = 0;
        //! OF_LINE: transpiler.of:592
        while (char_idx < line_len) {
          //! OF_LINE: transpiler.of:593
          let char = strsub2(line, char_idx, 1);
          //! OF_LINE: transpiler.of:594
          if (char == "{") {
            //! OF_LINE: transpiler.of:595
            arraypush2(local_stack, "{");
            //! OF_LINE: transpiler.of:596
          } else if (char == "}") {
            //! OF_LINE: transpiler.of:597
            if (len2(local_stack) > 0) {
              //! OF_LINE: transpiler.of:598
              arraypop2(local_stack);
              //! OF_LINE: transpiler.of:599
            } else {
              //! OF_LINE: transpiler.of:600
              unmatched_closes = unmatched_closes + 1;
              //! OF_LINE: transpiler.of:601
            }
            //! OF_LINE: transpiler.of:602
          }
          //! OF_LINE: transpiler.of:603
          char_idx = char_idx + 1;
          //! OF_LINE: transpiler.of:604
        }
        //! OF_LINE: transpiler.of:605
        unmatched_opens = len2(local_stack);
        //! OF_LINE: transpiler.of:606
        //! OF_LINE: transpiler.of:607
        if (unmatched_closes > 0) {
          //! OF_LINE: transpiler.of:608
          let c_idx = 0;
          //! OF_LINE: transpiler.of:609
          while (c_idx < unmatched_closes) {
            //! OF_LINE: transpiler.of:610
            let top = "";
            //! OF_LINE: transpiler.of:611
            if (len2(state.block_stack) > 0) {
              //! OF_LINE: transpiler.of:612
              top = arraypop2(state.block_stack);
              //! OF_LINE: transpiler.of:613
            }
            //! OF_LINE: transpiler.of:614
            //! OF_LINE: transpiler.of:615
            //! OF_LINE: transpiler.of:616
            let close_js = "}";
            //! OF_LINE: transpiler.of:617
            if (match2(top, "/^every:/")) {
              //! OF_LINE: transpiler.of:618
              let ev_ms_val = strsub2(top, 6, len2(top) - 6);
              //! OF_LINE: transpiler.of:619
              close_js = "}, " + ev_ms_val + ");";
              //! OF_LINE: transpiler.of:620
            } else if (match2(top, "/^timer:/")) {
              //! OF_LINE: transpiler.of:621
              let parts = strsplit2(top, ":");
              //! OF_LINE: transpiler.of:622
              let v_name = parts[1];
              //! OF_LINE: transpiler.of:623
              let t_id = parts[3];
              //! OF_LINE: transpiler.of:624
              if (v_name != "") {
                //! OF_LINE: transpiler.of:625
                close_js = v_name + " = global.microtime() - __timer_start_" + t_id + "; }";
                //! OF_LINE: transpiler.of:626
              } else {
                //! OF_LINE: transpiler.of:627
                close_js = 'console.log("Timer took: " + (global.microtime() - __timer_start_' + t_id + ').toFixed(6) + " seconds"); }';
                //! OF_LINE: transpiler.of:628
              }
              //! OF_LINE: transpiler.of:629
            } else if (top == "route") {
              //! OF_LINE: transpiler.of:630
              close_js = "} catch (err) {\n    if (typeof next === 'function') { next(err); } else { console.error(err); if (!res.headersSent) { res.status(500).json({ error: err.message }); } }\n} });";
              //! OF_LINE: transpiler.of:631
            } else if (top == "route_error") {
              //! OF_LINE: transpiler.of:632
              close_js = "} });";
              //! OF_LINE: transpiler.of:633
            } else if (top == "task") {
              //! OF_LINE: transpiler.of:634
              close_js = "} catch (err) { throw err; }\n}";
              //! OF_LINE: transpiler.of:635
            } else if (top == "task_error") {
              //! OF_LINE: transpiler.of:636
              close_js = "}\n}";
              //! OF_LINE: transpiler.of:637
            }
            //! OF_LINE: transpiler.of:638
            //! OF_LINE: transpiler.of:639
            //! OF_LINE: transpiler.of:640
            //! OF_LINE: transpiler.of:641
            line = line.replace("}", close_js);
            //! OF_LINE: transpiler.of:642
            //! OF_LINE: transpiler.of:643
            c_idx = c_idx + 1;
            //! OF_LINE: transpiler.of:644
          }
          //! OF_LINE: transpiler.of:645
        }
        //! OF_LINE: transpiler.of:646
        //! OF_LINE: transpiler.of:647
        if (unmatched_opens > 0) {
          //! OF_LINE: transpiler.of:648
          let o_idx = 0;
          //! OF_LINE: transpiler.of:649
          while (o_idx < unmatched_opens) {
            //! OF_LINE: transpiler.of:650
            arraypush2(state.block_stack, "normal");
            //! OF_LINE: transpiler.of:651
            o_idx = o_idx + 1;
            //! OF_LINE: transpiler.of:652
          }
          //! OF_LINE: transpiler.of:653
        }
        //! OF_LINE: transpiler.of:654
        //! OF_LINE: transpiler.of:655
        arraypush2(out, line);
        //! OF_LINE: transpiler.of:656
        return out;
        //! OF_LINE: transpiler.of:657
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: transpiler.of:658
    //! OF_LINE: transpiler.of:659
    //! OF_LINE: transpiler.of:660
    //! OF_LINE: transpiler.of:661
    //! OF_LINE: transpiler.of:662
    //! OF_LINE: transpiler.of:663
    async function transpile2(content, filename, user_tasks, runtime_js) {
      try {
        //! OF_LINE: transpiler.of:664
        let strings_list = [];
        //! OF_LINE: transpiler.of:665
        let content_with_placeholders = content;
        //! OF_LINE: transpiler.of:666
        //! OF_LINE: transpiler.of:667
        const dq = String.fromCharCode(34);
        //! OF_LINE: transpiler.of:668
        const sq = String.fromCharCode(39);
        //! OF_LINE: transpiler.of:669
        const pattern = "(\\binclude\\s+)(" + dq + "(\\\\.|[^\\\\\\" + dq + "])*" + dq + "|" + sq + "(\\\\.|[^\\\\\\" + sq + "])*" + sq + ")|(" + dq + "(\\\\.|[^\\\\\\" + dq + "])*" + dq + "|" + sq + "(\\\\.|[^\\\\\\" + sq + "])*" + sq + ")";
        //! OF_LINE: transpiler.of:670
        const str_regex = new RegExp(pattern, "g");
        //! OF_LINE: transpiler.of:671
        content_with_placeholders = content.replace(str_regex, (match3, p1, p2, p3, p4, p5) => {
          //! OF_LINE: transpiler.of:672
          if (p1) {
            //! OF_LINE: transpiler.of:673
            return match3;
            //! OF_LINE: transpiler.of:674
          }
          //! OF_LINE: transpiler.of:675
          const newlines = match3.split("\n").length - 1;
          //! OF_LINE: transpiler.of:676
          let safe_str = match3;
          //! OF_LINE: transpiler.of:677
          if (newlines > 0) {
            //! OF_LINE: transpiler.of:678
            if (match3.startsWith('"') || match3.startsWith("'")) {
              //! OF_LINE: transpiler.of:679
              const quote = match3[0];
              //! OF_LINE: transpiler.of:680
              const inner = match3.slice(1, -1).replace(/\n/g, "\\n");
              //! OF_LINE: transpiler.of:681
              safe_str = quote + inner + quote;
              //! OF_LINE: transpiler.of:682
            }
            //! OF_LINE: transpiler.of:683
          }
          //! OF_LINE: transpiler.of:684
          strings_list.push(safe_str);
          //! OF_LINE: transpiler.of:685
          return "__STR_PLACEHOLDER_" + (strings_list.length - 1) + "__" + "\n".repeat(newlines);
          //! OF_LINE: transpiler.of:686
        });
        //! OF_LINE: transpiler.of:687
        //! OF_LINE: transpiler.of:688
        //! OF_LINE: transpiler.of:689
        let state = {
          //! OF_LINE: transpiler.of:690
          block_stack: [],
          //! OF_LINE: transpiler.of:691
          tasks: [],
          //! OF_LINE: transpiler.of:692
          on_start_funcs: [],
          //! OF_LINE: transpiler.of:693
          on_shutdown_funcs: [],
          //! OF_LINE: transpiler.of:694
          on_error_funcs: [],
          //! OF_LINE: transpiler.of:695
          on_request_funcs: [],
          //! OF_LINE: transpiler.of:696
          on_start_count: 0,
          //! OF_LINE: transpiler.of:697
          on_shutdown_count: 0,
          //! OF_LINE: transpiler.of:698
          on_error_count: 0,
          //! OF_LINE: transpiler.of:699
          on_request_count: 0,
          //! OF_LINE: transpiler.of:700
          timer_count: 0,
          //! OF_LINE: transpiler.of:701
          server_port: "",
          //! OF_LINE: transpiler.of:702
          in_node_block: false,
          //! OF_LINE: transpiler.of:703
          strings_list
          //! OF_LINE: transpiler.of:704
        };
        //! OF_LINE: transpiler.of:705
        //! OF_LINE: transpiler.of:706
        let output_lines = [];
        //! OF_LINE: transpiler.of:707
        let deferred_stmts = [];
        //! OF_LINE: transpiler.of:708
        //! OF_LINE: transpiler.of:709
        //! OF_LINE: transpiler.of:710
        let runtime_lines = strsplit2(runtime_js, "\n");
        //! OF_LINE: transpiler.of:711
        for (let rtl of runtime_lines) {
          //! OF_LINE: transpiler.of:712
          arraypush2(output_lines, rtl);
          //! OF_LINE: transpiler.of:713
        }
        //! OF_LINE: transpiler.of:714
        arraypush2(output_lines, "");
        //! OF_LINE: transpiler.of:715
        //! OF_LINE: transpiler.of:716
        let source_lines = strsplit2(content_with_placeholders, "\n");
        //! OF_LINE: transpiler.of:717
        state.source_lines = source_lines;
        //! OF_LINE: transpiler.of:718
        //! OF_LINE: transpiler.of:719
        let num_lines = len2(source_lines);
        //! OF_LINE: transpiler.of:720
        let i = 0;
        //! OF_LINE: transpiler.of:721
        while (i < num_lines) {
          //! OF_LINE: transpiler.of:722
          state.current_index = i;
          //! OF_LINE: transpiler.of:723
          let src_line = source_lines[i];
          //! OF_LINE: transpiler.of:724
          let was_toplevel = len2(state.block_stack) == 0;
          //! OF_LINE: transpiler.of:725
          let result_lines = await transpile_line(src_line, filename, user_tasks, i + 1, state);
          //! OF_LINE: transpiler.of:726
          if (state.error) {
            //! OF_LINE: transpiler.of:727
            return { ok: false, error: state.error };
            //! OF_LINE: transpiler.of:728
          }
          //! OF_LINE: transpiler.of:729
          if (result_lines) {
            //! OF_LINE: transpiler.of:730
            for (let ol of result_lines) {
              //! OF_LINE: transpiler.of:731
              //! OF_LINE: transpiler.of:732
              //! OF_LINE: transpiler.of:733
              if (was_toplevel && match2(ol, "/^await /")) {
                //! OF_LINE: transpiler.of:734
                arraypush2(deferred_stmts, ol);
                //! OF_LINE: transpiler.of:735
              } else {
                //! OF_LINE: transpiler.of:736
                arraypush2(output_lines, ol);
                //! OF_LINE: transpiler.of:737
              }
              //! OF_LINE: transpiler.of:738
            }
            //! OF_LINE: transpiler.of:739
          }
          //! OF_LINE: transpiler.of:740
          i = state.current_index + 1;
          //! OF_LINE: transpiler.of:741
        }
        //! OF_LINE: transpiler.of:742
        //! OF_LINE: transpiler.of:743
        //! OF_LINE: transpiler.of:744
        for (let exported_task of state.tasks) {
          //! OF_LINE: transpiler.of:745
          arraypush2(output_lines, "global." + exported_task + " = " + exported_task + ";");
          //! OF_LINE: transpiler.of:746
        }
        //! OF_LINE: transpiler.of:747
        //! OF_LINE: transpiler.of:748
        //! OF_LINE: transpiler.of:749
        if (len2(state.on_start_funcs) > 0) {
          //! OF_LINE: transpiler.of:750
          arraypush2(output_lines, "global.__on_start_hooks = global.__on_start_hooks || [];");
          //! OF_LINE: transpiler.of:751
          for (let f of state.on_start_funcs) {
            //! OF_LINE: transpiler.of:752
            arraypush2(output_lines, "global.__on_start_hooks.push(" + f + ");");
            //! OF_LINE: transpiler.of:753
          }
          //! OF_LINE: transpiler.of:754
        }
        //! OF_LINE: transpiler.of:755
        if (len2(state.on_shutdown_funcs) > 0) {
          //! OF_LINE: transpiler.of:756
          arraypush2(output_lines, "global.__on_shutdown_hooks = global.__on_shutdown_hooks || [];");
          //! OF_LINE: transpiler.of:757
          for (let f2 of state.on_shutdown_funcs) {
            //! OF_LINE: transpiler.of:758
            arraypush2(output_lines, "global.__on_shutdown_hooks.push(" + f2 + ");");
            //! OF_LINE: transpiler.of:759
          }
          //! OF_LINE: transpiler.of:760
        }
        //! OF_LINE: transpiler.of:761
        if (len2(state.on_error_funcs) > 0) {
          //! OF_LINE: transpiler.of:762
          arraypush2(output_lines, "global.__on_error_hooks = global.__on_error_hooks || [];");
          //! OF_LINE: transpiler.of:763
          for (let f3 of state.on_error_funcs) {
            //! OF_LINE: transpiler.of:764
            arraypush2(output_lines, "global.__on_error_hooks.push(" + f3 + ");");
            //! OF_LINE: transpiler.of:765
          }
          //! OF_LINE: transpiler.of:766
        }
        //! OF_LINE: transpiler.of:767
        if (len2(state.on_request_funcs) > 0) {
          //! OF_LINE: transpiler.of:768
          arraypush2(output_lines, "global.__on_request_hooks = global.__on_request_hooks || [];");
          //! OF_LINE: transpiler.of:769
          for (let f4 of state.on_request_funcs) {
            //! OF_LINE: transpiler.of:770
            arraypush2(output_lines, "global.__on_request_hooks.push(" + f4 + ");");
            //! OF_LINE: transpiler.of:771
          }
          //! OF_LINE: transpiler.of:772
        }
        //! OF_LINE: transpiler.of:773
        //! OF_LINE: transpiler.of:774
        //! OF_LINE: transpiler.of:775
        if (len2(state.on_shutdown_funcs) > 0) {
          //! OF_LINE: transpiler.of:776
          arraypush2(output_lines, "if (!global.__on_shutdown_registered) {");
          //! OF_LINE: transpiler.of:777
          arraypush2(output_lines, "    global.__on_shutdown_registered = true;");
          //! OF_LINE: transpiler.of:778
          arraypush2(output_lines, "    const __shutdown_wrapper = async () => {");
          //! OF_LINE: transpiler.of:779
          arraypush2(output_lines, "        try { const hooks = global.__on_shutdown_hooks || []; for (const hook of hooks) { await hook(); } } catch(e) {}");
          //! OF_LINE: transpiler.of:780
          arraypush2(output_lines, "        process.exit(0);");
          //! OF_LINE: transpiler.of:781
          arraypush2(output_lines, "    };");
          //! OF_LINE: transpiler.of:782
          arraypush2(output_lines, "    process.on('SIGTERM', __shutdown_wrapper);");
          //! OF_LINE: transpiler.of:783
          arraypush2(output_lines, "    process.on('SIGINT', __shutdown_wrapper);");
          //! OF_LINE: transpiler.of:784
          arraypush2(output_lines, "}");
          //! OF_LINE: transpiler.of:785
        }
        //! OF_LINE: transpiler.of:786
        //! OF_LINE: transpiler.of:787
        //! OF_LINE: transpiler.of:788
        if (len2(state.on_error_funcs) > 0) {
          //! OF_LINE: transpiler.of:789
          arraypush2(output_lines, "if (!global.__on_error_registered) {");
          //! OF_LINE: transpiler.of:790
          arraypush2(output_lines, "    global.__on_error_registered = true;");
          //! OF_LINE: transpiler.of:791
          arraypush2(output_lines, "    process.on('uncaughtException', (err) => { const hooks = global.__on_error_hooks || []; for (const hook of hooks) { hook(err).catch(() => {}); } });");
          //! OF_LINE: transpiler.of:792
          arraypush2(output_lines, "    process.on('unhandledRejection', (reason) => { const hooks = global.__on_error_hooks || []; for (const hook of hooks) { hook(reason).catch(() => {}); } });");
          //! OF_LINE: transpiler.of:793
          arraypush2(output_lines, "}");
          //! OF_LINE: transpiler.of:794
        }
        //! OF_LINE: transpiler.of:795
        //! OF_LINE: transpiler.of:796
        //! OF_LINE: transpiler.of:797
        if (state.server_port != "") {
          //! OF_LINE: transpiler.of:798
          arraypush2(output_lines, "global.__app_root = process.cwd();");
          //! OF_LINE: transpiler.of:799
          arraypush2(output_lines, "app.listen(" + state.server_port + ", () => console.log('Server started on port ' + " + state.server_port + "));");
          //! OF_LINE: transpiler.of:800
        }
        //! OF_LINE: transpiler.of:801
        //! OF_LINE: transpiler.of:802
        //! OF_LINE: transpiler.of:803
        arraypush2(output_lines, "if (require.main === module) {");
        //! OF_LINE: transpiler.of:804
        arraypush2(output_lines, "    (async () => {");
        //! OF_LINE: transpiler.of:805
        //! OF_LINE: transpiler.of:806
        for (let ds of deferred_stmts) {
          //! OF_LINE: transpiler.of:807
          arraypush2(output_lines, "        " + ds);
          //! OF_LINE: transpiler.of:808
        }
        //! OF_LINE: transpiler.of:809
        arraypush2(output_lines, "        const hooks = global.__on_start_hooks || [];");
        //! OF_LINE: transpiler.of:810
        arraypush2(output_lines, "        for (const hook of hooks) { await hook(); }");
        //! OF_LINE: transpiler.of:811
        arraypush2(output_lines, "    })().catch(err => {");
        //! OF_LINE: transpiler.of:812
        arraypush2(output_lines, "        if (typeof formatRuntimeError === 'function') { formatRuntimeError(err); } else { console.error(err); }");
        //! OF_LINE: transpiler.of:813
        arraypush2(output_lines, "        process.exit(1);");
        //! OF_LINE: transpiler.of:814
        arraypush2(output_lines, "    });");
        //! OF_LINE: transpiler.of:815
        arraypush2(output_lines, "}");
        //! OF_LINE: transpiler.of:816
        //! OF_LINE: transpiler.of:817
        return { ok: true, js: arrayjoin2(output_lines, "\n") };
        //! OF_LINE: transpiler.of:818
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: transpiler.of:819
    //! OF_LINE: transpiler.of:820
    global.ensure_parens = ensure_parens;
    global.clean_template_args = clean_template_args;
    global.replace_globals = replace_globals;
    global.insert_awaits = insert_awaits;
    global.transpile_line = transpile_line;
    global.count_braces = count_braces;
    global.pre_process_line = pre_process_line;
    global.transpile_line_core = transpile_line_core;
    global.transpile = transpile2;
    if (require.main === module2) {
      (async () => {
        const hooks = global.__on_start_hooks || [];
        for (const hook of hooks) {
          await hook();
        }
      })().catch((err) => {
        if (typeof formatRuntimeError2 === "function") {
          formatRuntimeError2(err);
        } else {
          console.error(err);
        }
        process.exit(1);
      });
    }
  }
});

// .omniflux_cache/stdlib/network.js
var require_network = __commonJS({
  ".omniflux_cache/stdlib/network.js"(exports2, module2) {
    var fs2 = require("fs");
    var util2 = require("util");
    var path2 = require("path");
    global.args = process.argv.slice(2);
    global.vars = new Proxy({ args: global.args }, {
      get(target, prop) {
        return prop in target ? target[prop] : null;
      },
      set(target, prop, value) {
        target[prop] = value;
        return true;
      }
    });
    function sprintf2(format, ...args2) {
      if (typeof format !== "string") {
        return util2.format(format, ...args2);
      }
      let argIndex = 0;
      const hasSpecifiers = /%([+-]?)(0?\d*)(?:\.(\d+))?([asdijf%])/g.test(format);
      if (!hasSpecifiers) {
        return util2.format(format, ...args2);
      }
      let formatted = format.replace(/%(?<flags>[+-]?)(?<width>0?\d*)(?:\.(?<precision>\d+))?(?<type>[asdijf%])/g, (match3, flags, widthStr, precisionStr, type) => {
        if (type === "%")
          return "%";
        if (argIndex >= args2.length)
          return match3;
        const val = args2[argIndex++];
        let padChar = " ";
        let width = 0;
        let leftAlign = flags === "-";
        if (widthStr) {
          if (widthStr.startsWith("0")) {
            padChar = "0";
            width = parseInt(widthStr, 10);
          } else {
            width = parseInt(widthStr, 10);
          }
        }
        let precision = precisionStr ? parseInt(precisionStr, 10) : -1;
        let result = "";
        if (type === "d" || type === "i") {
          let intVal = parseInt(val, 10);
          if (isNaN(intVal))
            intVal = 0;
          result = intVal.toString();
          if (precision >= 0) {
            result = result.padStart(precision, "0");
          }
        } else if (type === "f") {
          let floatVal = parseFloat(val);
          if (isNaN(floatVal))
            floatVal = 0;
          if (precision >= 0) {
            result = floatVal.toFixed(precision);
          } else {
            result = floatVal.toString();
          }
        } else if (type === "s") {
          result = String(val);
          if (precision >= 0) {
            result = result.slice(0, precision);
          }
        } else if (type === "j") {
          result = JSON.stringify(val);
        } else {
          result = String(val);
        }
        if (result.length < width) {
          const padLen = width - result.length;
          const padding = padChar.repeat(padLen);
          result = leftAlign ? result + padding : padding + result;
        }
        return result;
      });
      if (argIndex < args2.length) {
        const leftover = args2.slice(argIndex).map((x) => typeof x === "object" ? util2.inspect(x) : String(x));
        formatted += " " + leftover.join(" ");
      }
      return formatted;
    }
    function print2(format, ...args2) {
      console.log(sprintf2(format, ...args2));
    }
    function printf2(format, ...args2) {
      process.stdout.write(sprintf2(format, ...args2));
    }
    var _rl2 = null;
    function _getRL2() {
      if (!_rl2 && process.stdin.isTTY) {
        const readline = require("readline");
        _rl2 = readline.createInterface({
          input: process.stdin,
          output: process.stdout,
          terminal: true,
          historySize: 200
        });
        _rl2.on("close", () => {
          _rl2 = null;
        });
      }
      return _rl2;
    }
    function input2(promptMessage) {
      const rl = _getRL2();
      if (rl) {
        return new Promise((resolve) => {
          rl.question(promptMessage !== void 0 ? String(promptMessage) : "", resolve);
        });
      }
      if (promptMessage !== void 0) {
        process.stdout.write(String(promptMessage));
      }
      const buffer = Buffer.alloc(65536);
      let bytesRead = 0;
      try {
        bytesRead = fs2.readSync(0, buffer, 0, 65536, null);
      } catch (e) {
        return "";
      }
      return buffer.toString("utf8", 0, bytesRead).replace(/\r?\n$/, "");
    }
    global.input = input2;
    function readchar2() {
      const hasRaw = typeof process.stdin.setRawMode === "function";
      const isRaw = process.stdin.isRaw;
      if (hasRaw) {
        process.stdin.setRawMode(true);
      }
      const buffer = Buffer.alloc(1);
      let bytesRead = 0;
      try {
        bytesRead = fs2.readSync(0, buffer, 0, 1, null);
      } catch (e) {
      } finally {
        if (hasRaw) {
          process.stdin.setRawMode(isRaw);
        }
      }
      const char = buffer.toString("utf8", 0, bytesRead);
      if (char === "") {
        process.exit(130);
      }
      return char;
    }
    function fileread2(path3) {
      return fs2.readFileSync(path3, "utf8");
    }
    function filewrite2(path3, data) {
      fs2.writeFileSync(path3, typeof data === "object" ? JSON.stringify(data, null, 2) : data, "utf8");
    }
    function fileexists2(path3) {
      return fs2.existsSync(path3);
    }
    function filecopy2(src, dest) {
      fs2.copyFileSync(src, dest);
    }
    function filerename2(src, dest) {
      fs2.renameSync(src, dest);
    }
    function fileappend2(path3, data) {
      fs2.appendFileSync(path3, typeof data === "object" ? JSON.stringify(data, null, 2) : data, "utf8");
    }
    function fprintf2(path3, format, ...args2) {
      const data = sprintf2(format, ...args2);
      fs2.appendFileSync(path3, data, "utf8");
    }
    function fprint2(path3, format, ...args2) {
      const data = sprintf2(format, ...args2) + "\n";
      fs2.appendFileSync(path3, data, "utf8");
    }
    function filedelete2(path3) {
      fs2.unlinkSync(path3);
    }
    function dirlist2(path3) {
      return fs2.readdirSync(path3);
    }
    function filestat2(path3) {
      const stats = fs2.statSync(path3);
      return {
        size: stats.size,
        isdirectory: stats.isDirectory(),
        is_directory: stats.isDirectory(),
        isfile: stats.isFile(),
        is_file: stats.isFile(),
        createdat: stats.birthtimeMs,
        created_at: stats.birthtimeMs,
        modifiedat: stats.mtimeMs,
        modified_at: stats.mtimeMs
      };
    }
    function dircreate2(path3) {
      if (typeof path3 !== "string")
        throw new Error("Directory path must be a string");
      fs2.mkdirSync(path3, { recursive: true });
    }
    function scriptdir2() {
      const path3 = require("path");
      return path3.dirname(require.main.filename);
    }
    function setenv2(name, value) {
      if (typeof name !== "string")
        throw new Error("Environment variable name must be a string");
      process.env[name] = value !== void 0 ? String(value) : "";
    }
    function getenv2(name) {
      if (typeof name !== "string")
        throw new Error("Environment variable name must be a string");
      return process.env[name] !== void 0 ? process.env[name] : null;
    }
    function sha2562(text) {
      if (typeof text !== "string")
        throw new Error("Input to sha256 must be a string");
      const crypto = require("crypto");
      return crypto.createHash("sha256").update(text).digest("hex");
    }
    function getcookie2(req, name) {
      if (!req || !req.headers || !req.headers.cookie)
        return null;
      const cookies = req.headers.cookie.split(";");
      for (let c of cookies) {
        const parts = c.split("=");
        if (parts[0].trim() === name)
          return parts[1].trim();
      }
      return null;
    }
    function setcookie2(res, name, value, options = {}) {
      if (!res || typeof res.cookie !== "function") {
        throw new Error("First argument to setcookie must be an HTTP response object");
      }
      res.cookie(name, value, options);
    }
    var dbFilePath2 = process.env.DB_FILE || "db.json";
    var cachedDb2 = null;
    var cachedDbMtime2 = 0;
    function dbsetfile2(path3) {
      if (typeof path3 !== "string")
        throw new Error("Database file path must be a string");
      dbFilePath2 = path3;
      cachedDb2 = null;
      cachedDbMtime2 = 0;
    }
    function loadDb2() {
      try {
        if (fs2.existsSync(dbFilePath2)) {
          const stats = fs2.statSync(dbFilePath2);
          if (cachedDb2 && stats.mtimeMs === cachedDbMtime2) {
            return cachedDb2;
          }
          const content = fs2.readFileSync(dbFilePath2, "utf8");
          cachedDb2 = JSON.parse(content || "{}");
          cachedDbMtime2 = stats.mtimeMs;
          return cachedDb2;
        }
      } catch (e) {
      }
      if (!cachedDb2) {
        cachedDb2 = {};
        cachedDbMtime2 = 0;
      }
      return cachedDb2;
    }
    function saveDb2(data) {
      cachedDb2 = data;
      fs2.writeFileSync(dbFilePath2, JSON.stringify(data, null, 2), "utf8");
      try {
        if (fs2.existsSync(dbFilePath2)) {
          cachedDbMtime2 = fs2.statSync(dbFilePath2).mtimeMs;
        }
      } catch (e) {
        cachedDbMtime2 = Date.now();
      }
    }
    function dbinsert2(collection, doc) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      if (!doc || typeof doc !== "object")
        throw new Error("Document must be an object");
      const db = loadDb2();
      if (!db[collection]) {
        db[collection] = [];
      }
      const newDoc = { ...doc };
      if (newDoc.id === void 0) {
        newDoc.id = Date.now().toString(36) + Math.random().toString(36).substring(2, 6);
      }
      db[collection].push(newDoc);
      saveDb2(db);
      return newDoc;
    }
    function dbselect2(collection, filter) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      const db = loadDb2();
      const list = db[collection] || [];
      if (!filter || typeof filter !== "object" || Object.keys(filter).length === 0) {
        return list.map((d) => ({ ...d }));
      }
      return list.filter((doc) => {
        for (const key in filter) {
          if (doc[key] !== filter[key]) {
            return false;
          }
        }
        return true;
      }).map((d) => ({ ...d }));
    }
    function dbupdate2(collection, filter, update) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      if (!update || typeof update !== "object")
        throw new Error("Update payload must be an object");
      const db = loadDb2();
      const list = db[collection] || [];
      let count = 0;
      for (let i = 0; i < list.length; i++) {
        const doc = list[i];
        let matches = true;
        if (filter && typeof filter === "object") {
          for (const key in filter) {
            if (doc[key] !== filter[key]) {
              matches = false;
              break;
            }
          }
        }
        if (matches) {
          list[i] = { ...doc, ...update };
          count++;
        }
      }
      if (count > 0) {
        db[collection] = list;
        saveDb2(db);
      }
      return count;
    }
    function dbdelete2(collection, filter) {
      if (typeof collection !== "string")
        throw new Error("Collection name must be a string");
      const db = loadDb2();
      const list = db[collection] || [];
      let count = 0;
      const newList = [];
      for (const doc of list) {
        let matches = true;
        if (filter && typeof filter === "object") {
          for (const key in filter) {
            if (doc[key] !== filter[key]) {
              matches = false;
              break;
            }
          }
        }
        if (matches) {
          count++;
        } else {
          newList.push(doc);
        }
      }
      if (count > 0) {
        db[collection] = newList;
        saveDb2(db);
      }
      return count;
    }
    var redisModule2 = null;
    var redisClient2 = null;
    async function getRedisClient2() {
      if (!redisModule2) {
        redisModule2 = require("redis");
      }
      if (!redisClient2) {
        const url = process.env.REDIS_URL || "redis://localhost:6379";
        redisClient2 = redisModule2.createClient({ url });
        await redisClient2.connect();
      }
      return redisClient2;
    }
    async function cacheset2(key, val, ttl) {
      const client = await getRedisClient2();
      const stringVal = typeof val === "object" ? JSON.stringify(val) : String(val);
      if (ttl) {
        await client.setEx(key, ttl, stringVal);
      } else {
        await client.set(key, stringVal);
      }
    }
    async function cacheget2(key) {
      const client = await getRedisClient2();
      const val = await client.get(key);
      if (!val)
        return null;
      try {
        return JSON.parse(val);
      } catch (e) {
        return val;
      }
    }
    function describe2(val) {
      if (val === null)
        return "null";
      if (val === void 0)
        return "undefined";
      if (Array.isArray(val))
        return "array";
      return typeof val;
    }
    global.describe = describe2;
    function jsonparse2(str) {
      try {
        return JSON.parse(String(str || ""));
      } catch (e) {
        return null;
      }
    }
    global.jsonparse = jsonparse2;
    global.jsondecode = jsonparse2;
    function jsonencode2(val, pretty = false) {
      try {
        return pretty ? JSON.stringify(val, null, 2) : JSON.stringify(val);
      } catch (e) {
        return "null";
      }
    }
    global.jsonencode = jsonencode2;
    function len2(val) {
      if (val === null || val === void 0)
        return 0;
      return val.length || 0;
    }
    function toRegExp2(regex) {
      if (regex instanceof RegExp) {
        return regex;
      }
      if (typeof regex === "string") {
        const firstChar = regex.charAt(0);
        if (/^[^a-zA-Z0-9\s\\]/.test(firstChar)) {
          const lastDelim = regex.lastIndexOf(firstChar);
          if (lastDelim > 0) {
            const pattern = regex.substring(1, lastDelim);
            const flags = regex.substring(lastDelim + 1);
            return new RegExp(pattern, flags);
          }
        }
        return new RegExp(regex);
      }
      return null;
    }
    function strsplit2(str, sep) {
      if (typeof str !== "string")
        return [];
      if (sep === void 0)
        return [str];
      const regexObj = toRegExp2(sep);
      if (regexObj) {
        return str.split(regexObj);
      }
      return str.split(sep);
    }
    function match2(str, regex) {
      if (str === null || str === void 0)
        return false;
      const regexObj = toRegExp2(regex);
      if (!regexObj)
        return false;
      const res = String(str).match(regexObj);
      if (!res)
        return false;
      if (res.length > 1) {
        return res.slice(1);
      }
      return true;
    }
    function strtrim2(str, side = "both") {
      const s = String(str || "");
      if (side === "left")
        return s.trimStart();
      if (side === "right")
        return s.trimEnd();
      return s.trim();
    }
    function strsub2(str, start, length) {
      const s = String(str || "");
      if (length === void 0) {
        return s.substring(start);
      }
      return s.substring(start, start + length);
    }
    function strindexof2(str, search) {
      const s = String(str || "");
      return s.indexOf(search);
    }
    function strlastindexof2(str, search) {
      const s = String(str || "");
      return s.lastIndexOf(search);
    }
    function strrepeat2(str, n) {
      const s = String(str || "");
      const count = parseInt(n, 10);
      if (isNaN(count) || count <= 0)
        return "";
      return s.repeat(count);
    }
    function strreplace2(str, search, replacement) {
      const s = String(str || "");
      const r = String(replacement !== void 0 ? replacement : "");
      const regexObj = toRegExp2(search);
      if (regexObj) {
        return s.replace(regexObj, r);
      }
      return s.replaceAll(search, r);
    }
    function strupper2(str) {
      return String(str || "").toUpperCase();
    }
    function strlower2(str) {
      return String(str || "").toLowerCase();
    }
    function arraypush2(arr, item) {
      if (Array.isArray(arr)) {
        arr.push(item);
      }
    }
    function arrayunshift2(arr, item) {
      if (Array.isArray(arr)) {
        arr.unshift(item);
      }
    }
    function arraypop2(arr) {
      if (Array.isArray(arr)) {
        return arr.pop();
      }
      return null;
    }
    function arraycontains2(arr, item) {
      if (Array.isArray(arr)) {
        return arr.includes(item);
      }
      return false;
    }
    function arrayjoin2(arr, sep) {
      if (Array.isArray(arr)) {
        return arr.join(sep);
      }
      return "";
    }
    function arrayslice2(arr, start, end) {
      if (Array.isArray(arr)) {
        return arr.slice(start, end);
      }
      return [];
    }
    function pathjoin2(...paths) {
      const path3 = require("path");
      return path3.join(...paths.map((p) => String(p || "")));
    }
    function pathresolve2(base, relative) {
      const path3 = require("path");
      if (relative === void 0) {
        return path3.resolve(String(base || ""));
      }
      return path3.resolve(String(base || ""), String(relative || ""));
    }
    function pathdirname2(filepath) {
      const path3 = require("path");
      return path3.dirname(String(filepath || ""));
    }
    function pathbasename2(filepath) {
      const path3 = require("path");
      return path3.basename(String(filepath || ""));
    }
    function pathextension2(filepath) {
      const path3 = require("path");
      return path3.extname(String(filepath || ""));
    }
    function pathisabsolute2(filepath) {
      const path3 = require("path");
      return path3.isAbsolute(String(filepath || ""));
    }
    function exit2(code = 0) {
      process.exit(parseInt(code, 10) || 0);
    }
    function arrayreverse2(arr) {
      if (!Array.isArray(arr))
        return [];
      return [...arr].reverse();
    }
    async function asyncMergeSort2(arr, compareFn) {
      if (arr.length <= 1)
        return arr;
      const mid = Math.floor(arr.length / 2);
      const left = arr.slice(0, mid);
      const right = arr.slice(mid);
      return await asyncMerge2(
        await asyncMergeSort2(left, compareFn),
        await asyncMergeSort2(right, compareFn),
        compareFn
      );
    }
    async function asyncMerge2(left, right, compareFn) {
      const result = [];
      let l = 0, r = 0;
      while (l < left.length && r < right.length) {
        const cmp = await compareFn(left[l], right[r]);
        if (cmp <= 0) {
          result.push(left[l]);
          l++;
        } else {
          result.push(right[r]);
          r++;
        }
      }
      return result.concat(left.slice(l)).concat(right.slice(r));
    }
    async function arraysort2(arr, compareFn) {
      if (!Array.isArray(arr))
        return [];
      if (typeof compareFn === "function") {
        const temp = [...arr];
        return await asyncMergeSort2(temp, compareFn);
      }
      return [...arr].sort();
    }
    async function arraymap2(arr, task) {
      if (!Array.isArray(arr))
        return [];
      if (typeof task !== "function")
        return [...arr];
      const results = [];
      for (let i = 0; i < arr.length; i++) {
        results.push(await task(arr[i], i, arr));
      }
      return results;
    }
    async function arrayfilter2(arr, task) {
      if (!Array.isArray(arr))
        return [];
      if (typeof task !== "function")
        return [...arr];
      const results = [];
      for (let i = 0; i < arr.length; i++) {
        if (await task(arr[i], i, arr)) {
          results.push(arr[i]);
        }
      }
      return results;
    }
    function arrayshift2(arr) {
      if (!Array.isArray(arr))
        return null;
      return arr.shift();
    }
    async function arrayfind2(arr, task) {
      if (!Array.isArray(arr))
        return null;
      if (typeof task !== "function")
        return null;
      for (let i = 0; i < arr.length; i++) {
        if (await task(arr[i], i, arr)) {
          return arr[i];
        }
      }
      return null;
    }
    function time2() {
      return Math.floor(Date.now() / 1e3);
    }
    function microtime2(getAsFloat = true) {
      if (getAsFloat === false) {
        const now = Date.now();
        const sec = Math.floor(now / 1e3);
        const msec = now % 1e3;
        return `${(msec / 1e3).toFixed(8)} ${sec}`;
      }
      return Date.now() / 1e3;
    }
    function dateyear2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getFullYear();
    }
    function datemonth2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getMonth() + 1;
    }
    function dateday2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getDate();
    }
    function datehour2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getHours();
    }
    function dateminute2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getMinutes();
    }
    function datesecond2(ts) {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      return d.getSeconds();
    }
    function dateweekday2(ts, format = "number") {
      const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
      const day = d.getDay();
      if (format === "short" || format === "text") {
        const days = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];
        return days[day];
      }
      return day;
    }
    function template2(source, context = {}) {
      if (typeof source !== "string")
        return "";
      const appRoot = typeof global !== "undefined" && global.__app_root ? global.__app_root : process.cwd();
      const resolvePath = (p) => path2.isAbsolute(p) ? p : path2.resolve(appRoot, p);
      let html = source;
      try {
        const resolvedSource = resolvePath(source);
        if (fs2.existsSync(resolvedSource)) {
          html = fs2.readFileSync(resolvedSource, "utf8");
        }
        let includeDepth = 0;
        const maxIncludeDepth = 10;
        while (/@include\s*\(\s*["'](.*?)["']\s*\)/.test(html) && includeDepth < maxIncludeDepth) {
          html = html.replace(/@include\s*\(\s*["'](.*?)["']\s*\)/g, (match3, filepath) => {
            const resolvedInclude = resolvePath(filepath);
            if (!fs2.existsSync(resolvedInclude)) {
              throw new Error(`Include file not found: '${filepath}'`);
            }
            return fs2.readFileSync(resolvedInclude, "utf8");
          });
          includeDepth++;
        }
        html = html.replace(/@\}\s*(?=@else)/g, "@}");
        if (html.includes("</body>")) {
          const clientScript = `
<script>
function _of_exec_scripts(container) {
    const scripts = container.querySelectorAll('script');
    for (let oldScript of scripts) {
        const newScript = document.createElement('script');
        for (let attr of oldScript.attributes) {
            newScript.setAttribute(attr.name, attr.value);
        }
        newScript.textContent = oldScript.textContent;
        oldScript.parentNode.replaceChild(newScript, oldScript);
    }
}

document.addEventListener('submit', async (e) => {
    const form = e.target;
    const targetSelector = form.getAttribute('of-target');
    if (!targetSelector) return;
    
    e.preventDefault();
    try {
        const response = await fetch(form.action || window.location.href, {
            method: form.method || 'POST',
            body: new URLSearchParams(new FormData(form))
        });
        const htmlResult = await response.text();
        const targetEl = document.querySelector(targetSelector);
        if (targetEl) {
            targetEl.innerHTML = htmlResult;
            _of_exec_scripts(targetEl);
        }
    } catch (err) {
        console.error('OmniFlux Form Submission Error:', err);
    }
});

document.addEventListener('click', async (e) => {
    const link = e.target.closest('a');
    if (!link) return;
    const targetSelector = link.getAttribute('of-target');
    if (!targetSelector) return;
    
    e.preventDefault();
    try {
        const response = await fetch(link.href);
        const htmlResult = await response.text();
        const targetEl = document.querySelector(targetSelector);
        if (targetEl) {
            targetEl.innerHTML = htmlResult;
            _of_exec_scripts(targetEl);
        }
    } catch (err) {
        console.error('OmniFlux Link Navigation Error:', err);
    }
});
</script>
`;
          html = html.replace("</body>", clientScript + "</body>");
        }
        const tokenRegex = /(@if\s*\(.*?\)\s*\{|@else\s*if\s*\(.*?\)\s*\{|@else\s*\{|@for\s*\(.*?\)\s*\{|@\})/g;
        const parts = html.split(tokenRegex);
        let jsCode = "let _out = '';\n";
        jsCode += "with(global.vars || {}) {\n";
        jsCode += "with(context || {}) {\n";
        for (let part of parts) {
          if (!part)
            continue;
          if (part.startsWith("@if")) {
            const cond = part.match(/@if\s*\((.*?)\)\s*\{/)[1];
            jsCode += `if (${cond}) {
`;
          } else if (part.startsWith("@else if")) {
            const cond = part.match(/@else\s*if\s*\((.*?)\)\s*\{/)[1];
            jsCode += `} else if (${cond}) {
`;
          } else if (part.startsWith("@else")) {
            jsCode += `} else {
`;
          } else if (part.startsWith("@for")) {
            const loopMatch = part.match(/@for\s*\(\s*(?:let|var)?\s*(\w+)\s+of\s+(.*?)\)\s*\{/);
            if (loopMatch) {
              const item = loopMatch[1];
              const list = loopMatch[2];
              jsCode += `for (let ${item} of ${list}) {
`;
            }
          } else if (part === "@}") {
            jsCode += `}
`;
          } else {
            let text = part.replace(/\\/g, "\\\\").replace(/`/g, "\\`").replace(/\$/g, "\\$");
            text = text.replace(/\{\{\s*([\s\S]+?)\s*\}\}/g, "${$1}");
            jsCode += `_out += \`${text}\`;
`;
          }
        }
        jsCode += "}\n}\n";
        jsCode += "return _out;";
        const keys = ["context", ...Object.keys(context)];
        const values = [context, ...Object.values(context)];
        return new Function(...keys, jsCode)(...values);
      } catch (e) {
        return `<!-- Template Error: ${e.message} -->`;
      }
    }
    global.sprintf = sprintf2;
    global.print = print2;
    global.printf = printf2;
    global.fprint = fprint2;
    global.f_print = fprint2;
    global.fprintf = fprintf2;
    global.f_printf = fprintf2;
    global.input = input2;
    global.readchar = readchar2;
    global.read_char = readchar2;
    global.fileread = fileread2;
    global.file_read = fileread2;
    global.filewrite = filewrite2;
    global.file_write = filewrite2;
    global.fileexists = fileexists2;
    global.file_exists = fileexists2;
    global.fileappend = fileappend2;
    global.file_append = fileappend2;
    global.filedelete = filedelete2;
    global.file_delete = filedelete2;
    global.filecopy = filecopy2;
    global.file_copy = filecopy2;
    global.filerename = filerename2;
    global.file_rename = filerename2;
    global.dirlist = dirlist2;
    global.dir_list = dirlist2;
    global.dircreate = dircreate2;
    global.dir_create = dircreate2;
    global.scriptdir = scriptdir2;
    global.script_dir = scriptdir2;
    global.setenv = setenv2;
    global.set_env = setenv2;
    global.getenv = getenv2;
    global.get_env = getenv2;
    global.sha256 = sha2562;
    global.getcookie = getcookie2;
    global.get_cookie = getcookie2;
    global.setcookie = setcookie2;
    global.set_cookie = setcookie2;
    global.filestat = filestat2;
    global.file_stat = filestat2;
    global.dbsetfile = dbsetfile2;
    global.db_set_file = dbsetfile2;
    global.dbinsert = dbinsert2;
    global.db_insert = dbinsert2;
    global.dbselect = dbselect2;
    global.db_select = dbselect2;
    global.dbupdate = dbupdate2;
    global.db_update = dbupdate2;
    global.dbdelete = dbdelete2;
    global.db_delete = dbdelete2;
    global.cacheset = cacheset2;
    global.cache_set = cacheset2;
    global.cacheget = cacheget2;
    global.cache_get = cacheget2;
    global.len = len2;
    global.strsplit = strsplit2;
    global.str_split = strsplit2;
    global.match = match2;
    global.arraypush = arraypush2;
    global.array_push = arraypush2;
    global.arrayunshift = arrayunshift2;
    global.array_unshift = arrayunshift2;
    global.arraypop = arraypop2;
    global.array_pop = arraypop2;
    global.arraycontains = arraycontains2;
    global.array_contains = arraycontains2;
    global.arrayjoin = arrayjoin2;
    global.array_join = arrayjoin2;
    global.arrayslice = arrayslice2;
    global.array_slice = arrayslice2;
    global.strtrim = strtrim2;
    global.str_trim = strtrim2;
    global.strsub = strsub2;
    global.str_sub = strsub2;
    global.strindexof = strindexof2;
    global.str_index_of = strindexof2;
    global.strlastindexof = strlastindexof2;
    global.str_last_index_of = strlastindexof2;
    global.strrepeat = strrepeat2;
    global.str_repeat = strrepeat2;
    global.strreplace = strreplace2;
    global.str_replace = strreplace2;
    global.strupper = strupper2;
    global.str_upper = strupper2;
    global.strlower = strlower2;
    global.str_lower = strlower2;
    global.pathjoin = pathjoin2;
    global.path_join = pathjoin2;
    global.pathresolve = pathresolve2;
    global.path_resolve = pathresolve2;
    global.pathdirname = pathdirname2;
    global.path_dir_name = pathdirname2;
    global.pathbasename = pathbasename2;
    global.path_base_name = pathbasename2;
    global.pathextension = pathextension2;
    global.path_extension = pathextension2;
    global.pathisabsolute = pathisabsolute2;
    global.path_is_absolute = pathisabsolute2;
    global.exit = exit2;
    global.arrayreverse = arrayreverse2;
    global.array_reverse = arrayreverse2;
    global.arraysort = arraysort2;
    global.array_sort = arraysort2;
    global.arraymap = arraymap2;
    global.array_map = arraymap2;
    global.arrayfilter = arrayfilter2;
    global.array_filter = arrayfilter2;
    global.arrayshift = arrayshift2;
    global.array_shift = arrayshift2;
    global.arrayfind = arrayfind2;
    global.array_find = arrayfind2;
    global.time = time2;
    global.microtime = microtime2;
    global.micro_time = microtime2;
    global.dateyear = dateyear2;
    global.date_year = dateyear2;
    global.datemonth = datemonth2;
    global.date_month = datemonth2;
    global.dateday = dateday2;
    global.date_day = dateday2;
    global.datehour = datehour2;
    global.date_hour = datehour2;
    global.dateminute = dateminute2;
    global.date_minute = dateminute2;
    global.datesecond = datesecond2;
    global.date_second = datesecond2;
    global.dateweekday = dateweekday2;
    global.date_weekday = dateweekday2;
    global.template = template2;
    function formatRuntimeError2(err, label = "OmniFlux Runtime Error") {
      console.error("\n\x1B[1m\x1B[31m" + label + ":\x1B[0m");
      console.error("\x1B[33m" + err.message + "\x1B[0m");
      if (err.stack) {
        const stackLines = err.stack.split("\n");
        const lineMatch = stackLines.find(
          (line) => line.includes("/") && !line.includes("node:internal") && !line.includes("node_modules")
        );
        if (lineMatch) {
          const match3 = lineMatch.match(/\(([^)]+):(\d+):(\d+)\)/) || lineMatch.match(/at\s+(.+?):(\d+):(\d+)/);
          if (match3) {
            const filePath = match3[1];
            const lineNum = parseInt(match3[2], 10);
            try {
              const content = fs2.readFileSync(filePath, "utf8");
              const fileLines = content.split("\n");
              const indicesToCheck = [lineNum, lineNum - 1, lineNum - 2];
              for (const i of indicesToCheck) {
                if (i >= 0 && i < fileLines.length) {
                  const targetLine = fileLines[i];
                  const ofLineMatch = targetLine.match(/\/\/\! OF_LINE: (.+?)(?:\r?\n|$)/) || targetLine.match(/\/\/ OF_LINE: (.+?)(?:\r?\n|$)/);
                  if (ofLineMatch) {
                    console.error("\x1B[1m\x1B[32mAt: " + ofLineMatch[1] + "\x1B[0m");
                    console.error();
                    return;
                  }
                }
              }
            } catch (e) {
            }
            console.error("\x1B[2mAt: " + path2.basename(filePath) + ":" + lineNum + "\x1B[0m");
          }
        }
      }
      console.error();
    }
    process.on("uncaughtException", (err) => {
      const hooks = global.__on_error_hooks || [];
      if (hooks.length > 0) {
        for (const hook of hooks) {
          hook(err).catch(() => {
          });
        }
        return;
      }
      formatRuntimeError2(err);
      process.exit(1);
    });
    process.on("unhandledRejection", (reason) => {
      const hooks = global.__on_error_hooks || [];
      if (hooks.length > 0) {
        for (const hook of hooks) {
          hook(reason).catch(() => {
          });
        }
        return;
      }
      const err = reason instanceof Error ? reason : new Error(String(reason));
      formatRuntimeError2(err, "OmniFlux Runtime Error (Unhandled Rejection)");
      process.exit(1);
    });
    //! OF_LINE: network.of:1
    //! OF_LINE: network.of:2
    //! OF_LINE: network.of:3
    var myFetch = typeof fetch === "function" ? fetch : (url, options = {}) => {
      //! OF_LINE: network.of:4
      return new Promise((resolve, reject) => {
        //! OF_LINE: network.of:5
        const urlObj = new URL(url);
        //! OF_LINE: network.of:6
        const lib = urlObj.protocol === "https:" ? require("https") : require("http");
        //! OF_LINE: network.of:7
        const reqOptions = {
          //! OF_LINE: network.of:8
          method: options.method || "GET",
          //! OF_LINE: network.of:9
          headers: { ...options.headers || {} }
          //! OF_LINE: network.of:10
        };
        //! OF_LINE: network.of:11
        if (options.body) {
          //! OF_LINE: network.of:12
          reqOptions.headers["Content-Length"] = Buffer.byteLength(options.body);
          //! OF_LINE: network.of:13
        }
        //! OF_LINE: network.of:14
        const req = lib.request(url, reqOptions, (res) => {
          //! OF_LINE: network.of:15
          let data = "";
          //! OF_LINE: network.of:16
          res.on("data", (chunk) => data += chunk);
          //! OF_LINE: network.of:17
          res.on("end", () => resolve({
            //! OF_LINE: network.of:18
            text: () => Promise.resolve(data)
            //! OF_LINE: network.of:19
          }));
          //! OF_LINE: network.of:20
        });
        //! OF_LINE: network.of:21
        req.on("error", (err) => reject(err));
        //! OF_LINE: network.of:22
        if (options.body) {
          //! OF_LINE: network.of:23
          req.write(options.body);
          //! OF_LINE: network.of:24
        }
        //! OF_LINE: network.of:25
        req.end();
        //! OF_LINE: network.of:26
      });
      //! OF_LINE: network.of:27
    };
    //! OF_LINE: network.of:28
    global.myFetch = myFetch;
    //! OF_LINE: network.of:29
    //! OF_LINE: network.of:30
    //! OF_LINE: network.of:31
    async function networkget(url) {
      try {
        //! OF_LINE: network.of:32
        //! OF_LINE: network.of:33
        try {
          //! OF_LINE: network.of:34
          const res = await myFetch(url);
          //! OF_LINE: network.of:35
          const text = await res.text();
          //! OF_LINE: network.of:36
          try {
            //! OF_LINE: network.of:37
            return JSON.parse(text);
            //! OF_LINE: network.of:38
          } catch (e) {
            //! OF_LINE: network.of:39
            return text;
            //! OF_LINE: network.of:40
          }
          //! OF_LINE: network.of:41
        } catch (err) {
          //! OF_LINE: network.of:42
          throw new Error("HTTP GET request failed: " + err.message);
          //! OF_LINE: network.of:43
        }
        //! OF_LINE: network.of:44
        //! OF_LINE: network.of:45
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: network.of:46
    //! OF_LINE: network.of:47
    async function network_get(url) {
      try {
        //! OF_LINE: network.of:48
        return await networkget(url);
        //! OF_LINE: network.of:49
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: network.of:50
    //! OF_LINE: network.of:51
    async function networkpost2(url, data, headers = {}) {
      try {
        //! OF_LINE: network.of:52
        //! OF_LINE: network.of:53
        try {
          //! OF_LINE: network.of:54
          const isObject = typeof data === "object" && data !== null;
          //! OF_LINE: network.of:55
          const finalHeaders = {
            //! OF_LINE: network.of:56
            ...isObject ? { "Content-Type": "application/json" } : {},
            //! OF_LINE: network.of:57
            ...headers || {}
            //! OF_LINE: network.of:58
          };
          //! OF_LINE: network.of:59
          const body = isObject ? JSON.stringify(data) : data;
          //! OF_LINE: network.of:60
          //! OF_LINE: network.of:61
          const res = await myFetch(url, {
            //! OF_LINE: network.of:62
            method: "POST",
            //! OF_LINE: network.of:63
            headers: finalHeaders,
            //! OF_LINE: network.of:64
            body
            //! OF_LINE: network.of:65
          });
          //! OF_LINE: network.of:66
          const text = await res.text();
          //! OF_LINE: network.of:67
          try {
            //! OF_LINE: network.of:68
            return JSON.parse(text);
            //! OF_LINE: network.of:69
          } catch (e) {
            //! OF_LINE: network.of:70
            return text;
            //! OF_LINE: network.of:71
          }
          //! OF_LINE: network.of:72
        } catch (err) {
          //! OF_LINE: network.of:73
          throw new Error("HTTP POST request failed: " + err.message);
          //! OF_LINE: network.of:74
        }
        //! OF_LINE: network.of:75
        //! OF_LINE: network.of:76
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: network.of:77
    //! OF_LINE: network.of:78
    async function network_post(url, data, headers = {}) {
      try {
        //! OF_LINE: network.of:79
        return await networkpost2(url, data, headers);
        //! OF_LINE: network.of:80
      } catch (err) {
        throw err;
      }
    }
    //! OF_LINE: network.of:81
    //! OF_LINE: network.of:82
    global.networkget = networkget;
    global.network_get = network_get;
    global.networkpost = networkpost2;
    global.network_post = network_post;
    if (require.main === module2) {
      (async () => {
        const hooks = global.__on_start_hooks || [];
        for (const hook of hooks) {
          await hook();
        }
      })().catch((err) => {
        if (typeof formatRuntimeError2 === "function") {
          formatRuntimeError2(err);
        } else {
          console.error(err);
        }
        process.exit(1);
      });
    }
  }
});

// .omniflux_cache/compiler/omniflux.js
var fs = require("fs");
var util = require("util");
var path = require("path");
global.args = process.argv.slice(2);
global.vars = new Proxy({ args: global.args }, {
  get(target, prop) {
    return prop in target ? target[prop] : null;
  },
  set(target, prop, value) {
    target[prop] = value;
    return true;
  }
});
function sprintf(format, ...args2) {
  if (typeof format !== "string") {
    return util.format(format, ...args2);
  }
  let argIndex = 0;
  const hasSpecifiers = /%([+-]?)(0?\d*)(?:\.(\d+))?([asdijf%])/g.test(format);
  if (!hasSpecifiers) {
    return util.format(format, ...args2);
  }
  let formatted = format.replace(/%(?<flags>[+-]?)(?<width>0?\d*)(?:\.(?<precision>\d+))?(?<type>[asdijf%])/g, (match2, flags, widthStr, precisionStr, type) => {
    if (type === "%")
      return "%";
    if (argIndex >= args2.length)
      return match2;
    const val = args2[argIndex++];
    let padChar = " ";
    let width = 0;
    let leftAlign = flags === "-";
    if (widthStr) {
      if (widthStr.startsWith("0")) {
        padChar = "0";
        width = parseInt(widthStr, 10);
      } else {
        width = parseInt(widthStr, 10);
      }
    }
    let precision = precisionStr ? parseInt(precisionStr, 10) : -1;
    let result = "";
    if (type === "d" || type === "i") {
      let intVal = parseInt(val, 10);
      if (isNaN(intVal))
        intVal = 0;
      result = intVal.toString();
      if (precision >= 0) {
        result = result.padStart(precision, "0");
      }
    } else if (type === "f") {
      let floatVal = parseFloat(val);
      if (isNaN(floatVal))
        floatVal = 0;
      if (precision >= 0) {
        result = floatVal.toFixed(precision);
      } else {
        result = floatVal.toString();
      }
    } else if (type === "s") {
      result = String(val);
      if (precision >= 0) {
        result = result.slice(0, precision);
      }
    } else if (type === "j") {
      result = JSON.stringify(val);
    } else {
      result = String(val);
    }
    if (result.length < width) {
      const padLen = width - result.length;
      const padding = padChar.repeat(padLen);
      result = leftAlign ? result + padding : padding + result;
    }
    return result;
  });
  if (argIndex < args2.length) {
    const leftover = args2.slice(argIndex).map((x) => typeof x === "object" ? util.inspect(x) : String(x));
    formatted += " " + leftover.join(" ");
  }
  return formatted;
}
function print(format, ...args2) {
  console.log(sprintf(format, ...args2));
}
function printf(format, ...args2) {
  process.stdout.write(sprintf(format, ...args2));
}
var _rl = null;
function _getRL() {
  if (!_rl && process.stdin.isTTY) {
    const readline = require("readline");
    _rl = readline.createInterface({
      input: process.stdin,
      output: process.stdout,
      terminal: true,
      historySize: 200
    });
    _rl.on("close", () => {
      _rl = null;
    });
  }
  return _rl;
}
function input(promptMessage) {
  const rl = _getRL();
  if (rl) {
    return new Promise((resolve) => {
      rl.question(promptMessage !== void 0 ? String(promptMessage) : "", resolve);
    });
  }
  if (promptMessage !== void 0) {
    process.stdout.write(String(promptMessage));
  }
  const buffer = Buffer.alloc(65536);
  let bytesRead = 0;
  try {
    bytesRead = fs.readSync(0, buffer, 0, 65536, null);
  } catch (e) {
    return "";
  }
  return buffer.toString("utf8", 0, bytesRead).replace(/\r?\n$/, "");
}
global.input = input;
function readchar() {
  const hasRaw = typeof process.stdin.setRawMode === "function";
  const isRaw = process.stdin.isRaw;
  if (hasRaw) {
    process.stdin.setRawMode(true);
  }
  const buffer = Buffer.alloc(1);
  let bytesRead = 0;
  try {
    bytesRead = fs.readSync(0, buffer, 0, 1, null);
  } catch (e) {
  } finally {
    if (hasRaw) {
      process.stdin.setRawMode(isRaw);
    }
  }
  const char = buffer.toString("utf8", 0, bytesRead);
  if (char === "") {
    process.exit(130);
  }
  return char;
}
function fileread(path2) {
  return fs.readFileSync(path2, "utf8");
}
function filewrite(path2, data) {
  fs.writeFileSync(path2, typeof data === "object" ? JSON.stringify(data, null, 2) : data, "utf8");
}
function fileexists(path2) {
  return fs.existsSync(path2);
}
function filecopy(src, dest) {
  fs.copyFileSync(src, dest);
}
function filerename(src, dest) {
  fs.renameSync(src, dest);
}
function fileappend(path2, data) {
  fs.appendFileSync(path2, typeof data === "object" ? JSON.stringify(data, null, 2) : data, "utf8");
}
function fprintf(path2, format, ...args2) {
  const data = sprintf(format, ...args2);
  fs.appendFileSync(path2, data, "utf8");
}
function fprint(path2, format, ...args2) {
  const data = sprintf(format, ...args2) + "\n";
  fs.appendFileSync(path2, data, "utf8");
}
function filedelete(path2) {
  fs.unlinkSync(path2);
}
function dirlist(path2) {
  return fs.readdirSync(path2);
}
function filestat(path2) {
  const stats = fs.statSync(path2);
  return {
    size: stats.size,
    isdirectory: stats.isDirectory(),
    is_directory: stats.isDirectory(),
    isfile: stats.isFile(),
    is_file: stats.isFile(),
    createdat: stats.birthtimeMs,
    created_at: stats.birthtimeMs,
    modifiedat: stats.mtimeMs,
    modified_at: stats.mtimeMs
  };
}
function dircreate(path2) {
  if (typeof path2 !== "string")
    throw new Error("Directory path must be a string");
  fs.mkdirSync(path2, { recursive: true });
}
function scriptdir() {
  const path2 = require("path");
  return path2.dirname(require.main.filename);
}
function setenv(name, value) {
  if (typeof name !== "string")
    throw new Error("Environment variable name must be a string");
  process.env[name] = value !== void 0 ? String(value) : "";
}
function getenv(name) {
  if (typeof name !== "string")
    throw new Error("Environment variable name must be a string");
  return process.env[name] !== void 0 ? process.env[name] : null;
}
function sha256(text) {
  if (typeof text !== "string")
    throw new Error("Input to sha256 must be a string");
  const crypto = require("crypto");
  return crypto.createHash("sha256").update(text).digest("hex");
}
function getcookie(req, name) {
  if (!req || !req.headers || !req.headers.cookie)
    return null;
  const cookies = req.headers.cookie.split(";");
  for (let c of cookies) {
    const parts = c.split("=");
    if (parts[0].trim() === name)
      return parts[1].trim();
  }
  return null;
}
function setcookie(res, name, value, options = {}) {
  if (!res || typeof res.cookie !== "function") {
    throw new Error("First argument to setcookie must be an HTTP response object");
  }
  res.cookie(name, value, options);
}
var dbFilePath = process.env.DB_FILE || "db.json";
var cachedDb = null;
var cachedDbMtime = 0;
function dbsetfile(path2) {
  if (typeof path2 !== "string")
    throw new Error("Database file path must be a string");
  dbFilePath = path2;
  cachedDb = null;
  cachedDbMtime = 0;
}
function loadDb() {
  try {
    if (fs.existsSync(dbFilePath)) {
      const stats = fs.statSync(dbFilePath);
      if (cachedDb && stats.mtimeMs === cachedDbMtime) {
        return cachedDb;
      }
      const content = fs.readFileSync(dbFilePath, "utf8");
      cachedDb = JSON.parse(content || "{}");
      cachedDbMtime = stats.mtimeMs;
      return cachedDb;
    }
  } catch (e) {
  }
  if (!cachedDb) {
    cachedDb = {};
    cachedDbMtime = 0;
  }
  return cachedDb;
}
function saveDb(data) {
  cachedDb = data;
  fs.writeFileSync(dbFilePath, JSON.stringify(data, null, 2), "utf8");
  try {
    if (fs.existsSync(dbFilePath)) {
      cachedDbMtime = fs.statSync(dbFilePath).mtimeMs;
    }
  } catch (e) {
    cachedDbMtime = Date.now();
  }
}
function dbinsert(collection, doc) {
  if (typeof collection !== "string")
    throw new Error("Collection name must be a string");
  if (!doc || typeof doc !== "object")
    throw new Error("Document must be an object");
  const db = loadDb();
  if (!db[collection]) {
    db[collection] = [];
  }
  const newDoc = { ...doc };
  if (newDoc.id === void 0) {
    newDoc.id = Date.now().toString(36) + Math.random().toString(36).substring(2, 6);
  }
  db[collection].push(newDoc);
  saveDb(db);
  return newDoc;
}
function dbselect(collection, filter) {
  if (typeof collection !== "string")
    throw new Error("Collection name must be a string");
  const db = loadDb();
  const list = db[collection] || [];
  if (!filter || typeof filter !== "object" || Object.keys(filter).length === 0) {
    return list.map((d) => ({ ...d }));
  }
  return list.filter((doc) => {
    for (const key in filter) {
      if (doc[key] !== filter[key]) {
        return false;
      }
    }
    return true;
  }).map((d) => ({ ...d }));
}
function dbupdate(collection, filter, update) {
  if (typeof collection !== "string")
    throw new Error("Collection name must be a string");
  if (!update || typeof update !== "object")
    throw new Error("Update payload must be an object");
  const db = loadDb();
  const list = db[collection] || [];
  let count = 0;
  for (let i = 0; i < list.length; i++) {
    const doc = list[i];
    let matches = true;
    if (filter && typeof filter === "object") {
      for (const key in filter) {
        if (doc[key] !== filter[key]) {
          matches = false;
          break;
        }
      }
    }
    if (matches) {
      list[i] = { ...doc, ...update };
      count++;
    }
  }
  if (count > 0) {
    db[collection] = list;
    saveDb(db);
  }
  return count;
}
function dbdelete(collection, filter) {
  if (typeof collection !== "string")
    throw new Error("Collection name must be a string");
  const db = loadDb();
  const list = db[collection] || [];
  let count = 0;
  const newList = [];
  for (const doc of list) {
    let matches = true;
    if (filter && typeof filter === "object") {
      for (const key in filter) {
        if (doc[key] !== filter[key]) {
          matches = false;
          break;
        }
      }
    }
    if (matches) {
      count++;
    } else {
      newList.push(doc);
    }
  }
  if (count > 0) {
    db[collection] = newList;
    saveDb(db);
  }
  return count;
}
var redisModule = null;
var redisClient = null;
async function getRedisClient() {
  if (!redisModule) {
    redisModule = require("redis");
  }
  if (!redisClient) {
    const url = process.env.REDIS_URL || "redis://localhost:6379";
    redisClient = redisModule.createClient({ url });
    await redisClient.connect();
  }
  return redisClient;
}
async function cacheset(key, val, ttl) {
  const client = await getRedisClient();
  const stringVal = typeof val === "object" ? JSON.stringify(val) : String(val);
  if (ttl) {
    await client.setEx(key, ttl, stringVal);
  } else {
    await client.set(key, stringVal);
  }
}
async function cacheget(key) {
  const client = await getRedisClient();
  const val = await client.get(key);
  if (!val)
    return null;
  try {
    return JSON.parse(val);
  } catch (e) {
    return val;
  }
}
function describe(val) {
  if (val === null)
    return "null";
  if (val === void 0)
    return "undefined";
  if (Array.isArray(val))
    return "array";
  return typeof val;
}
global.describe = describe;
function jsonparse(str) {
  try {
    return JSON.parse(String(str || ""));
  } catch (e) {
    return null;
  }
}
global.jsonparse = jsonparse;
global.jsondecode = jsonparse;
function jsonencode(val, pretty = false) {
  try {
    return pretty ? JSON.stringify(val, null, 2) : JSON.stringify(val);
  } catch (e) {
    return "null";
  }
}
global.jsonencode = jsonencode;
function len(val) {
  if (val === null || val === void 0)
    return 0;
  return val.length || 0;
}
function toRegExp(regex) {
  if (regex instanceof RegExp) {
    return regex;
  }
  if (typeof regex === "string") {
    const firstChar = regex.charAt(0);
    if (/^[^a-zA-Z0-9\s\\]/.test(firstChar)) {
      const lastDelim = regex.lastIndexOf(firstChar);
      if (lastDelim > 0) {
        const pattern = regex.substring(1, lastDelim);
        const flags = regex.substring(lastDelim + 1);
        return new RegExp(pattern, flags);
      }
    }
    return new RegExp(regex);
  }
  return null;
}
function strsplit(str, sep) {
  if (typeof str !== "string")
    return [];
  if (sep === void 0)
    return [str];
  const regexObj = toRegExp(sep);
  if (regexObj) {
    return str.split(regexObj);
  }
  return str.split(sep);
}
function match(str, regex) {
  if (str === null || str === void 0)
    return false;
  const regexObj = toRegExp(regex);
  if (!regexObj)
    return false;
  const res = String(str).match(regexObj);
  if (!res)
    return false;
  if (res.length > 1) {
    return res.slice(1);
  }
  return true;
}
function strtrim(str, side = "both") {
  const s = String(str || "");
  if (side === "left")
    return s.trimStart();
  if (side === "right")
    return s.trimEnd();
  return s.trim();
}
function strsub(str, start, length) {
  const s = String(str || "");
  if (length === void 0) {
    return s.substring(start);
  }
  return s.substring(start, start + length);
}
function strindexof(str, search) {
  const s = String(str || "");
  return s.indexOf(search);
}
function strlastindexof(str, search) {
  const s = String(str || "");
  return s.lastIndexOf(search);
}
function strrepeat(str, n) {
  const s = String(str || "");
  const count = parseInt(n, 10);
  if (isNaN(count) || count <= 0)
    return "";
  return s.repeat(count);
}
function strreplace(str, search, replacement) {
  const s = String(str || "");
  const r = String(replacement !== void 0 ? replacement : "");
  const regexObj = toRegExp(search);
  if (regexObj) {
    return s.replace(regexObj, r);
  }
  return s.replaceAll(search, r);
}
function strupper(str) {
  return String(str || "").toUpperCase();
}
function strlower(str) {
  return String(str || "").toLowerCase();
}
function arraypush(arr, item) {
  if (Array.isArray(arr)) {
    arr.push(item);
  }
}
function arrayunshift(arr, item) {
  if (Array.isArray(arr)) {
    arr.unshift(item);
  }
}
function arraypop(arr) {
  if (Array.isArray(arr)) {
    return arr.pop();
  }
  return null;
}
function arraycontains(arr, item) {
  if (Array.isArray(arr)) {
    return arr.includes(item);
  }
  return false;
}
function arrayjoin(arr, sep) {
  if (Array.isArray(arr)) {
    return arr.join(sep);
  }
  return "";
}
function arrayslice(arr, start, end) {
  if (Array.isArray(arr)) {
    return arr.slice(start, end);
  }
  return [];
}
function pathjoin(...paths) {
  const path2 = require("path");
  return path2.join(...paths.map((p) => String(p || "")));
}
function pathresolve(base, relative) {
  const path2 = require("path");
  if (relative === void 0) {
    return path2.resolve(String(base || ""));
  }
  return path2.resolve(String(base || ""), String(relative || ""));
}
function pathdirname(filepath) {
  const path2 = require("path");
  return path2.dirname(String(filepath || ""));
}
function pathbasename(filepath) {
  const path2 = require("path");
  return path2.basename(String(filepath || ""));
}
function pathextension(filepath) {
  const path2 = require("path");
  return path2.extname(String(filepath || ""));
}
function pathisabsolute(filepath) {
  const path2 = require("path");
  return path2.isAbsolute(String(filepath || ""));
}
function exit(code = 0) {
  process.exit(parseInt(code, 10) || 0);
}
function arrayreverse(arr) {
  if (!Array.isArray(arr))
    return [];
  return [...arr].reverse();
}
async function asyncMergeSort(arr, compareFn) {
  if (arr.length <= 1)
    return arr;
  const mid = Math.floor(arr.length / 2);
  const left = arr.slice(0, mid);
  const right = arr.slice(mid);
  return await asyncMerge(
    await asyncMergeSort(left, compareFn),
    await asyncMergeSort(right, compareFn),
    compareFn
  );
}
async function asyncMerge(left, right, compareFn) {
  const result = [];
  let l = 0, r = 0;
  while (l < left.length && r < right.length) {
    const cmp = await compareFn(left[l], right[r]);
    if (cmp <= 0) {
      result.push(left[l]);
      l++;
    } else {
      result.push(right[r]);
      r++;
    }
  }
  return result.concat(left.slice(l)).concat(right.slice(r));
}
async function arraysort(arr, compareFn) {
  if (!Array.isArray(arr))
    return [];
  if (typeof compareFn === "function") {
    const temp = [...arr];
    return await asyncMergeSort(temp, compareFn);
  }
  return [...arr].sort();
}
async function arraymap(arr, task) {
  if (!Array.isArray(arr))
    return [];
  if (typeof task !== "function")
    return [...arr];
  const results = [];
  for (let i = 0; i < arr.length; i++) {
    results.push(await task(arr[i], i, arr));
  }
  return results;
}
async function arrayfilter(arr, task) {
  if (!Array.isArray(arr))
    return [];
  if (typeof task !== "function")
    return [...arr];
  const results = [];
  for (let i = 0; i < arr.length; i++) {
    if (await task(arr[i], i, arr)) {
      results.push(arr[i]);
    }
  }
  return results;
}
function arrayshift(arr) {
  if (!Array.isArray(arr))
    return null;
  return arr.shift();
}
async function arrayfind(arr, task) {
  if (!Array.isArray(arr))
    return null;
  if (typeof task !== "function")
    return null;
  for (let i = 0; i < arr.length; i++) {
    if (await task(arr[i], i, arr)) {
      return arr[i];
    }
  }
  return null;
}
function time() {
  return Math.floor(Date.now() / 1e3);
}
function microtime(getAsFloat = true) {
  if (getAsFloat === false) {
    const now = Date.now();
    const sec = Math.floor(now / 1e3);
    const msec = now % 1e3;
    return `${(msec / 1e3).toFixed(8)} ${sec}`;
  }
  return Date.now() / 1e3;
}
function dateyear(ts) {
  const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
  return d.getFullYear();
}
function datemonth(ts) {
  const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
  return d.getMonth() + 1;
}
function dateday(ts) {
  const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
  return d.getDate();
}
function datehour(ts) {
  const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
  return d.getHours();
}
function dateminute(ts) {
  const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
  return d.getMinutes();
}
function datesecond(ts) {
  const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
  return d.getSeconds();
}
function dateweekday(ts, format = "number") {
  const d = ts ? new Date(ts * 1e3) : /* @__PURE__ */ new Date();
  const day = d.getDay();
  if (format === "short" || format === "text") {
    const days = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];
    return days[day];
  }
  return day;
}
function template(source, context = {}) {
  if (typeof source !== "string")
    return "";
  const appRoot = typeof global !== "undefined" && global.__app_root ? global.__app_root : process.cwd();
  const resolvePath = (p) => path.isAbsolute(p) ? p : path.resolve(appRoot, p);
  let html = source;
  try {
    const resolvedSource = resolvePath(source);
    if (fs.existsSync(resolvedSource)) {
      html = fs.readFileSync(resolvedSource, "utf8");
    }
    let includeDepth = 0;
    const maxIncludeDepth = 10;
    while (/@include\s*\(\s*["'](.*?)["']\s*\)/.test(html) && includeDepth < maxIncludeDepth) {
      html = html.replace(/@include\s*\(\s*["'](.*?)["']\s*\)/g, (match2, filepath) => {
        const resolvedInclude = resolvePath(filepath);
        if (!fs.existsSync(resolvedInclude)) {
          throw new Error(`Include file not found: '${filepath}'`);
        }
        return fs.readFileSync(resolvedInclude, "utf8");
      });
      includeDepth++;
    }
    html = html.replace(/@\}\s*(?=@else)/g, "@}");
    if (html.includes("</body>")) {
      const clientScript = `
<script>
function _of_exec_scripts(container) {
    const scripts = container.querySelectorAll('script');
    for (let oldScript of scripts) {
        const newScript = document.createElement('script');
        for (let attr of oldScript.attributes) {
            newScript.setAttribute(attr.name, attr.value);
        }
        newScript.textContent = oldScript.textContent;
        oldScript.parentNode.replaceChild(newScript, oldScript);
    }
}

document.addEventListener('submit', async (e) => {
    const form = e.target;
    const targetSelector = form.getAttribute('of-target');
    if (!targetSelector) return;
    
    e.preventDefault();
    try {
        const response = await fetch(form.action || window.location.href, {
            method: form.method || 'POST',
            body: new URLSearchParams(new FormData(form))
        });
        const htmlResult = await response.text();
        const targetEl = document.querySelector(targetSelector);
        if (targetEl) {
            targetEl.innerHTML = htmlResult;
            _of_exec_scripts(targetEl);
        }
    } catch (err) {
        console.error('OmniFlux Form Submission Error:', err);
    }
});

document.addEventListener('click', async (e) => {
    const link = e.target.closest('a');
    if (!link) return;
    const targetSelector = link.getAttribute('of-target');
    if (!targetSelector) return;
    
    e.preventDefault();
    try {
        const response = await fetch(link.href);
        const htmlResult = await response.text();
        const targetEl = document.querySelector(targetSelector);
        if (targetEl) {
            targetEl.innerHTML = htmlResult;
            _of_exec_scripts(targetEl);
        }
    } catch (err) {
        console.error('OmniFlux Link Navigation Error:', err);
    }
});
</script>
`;
      html = html.replace("</body>", clientScript + "</body>");
    }
    const tokenRegex = /(@if\s*\(.*?\)\s*\{|@else\s*if\s*\(.*?\)\s*\{|@else\s*\{|@for\s*\(.*?\)\s*\{|@\})/g;
    const parts = html.split(tokenRegex);
    let jsCode = "let _out = '';\n";
    jsCode += "with(global.vars || {}) {\n";
    jsCode += "with(context || {}) {\n";
    for (let part of parts) {
      if (!part)
        continue;
      if (part.startsWith("@if")) {
        const cond = part.match(/@if\s*\((.*?)\)\s*\{/)[1];
        jsCode += `if (${cond}) {
`;
      } else if (part.startsWith("@else if")) {
        const cond = part.match(/@else\s*if\s*\((.*?)\)\s*\{/)[1];
        jsCode += `} else if (${cond}) {
`;
      } else if (part.startsWith("@else")) {
        jsCode += `} else {
`;
      } else if (part.startsWith("@for")) {
        const loopMatch = part.match(/@for\s*\(\s*(?:let|var)?\s*(\w+)\s+of\s+(.*?)\)\s*\{/);
        if (loopMatch) {
          const item = loopMatch[1];
          const list = loopMatch[2];
          jsCode += `for (let ${item} of ${list}) {
`;
        }
      } else if (part === "@}") {
        jsCode += `}
`;
      } else {
        let text = part.replace(/\\/g, "\\\\").replace(/`/g, "\\`").replace(/\$/g, "\\$");
        text = text.replace(/\{\{\s*([\s\S]+?)\s*\}\}/g, "${$1}");
        jsCode += `_out += \`${text}\`;
`;
      }
    }
    jsCode += "}\n}\n";
    jsCode += "return _out;";
    const keys = ["context", ...Object.keys(context)];
    const values = [context, ...Object.values(context)];
    return new Function(...keys, jsCode)(...values);
  } catch (e) {
    return `<!-- Template Error: ${e.message} -->`;
  }
}
global.sprintf = sprintf;
global.print = print;
global.printf = printf;
global.fprint = fprint;
global.f_print = fprint;
global.fprintf = fprintf;
global.f_printf = fprintf;
global.input = input;
global.readchar = readchar;
global.read_char = readchar;
global.fileread = fileread;
global.file_read = fileread;
global.filewrite = filewrite;
global.file_write = filewrite;
global.fileexists = fileexists;
global.file_exists = fileexists;
global.fileappend = fileappend;
global.file_append = fileappend;
global.filedelete = filedelete;
global.file_delete = filedelete;
global.filecopy = filecopy;
global.file_copy = filecopy;
global.filerename = filerename;
global.file_rename = filerename;
global.dirlist = dirlist;
global.dir_list = dirlist;
global.dircreate = dircreate;
global.dir_create = dircreate;
global.scriptdir = scriptdir;
global.script_dir = scriptdir;
global.setenv = setenv;
global.set_env = setenv;
global.getenv = getenv;
global.get_env = getenv;
global.sha256 = sha256;
global.getcookie = getcookie;
global.get_cookie = getcookie;
global.setcookie = setcookie;
global.set_cookie = setcookie;
global.filestat = filestat;
global.file_stat = filestat;
global.dbsetfile = dbsetfile;
global.db_set_file = dbsetfile;
global.dbinsert = dbinsert;
global.db_insert = dbinsert;
global.dbselect = dbselect;
global.db_select = dbselect;
global.dbupdate = dbupdate;
global.db_update = dbupdate;
global.dbdelete = dbdelete;
global.db_delete = dbdelete;
global.cacheset = cacheset;
global.cache_set = cacheset;
global.cacheget = cacheget;
global.cache_get = cacheget;
global.len = len;
global.strsplit = strsplit;
global.str_split = strsplit;
global.match = match;
global.arraypush = arraypush;
global.array_push = arraypush;
global.arrayunshift = arrayunshift;
global.array_unshift = arrayunshift;
global.arraypop = arraypop;
global.array_pop = arraypop;
global.arraycontains = arraycontains;
global.array_contains = arraycontains;
global.arrayjoin = arrayjoin;
global.array_join = arrayjoin;
global.arrayslice = arrayslice;
global.array_slice = arrayslice;
global.strtrim = strtrim;
global.str_trim = strtrim;
global.strsub = strsub;
global.str_sub = strsub;
global.strindexof = strindexof;
global.str_index_of = strindexof;
global.strlastindexof = strlastindexof;
global.str_last_index_of = strlastindexof;
global.strrepeat = strrepeat;
global.str_repeat = strrepeat;
global.strreplace = strreplace;
global.str_replace = strreplace;
global.strupper = strupper;
global.str_upper = strupper;
global.strlower = strlower;
global.str_lower = strlower;
global.pathjoin = pathjoin;
global.path_join = pathjoin;
global.pathresolve = pathresolve;
global.path_resolve = pathresolve;
global.pathdirname = pathdirname;
global.path_dir_name = pathdirname;
global.pathbasename = pathbasename;
global.path_base_name = pathbasename;
global.pathextension = pathextension;
global.path_extension = pathextension;
global.pathisabsolute = pathisabsolute;
global.path_is_absolute = pathisabsolute;
global.exit = exit;
global.arrayreverse = arrayreverse;
global.array_reverse = arrayreverse;
global.arraysort = arraysort;
global.array_sort = arraysort;
global.arraymap = arraymap;
global.array_map = arraymap;
global.arrayfilter = arrayfilter;
global.array_filter = arrayfilter;
global.arrayshift = arrayshift;
global.array_shift = arrayshift;
global.arrayfind = arrayfind;
global.array_find = arrayfind;
global.time = time;
global.microtime = microtime;
global.micro_time = microtime;
global.dateyear = dateyear;
global.date_year = dateyear;
global.datemonth = datemonth;
global.date_month = datemonth;
global.dateday = dateday;
global.date_day = dateday;
global.datehour = datehour;
global.date_hour = datehour;
global.dateminute = dateminute;
global.date_minute = dateminute;
global.datesecond = datesecond;
global.date_second = datesecond;
global.dateweekday = dateweekday;
global.date_weekday = dateweekday;
global.template = template;
function formatRuntimeError(err, label = "OmniFlux Runtime Error") {
  console.error("\n\x1B[1m\x1B[31m" + label + ":\x1B[0m");
  console.error("\x1B[33m" + err.message + "\x1B[0m");
  if (err.stack) {
    const stackLines = err.stack.split("\n");
    const lineMatch = stackLines.find(
      (line) => line.includes("/") && !line.includes("node:internal") && !line.includes("node_modules")
    );
    if (lineMatch) {
      const match2 = lineMatch.match(/\(([^)]+):(\d+):(\d+)\)/) || lineMatch.match(/at\s+(.+?):(\d+):(\d+)/);
      if (match2) {
        const filePath = match2[1];
        const lineNum = parseInt(match2[2], 10);
        try {
          const content = fs.readFileSync(filePath, "utf8");
          const fileLines = content.split("\n");
          const indicesToCheck = [lineNum, lineNum - 1, lineNum - 2];
          for (const i of indicesToCheck) {
            if (i >= 0 && i < fileLines.length) {
              const targetLine = fileLines[i];
              const ofLineMatch = targetLine.match(/\/\/\! OF_LINE: (.+?)(?:\r?\n|$)/) || targetLine.match(/\/\/ OF_LINE: (.+?)(?:\r?\n|$)/);
              if (ofLineMatch) {
                console.error("\x1B[1m\x1B[32mAt: " + ofLineMatch[1] + "\x1B[0m");
                console.error();
                return;
              }
            }
          }
        } catch (e) {
        }
        console.error("\x1B[2mAt: " + path.basename(filePath) + ":" + lineNum + "\x1B[0m");
      }
    }
  }
  console.error();
}
process.on("uncaughtException", (err) => {
  const hooks = global.__on_error_hooks || [];
  if (hooks.length > 0) {
    for (const hook of hooks) {
      hook(err).catch(() => {
      });
    }
    return;
  }
  formatRuntimeError(err);
  process.exit(1);
});
process.on("unhandledRejection", (reason) => {
  const hooks = global.__on_error_hooks || [];
  if (hooks.length > 0) {
    for (const hook of hooks) {
      hook(reason).catch(() => {
      });
    }
    return;
  }
  const err = reason instanceof Error ? reason : new Error(String(reason));
  formatRuntimeError(err, "OmniFlux Runtime Error (Unhandled Rejection)");
  process.exit(1);
});
//! OF_LINE: omniflux.of:1
//! OF_LINE: omniflux.of:2
//! OF_LINE: omniflux.of:3
//! OF_LINE: omniflux.of:4
//! OF_LINE: omniflux.of:5
//! OF_LINE: omniflux.of:6
//! OF_LINE: omniflux.of:7
//! OF_LINE: omniflux.of:8
//! OF_LINE: omniflux.of:9
//! OF_LINE: omniflux.of:10
//! OF_LINE: omniflux.of:11
//! OF_LINE: omniflux.of:12
//! OF_LINE: omniflux.of:13
//! OF_LINE: omniflux.of:14
//! OF_LINE: omniflux.of:15
require_bundler();
//! OF_LINE: omniflux.of:16
require_validator();
//! OF_LINE: omniflux.of:17
require_transpiler();
//! OF_LINE: omniflux.of:18
require_symbols();
//! OF_LINE: omniflux.of:19
require_system();
//! OF_LINE: omniflux.of:20
require_network();
//! OF_LINE: omniflux.of:21
//! OF_LINE: omniflux.of:22
//! OF_LINE: omniflux.of:23
//! OF_LINE: omniflux.of:24
//! OF_LINE: omniflux.of:25
async function parse_args(argv) {
  try {
    //! OF_LINE: omniflux.of:26
    let result = {
      //! OF_LINE: omniflux.of:27
      input_file: "",
      //! OF_LINE: omniflux.of:28
      compile_only: false,
      //! OF_LINE: omniflux.of:29
      strict: false,
      //! OF_LINE: omniflux.of:30
      watch: false,
      //! OF_LINE: omniflux.of:31
      setup: false,
      //! OF_LINE: omniflux.of:32
      help: false,
      //! OF_LINE: omniflux.of:33
      verbose: false,
      //! OF_LINE: omniflux.of:34
      force: false,
      //! OF_LINE: omniflux.of:35
      run_args: []
      //! OF_LINE: omniflux.of:36
    };
    //! OF_LINE: omniflux.of:37
    let after_dashdash = false;
    //! OF_LINE: omniflux.of:38
    for (let arg of argv) {
      //! OF_LINE: omniflux.of:39
      if (after_dashdash) {
        //! OF_LINE: omniflux.of:40
        arraypush(result.run_args, arg);
        //! OF_LINE: omniflux.of:41
      } else if (arg == "--") {
        //! OF_LINE: omniflux.of:42
        after_dashdash = true;
        //! OF_LINE: omniflux.of:43
      } else if (arg == "--compile-only" || arg == "--no-run") {
        //! OF_LINE: omniflux.of:44
        result.compile_only = true;
        //! OF_LINE: omniflux.of:45
      } else if (arg == "--strict" || arg == "--no-llm") {
        //! OF_LINE: omniflux.of:46
        result.strict = true;
        //! OF_LINE: omniflux.of:47
      } else if (arg == "--watch" || arg == "-w") {
        //! OF_LINE: omniflux.of:48
        result.watch = true;
        //! OF_LINE: omniflux.of:49
      } else if (arg == "--setup" || arg == "--config") {
        //! OF_LINE: omniflux.of:50
        result.setup = true;
        //! OF_LINE: omniflux.of:51
      } else if (arg == "--help" || arg == "-h") {
        //! OF_LINE: omniflux.of:52
        result.help = true;
        //! OF_LINE: omniflux.of:53
      } else if (arg == "--verbose" || arg == "-v") {
        //! OF_LINE: omniflux.of:54
        result.verbose = true;
        //! OF_LINE: omniflux.of:55
      } else if (arg == "--force" || arg == "-f") {
        //! OF_LINE: omniflux.of:56
        result.force = true;
        //! OF_LINE: omniflux.of:57
      } else if (result.input_file == "") {
        //! OF_LINE: omniflux.of:58
        result.input_file = arg;
        //! OF_LINE: omniflux.of:59
      } else {
        //! OF_LINE: omniflux.of:60
        arraypush(result.run_args, arg);
        //! OF_LINE: omniflux.of:61
      }
      //! OF_LINE: omniflux.of:62
    }
    //! OF_LINE: omniflux.of:63
    if (result.input_file != "") {
      //! OF_LINE: omniflux.of:64
      if (!match(result.input_file, "/\\.of$/i")) {
        //! OF_LINE: omniflux.of:65
        result.input_file = result.input_file + ".of";
        //! OF_LINE: omniflux.of:66
      }
      //! OF_LINE: omniflux.of:67
    }
    //! OF_LINE: omniflux.of:68
    return result;
    //! OF_LINE: omniflux.of:69
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:70
//! OF_LINE: omniflux.of:71
//! OF_LINE: omniflux.of:72
//! OF_LINE: omniflux.of:73
//! OF_LINE: omniflux.of:74
async function print_usage() {
  try {
    //! OF_LINE: omniflux.of:75
    print("Usage: omniflux <file.of> [options] [-- args...]");
    //! OF_LINE: omniflux.of:76
    print("");
    //! OF_LINE: omniflux.of:77
    print("Options:");
    //! OF_LINE: omniflux.of:78
    print("  -h, --help       Show this help information");
    //! OF_LINE: omniflux.of:79
    print("  -v, --verbose    Print detailed AI usage statistics");
    //! OF_LINE: omniflux.of:80
    print("  -f, --force      Force rebuild even if output is up-to-date");
    //! OF_LINE: omniflux.of:81
    print("  --compile-only   Compile only, do not run the output");
    //! OF_LINE: omniflux.of:82
    print("  --strict         Fail if compilation requires AI assistance");
    //! OF_LINE: omniflux.of:83
    print("  --watch          Watch for changes and rebuild automatically");
    //! OF_LINE: omniflux.of:84
    print("  --setup / --config Run the interactive setup wizard to configure the API key");
    //! OF_LINE: omniflux.of:85
    print("  --               Pass remaining arguments to the compiled program");
    //! OF_LINE: omniflux.of:86
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:87
//! OF_LINE: omniflux.of:88
//! OF_LINE: omniflux.of:89
//! OF_LINE: omniflux.of:90
//! OF_LINE: omniflux.of:91
async function config_path() {
  try {
    //! OF_LINE: omniflux.of:92
    let home = getenv("HOME");
    //! OF_LINE: omniflux.of:93
    return home + "/.omnifluxrc";
    //! OF_LINE: omniflux.of:94
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:95
//! OF_LINE: omniflux.of:96
//! OF_LINE: omniflux.of:97
//! OF_LINE: omniflux.of:98
//! OF_LINE: omniflux.of:99
async function config_exists() {
  try {
    //! OF_LINE: omniflux.of:100
    return fileexists(await config_path());
    //! OF_LINE: omniflux.of:101
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:102
//! OF_LINE: omniflux.of:103
//! OF_LINE: omniflux.of:104
//! OF_LINE: omniflux.of:105
//! OF_LINE: omniflux.of:106
async function config_load() {
  try {
    //! OF_LINE: omniflux.of:107
    let raw = fileread(await config_path());
    //! OF_LINE: omniflux.of:108
    return jsonparse(raw);
    //! OF_LINE: omniflux.of:109
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:110
//! OF_LINE: omniflux.of:111
//! OF_LINE: omniflux.of:112
//! OF_LINE: omniflux.of:113
//! OF_LINE: omniflux.of:114
async function config_save(cfg) {
  try {
    //! OF_LINE: omniflux.of:115
    filewrite(await config_path(), jsonencode(cfg, true));
    //! OF_LINE: omniflux.of:116
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:117
//! OF_LINE: omniflux.of:118
//! OF_LINE: omniflux.of:119
//! OF_LINE: omniflux.of:120
//! OF_LINE: omniflux.of:121
async function run_wizard() {
  try {
    //! OF_LINE: omniflux.of:122
    print("=== Welcome to OmniFlux ===");
    //! OF_LINE: omniflux.of:123
    print("To use the AI Compiler features, you need an API key from the developer portal.");
    //! OF_LINE: omniflux.of:124
    print("If you do not have a key, please sign up and get one at:");
    //! OF_LINE: omniflux.of:125
    print("\u{1F449} https://omniflux.helicontech.co.il");
    //! OF_LINE: omniflux.of:126
    print("");
    //! OF_LINE: omniflux.of:127
    //! OF_LINE: omniflux.of:128
    let api_key = await input("Enter your OmniFlux API key: ");
    //! OF_LINE: omniflux.of:129
    if (api_key == "") {
      //! OF_LINE: omniflux.of:130
      print("Error: API key required.");
      //! OF_LINE: omniflux.of:131
      exit(1);
      //! OF_LINE: omniflux.of:132
    }
    //! OF_LINE: omniflux.of:133
    //! OF_LINE: omniflux.of:134
    let cfg = {
      //! OF_LINE: omniflux.of:135
      api_key
      //! OF_LINE: omniflux.of:136
    };
    //! OF_LINE: omniflux.of:137
    await config_save(cfg);
    //! OF_LINE: omniflux.of:138
    print("\n[OmniFlux Setup]: Successfully configured!\n");
    //! OF_LINE: omniflux.of:139
    return cfg;
    //! OF_LINE: omniflux.of:140
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:141
//! OF_LINE: omniflux.of:142
//! OF_LINE: omniflux.of:143
//! OF_LINE: omniflux.of:144
//! OF_LINE: omniflux.of:145
async function send_ai_request(cfg, messages) {
  try {
    //! OF_LINE: omniflux.of:146
    let url = "https://omniflux.helicontech.co.il/v1/chat/completions";
    //! OF_LINE: omniflux.of:147
    let body = { messages, temperature: 0.2 };
    //! OF_LINE: omniflux.of:148
    let headers = { "Authorization": "Bearer " + cfg.api_key };
    //! OF_LINE: omniflux.of:149
    //! OF_LINE: omniflux.of:150
    let t0 = 0;
    //! OF_LINE: omniflux.of:151
    t0 = Date.now();
    //! OF_LINE: omniflux.of:152
    //! OF_LINE: omniflux.of:153
    let response = await networkpost(url, body, headers);
    //! OF_LINE: omniflux.of:154
    //! OF_LINE: omniflux.of:155
    let elapsed = 0;
    //! OF_LINE: omniflux.of:156
    elapsed = (Date.now() - t0) / 1e3;
    //! OF_LINE: omniflux.of:157
    //! OF_LINE: omniflux.of:158
    let err_msg = null;
    //! OF_LINE: omniflux.of:159
    if (!response || !response.choices) {
      //! OF_LINE: omniflux.of:160
      //! OF_LINE: omniflux.of:161
      if (response && response.error) {
        //! OF_LINE: omniflux.of:162
        err_msg = typeof response.error === "object" ? response.error.message : response.error;
        //! OF_LINE: omniflux.of:163
      }
      //! OF_LINE: omniflux.of:164
      //! OF_LINE: omniflux.of:165
      if (err_msg) {
        //! OF_LINE: omniflux.of:166
        print("API Error: %s", err_msg);
        //! OF_LINE: omniflux.of:167
      }
      //! OF_LINE: omniflux.of:168
      return null;
      //! OF_LINE: omniflux.of:169
    }
    //! OF_LINE: omniflux.of:170
    //! OF_LINE: omniflux.of:171
    cfg.last_verbose_data = response;
    //! OF_LINE: omniflux.of:172
    cfg.last_elapsed = elapsed;
    //! OF_LINE: omniflux.of:173
    //! OF_LINE: omniflux.of:174
    return response.choices[0].message.content;
    //! OF_LINE: omniflux.of:175
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:176
//! OF_LINE: omniflux.of:177
//! OF_LINE: omniflux.of:178
//! OF_LINE: omniflux.of:179
//! OF_LINE: omniflux.of:180
async function print_verbose_usage(data, elapsed) {
  try {
    //! OF_LINE: omniflux.of:181
    let usage = data.usage;
    //! OF_LINE: omniflux.of:182
    if (usage == null) {
      //! OF_LINE: omniflux.of:183
      usage = {};
      //! OF_LINE: omniflux.of:184
    }
    //! OF_LINE: omniflux.of:185
    let prompt_tokens = usage.prompt_tokens;
    //! OF_LINE: omniflux.of:186
    if (prompt_tokens == null) {
      //! OF_LINE: omniflux.of:187
      prompt_tokens = 0;
      //! OF_LINE: omniflux.of:188
    }
    //! OF_LINE: omniflux.of:189
    //! OF_LINE: omniflux.of:190
    let completion_tokens = usage.completion_tokens;
    //! OF_LINE: omniflux.of:191
    if (completion_tokens == null) {
      //! OF_LINE: omniflux.of:192
      completion_tokens = 0;
      //! OF_LINE: omniflux.of:193
    }
    //! OF_LINE: omniflux.of:194
    //! OF_LINE: omniflux.of:195
    let total_tokens = usage.total_tokens;
    //! OF_LINE: omniflux.of:196
    if (total_tokens == null) {
      //! OF_LINE: omniflux.of:197
      total_tokens = 0;
      //! OF_LINE: omniflux.of:198
    }
    //! OF_LINE: omniflux.of:199
    //! OF_LINE: omniflux.of:200
    let reasoning_tokens = 0;
    //! OF_LINE: omniflux.of:201
    if (usage.completion_tokens_details != null) {
      //! OF_LINE: omniflux.of:202
      if (usage.completion_tokens_details.reasoning_tokens != null) {
        //! OF_LINE: omniflux.of:203
        reasoning_tokens = usage.completion_tokens_details.reasoning_tokens;
        //! OF_LINE: omniflux.of:204
      }
      //! OF_LINE: omniflux.of:205
    }
    //! OF_LINE: omniflux.of:206
    //! OF_LINE: omniflux.of:207
    let diff_tokens = total_tokens - prompt_tokens - completion_tokens;
    //! OF_LINE: omniflux.of:208
    let thinking_tokens = reasoning_tokens;
    //! OF_LINE: omniflux.of:209
    if (thinking_tokens == 0 && diff_tokens > 0) {
      //! OF_LINE: omniflux.of:210
      thinking_tokens = diff_tokens;
      //! OF_LINE: omniflux.of:211
    }
    //! OF_LINE: omniflux.of:212
    //! OF_LINE: omniflux.of:213
    let resp_model = data.model;
    //! OF_LINE: omniflux.of:214
    if (resp_model == null) {
      //! OF_LINE: omniflux.of:215
      resp_model = "unknown";
      //! OF_LINE: omniflux.of:216
    }
    //! OF_LINE: omniflux.of:217
    //! OF_LINE: omniflux.of:218
    print("\x1B[1;33m\n[Verbose - AI Usage Details]:\x1B[0m");
    //! OF_LINE: omniflux.of:219
    print("  Model Used:          " + resp_model);
    //! OF_LINE: omniflux.of:220
    print("  Input (Prompt):      " + prompt_tokens + " tokens");
    //! OF_LINE: omniflux.of:221
    print("  Output (Completion): " + completion_tokens + " tokens");
    //! OF_LINE: omniflux.of:222
    if (thinking_tokens != 0) {
      //! OF_LINE: omniflux.of:223
      print("  Thinking (Reasoning):" + thinking_tokens + " tokens");
      //! OF_LINE: omniflux.of:224
    }
    //! OF_LINE: omniflux.of:225
    print("  Total Tokens:        " + total_tokens + " tokens");
    //! OF_LINE: omniflux.of:226
    print(sprintf("  Request Duration:    %.2fs", elapsed));
    //! OF_LINE: omniflux.of:227
    print("\x1B[1;33m----------------------------------------\n\x1B[0m");
    //! OF_LINE: omniflux.of:228
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:229
//! OF_LINE: omniflux.of:230
//! OF_LINE: omniflux.of:231
//! OF_LINE: omniflux.of:232
//! OF_LINE: omniflux.of:233
async function format_chat_response(txt) {
  try {
    //! OF_LINE: omniflux.of:234
    if (!txt) {
      //! OF_LINE: omniflux.of:235
      return "";
      //! OF_LINE: omniflux.of:236
    }
    //! OF_LINE: omniflux.of:237
    //! OF_LINE: omniflux.of:238
    txt = strreplace(txt, "/```[a-zA-Z0-9]*\\n([\\s\\S]*?)\\n```/gm", "\n$1\n");
    //! OF_LINE: omniflux.of:239
    //! OF_LINE: omniflux.of:240
    txt = strreplace(txt, "/\\*\\*([^*]+)\\*\\*/g", "$1");
    //! OF_LINE: omniflux.of:241
    //! OF_LINE: omniflux.of:242
    txt = strreplace(txt, "/^#{1,6}\\s+(.+)$/gm", "$1");
    //! OF_LINE: omniflux.of:243
    return txt;
    //! OF_LINE: omniflux.of:244
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:245
//! OF_LINE: omniflux.of:246
//! OF_LINE: omniflux.of:247
//! OF_LINE: omniflux.of:248
//! OF_LINE: omniflux.of:249
async function read_system_prompt() {
  try {
    //! OF_LINE: omniflux.of:250
    //! OF_LINE: omniflux.of:251
    return "";
    //! OF_LINE: omniflux.of:252
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:253
//! OF_LINE: omniflux.of:254
//! OF_LINE: omniflux.of:255
//! OF_LINE: omniflux.of:256
//! OF_LINE: omniflux.of:257
async function detect_missing_includes(source_code) {
  try {
    //! OF_LINE: omniflux.of:258
    let missing = [];
    //! OF_LINE: omniflux.of:259
    //! OF_LINE: omniflux.of:260
    //! OF_LINE: omniflux.of:261
    if (match(source_code, "/\\b(encrypt|decrypt|base64_encode|base64encode|base64_decode|base64decode)\\b/")) {
      //! OF_LINE: omniflux.of:262
      if (!match(source_code, "/stdlib\\/encrypt\\.of/")) {
        //! OF_LINE: omniflux.of:263
        arraypush(missing, "stdlib/encrypt.of");
        //! OF_LINE: omniflux.of:264
      }
      //! OF_LINE: omniflux.of:265
    }
    //! OF_LINE: omniflux.of:266
    //! OF_LINE: omniflux.of:267
    //! OF_LINE: omniflux.of:268
    if (match(source_code, "/\\b(networkget|networkpost)\\b/")) {
      //! OF_LINE: omniflux.of:269
      if (!match(source_code, "/stdlib\\/network\\.of/")) {
        //! OF_LINE: omniflux.of:270
        arraypush(missing, "stdlib/network.of");
        //! OF_LINE: omniflux.of:271
      }
      //! OF_LINE: omniflux.of:272
    }
    //! OF_LINE: omniflux.of:273
    //! OF_LINE: omniflux.of:274
    //! OF_LINE: omniflux.of:275
    if (match(source_code, "/\\b(datetime_now|datetime_format)\\b/")) {
      //! OF_LINE: omniflux.of:276
      if (!match(source_code, "/stdlib\\/datetime\\.of/")) {
        //! OF_LINE: omniflux.of:277
        arraypush(missing, "stdlib/datetime.of");
        //! OF_LINE: omniflux.of:278
      }
      //! OF_LINE: omniflux.of:279
    }
    //! OF_LINE: omniflux.of:280
    //! OF_LINE: omniflux.of:281
    return missing;
    //! OF_LINE: omniflux.of:282
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:283
//! OF_LINE: omniflux.of:284
//! OF_LINE: omniflux.of:285
//! OF_LINE: omniflux.of:286
//! OF_LINE: omniflux.of:287
async function ai_compile_file(cfg, src_path, target_js, runtime_error = null) {
  try {
    //! OF_LINE: omniflux.of:288
    let source_code = fileread(src_path);
    //! OF_LINE: omniflux.of:289
    let missing_libs = await detect_missing_includes(source_code);
    //! OF_LINE: omniflux.of:290
    //! OF_LINE: omniflux.of:291
    source_code = await strip_comments(source_code);
    //! OF_LINE: omniflux.of:292
    //! OF_LINE: omniflux.of:293
    //! OF_LINE: omniflux.of:294
    source_code = strreplace(source_code, `/\\binclude\\s+["']?([^\\."'\\s;]+(?:\\.[a-z]+)?)["']?/g`, "require('./$1')");
    //! OF_LINE: omniflux.of:295
    //! OF_LINE: omniflux.of:296
    print("Falling back to AI Compiler...");
    //! OF_LINE: omniflux.of:297
    //! OF_LINE: omniflux.of:298
    let prompt_addition = "";
    //! OF_LINE: omniflux.of:299
    if (len(missing_libs) > 0) {
      //! OF_LINE: omniflux.of:300
      let lib_list = arrayjoin(missing_libs, ", ");
      //! OF_LINE: omniflux.of:301
      prompt_addition = "\n\nNOTE: The code uses functions from the following standard libraries but is missing their include statements: " + lib_list + '.\nYou MUST prepend the appropriate include directive(s) (e.g. include "' + missing_libs[0] + '") to the top of the OmniFlux source code, and you MUST return BOTH the [OmniFlux Source] block (containing the healed OmniFlux code) and the [OmniFlux Ready] block (containing the compiled JavaScript code).';
      //! OF_LINE: omniflux.of:302
    }
    //! OF_LINE: omniflux.of:303
    if (runtime_error != null && runtime_error != "") {
      //! OF_LINE: omniflux.of:304
      prompt_addition = prompt_addition + "\n\nCRITICAL: A compiler/runtime error occurred when executing this code:\n" + runtime_error + "\nPlease fix this error in the OmniFlux code (such as adding missing include statements, fixing variables, typos, missing 'ref' keywords, or syntax errors). You MUST return BOTH the [OmniFlux Source] block (containing the healed OmniFlux code) and the [OmniFlux Ready] block (containing the compiled JavaScript code).";
      //! OF_LINE: omniflux.of:305
    }
    //! OF_LINE: omniflux.of:306
    //! OF_LINE: omniflux.of:307
    let messages = [
      //! OF_LINE: omniflux.of:308
      { role: "user", content: "Please compile this code:" + prompt_addition + "\n\n" + source_code }
      //! OF_LINE: omniflux.of:309
    ];
    //! OF_LINE: omniflux.of:310
    //! OF_LINE: omniflux.of:311
    let done = false;
    //! OF_LINE: omniflux.of:312
    while (!done) {
      //! OF_LINE: omniflux.of:313
      print("Thinking...");
      //! OF_LINE: omniflux.of:314
      let ai_response = await send_ai_request(cfg, messages);
      //! OF_LINE: omniflux.of:315
      if (ai_response == null) {
        //! OF_LINE: omniflux.of:316
        print("Error: AI request failed.");
        //! OF_LINE: omniflux.of:317
        return false;
        //! OF_LINE: omniflux.of:318
      }
      //! OF_LINE: omniflux.of:319
      //! OF_LINE: omniflux.of:320
      if (match(ai_response, "/\\[OmniFlux Question\\]:/")) {
        //! OF_LINE: omniflux.of:321
        print("%s", await format_chat_response(ai_response));
        //! OF_LINE: omniflux.of:322
        if (cfg.verbose) {
          //! OF_LINE: omniflux.of:323
          await print_verbose_usage(cfg.last_verbose_data, cfg.last_elapsed);
          //! OF_LINE: omniflux.of:324
        }
        //! OF_LINE: omniflux.of:325
        let user_answer = await input("Your response > ");
        //! OF_LINE: omniflux.of:326
        arraypush(messages, { role: "assistant", content: ai_response });
        //! OF_LINE: omniflux.of:327
        arraypush(messages, { role: "user", content: user_answer });
        //! OF_LINE: omniflux.of:328
      } else {
        //! OF_LINE: omniflux.of:329
        //! OF_LINE: omniflux.of:330
        let updated_source = null;
        //! OF_LINE: omniflux.of:331
        //! OF_LINE: omniflux.of:332
        const source_marker = "[OmniFlux Source]";
        //! OF_LINE: omniflux.of:333
        const ready_marker = "[OmniFlux Ready]";
        //! OF_LINE: omniflux.of:334
        const s_idx = ai_response.indexOf(source_marker);
        //! OF_LINE: omniflux.of:335
        const r_idx = ai_response.indexOf(ready_marker);
        //! OF_LINE: omniflux.of:336
        if (s_idx !== -1 && r_idx !== -1 && s_idx < r_idx) {
          //! OF_LINE: omniflux.of:337
          let content = ai_response.substring(s_idx + source_marker.length, r_idx).trim();
          //! OF_LINE: omniflux.of:338
          content = content.replace(/^```(?:omniflux|of|[a-zA-Z0-9]*)\s*\n/i, "");
          //! OF_LINE: omniflux.of:340
          content = content.replace(/\n```\s*$/, "");
          //! OF_LINE: omniflux.of:341
          updated_source = content;
          //! OF_LINE: omniflux.of:342
        }
        //! OF_LINE: omniflux.of:343
        //! OF_LINE: omniflux.of:344
        //! OF_LINE: omniflux.of:345
        if (updated_source != null && updated_source != "") {
          //! OF_LINE: omniflux.of:346
          filewrite(src_path, updated_source);
          //! OF_LINE: omniflux.of:347
          print("\x1B[1;32m[Source file '%s' automatically updated/healed by AI!]\x1B[0m", pathbasename(src_path));
          //! OF_LINE: omniflux.of:348
          if (cfg != null) {
            //! OF_LINE: omniflux.of:349
            cfg.was_healed = true;
            //! OF_LINE: omniflux.of:350
          }
          //! OF_LINE: omniflux.of:351
        }
        //! OF_LINE: omniflux.of:352
        //! OF_LINE: omniflux.of:353
        //! OF_LINE: omniflux.of:354
        let js_code = ai_response;
        //! OF_LINE: omniflux.of:355
        //! OF_LINE: omniflux.of:356
        {
          //! OF_LINE: omniflux.of:357
          const ready_marker2 = "[OmniFlux Ready]";
          //! OF_LINE: omniflux.of:358
          const r_idx2 = ai_response.indexOf(ready_marker2);
          //! OF_LINE: omniflux.of:359
          if (r_idx2 !== -1) {
            //! OF_LINE: omniflux.of:360
            js_code = ai_response.substring(r_idx2 + ready_marker2.length).trim();
            //! OF_LINE: omniflux.of:361
          }
          //! OF_LINE: omniflux.of:362
        }
        //! OF_LINE: omniflux.of:363
        //! OF_LINE: omniflux.of:364
        //! OF_LINE: omniflux.of:365
        js_code = strreplace(js_code, "/^```(?:javascript|js)?\\s*\\n/i", "");
        //! OF_LINE: omniflux.of:366
        js_code = strreplace(js_code, "/\\n```\\s*$/", "");
        //! OF_LINE: omniflux.of:367
        //! OF_LINE: omniflux.of:368
        let dir = pathdirname(target_js);
        //! OF_LINE: omniflux.of:369
        dircreate(dir);
        //! OF_LINE: omniflux.of:370
        filewrite(target_js, js_code);
        //! OF_LINE: omniflux.of:371
        done = true;
        //! OF_LINE: omniflux.of:372
        if (cfg.verbose) {
          //! OF_LINE: omniflux.of:373
          await print_verbose_usage(cfg.last_verbose_data, cfg.last_elapsed);
          //! OF_LINE: omniflux.of:374
        }
        //! OF_LINE: omniflux.of:375
      }
      //! OF_LINE: omniflux.of:376
    }
    //! OF_LINE: omniflux.of:377
    return true;
    //! OF_LINE: omniflux.of:378
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:379
//! OF_LINE: omniflux.of:380
//! OF_LINE: omniflux.of:381
//! OF_LINE: omniflux.of:382
//! OF_LINE: omniflux.of:383
async function interactive_chat(cfg) {
  try {
    //! OF_LINE: omniflux.of:384
    let messages = [];
    //! OF_LINE: omniflux.of:385
    //! OF_LINE: omniflux.of:386
    print("=== OmniFlux Interactive Compiler ===");
    //! OF_LINE: omniflux.of:387
    print("Type a file name (e.g., script.of) to compile it,");
    //! OF_LINE: omniflux.of:388
    print("or just type a message to chat with the compiler.");
    //! OF_LINE: omniflux.of:389
    print("Type 'exit' or 'quit' to exit.");
    //! OF_LINE: omniflux.of:390
    print("");
    //! OF_LINE: omniflux.of:391
    //! OF_LINE: omniflux.of:392
    while (true) {
      //! OF_LINE: omniflux.of:393
      let user_input = await input("OmniFlux > ");
      //! OF_LINE: omniflux.of:394
      if (user_input == null || user_input == "") {
        //! OF_LINE: omniflux.of:395
        continue;
        //! OF_LINE: omniflux.of:396
      }
      //! OF_LINE: omniflux.of:397
      if (user_input == "exit" || user_input == "quit") {
        //! OF_LINE: omniflux.of:398
        break;
        //! OF_LINE: omniflux.of:399
      }
      //! OF_LINE: omniflux.of:400
      if (user_input == "run") {
        //! OF_LINE: omniflux.of:401
        if (fileexists("sandbox.of")) {
          //! OF_LINE: omniflux.of:402
          let br = await build_once("sandbox.of", false, false, cfg);
          //! OF_LINE: omniflux.of:403
          if (br.ok) {
            //! OF_LINE: omniflux.of:404
            let chat_out = await system(br.output);
            //! OF_LINE: omniflux.of:405
            printf("%s", chat_out);
            //! OF_LINE: omniflux.of:406
          }
          //! OF_LINE: omniflux.of:407
        } else {
          //! OF_LINE: omniflux.of:408
          print("Error: No code found in sandbox.of. Please ask the compiler to generate some code first!");
          //! OF_LINE: omniflux.of:409
          print("");
          //! OF_LINE: omniflux.of:410
        }
        //! OF_LINE: omniflux.of:411
        continue;
        //! OF_LINE: omniflux.of:412
      }
      //! OF_LINE: omniflux.of:413
      if (fileexists(user_input)) {
        //! OF_LINE: omniflux.of:414
        let fbr = await build_once(user_input, false, false, cfg);
        //! OF_LINE: omniflux.of:415
        if (fbr.ok) {
          //! OF_LINE: omniflux.of:416
          let fout = await system(fbr.output);
          //! OF_LINE: omniflux.of:417
          printf("%s", fout);
          //! OF_LINE: omniflux.of:418
        }
        //! OF_LINE: omniflux.of:419
        continue;
        //! OF_LINE: omniflux.of:420
      }
      //! OF_LINE: omniflux.of:421
      //! OF_LINE: omniflux.of:422
      arraypush(messages, { role: "user", content: user_input });
      //! OF_LINE: omniflux.of:423
      print("Thinking...");
      //! OF_LINE: omniflux.of:424
      let response = await send_ai_request(cfg, messages);
      //! OF_LINE: omniflux.of:425
      if (response == null) {
        //! OF_LINE: omniflux.of:426
        print("Error: AI request failed.");
        //! OF_LINE: omniflux.of:427
        continue;
        //! OF_LINE: omniflux.of:428
      }
      //! OF_LINE: omniflux.of:429
      //! OF_LINE: omniflux.of:430
      print("");
      //! OF_LINE: omniflux.of:431
      print("%s", await format_chat_response(response));
      //! OF_LINE: omniflux.of:432
      print("");
      //! OF_LINE: omniflux.of:433
      //! OF_LINE: omniflux.of:434
      //! OF_LINE: omniflux.of:435
      let code_m = match(response, "/```(?:omniflux|of|[a-zA-Z0-9]*)\\s*\\n([\\s\\S]*?)\\n```/");
      //! OF_LINE: omniflux.of:436
      let code_saved = false;
      //! OF_LINE: omniflux.of:437
      if (code_m) {
        //! OF_LINE: omniflux.of:438
        filewrite("sandbox.of", code_m[0]);
        //! OF_LINE: omniflux.of:439
        code_saved = true;
        //! OF_LINE: omniflux.of:440
      }
      //! OF_LINE: omniflux.of:441
      if (code_saved) {
        //! OF_LINE: omniflux.of:442
        print("\x1B[1;32m[Code saved to sandbox.of. Type 'run' to execute it!]\x1B[0m");
        //! OF_LINE: omniflux.of:443
        print("");
        //! OF_LINE: omniflux.of:444
      }
      //! OF_LINE: omniflux.of:445
      //! OF_LINE: omniflux.of:446
      if (cfg.verbose) {
        //! OF_LINE: omniflux.of:447
        await print_verbose_usage(cfg.last_verbose_data, cfg.last_elapsed);
        //! OF_LINE: omniflux.of:448
      }
      //! OF_LINE: omniflux.of:449
      //! OF_LINE: omniflux.of:450
      arraypush(messages, { role: "assistant", content: response });
      //! OF_LINE: omniflux.of:451
    }
    //! OF_LINE: omniflux.of:452
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:453
//! OF_LINE: omniflux.of:454
//! OF_LINE: omniflux.of:455
//! OF_LINE: omniflux.of:456
//! OF_LINE: omniflux.of:457
async function read_runtime() {
  try {
    //! OF_LINE: omniflux.of:458
    let parent_dir = pathdirname(scriptdir());
    //! OF_LINE: omniflux.of:459
    let candidates = [
      //! OF_LINE: omniflux.of:460
      pathjoin(scriptdir(), "runtime.js"),
      //! OF_LINE: omniflux.of:461
      pathjoin(scriptdir(), "OmniFlux/runtime.js"),
      //! OF_LINE: omniflux.of:462
      pathjoin(parent_dir, "runtime.js"),
      //! OF_LINE: omniflux.of:463
      pathjoin(parent_dir, "OmniFlux/runtime.js"),
      //! OF_LINE: omniflux.of:464
      "/usr/local/share/omniflux/runtime.js",
      //! OF_LINE: omniflux.of:465
      "/usr/local/share/omniflux/OmniFlux/runtime.js",
      //! OF_LINE: omniflux.of:466
      "/usr/share/omniflux/runtime.js",
      //! OF_LINE: omniflux.of:467
      "/usr/share/omniflux/OmniFlux/runtime.js"
      //! OF_LINE: omniflux.of:468
    ];
    //! OF_LINE: omniflux.of:469
    for (let path2 of candidates) {
      //! OF_LINE: omniflux.of:470
      if (fileexists(path2)) {
        //! OF_LINE: omniflux.of:471
        return fileread(path2);
        //! OF_LINE: omniflux.of:472
      }
      //! OF_LINE: omniflux.of:473
    }
    //! OF_LINE: omniflux.of:474
    print("Error: Cannot find OmniFlux runtime.js");
    //! OF_LINE: omniflux.of:475
    exit(1);
    //! OF_LINE: omniflux.of:476
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:477
//! OF_LINE: omniflux.of:478
//! OF_LINE: omniflux.of:479
//! OF_LINE: omniflux.of:480
//! OF_LINE: omniflux.of:481
async function compile_file(abs_path, runtime_js, global_user_tasks) {
  try {
    //! OF_LINE: omniflux.of:482
    let raw = fileread(abs_path);
    //! OF_LINE: omniflux.of:483
    let filename = pathbasename(abs_path);
    //! OF_LINE: omniflux.of:484
    //! OF_LINE: omniflux.of:485
    //! OF_LINE: omniflux.of:486
    let content = await strip_comments(raw);
    //! OF_LINE: omniflux.of:487
    //! OF_LINE: omniflux.of:488
    //! OF_LINE: omniflux.of:489
    let vresult = await validate(content, filename);
    //! OF_LINE: omniflux.of:490
    if (!vresult.ok) {
      //! OF_LINE: omniflux.of:491
      print("Compilation Error in %s:", filename);
      //! OF_LINE: omniflux.of:492
      print("%s", vresult.error);
      //! OF_LINE: omniflux.of:493
      return null;
      //! OF_LINE: omniflux.of:494
    }
    //! OF_LINE: omniflux.of:495
    //! OF_LINE: omniflux.of:496
    //! OF_LINE: omniflux.of:497
    let file_tasks = await prescan_tasks(content);
    //! OF_LINE: omniflux.of:498
    Object.assign(global_user_tasks, file_tasks);
    //! OF_LINE: omniflux.of:499
    //! OF_LINE: omniflux.of:500
    //! OF_LINE: omniflux.of:501
    var user_task_keys = Object.keys(global_user_tasks);
    //! OF_LINE: omniflux.of:502
    let tresult = await transpile(content, abs_path, global_user_tasks, runtime_js);
    //! OF_LINE: omniflux.of:503
    if (!tresult.ok) {
      //! OF_LINE: omniflux.of:504
      print("Transpile Error in %s:", filename);
      //! OF_LINE: omniflux.of:505
      print("%s", tresult.error);
      //! OF_LINE: omniflux.of:506
      return null;
      //! OF_LINE: omniflux.of:507
    }
    //! OF_LINE: omniflux.of:508
    //! OF_LINE: omniflux.of:509
    return tresult.js;
    //! OF_LINE: omniflux.of:510
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:511
//! OF_LINE: omniflux.of:512
//! OF_LINE: omniflux.of:513
//! OF_LINE: omniflux.of:514
//! OF_LINE: omniflux.of:515
//! OF_LINE: omniflux.of:516
async function build_once(input_file, compile_only, strict, cfg, force) {
  try {
    //! OF_LINE: omniflux.of:517
    let start_time = 0;
    //! OF_LINE: omniflux.of:518
    start_time = Date.now();
    //! OF_LINE: omniflux.of:519
    let abs_input = pathresolve(input_file);
    //! OF_LINE: omniflux.of:520
    //! OF_LINE: omniflux.of:521
    if (!fileexists(abs_input)) {
      //! OF_LINE: omniflux.of:522
      print("Error: File '%s' does not exist.", input_file);
      //! OF_LINE: omniflux.of:523
      return { ok: false };
      //! OF_LINE: omniflux.of:524
    }
    //! OF_LINE: omniflux.of:525
    //! OF_LINE: omniflux.of:526
    //! OF_LINE: omniflux.of:527
    let deps = await get_all_dependencies(abs_input, null);
    //! OF_LINE: omniflux.of:528
    //! OF_LINE: omniflux.of:529
    let output_file = await get_output_path(abs_input);
    //! OF_LINE: omniflux.of:530
    //! OF_LINE: omniflux.of:531
    //! OF_LINE: omniflux.of:532
    let force_build = false;
    //! OF_LINE: omniflux.of:533
    if (force != null) {
      //! OF_LINE: omniflux.of:534
      force_build = force;
      //! OF_LINE: omniflux.of:535
    }
    //! OF_LINE: omniflux.of:536
    //! OF_LINE: omniflux.of:537
    let can_skip = false;
    //! OF_LINE: omniflux.of:538
    if (!force_build) {
      //! OF_LINE: omniflux.of:539
      if (fileexists(output_file)) {
        //! OF_LINE: omniflux.of:540
        let out_stat = filestat(output_file);
        //! OF_LINE: omniflux.of:541
        if (out_stat) {
          //! OF_LINE: omniflux.of:542
          can_skip = true;
          //! OF_LINE: omniflux.of:543
          for (let dep_path of deps) {
            //! OF_LINE: omniflux.of:544
            let dep_stat = filestat(dep_path);
            //! OF_LINE: omniflux.of:545
            if (dep_stat) {
              //! OF_LINE: omniflux.of:546
              if (dep_stat.modifiedat > out_stat.modifiedat) {
                //! OF_LINE: omniflux.of:547
                can_skip = false;
                //! OF_LINE: omniflux.of:548
                break;
                //! OF_LINE: omniflux.of:549
              }
              //! OF_LINE: omniflux.of:550
            }
            //! OF_LINE: omniflux.of:551
          }
          //! OF_LINE: omniflux.of:552
        }
        //! OF_LINE: omniflux.of:553
      }
      //! OF_LINE: omniflux.of:554
    }
    //! OF_LINE: omniflux.of:555
    //! OF_LINE: omniflux.of:556
    if (can_skip) {
      //! OF_LINE: omniflux.of:557
      print("Build up-to-date: %s", output_file);
      //! OF_LINE: omniflux.of:558
      return { ok: true, output: output_file };
      //! OF_LINE: omniflux.of:559
    }
    //! OF_LINE: omniflux.of:560
    //! OF_LINE: omniflux.of:561
    //! OF_LINE: omniflux.of:562
    let runtime_js = await read_runtime();
    //! OF_LINE: omniflux.of:563
    //! OF_LINE: omniflux.of:564
    let rebuild_needed = true;
    //! OF_LINE: omniflux.of:565
    let compiled_map = {};
    //! OF_LINE: omniflux.of:566
    let ok = true;
    //! OF_LINE: omniflux.of:567
    let heal_attempts = 0;
    //! OF_LINE: omniflux.of:568
    let max_heal_attempts = 2;
    //! OF_LINE: omniflux.of:569
    //! OF_LINE: omniflux.of:570
    while (rebuild_needed) {
      //! OF_LINE: omniflux.of:571
      rebuild_needed = false;
      //! OF_LINE: omniflux.of:572
      if (cfg != null) {
        //! OF_LINE: omniflux.of:573
        cfg.was_healed = false;
        //! OF_LINE: omniflux.of:574
      }
      //! OF_LINE: omniflux.of:575
      //! OF_LINE: omniflux.of:576
      compiled_map = {};
      //! OF_LINE: omniflux.of:577
      let global_user_tasks = {};
      //! OF_LINE: omniflux.of:578
      ok = true;
      //! OF_LINE: omniflux.of:579
      //! OF_LINE: omniflux.of:580
      //! OF_LINE: omniflux.of:581
      for (let dep_path of deps) {
        //! OF_LINE: omniflux.of:582
        let raw = fileread(dep_path);
        //! OF_LINE: omniflux.of:583
        let content = await strip_comments(raw);
        //! OF_LINE: omniflux.of:584
        let tasks = await prescan_tasks(content);
        //! OF_LINE: omniflux.of:585
        Object.assign(global_user_tasks, tasks);
        //! OF_LINE: omniflux.of:586
      }
      //! OF_LINE: omniflux.of:587
      //! OF_LINE: omniflux.of:588
      //! OF_LINE: omniflux.of:589
      for (let dep_path of deps) {
        //! OF_LINE: omniflux.of:590
        let js = await compile_file(dep_path, runtime_js, global_user_tasks);
        //! OF_LINE: omniflux.of:591
        if (!js) {
          //! OF_LINE: omniflux.of:592
          //! OF_LINE: omniflux.of:593
          if (strict || !cfg) {
            //! OF_LINE: omniflux.of:594
            print("\x1B[1;31mStrict Mode Error: Local compilation failed on %s. AI compilation is disabled.\x1B[0m", dep_path);
            //! OF_LINE: omniflux.of:595
            ok = false;
            //! OF_LINE: omniflux.of:596
            break;
            //! OF_LINE: omniflux.of:597
          }
          //! OF_LINE: omniflux.of:598
          let cache_path = await get_cache_path(dep_path);
          //! OF_LINE: omniflux.of:599
          let ai_ok = await ai_compile_file(cfg, dep_path, cache_path);
          //! OF_LINE: omniflux.of:600
          if (!ai_ok) {
            //! OF_LINE: omniflux.of:601
            ok = false;
            //! OF_LINE: omniflux.of:602
            break;
            //! OF_LINE: omniflux.of:603
          }
          //! OF_LINE: omniflux.of:604
          //! OF_LINE: omniflux.of:605
          js = fileread(cache_path);
          //! OF_LINE: omniflux.of:606
        }
        //! OF_LINE: omniflux.of:607
        compiled_map[dep_path] = js;
        //! OF_LINE: omniflux.of:608
        print("Compiled: %s", dep_path);
        //! OF_LINE: omniflux.of:609
      }
      //! OF_LINE: omniflux.of:610
      //! OF_LINE: omniflux.of:611
      if (!ok) {
        //! OF_LINE: omniflux.of:612
        return { ok: false };
        //! OF_LINE: omniflux.of:613
      }
      //! OF_LINE: omniflux.of:614
      //! OF_LINE: omniflux.of:615
      if (cfg != null && cfg.was_healed) {
        //! OF_LINE: omniflux.of:616
        heal_attempts = heal_attempts + 1;
        //! OF_LINE: omniflux.of:617
        if (heal_attempts > max_heal_attempts) {
          //! OF_LINE: omniflux.of:618
          print("\x1B[1;31mError: Exceeded maximum AI healing attempts (%d). Aborting compilation to prevent infinite loops.\x1B[0m", max_heal_attempts);
          //! OF_LINE: omniflux.of:619
          return { ok: false };
          //! OF_LINE: omniflux.of:620
        }
        //! OF_LINE: omniflux.of:621
        rebuild_needed = true;
        //! OF_LINE: omniflux.of:622
        //! OF_LINE: omniflux.of:623
        deps = await get_all_dependencies(abs_input, null);
        //! OF_LINE: omniflux.of:624
        continue;
        //! OF_LINE: omniflux.of:625
      }
      //! OF_LINE: omniflux.of:626
      //! OF_LINE: omniflux.of:627
      //! OF_LINE: omniflux.of:628
      bresult = await build(abs_input, output_file, compiled_map);
      //! OF_LINE: omniflux.of:629
      //! OF_LINE: omniflux.of:630
      if (!bresult.ok) {
        //! OF_LINE: omniflux.of:631
        if (strict || !cfg) {
          //! OF_LINE: omniflux.of:632
          return bresult;
          //! OF_LINE: omniflux.of:633
        }
        //! OF_LINE: omniflux.of:634
        //! OF_LINE: omniflux.of:635
        let failed_file = null;
        //! OF_LINE: omniflux.of:636
        let err_msg = bresult.error;
        //! OF_LINE: omniflux.of:637
        //! OF_LINE: omniflux.of:638
        const m = /\.omniflux_cache\/([^\s:]+)\.js/.exec(err_msg);
        //! OF_LINE: omniflux.of:639
        if (m) {
          //! OF_LINE: omniflux.of:640
          const rel = m[1] + ".of";
          //! OF_LINE: omniflux.of:641
          for (const abs_path of Object.keys(compiled_map)) {
            //! OF_LINE: omniflux.of:643
            if (abs_path.endsWith(rel)) {
              //! OF_LINE: omniflux.of:644
              failed_file = abs_path;
              //! OF_LINE: omniflux.of:645
              break;
              //! OF_LINE: omniflux.of:646
            }
            //! OF_LINE: omniflux.of:647
          }
          //! OF_LINE: omniflux.of:648
        }
        //! OF_LINE: omniflux.of:649
        //! OF_LINE: omniflux.of:650
        if (!failed_file) {
          //! OF_LINE: omniflux.of:651
          //! OF_LINE: omniflux.of:652
          failed_file = abs_input;
          //! OF_LINE: omniflux.of:653
        }
        //! OF_LINE: omniflux.of:654
        //! OF_LINE: omniflux.of:655
        print("\x1B[1;33mBundling failed due to JS syntax error. Falling back to AI Compiler for %s...\x1B[0m", failed_file);
        //! OF_LINE: omniflux.of:656
        let cache_path = await get_cache_path(failed_file);
        //! OF_LINE: omniflux.of:657
        if (fileexists(cache_path)) {
          //! OF_LINE: omniflux.of:658
          filedelete(cache_path);
          //! OF_LINE: omniflux.of:659
        }
        //! OF_LINE: omniflux.of:660
        let ai_ok = await ai_compile_file(cfg, failed_file, cache_path, bresult.error);
        //! OF_LINE: omniflux.of:661
        if (!ai_ok) {
          //! OF_LINE: omniflux.of:662
          return bresult;
          //! OF_LINE: omniflux.of:663
        }
        //! OF_LINE: omniflux.of:664
        //! OF_LINE: omniflux.of:665
        if (cfg != null && cfg.was_healed) {
          //! OF_LINE: omniflux.of:666
          heal_attempts = heal_attempts + 1;
          //! OF_LINE: omniflux.of:667
          if (heal_attempts > max_heal_attempts) {
            //! OF_LINE: omniflux.of:668
            print("\x1B[1;31mError: Exceeded maximum AI healing attempts (%d). Aborting compilation to prevent infinite loops.\x1B[0m", max_heal_attempts);
            //! OF_LINE: omniflux.of:669
            return { ok: false };
            //! OF_LINE: omniflux.of:670
          }
          //! OF_LINE: omniflux.of:671
          rebuild_needed = true;
          //! OF_LINE: omniflux.of:672
          deps = await get_all_dependencies(abs_input, null);
          //! OF_LINE: omniflux.of:673
          continue;
          //! OF_LINE: omniflux.of:674
        }
        //! OF_LINE: omniflux.of:675
        //! OF_LINE: omniflux.of:676
        //! OF_LINE: omniflux.of:677
        compiled_map[failed_file] = fileread(cache_path);
        //! OF_LINE: omniflux.of:678
        //! OF_LINE: omniflux.of:679
        bresult = await build(abs_input, output_file, compiled_map);
        //! OF_LINE: omniflux.of:680
      }
      //! OF_LINE: omniflux.of:681
    }
    //! OF_LINE: omniflux.of:682
    //! OF_LINE: omniflux.of:683
    if (bresult.ok) {
      //! OF_LINE: omniflux.of:684
      let end_time = 0;
      //! OF_LINE: omniflux.of:685
      end_time = Date.now();
      //! OF_LINE: omniflux.of:686
      let duration = end_time - start_time;
      //! OF_LINE: omniflux.of:687
      print("Build Successful -> %s (%dms)", output_file, duration);
      //! OF_LINE: omniflux.of:688
    }
    //! OF_LINE: omniflux.of:689
    return bresult;
    //! OF_LINE: omniflux.of:690
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:691
//! OF_LINE: omniflux.of:692
//! OF_LINE: omniflux.of:693
//! OF_LINE: omniflux.of:694
//! OF_LINE: omniflux.of:695
async function run_output(output_file, run_args) {
  try {
    //! OF_LINE: omniflux.of:696
    print("Running: %s", output_file);
    //! OF_LINE: omniflux.of:697
    print("----------------------------------------");
    //! OF_LINE: omniflux.of:698
    //! OF_LINE: omniflux.of:699
    const { spawn } = require("child_process");
    //! OF_LINE: omniflux.of:700
    const child = spawn(output_file, run_args || [], { stdio: ["inherit", "pipe", "pipe"] });
    //! OF_LINE: omniflux.of:701
    let output = "";
    //! OF_LINE: omniflux.of:702
    //! OF_LINE: omniflux.of:703
    child.stdout.on("data", (data) => {
      //! OF_LINE: omniflux.of:704
      process.stdout.write(data);
      //! OF_LINE: omniflux.of:705
      output += data.toString();
      //! OF_LINE: omniflux.of:706
    });
    //! OF_LINE: omniflux.of:707
    //! OF_LINE: omniflux.of:708
    child.stderr.on("data", (data) => {
      //! OF_LINE: omniflux.of:709
      process.stderr.write(data);
      //! OF_LINE: omniflux.of:710
      output += data.toString();
      //! OF_LINE: omniflux.of:711
    });
    //! OF_LINE: omniflux.of:712
    //! OF_LINE: omniflux.of:713
    return new Promise((resolve) => {
      //! OF_LINE: omniflux.of:714
      child.on("close", (code) => {
        //! OF_LINE: omniflux.of:715
        console.log("----------------------------------------");
        //! OF_LINE: omniflux.of:716
        resolve(output);
        //! OF_LINE: omniflux.of:717
      });
      //! OF_LINE: omniflux.of:718
      child.on("error", (err) => {
        //! OF_LINE: omniflux.of:719
        console.error("Failed to start process:", err.message);
        //! OF_LINE: omniflux.of:720
        console.log("----------------------------------------");
        //! OF_LINE: omniflux.of:721
        resolve(output);
        //! OF_LINE: omniflux.of:722
      });
      //! OF_LINE: omniflux.of:723
    });
    //! OF_LINE: omniflux.of:724
    //! OF_LINE: omniflux.of:725
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:726
//! OF_LINE: omniflux.of:727
//! OF_LINE: omniflux.of:728
//! OF_LINE: omniflux.of:729
//! OF_LINE: omniflux.of:730
async function restart_watch_process(output_file) {
  try {
    //! OF_LINE: omniflux.of:731
    //! OF_LINE: omniflux.of:732
    if (global.__running_watch_child) {
      //! OF_LINE: omniflux.of:733
      try {
        //! OF_LINE: omniflux.of:734
        global.__running_watch_child.kill("SIGTERM");
        //! OF_LINE: omniflux.of:735
      } catch (e) {
      }
      //! OF_LINE: omniflux.of:736
      global.__running_watch_child = null;
      //! OF_LINE: omniflux.of:737
    }
    //! OF_LINE: omniflux.of:738
    const { spawn } = require("child_process");
    //! OF_LINE: omniflux.of:739
    global.__running_watch_child = spawn(output_file, [], { stdio: "inherit" });
    //! OF_LINE: omniflux.of:740
    //! OF_LINE: omniflux.of:741
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:742
//! OF_LINE: omniflux.of:743
//! OF_LINE: omniflux.of:744
//! OF_LINE: omniflux.of:745
//! OF_LINE: omniflux.of:746
async function watch_loop(input_file, compile_only) {
  try {
    //! OF_LINE: omniflux.of:747
    let last_build_ok = false;
    //! OF_LINE: omniflux.of:748
    let output_file = "";
    //! OF_LINE: omniflux.of:749
    //! OF_LINE: omniflux.of:750
    let result = await build_once(input_file, compile_only, false, null);
    //! OF_LINE: omniflux.of:751
    if (result.ok) {
      //! OF_LINE: omniflux.of:752
      last_build_ok = true;
      //! OF_LINE: omniflux.of:753
      output_file = result.output;
      //! OF_LINE: omniflux.of:754
      if (!compile_only) {
        //! OF_LINE: omniflux.of:755
        await restart_watch_process(output_file);
        //! OF_LINE: omniflux.of:756
      }
      //! OF_LINE: omniflux.of:757
    }
    //! OF_LINE: omniflux.of:758
    //! OF_LINE: omniflux.of:759
    print("Watching for changes... (Press Ctrl+C to stop)");
    //! OF_LINE: omniflux.of:760
    //! OF_LINE: omniflux.of:761
    let abs_input = pathresolve(input_file);
    //! OF_LINE: omniflux.of:762
    let deps = await get_all_dependencies(abs_input, null);
    //! OF_LINE: omniflux.of:763
    //! OF_LINE: omniflux.of:764
    //! OF_LINE: omniflux.of:765
    let mod_times = {};
    //! OF_LINE: omniflux.of:766
    for (let dep_path of deps) {
      //! OF_LINE: omniflux.of:767
      let stat = filestat(dep_path);
      //! OF_LINE: omniflux.of:768
      if (stat) {
        //! OF_LINE: omniflux.of:769
        mod_times[dep_path] = stat.modifiedat;
        //! OF_LINE: omniflux.of:770
      }
      //! OF_LINE: omniflux.of:771
    }
    //! OF_LINE: omniflux.of:772
    //! OF_LINE: omniflux.of:773
    while (true) {
      //! OF_LINE: omniflux.of:774
      await new Promise((resolve) => setTimeout(resolve, 1 * 1e3));
      //! OF_LINE: omniflux.of:775
      //! OF_LINE: omniflux.of:776
      //! OF_LINE: omniflux.of:777
      let changed = false;
      //! OF_LINE: omniflux.of:778
      for (let dep_path of deps) {
        //! OF_LINE: omniflux.of:779
        if (!fileexists(dep_path)) {
          //! OF_LINE: omniflux.of:780
          changed = true;
          //! OF_LINE: omniflux.of:781
          break;
          //! OF_LINE: omniflux.of:782
        }
        //! OF_LINE: omniflux.of:783
        let stat2 = filestat(dep_path);
        //! OF_LINE: omniflux.of:784
        if (stat2 && mod_times[dep_path] && stat2.modifiedat > mod_times[dep_path]) {
          //! OF_LINE: omniflux.of:785
          changed = true;
          //! OF_LINE: omniflux.of:786
          break;
          //! OF_LINE: omniflux.of:787
        }
        //! OF_LINE: omniflux.of:788
      }
      //! OF_LINE: omniflux.of:789
      //! OF_LINE: omniflux.of:790
      if (changed) {
        //! OF_LINE: omniflux.of:791
        print("\nChange detected. Rebuilding...");
        //! OF_LINE: omniflux.of:792
        let new_result = await build_once(input_file, compile_only, false, null);
        //! OF_LINE: omniflux.of:793
        if (new_result.ok) {
          //! OF_LINE: omniflux.of:794
          output_file = new_result.output;
          //! OF_LINE: omniflux.of:795
          if (!compile_only) {
            //! OF_LINE: omniflux.of:796
            await restart_watch_process(output_file);
            //! OF_LINE: omniflux.of:797
          }
          //! OF_LINE: omniflux.of:798
        } else {
          //! OF_LINE: omniflux.of:799
          print("Rebuild failed. Waiting for fixes...");
          //! OF_LINE: omniflux.of:800
        }
        //! OF_LINE: omniflux.of:801
        //! OF_LINE: omniflux.of:802
        //! OF_LINE: omniflux.of:803
        deps = await get_all_dependencies(abs_input, null);
        //! OF_LINE: omniflux.of:804
        mod_times = {};
        //! OF_LINE: omniflux.of:805
        for (let dep_path2 of deps) {
          //! OF_LINE: omniflux.of:806
          let stat3 = filestat(dep_path2);
          //! OF_LINE: omniflux.of:807
          if (stat3) {
            //! OF_LINE: omniflux.of:808
            mod_times[dep_path2] = stat3.modifiedat;
            //! OF_LINE: omniflux.of:809
          }
          //! OF_LINE: omniflux.of:810
        }
        //! OF_LINE: omniflux.of:811
      }
      //! OF_LINE: omniflux.of:812
    }
    //! OF_LINE: omniflux.of:813
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:814
//! OF_LINE: omniflux.of:815
//! OF_LINE: omniflux.of:816
//! OF_LINE: omniflux.of:817
//! OF_LINE: omniflux.of:818
async function watch_loop_ai(input_file, compile_only, strict, cfg) {
  try {
    //! OF_LINE: omniflux.of:819
    let output_file = "";
    //! OF_LINE: omniflux.of:820
    //! OF_LINE: omniflux.of:821
    let result = await build_once(input_file, compile_only, strict, cfg);
    //! OF_LINE: omniflux.of:822
    if (result.ok) {
      //! OF_LINE: omniflux.of:823
      output_file = result.output;
      //! OF_LINE: omniflux.of:824
      if (!compile_only) {
        //! OF_LINE: omniflux.of:825
        await restart_watch_process(output_file);
        //! OF_LINE: omniflux.of:826
      }
      //! OF_LINE: omniflux.of:827
    }
    //! OF_LINE: omniflux.of:828
    //! OF_LINE: omniflux.of:829
    print("Watching for changes... (Press Ctrl+C to stop)");
    //! OF_LINE: omniflux.of:830
    //! OF_LINE: omniflux.of:831
    let abs_input = pathresolve(input_file);
    //! OF_LINE: omniflux.of:832
    let deps = await get_all_dependencies(abs_input, null);
    //! OF_LINE: omniflux.of:833
    //! OF_LINE: omniflux.of:834
    let mod_times = {};
    //! OF_LINE: omniflux.of:835
    for (let dep_path of deps) {
      //! OF_LINE: omniflux.of:836
      let stat = filestat(dep_path);
      //! OF_LINE: omniflux.of:837
      if (stat) {
        //! OF_LINE: omniflux.of:838
        mod_times[dep_path] = stat.modifiedat;
        //! OF_LINE: omniflux.of:839
      }
      //! OF_LINE: omniflux.of:840
    }
    //! OF_LINE: omniflux.of:841
    //! OF_LINE: omniflux.of:842
    while (true) {
      //! OF_LINE: omniflux.of:843
      await new Promise((resolve) => setTimeout(resolve, 1 * 1e3));
      //! OF_LINE: omniflux.of:844
      //! OF_LINE: omniflux.of:845
      let changed = false;
      //! OF_LINE: omniflux.of:846
      for (let dep_path of deps) {
        //! OF_LINE: omniflux.of:847
        if (!fileexists(dep_path)) {
          //! OF_LINE: omniflux.of:848
          changed = true;
          //! OF_LINE: omniflux.of:849
          break;
          //! OF_LINE: omniflux.of:850
        }
        //! OF_LINE: omniflux.of:851
        let stat2 = filestat(dep_path);
        //! OF_LINE: omniflux.of:852
        if (stat2 && mod_times[dep_path] && stat2.modifiedat > mod_times[dep_path]) {
          //! OF_LINE: omniflux.of:853
          changed = true;
          //! OF_LINE: omniflux.of:854
          break;
          //! OF_LINE: omniflux.of:855
        }
        //! OF_LINE: omniflux.of:856
      }
      //! OF_LINE: omniflux.of:857
      //! OF_LINE: omniflux.of:858
      if (changed) {
        //! OF_LINE: omniflux.of:859
        print("\nChange detected. Rebuilding...");
        //! OF_LINE: omniflux.of:860
        let new_result = await build_once(input_file, compile_only, strict, cfg);
        //! OF_LINE: omniflux.of:861
        if (new_result.ok) {
          //! OF_LINE: omniflux.of:862
          output_file = new_result.output;
          //! OF_LINE: omniflux.of:863
          if (!compile_only) {
            //! OF_LINE: omniflux.of:864
            await restart_watch_process(output_file);
            //! OF_LINE: omniflux.of:865
          }
          //! OF_LINE: omniflux.of:866
        } else {
          //! OF_LINE: omniflux.of:867
          print("Rebuild failed. Waiting for fixes...");
          //! OF_LINE: omniflux.of:868
        }
        //! OF_LINE: omniflux.of:869
        //! OF_LINE: omniflux.of:870
        deps = await get_all_dependencies(abs_input, null);
        //! OF_LINE: omniflux.of:871
        mod_times = {};
        //! OF_LINE: omniflux.of:872
        for (let dep_path2 of deps) {
          //! OF_LINE: omniflux.of:873
          let stat3 = filestat(dep_path2);
          //! OF_LINE: omniflux.of:874
          if (stat3) {
            //! OF_LINE: omniflux.of:875
            mod_times[dep_path2] = stat3.modifiedat;
            //! OF_LINE: omniflux.of:876
          }
          //! OF_LINE: omniflux.of:877
        }
        //! OF_LINE: omniflux.of:878
      }
      //! OF_LINE: omniflux.of:879
    }
    //! OF_LINE: omniflux.of:880
  } catch (err) {
    throw err;
  }
}
//! OF_LINE: omniflux.of:881
//! OF_LINE: omniflux.of:882
//! OF_LINE: omniflux.of:883
//! OF_LINE: omniflux.of:884
//! OF_LINE: omniflux.of:885
async function __on_start_1() {
  //! OF_LINE: omniflux.of:886
  let cli = await parse_args(args);
  //! OF_LINE: omniflux.of:887
  //! OF_LINE: omniflux.of:888
  if (cli.help) {
    //! OF_LINE: omniflux.of:889
    await print_usage();
    //! OF_LINE: omniflux.of:890
    exit(0);
    //! OF_LINE: omniflux.of:891
  }
  //! OF_LINE: omniflux.of:892
  //! OF_LINE: omniflux.of:893
  if (cli.setup) {
    //! OF_LINE: omniflux.of:894
    await run_wizard();
    //! OF_LINE: omniflux.of:895
    exit(0);
    //! OF_LINE: omniflux.of:896
  }
  //! OF_LINE: omniflux.of:897
  //! OF_LINE: omniflux.of:898
  //! OF_LINE: omniflux.of:899
  let cfg = null;
  //! OF_LINE: omniflux.of:900
  if (!await config_exists()) {
    //! OF_LINE: omniflux.of:901
    cfg = await run_wizard();
    //! OF_LINE: omniflux.of:902
  } else {
    //! OF_LINE: omniflux.of:903
    cfg = await config_load();
    //! OF_LINE: omniflux.of:904
  }
  //! OF_LINE: omniflux.of:905
  //! OF_LINE: omniflux.of:906
  cfg.verbose = cli.verbose;
  //! OF_LINE: omniflux.of:907
  //! OF_LINE: omniflux.of:908
  //! OF_LINE: omniflux.of:909
  if (cli.input_file == "") {
    //! OF_LINE: omniflux.of:910
    await interactive_chat(cfg);
    //! OF_LINE: omniflux.of:911
    exit(0);
    //! OF_LINE: omniflux.of:912
  }
  //! OF_LINE: omniflux.of:913
  //! OF_LINE: omniflux.of:914
  if (cli.watch) {
    //! OF_LINE: omniflux.of:915
    await watch_loop_ai(cli.input_file, cli.compile_only, cli.strict, cfg);
    //! OF_LINE: omniflux.of:916
  } else {
    //! OF_LINE: omniflux.of:917
    let result = await build_once(cli.input_file, cli.compile_only, cli.strict, cfg, cli.force);
    //! OF_LINE: omniflux.of:918
    if (!result.ok) {
      //! OF_LINE: omniflux.of:919
      exit(1);
      //! OF_LINE: omniflux.of:920
    }
    //! OF_LINE: omniflux.of:921
    if (!cli.compile_only) {
      //! OF_LINE: omniflux.of:922
      let run_out = await run_output(result.output, cli.run_args);
      //! OF_LINE: omniflux.of:923
      //! OF_LINE: omniflux.of:924
      //! OF_LINE: omniflux.of:925
      if (run_out && match(run_out, "/ReferenceError|TypeError|is not defined|Cannot read properties|is not a function/") && !cli.strict) {
        //! OF_LINE: omniflux.of:926
        print("\x1B[1;33mRuntime error detected. Falling back to AI Compiler...\x1B[0m");
        //! OF_LINE: omniflux.of:927
        let abs_input2 = pathresolve(cli.input_file);
        //! OF_LINE: omniflux.of:928
        let deps2 = await get_all_dependencies(abs_input2, null);
        //! OF_LINE: omniflux.of:929
        //! OF_LINE: omniflux.of:930
        for (let dep_path2 of deps2) {
          //! OF_LINE: omniflux.of:931
          let cache_path2 = await get_cache_path(dep_path2);
          //! OF_LINE: omniflux.of:932
          if (fileexists(cache_path2)) {
            //! OF_LINE: omniflux.of:933
            filedelete(cache_path2);
            //! OF_LINE: omniflux.of:934
          }
          //! OF_LINE: omniflux.of:935
          await ai_compile_file(cfg, dep_path2, cache_path2, run_out);
          //! OF_LINE: omniflux.of:936
        }
        //! OF_LINE: omniflux.of:937
        //! OF_LINE: omniflux.of:938
        if (cfg != null && cfg.was_healed) {
          //! OF_LINE: omniflux.of:939
          let rebuild_res = await build_once(cli.input_file, cli.compile_only, cli.strict, cfg, true);
          //! OF_LINE: omniflux.of:940
          if (rebuild_res.ok) {
            //! OF_LINE: omniflux.of:941
            await run_output(rebuild_res.output, cli.run_args);
            //! OF_LINE: omniflux.of:942
          }
          //! OF_LINE: omniflux.of:943
        } else {
          //! OF_LINE: omniflux.of:944
          //! OF_LINE: omniflux.of:945
          let abs_inp2 = pathresolve(cli.input_file);
          //! OF_LINE: omniflux.of:946
          let out_path2 = await get_output_path(abs_inp2);
          //! OF_LINE: omniflux.of:947
          let ai_deps = await get_all_dependencies(abs_inp2, null);
          //! OF_LINE: omniflux.of:948
          let ai_map = {};
          //! OF_LINE: omniflux.of:949
          for (let ai_dep of ai_deps) {
            //! OF_LINE: omniflux.of:950
            let ai_cache = await get_cache_path(ai_dep);
            //! OF_LINE: omniflux.of:951
            ai_map[ai_dep] = fileread(ai_cache);
            //! OF_LINE: omniflux.of:952
          }
          //! OF_LINE: omniflux.of:953
          let result2 = await build(abs_inp2, out_path2, ai_map);
          //! OF_LINE: omniflux.of:954
          if (result2.ok) {
            //! OF_LINE: omniflux.of:955
            await run_output(result2.output, cli.run_args);
            //! OF_LINE: omniflux.of:956
          }
          //! OF_LINE: omniflux.of:957
        }
        //! OF_LINE: omniflux.of:958
      }
      //! OF_LINE: omniflux.of:959
    }
    //! OF_LINE: omniflux.of:960
    exit(0);
    //! OF_LINE: omniflux.of:961
  }
  //! OF_LINE: omniflux.of:962
}
//! OF_LINE: omniflux.of:963
//! OF_LINE: omniflux.of:964
global.parse_args = parse_args;
global.print_usage = print_usage;
global.config_path = config_path;
global.config_exists = config_exists;
global.config_load = config_load;
global.config_save = config_save;
global.run_wizard = run_wizard;
global.send_ai_request = send_ai_request;
global.print_verbose_usage = print_verbose_usage;
global.format_chat_response = format_chat_response;
global.read_system_prompt = read_system_prompt;
global.detect_missing_includes = detect_missing_includes;
global.ai_compile_file = ai_compile_file;
global.interactive_chat = interactive_chat;
global.read_runtime = read_runtime;
global.compile_file = compile_file;
global.build_once = build_once;
global.run_output = run_output;
global.restart_watch_process = restart_watch_process;
global.watch_loop = watch_loop;
global.watch_loop_ai = watch_loop_ai;
global.__on_start_hooks = global.__on_start_hooks || [];
global.__on_start_hooks.push(__on_start_1);
if (require.main === module) {
  (async () => {
    const hooks = global.__on_start_hooks || [];
    for (const hook of hooks) {
      await hook();
    }
  })().catch((err) => {
    if (typeof formatRuntimeError === "function") {
      formatRuntimeError(err);
    } else {
      console.error(err);
    }
    process.exit(1);
  });
}
